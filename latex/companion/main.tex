\input{../shared/preamble}

\title{Why Rotating Fluids Make Polygons:\\[6pt]
\large A Companion for the Curious Reader}
\author{Gordon Speagle}
\date{}

\begin{document}
\maketitle

\section*{Prologue: The Polygon Number}

\subsection*{The hexagon that started this}

Saturn has a hexagon at its north pole. Not a rough hexagonal shape, not
a six-sided approximation---a persistent, symmetric, six-sided polygon
of clouds, each side longer than the diameter of Earth. The Voyager
spacecraft photographed it in 1981. The Cassini orbiter confirmed it
was still there thirty years later, rotating in lockstep with the
planet's interior. Jupiter has an octagon at its south pole: eight
cyclones arranged in a regular ring, each the size of the continental
United States, mapped by the Juno mission starting in 2017.

These are not weather in the way we think of weather. A hurricane on
Earth forms, wanders, and dies. These polygons persist for decades,
possibly centuries. They do not drift. They do not deform. They sit at
the poles and spin.

Why polygons? Why \emph{these} polygons---six for Saturn, eight for
Jupiter? I first encountered the Voyager photos years ago and did what
most people do: marveled, read a few explanations, and filed it under
``Nature Gonna Geometry.'' Crystals, snowflakes, beehives, the
nautilus shell---the physical world converges on geometric forms for
all sorts of reasons. I assumed the hexagon was another entry on that
list.

Years later I was watching an astronomy video with my kids and the
polar polygons came up again. My children were astonished. I was
curious. I dove back into the literature, ordered a copy of
\emph{Geophysical Fluid Dynamics} by Joseph Pedlosky, and started
writing code.

What I found was a formula---published in 1931 by a mathematician
named T.~H.~Havelock---that counts how many spinning objects can
sit in a stable ring. The formula is not complicated. It has two
parts: one from geometry, one from the surface the objects sit on.
The geometry part is universal; it does not care whether the objects
are storms on a gas giant, quantum vortices in superfluid helium, or
anything else that spins logarithmically. The surface part changes
from problem to problem.

On a flat surface, the formula says the largest stable ring has seven
members. Seven. Not six, not eight---seven. Saturn's hexagon has six
sides; Jupiter's octagon has eight. Both are one step away from seven,
on opposite sides.

I became determined to pull the connecting string until the knot
unraveled. The string led from fluid dynamics to algebraic number
theory to general relativity to particle physics to cosmology. I
followed it for years. The result is a six-paper technical series.
This companion document is my attempt to explain what that series
says, and why, without requiring any of the mathematics that makes
it work.


\subsection*{The one idea}

Here is the central claim, stated as plainly as I can manage.

When identical spinning objects arrange themselves in a ring on a
surface, there is a number---call it $N$---that counts how many
objects are in the ring. The ring's stability depends on a single
quantity that splits into two parts. The first part comes from the
surface: flat, curved like a sphere, curved like a saddle. The
second part comes from pure geometry: how many objects there are and
how they interact pairwise.

On a flat surface, the two parts balance exactly at $N = 7$. A ring
of seven is marginally stable---it neither grows nor shrinks when
disturbed. Below seven, the ring snaps back. Above seven, the ring
flies apart.

This number seven is not adjustable. It is not a parameter I chose or
a threshold I fit to data. It falls out of the formula the way four
falls out of the corners of a square. It is a property of the
two-dimensional logarithmic interaction---the mathematical law that
governs how point-like spinning objects push each other around on a
surface.

The technical papers argue that this same threshold determines far
more than atmospheric polygons. The number seven, and the algebraic
structure surrounding it, fixes the spin of the graviton (the
hypothetical particle that carries gravity); forces the gauge group
of particle physics to be $\mathrm{SU}(3) \times \mathrm{SU}(2)
\times \mathrm{U}(1)$ (the symmetry behind the strong, weak, and
electromagnetic forces); and constrains the cosmological constant
(the energy density of empty space). These are not separate
coincidences bolted together. They are consequences of one formula
applied to one threshold.

Whether the argument is correct is for the scientific community to
assess. My goal here is to make it accessible enough that you can
follow the logic, identify where the assumptions are, and form your
own judgment.


\subsection*{The scorecard}

The theory takes in one integer ($N = 7$), one scale (the Planck
mass, which sets the units of gravity), and three identifications
that connect the mathematical formalism to physics. From these
inputs it produces numerical predictions. The table below summarizes
the main ones.

\begin{center}
\small
\renewcommand{\arraystretch}{1.25}
\begin{tabular}{p{5.2cm} p{3.5cm} p{2cm} l}
\toprule
\textbf{What the theory predicts} & \textbf{What we measure} & \textbf{How close} & \textbf{Status} \\
\midrule
The gauge group of particle physics is $\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$
  & Confirmed experimentally & Exact match & Theorem \\[4pt]
Three generations of matter particles
  & Three observed & Exact match & Derived \\[4pt]
The CKM phase (a parameter governing matter--antimatter asymmetry) is $70.2^\circ$
  & $73.5^\circ \pm 5.1^\circ$ & $0.4$ standard deviations & Derived \\[4pt]
The Weinberg angle (which sets the ratio of electromagnetic to weak force strength) gives $\sin^2\theta_W = 3/11$
  & Measured value differs by $3.9\%$ after quantum corrections & $3.9\%$ & Derived \\[4pt]
A quark mixing parameter $|V_{cb}| = 0.044$
  & Measured $\approx 0.042$ & $6\%$ & Derived \\[4pt]
The strange-to-bottom quark mass ratio is $0.023$
  & Measured $\approx 0.024$ & $4\%$ & Derived \\[4pt]
The Hubble constant (expansion rate of the universe) is $67.4$ km/s/Mpc
  & $67.4 \pm 0.5$ km/s/Mpc & $0.002\%$ & Derived \\[4pt]
Dark matter makes up $26.9\%$ of the cosmic energy budget
  & $26.8\% \pm 0.5\%$ & $0.4$ standard deviations & Conjectured \\[4pt]
Ordinary matter makes up $5.0\%$
  & $4.9\% \pm 0.1\%$ & $0.6$ standard deviations & Derived \\[4pt]
The Higgs boson mass is $125 \pm 3$ GeV
  & $125.25 \pm 0.17$ GeV & $2\%$ & Derived \\[4pt]
The strong coupling constant $\alpha_s = 0.115$--$0.122$
  & $0.1180 \pm 0.0009$ & Within range & Derived \\[4pt]
The string tension ratio $\sigma/\Lambda^2 = 5.97$
  & Lattice QCD: $6.25 \pm 0.5$ & $5\%$ & Derived \\[4pt]
The proton is exactly stable (never decays)
  & No decay observed & Consistent & Derived \\[4pt]
The QCD vacuum angle $\theta_{\mathrm{QCD}} = 0$
  & $< 10^{-10}$ & Consistent & Derived \\[4pt]
Neutrinos follow the normal mass hierarchy
  & Pending (JUNO experiment) & Future test & Derived \\[4pt]
Dark energy equation of state $w = -1$
  & $-1.0 \pm 0.1$ (DESI) & Consistent & Derived \\
\bottomrule
\end{tabular}
\end{center}

\noindent
A few things to notice. First, the ``How close'' column. Most
predictions match observations within a few percent or better. Second,
the ``Status'' column distinguishes between results that are
mathematically proved, results that are derived through a chain of
identifications, and results that remain conjectured. Third, several
predictions are not yet testable---they await experiments currently
under construction. Those are genuine predictions in the scientific
sense: stated before the measurement, falsifiable by the outcome.


\subsection*{The honest disclaimer}

I am not a physicist by training. My background is philosophy with a
mathematical bent; professionally, I am a software developer working
in healthcare. A hobbyist's obsession with number theory is an apt
description of my post-educational years. I am unaffiliated with any
university or research institution.

This matters because the claims in the technical series are
extraordinary, and extraordinary claims face a higher bar. I want to
be transparent about both the strengths and the limitations.

The results fall into three tiers of confidence:

\textbf{Proved.} A small number of results are mathematical theorems.
If you accept the starting identification---that a point vortex on a
two-dimensional surface and a gravitational source share the same
Green's function---then these results follow by logical necessity. The
gauge group of particle physics being
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$ is in
this category. The proof uses finite group theory (specifically, the
symmetry group PSL(2,7) acting on the Klein quartic surface), and the
steps are checkable by anyone with an undergraduate algebra textbook.

\textbf{Derived.} Most results are in this tier. They follow from a
chain of identifications---each step uses standard physics or
standard mathematics---with controlled approximations whose errors
are estimated. The Weinberg angle, the Hubble constant, the quark
mixing parameters, and the Higgs mass are all derived. These results
could be wrong if one of the identifications in the chain fails, but
the chain is explicit: every link is written down, and each can be
tested independently.

\textbf{Conjectured.} A handful of results are numerically verified
but rely on at least one step that is physically motivated rather than
mathematically proved. The dark matter fraction is in this category.
The number matches, but the derivation has a gap that I have flagged
honestly.

Institutional research correctly forces disciplinary depth; the cost
is that cross-domain connections can go unnoticed. I am unaffiliated,
which meant I could follow the mathematics wherever it led, without
the professional incentive to stay within a single field. Whether that
freedom produced something worth the lack of expert supervision is for
the reader to assess.


\subsection*{The map}

The technical series has six papers. Here is what each one does.

\textbf{Paper I: The Mathematics.} This paper establishes the
foundation. The logarithmic interaction---the same law that governs
how spinning fluid parcels push each other---is the unique
two-dimensional interaction for which polygon stability is
scale-independent. The Havelock formula (1931) gives the stability
eigenvalues. On a flat surface the critical threshold is $N = 7$.
On curved surfaces the threshold shifts, and the shift values live
in algebraic number fields: the threshold for the transition from
seven to eight stable vortices is $8 - 3\sqrt{7}$, a unit in the
ring of integers of $\mathbb{Q}(\sqrt{7})$. This is where the
algebraic number theory enters, and it is not decoration---these
algebraic structures determine the physical predictions downstream.

\textbf{Paper II: The Physics.} This paper connects the mathematics
to actual fluid dynamics. Point vortices have no inertia; they are
pushed sideways by their neighbors. A central vortex can stabilize
rings that would otherwise be unstable. The paper derives the
critical central-vortex strength for each $N$, resolves the $N = 7$
marginal case with a quartic (fourth-order) calculation, and shows
that the stability results survive two realistic corrections: the
finite size of real vortices (the ``blob'' correction, which is
independent of the vortex profile shape) and the Rossby-wave
stationarity constraint that pins Saturn's hexagon at $N = 6$. The
final sections identify a structural isomorphism between point
vortices and gravitational sources: both produce conical
singularities with the same Green's function.

\textbf{Paper III: Gravity.} The marginal mode at $N = 7$ carries
spin 2---the same spin as the graviton, the hypothetical quantum of
gravity. This paper derives Einstein's equation (the field equation
of general relativity) from the statistical mechanics of vortex
configurations, using Jensen's inequality and the Onsager
large-energy limit. It constructs the Wheeler--DeWitt equation (the
quantum gravity wave equation) from the breathing mode of the
polygon, with the central charge $c = 12\,b(N)$ determined by the
Fisher--Rao metric on the space of vortex configurations. Black hole
thermodynamics, holography, and the emergence of quantum mechanics
from statistical mechanics are all addressed.

\textbf{Paper IV: Particle Physics.} The polygon hierarchy forces
the internal geometry to be an eleven-dimensional manifold (four
spacetime dimensions plus a seven-dimensional compact space). The
compact space is rigid: Thurston's geometrization theorem eliminates
all possibilities except one. From that geometry, the Standard Model
gauge group $\mathrm{SU}(3) \times \mathrm{SU}(2) \times
\mathrm{U}(1)$ is derived, not assumed. The Weinberg angle comes
out as $\sin^2\theta_W = 3/11$, a ratio of Casimir invariants. The
CKM phase, quark mass ratios, the Higgs mass, the strong coupling
constant, and the string tension of quark confinement are all
computed from the same central charge.

\textbf{Paper V: Cosmology.} The cosmological constant---the energy
density of empty space---is determined by the instanton action of the
$N = 11$ Born--Oppenheimer tunneling amplitude. The hierarchy between
the Planck scale and the electroweak scale (a factor of $10^{16}$)
is not put in by hand; it emerges from the tunneling exponent. The
cosmic energy budget (dark energy, dark matter, ordinary matter)
decomposes into tree-level, one-loop, and tunneling contributions.
The Hubble constant, the neutrino mass hierarchy, and the initial
conditions of inflation are derived.

\textbf{Paper VI: The Scorecard.} This paper collects every
prediction, classifies each by confidence tier, quantifies the
cumulative uncertainty, and lists six binary pass/fail experimental
tests---results from JUNO, LEGEND, Hyper-Kamiokande, LISA, and
DESI---that can confirm or falsify specific claims. It also
catalogues what remains open: the dark matter production mechanism,
the baryon asymmetry factor, and the light quark masses.


\newpage


\part{The Mathematics of Polygons}


\section{Why logarithms are special}
\noindent\textit{[Technical reference: Paper~I, \S1--2]}

\medskip

\noindent\textit{Bottom line: The logarithmic interaction is the only
law in two dimensions for which polygon stability does not depend on
size.}

\medskip

In three dimensions, gravity falls off as $1/r^2$. Drop to two
dimensions and the natural law becomes the logarithm: the force
between spinning objects falls off as $1/r$.

This is not a modeling choice. It is forced by the mathematics of
the two-dimensional Laplacian---the same equation that governs heat
diffusion, electrostatics, and fluid flow on a flat surface. The
Green's function (the fundamental solution) of that equation is the
logarithm. Period.

What makes the logarithm special among all possible force laws is a
specific kind of rigidity. If you take a polygon governed by
logarithmic forces and zoom in or zoom out---rescale the entire
configuration by a factor $b$---the stability does not change. The
energy shifts by a constant that depends on $b$ but not on the
positions. The equations of motion are identical before and after
rescaling.

Try any other force law. The power law $1/r^2$ (gravity in 3D,
projected onto a plane): zooming out makes the polygon \emph{less}
stable. The interaction weakens faster than the geometry spreads, and
eventually the ring flies apart. A steeper law like $1/r^3$: same
problem, worse. A shallower law like $1/\sqrt{r}$: zooming out makes
the polygon \emph{more} stable, but zooming in destabilizes it. The
logarithm sits exactly at the boundary. It is the unique fixed point.

The technical language for this classification borrows vocabulary from
the renormalization group---a tool from statistical mechanics that
sorts interactions by their behavior under rescaling. An interaction
steeper than the logarithm is ``relevant'': it grows under
coarse-graining and destabilizes at large scales. An interaction
shallower than the logarithm is ``irrelevant'': it shrinks under
coarse-graining and destabilizes at small scales. The logarithm itself
is ``marginal''---scale-invariant. Paper~I proves that this
classification is exhaustive: every two-dimensional pairwise
interaction falls into one of these three categories, and the
logarithm is the unique boundary between them.

One practical consequence: real vortices are not mathematical points.
They have finite cores---a smeared-out region of rotation rather than
an infinitely concentrated singularity. The interaction between two
finite-core vortices at large separation looks like
$-\kappa^2 \ln d + O(\varepsilon^2/d^2)$, where $d$ is the distance,
$\kappa$ is the circulation, and $\varepsilon$ is the core size. The
correction term $O(\varepsilon^2/d^2)$ falls in the ``irrelevant''
category ($1/r^2$, shrinking under rescaling). So at large
separations, any compact vortex distribution flows toward the
logarithmic interaction. The finite-core details wash out.

This is why the stability threshold $N = 7$ (which I have not derived
yet---that comes in Section~4) is not an artifact of an idealized
model. It is the fixed point that real vortex systems approach as
they separate.

\textbf{Status: (Proved)}---this is a mathematical theorem about the
two-dimensional Laplacian and the Cauchy functional equation. The
proof is in Paper~I, \S2.


%% ====================================================================
\section{The sign rule: bowls, not hilltops}
\noindent\textit{[Technical reference: Paper~I, \S3]}

\medskip

\noindent\textit{Bottom line: The regular polygon sits at the bottom
of an energy valley, not balanced on a hilltop.}

\medskip

Before this series, the standard treatment of polygon stability
described the regular $N$-gon as an energy saddle---stable in some
directions of perturbation, unstable in others. Think of sitting on a
horse saddle: rock forward and you slide down; rock sideways and
you're pushed back. A saddle is stable along one axis and unstable
along the other. Most of the point-vortex literature characterized the
polygon this way.

The sign rule (Theorem~3 in Paper~I) corrects this picture. The
regular polygon is a \emph{constrained energy minimum}: a bowl, not a
hilltop, not a saddle.

The analogy: a marble in a bowl. Displace the marble; it rolls back
to the center. A marble on a dome: displace it; it rolls away. The
polygon is the bowl.

There is a subtlety, and I want to be honest about it. The polygon is
a saddle of the \emph{unconstrained} energy---the energy considered as
a function of all possible configurations. If you could grab the
vortices and rearrange them arbitrarily, you would find directions
where the energy decreases. But vortices cannot be rearranged
arbitrarily. They are constrained by a conserved quantity called the
angular impulse (a measure of how much total spin the system carries).
The constraint removes the unstable directions. On the
angular-impulse surface---the set of all configurations with the same
total spin---the polygon sits at a minimum.

The sign rule itself is more general than the logarithm. It applies to
any pairwise interaction $h(r)$ satisfying two conditions: the force
is attractive ($h'(r) < 0$, meaning the interaction pulls objects
toward each other) and the quantity $(r \cdot h'(r))'$ is
non-positive (a concavity condition on the force law). Any interaction
satisfying both conditions produces a polygon that is a constrained
minimum.

The logarithmic interaction $h(r) = -\ln r$ is the borderline case.
It satisfies $h' < 0$ (attractive: $h' = -1/r$) and
$(rh')' = 0$ (the concavity condition is satisfied with
equality, not strict inequality). In the bowl analogy: for most
interactions satisfying the sign rule, the bowl has a standard curved
bottom---displace the marble and it accelerates back. For the
logarithm, the bowl is flat at the very bottom. The restoring force is
not quadratic (proportional to displacement) but quartic
(proportional to displacement cubed). The marble still returns to
center; it just takes longer. It sits in a flat-bottomed bowl.

This flatness at $N = 7$ is not a defect. It is the mathematical
signature of the critical threshold. The flat bottom of the bowl is
precisely where the quartic analysis of Section~4 becomes necessary.

Nature finds energy minima. That is the second law of thermodynamics
in one sentence. The sign rule tells us that the regular polygon
\emph{is} the energy minimum for the constrained system. This is why
polygons form, why they persist, and why they are regular rather than
irregular.

\textbf{Status: (Proved)}---the sign rule is Theorem~3 of Paper~I.
The proof is self-contained and uses only calculus and the properties
of trigonometric sums.


%% ====================================================================
\section{The Havelock formula: one equation, two parts}
\noindent\textit{[Technical reference: Paper~I, \S4]}

\medskip

\noindent\textit{Bottom line: The stability of any polygon on any
surface splits into one number from the surface and one number from
pure counting.}

\medskip

This is the only equation in this companion that I will ask you to
look at:
\begin{equation}\label{eq:havelock-companion}
  \lambda_m \;=\; C_1 \;-\; \frac{m(N-m)}{2}\,.
\end{equation}
Every symbol earns its keep, so let me translate each one.

\begin{itemize}
\item $\lambda_m$ (``lambda-sub-$m$''): the stability of the $m$-th
  wobble mode. If $\lambda_m$ is positive, that mode is stable---the
  polygon resists that particular distortion. If negative, the polygon
  is unstable against it. If zero, the polygon is on the edge: the
  linear restoring force has vanished, and you need to look at
  higher-order terms.

\item $m$: which wobble pattern. $m = 1$ is the gentlest perturbation,
  where the whole ring shifts slightly off-center. $m = 2$ is an
  elliptical distortion. $m = 3$ is a triangular distortion. The index
  $m$ runs from $1$ to $N - 1$, covering all independent ways the
  polygon can deform.

\item $N$: how many objects are in the ring.

\item $C_1$: one single number that encodes everything about the
  surface---flat plane, sphere, hyperbolic plane, whatever. On a flat
  surface, $C_1 = N - 1$. On a sphere, $C_1$ depends on the latitude.
  On a hyperbolic surface, $C_1$ depends on the curvature radius. This
  is the only part of the formula that changes from surface to surface.

\item $m(N-m)/2$: a counting pattern that depends only on $N$ and $m$.
  It is the same on every surface. It comes from the representation
  theory of the cyclic group $\mathbb{Z}_N$---a precise way of saying
  ``the symmetry of the ring.'' If you place $N$ identical objects
  equally spaced around a circle, the symmetry group of the
  configuration is $\mathbb{Z}_N$ (the group of rotations by multiples
  of $360^\circ/N$). The number $m(N-m)/2$ is the Casimir invariant of the
  $m$-th representation of that group. The name does not matter. What
  matters is that this term is universal.
\end{itemize}

In words: the stability of any polygon on any surface splits cleanly
into a piece from the surface ($C_1$) and a piece from pure counting
($m(N-m)/2$). The counting piece is the same whether the polygon sits
on a flat table, a sphere, or a saddle.

T.~H.~Havelock published this formula in 1931, for the specific case
of a flat surface. What is new in this series is the proof that the
counting part $m(N-m)/2$ is \emph{metric-independent}---it does not
change when you curve the surface. The proof (Paper~I, \S4) proceeds
by showing that the tangential Hessian of the energy is a circulant
matrix with a $\csc^2$ kernel, and the Fourier eigenvalue of that
kernel is exactly $m(N-m)/2$ by an identity in the representation
theory of $\mathbb{Z}_N$. The surface enters only through the radial
part of the Hessian, which collapses into the single number $C_1$.

The separation is clean because the Laplacian on any
constant-curvature surface gives a Green's function that depends only
on geodesic distance. The off-diagonal structure (how vortices
interact) is determined by $\mathbb{Z}_N$; the diagonal structure (how
each vortex sits in its potential well) is determined by the surface.
They do not mix.

This is the architectural fact on which everything else rests. If the
formula did not split, each surface would require a separate stability
analysis. Because it does split, every stability question on every
constant-curvature surface reduces to computing one number: $C_1$.

\textbf{Status: (Proved)}---Theorem~4 in Paper~I. The proof
uses $\mathbb{Z}_N$ Fourier analysis and the Laplacian Green's function.


%% ====================================================================
\section{Why seven is the magic number}
\noindent\textit{[Technical reference: Paper~I, \S4--5]}

\medskip

\noindent\textit{Bottom line: On a flat surface, a ring of seven is
the largest stable polygon. This is a theorem, not a guess.}

\medskip

On a flat surface, $C_1 = N - 1$. Substitute that into the Havelock
formula and the stability question becomes: is
$(N - 1) - m(N - m)/2 \ge 0$ for every mode $m$?

The most dangerous mode---the one closest to going negative---is the
one that maximizes $m(N - m)/2$. That maximum occurs at
$m = \lfloor N/2 \rfloor$: the halfway mode, the perturbation that
splits the polygon into two interleaved groups as far apart as
possible.

Work through the arithmetic for specific values of $N$.

At $N = 6$ (hexagon), the most dangerous mode is $m = 3$:
$\lambda_3 = 5 - 3 \times 3/2 = 5 - 4.5 = 0.5$. Positive. Stable.

At $N = 7$ (heptagon), the most dangerous mode is $m = 3$:
$\lambda_3 = 6 - 3 \times 4/2 = 6 - 6 = 0$. Exactly zero. On the
edge.

At $N = 8$ (octagon), the most dangerous mode is $m = 4$:
$\lambda_4 = 7 - 4 \times 4/2 = 7 - 8 = -1$. Negative. Unstable.

The transition is sharp. There is no $N = 7.5$. Seven is the last
integer where the formula stays non-negative.

Why does $N = 7$ produce zero? Paper~I, Theorem~10 gives the algebraic
answer: setting $\lambda_m = 0$ with $C_1 = N - 1$ and solving gives
$(N - 1)(N - 7) = 0$. The roots are $N = 1$ (below the polygon range)
and $N = 7$. The number seven is not chosen; it is the unique
nontrivial root of a quadratic that arises from the intersection of
the linear function $C_1 = N - 1$ (from dilation invariance) and the
quadratic Casimir $m(N-m)/2$ (from $\mathbb{Z}_N$ representation
theory). The coefficient $8$ in $N^2 - 8N + 7 = 0$ comes from two
factors of the representation theory: the $1/2$ in the Havelock
normalization and the critical-mode evaluation at $m = (N-1)/2$.

There is a loose end at $N = 7$. The eigenvalue $\lambda_3 = 0$ means
the linear restoring force vanishes. The marble in the bowl sits on a
flat bottom. Does it stay put, or does it eventually slide away? To
answer this, you need to check the next term in the expansion: the
quartic (fourth-order) term.

Paper~II computes this quartic coefficient:
$\alpha_0 = 45/14 \approx 3.21 > 0$. Positive. The flat-bottomed
bowl curves back up at fourth order. The marble returns to center; it
just accelerates more slowly than a standard quadratic bowl (quartic
restoring force $\propto x^3$ rather than linear $\propto x$).

The seven-sided polygon is stable, but marginally so. It is the last
polygon that holds together on a flat surface, and it holds together
by the quartic correction rather than the standard linear restoring
force.

\textbf{Status: (Proved)}---$N_{\mathrm{crit}} = 7$ is Theorem~10 of
Paper~I. The quartic resolution $\alpha_0 = 45/14 > 0$ is proved in
Paper~II.


%% ====================================================================
\section{Curved surfaces and algebraic number theory}
\noindent\textit{[Technical reference: Paper~I, \S5--6]}

\medskip

\noindent\textit{Bottom line: On curved surfaces, the stability
threshold shifts---and the shift values live in algebraic number
fields tied to $\sqrt{7}$.}

\medskip

On a flat surface, $C_1 = N - 1$ and the threshold is $N = 7$. On a
curved surface, $C_1$ changes and the threshold moves.

The sphere and the hyperbolic plane push in opposite directions.

\subsection*{The sphere: curvature destabilizes}

On a sphere of radius $a$, with the polygon sitting at colatitude
$\varphi_0$ (the angle from the north pole), the curvature coefficient
is $C_1 = (N-1)\cos\varphi_0$. At the pole ($\varphi_0 = 0$),
$\cos\varphi_0 = 1$ and you recover the flat result $C_1 = N - 1$.
As the polygon moves toward the equator, $\cos\varphi_0$ decreases,
$C_1$ drops, and the polygon destabilizes at smaller $N$.

The thresholds are all rational numbers. For a triangle ($N = 3$):
the critical colatitude is $60^\circ$, corresponding to
$\xi_{\mathrm{crit}} = 1/3$. For a square ($N = 4$): $48.2^\circ$,
$\xi_{\mathrm{crit}} = 1/5$. For a pentagon ($N = 5$): $41.4^\circ$,
$\xi_{\mathrm{crit}} = 1/7$. For a hexagon ($N = 6$): $25.8^\circ$,
$\xi_{\mathrm{crit}} = 1/19$. For a heptagon ($N = 7$): $0^\circ$---the
exact pole, and only marginally stable there. For $N \ge 8$: no
stable latitude exists on the sphere, at all.

The rationality of these thresholds is not obvious. The Green's
function on the sphere involves transcendental functions (logarithms
of sines). But $C_1$ on the sphere depends linearly on
$\cos\varphi_0$, and the marginal condition $C_1 = m(N-m)/2$ produces
a rational equation. Transcendental interaction, rational threshold.

\subsection*{The hyperbolic plane: curvature stabilizes}

On the hyperbolic plane (constant negative curvature, like a saddle
that extends in all directions), the opposite happens. The curvature
coefficient is
$C_1 = (N-1)(1 + \xi^2)/(1-\xi)^2$,
where $\xi = r_E^2/a^2$ measures how far the polygon sits from the
center of the Poincar\'e disk (the standard model of the hyperbolic
plane). Since $(1 + \xi^2)/(1-\xi)^2 > 1$ for any $\xi > 0$, we have
$C_1 > N - 1$: negative curvature \emph{boosts} $C_1$ above its flat
value. Polygons that would fly apart on a flat surface can be held
together by sufficient negative curvature.

The threshold for the $7 \to 8$ transition---the curvature at which an
octagon first becomes stable---is
$\xi^* = 8 - 3\sqrt{7} \approx 0.063$.
This number comes from solving the equation
$\xi^2 - 16\xi + 1 = 0$.

I want to draw attention to this equation. It reads the same forwards
and backwards: the coefficient of $\xi^2$ (which is $1$) equals the
constant term (also $1$). This palindromic structure forces the two
roots to be reciprocals of each other: if $\xi^*$ is a root, then
$1/\xi^*$ is the other root. The smaller root is the stability
threshold; the larger root is its conformal inverse.

The verification: $(8 - 3\sqrt{7})(8 + 3\sqrt{7}) = 64 - 63 = 1$.
The product of the two roots is exactly $1$, confirming the
palindromic structure.

The number $8 - 3\sqrt{7}$ is the inverse of the fundamental unit
of $\mathbb{Z}[\sqrt{7}]$---the ring of integers in the number field
$\mathbb{Q}(\sqrt{7})$. The fundamental unit is
$\varepsilon = 8 + 3\sqrt{7}$; its inverse is
$\varepsilon^{-1} = 8 - 3\sqrt{7}$. These are standard objects in
algebraic number theory: every quadratic number field has a
fundamental unit, and the Pell equation $x^2 - 7y^2 = 1$ (solved by
$x = 8$, $y = 3$) is how you find it.

The palindromic structure is not a coincidence. It encodes the
conformal inversion symmetry $\xi \leftrightarrow 1/\xi$ of the
Poincar\'e disk model---the same M\"obius covariance that makes the
logarithmic energy special (Section~1). The stability threshold is an
algebraic unit \emph{because} the hyperbolic energy is conformally
covariant.

\subsection*{The field extension pattern}

The pattern generalizes. For each $N \ge 8$, there is a threshold
$\xi^*(N)$ at which the $N$-gon first becomes stable on the hyperbolic
plane. Each threshold lives in a quadratic number field determined by
a simple rule:
\begin{itemize}
\item For even $N$: the field is
  $\mathbb{Q}(\sqrt{\mathrm{squarefree}(N-1)})$.
\item For odd $N$: the field is
  $\mathbb{Q}(\sqrt{\mathrm{squarefree}(N-3)})$.
\end{itemize}
Here ``squarefree'' means: divide out all perfect-square factors. So
$\mathrm{squarefree}(9) = 1$ (since $9 = 3^2$), and
$\mathrm{squarefree}(12) = 3$ (since $12 = 4 \times 3$).

The special case $N = 10$: $\mathrm{squarefree}(9) = 1$, so the
threshold lives in $\mathbb{Q}(\sqrt{1}) = \mathbb{Q}$. It is a plain
rational number: $\xi^*(10) = 1/7$. The number~$7$ appearing here is
the flat-plane threshold making a cameo in the hyperbolic setting.

\subsection*{The five algebraic integers}

Among all the thresholds, exactly five are algebraic integers---numbers
that satisfy a monic polynomial equation with integer coefficients:
$N = 8$, $9$, $11$, $15$, and $23$. The sequence of gaps between them
is $1$, $2$, $4$, $8$---powers of two. This is not numerology; it
follows from the divisibility condition $(N - 7) \mid 16$, which comes
directly from the structure of the palindromic equation.

The last algebraic-integer threshold is at $N = 23$:
$\xi^*(23) = \varphi^{-2} = (3 - \sqrt{5})/2 \approx 0.382$,
where $\varphi = (1 + \sqrt{5})/2$ is the golden ratio. Its
palindromic equation is $\xi^2 - 3\xi + 1 = 0$, with trace $c = 3$.
The trace $c = 2$ would give a degenerate equation ($(\xi - 1)^2 = 0$),
so $c = 3$ is the smallest trace compatible with two distinct positive
roots. The golden ratio is the algebraically simplest non-trivial
palindromic unit. The sequence terminates here; no further
algebraic-integer thresholds exist beyond $N = 23$.

\textbf{Status: (Proved)}---the field extension pattern, the
algebraic-integer classification, and the golden ratio terminal case
are all proved in Paper~I, \S6.


%% ====================================================================
\section{The stability phase diagram}
\noindent\textit{[Technical reference: Paper~I, \S7]}

\medskip

\noindent\textit{Bottom line: The stability boundaries form a
topological phase diagram with sharp, undeformable transitions.}

\medskip

Combine everything above. Each surface has its own $C_1$. Each $N$
has its own critical mode. Together they carve the $(N, C_1)$ plane
into stability zones. Inside a zone, the polygon is stable. Cross a
boundary, it is not.

These boundaries are not fuzzy. They are not approximate or subject
to perturbative corrections. They are sharp---a mathematical fact. The
Morse index (the count of unstable modes) is an integer, and it jumps
by exactly one at each boundary. At $N = 7$ on a flat surface, the
Morse index is zero (no unstable modes). At $N = 8$, it jumps to
three: three independent wobble patterns ($m = 3$, $4$, and $5$) are
simultaneously unstable. For general $N \ge 8$, the Morse index is
$N - 5$.

As the curvature parameter $\xi$ increases from $0$ (flat) toward $1$
(infinite negative curvature), each unstable mode crosses zero
\emph{exactly once}---it goes from negative (unstable) to positive
(stable). The crossings happen at the palindromic thresholds:
$\xi^*(8) = 8 - 3\sqrt{7}$, then $\xi^*(9) = 5 - 2\sqrt{6}$, then
$\xi^*(10) = 1/7$, and so on, each value sitting in its own algebraic
number field. The total number of crossings (the spectral flow) equals
the flat-plane Morse index $N - 5$.

Paper~I, Appendix~A shows that these stability phases have a
topological character. The technical tool is $K$-theory---a branch of
algebra that classifies objects by properties that cannot be smoothly
deformed away. In the same way that a sphere and a torus are
topologically distinct (you cannot deform one into the other without
tearing), the stability zones on either side of a palindromic
threshold are $K$-theoretically distinct. The Morse index
$[\mu_N(\xi)]$ defines a class in $K_0(C^*(\mathbb{Z}_N))$, the
$K$-theory group of the symmetry algebra. That class is locally
constant: it does not change under small perturbations. It changes
only when an eigenvalue crosses zero---at a palindromic threshold.

The practical consequence: the boundaries cannot be smoothed away. If
you perturb the system---change the interaction slightly, add a small
asymmetry, introduce a bounded coupling between Fourier modes---the
boundaries shift, but they do not disappear. The Morse index is
topologically protected. Standard Fredholm theory guarantees that any
bounded perturbation with $V(0) = 0$ preserves the spectral flow.

On quotient surfaces (the hyperbolic plane modded out by a discrete
group, producing a compact surface with higher genus), the thresholds
acquire a correction from the surface topology. The correction is
computed by the Selberg trace formula---the same tool that counts
prime geodesics. Each non-trivial geodesic on the surface contributes
a term to $C_1$ whose strength is proportional to
$\operatorname{csch}^2(\ell/2)$, where $\ell$ is the geodesic
length. Short geodesics contribute more; long geodesics are
exponentially suppressed.

On the Bolza surface---a particular compact hyperbolic surface of
genus $2$ with the largest automorphism group ($48$ elements) of any
genus-$2$ surface---the correction is small: $|\delta C_1| < 0.011$.
The fractional Morse index at the palindromic threshold is
$\tau(P_-) = 0.644$, meaning roughly $64\%$ of the stability weight
is in the unstable direction. This number is computed from the
Fuchsian group of the surface, bounded rigorously by the Selberg
trace formula, and verified numerically.

The stability phase diagram, taken as a whole, is a map of the
$(N, \xi)$ plane divided into regions by algebraic curves. The curves
are palindromic polynomials. The regions are labeled by Morse indices.
The labels are topological invariants. And the algebra connecting
surface to surface---the field extensions, the Pell units, the
geodesic correspondence---is not decorative. It is the scaffolding
that holds the later papers together.

\textbf{Status: (Proved)}---the spectral flow, the $K_0$ index
pairing, and the Bolza bound are proved in Paper~I, Appendix~A.
The higher-genus palindromic hierarchy (Conjecture~1 in Paper~I)
remains a conjecture.


\part{The Physics of Vortex Rings}


\section{How vortices actually move}
\noindent\textit{[Technical reference: Paper~II, \S1--3]}

\medskip

\noindent\textit{Bottom line: Point vortices have no inertia---they go
wherever the other vortices push them.}

\medskip

A vortex is not a particle. It is a localized patch of spinning fluid:
a whirlpool, a cyclone, a tornado. It has no mass. It does not
accelerate. Drop a ball and gravity pulls it; the ball gains speed,
picks up momentum, resists being stopped. A vortex does none of this.
It moves at whatever velocity the surrounding flow dictates, instantly,
with no lag.

This is the single most important fact about vortex dynamics, and it is
the fact that makes vortex systems fundamentally different from the
particle systems physicists usually study. Newton's second law says
$F = ma$: force equals mass times acceleration. For a vortex, $m = 0$.
There is no second derivative. The equation of motion is first-order:
velocity, not acceleration. The vortex goes where the flow tells it to
go, right now, with no memory of where it was heading a moment ago.

The equations governing $N$ point vortices on a surface are Hamiltonian
---they conserve energy---but the Hamiltonian plays a dual role. In
ordinary mechanics, the Hamiltonian generates the time evolution of
both positions \emph{and} momenta. For vortices, there are no momenta.
The positions are the entire state. The Hamiltonian (the energy) is a
function of the positions alone, and it directly prescribes the
velocities.

What is that energy? The interaction between two point vortices on a
flat surface is the logarithm of their separation:
$H = -\sum_{j<k}\ln|z_j - z_k|$. Each pair contributes. If you have
six vortices, there are fifteen pairs; each pair pulls logarithmically.
The total energy is the sum. On a curved surface---a sphere, a
hyperbolic plane---the logarithm is replaced by the Green's function
of the Laplacian on that surface, but the structure is the same: a sum
of pairwise interactions, each determined by the surface geometry.

Three quantities are conserved. The energy $H$ itself. The angular
impulse $L = \sum|z_k|^2$, which measures the total spin-weighted
spread of the vortices around the origin---think of it as a rotational
moment of inertia. And the linear impulse $\mathbf{P} = \sum z_k$, the
center of vorticity. These conservation laws constrain the motion.
The vortices cannot wander freely through configuration space; they are
confined to the surface defined by $\{L = \text{constant},\;
\mathbf{P} = 0\}$. That surface has $2N - 3$ dimensions (start with
$2N$ coordinates, subtract three constraints).

The regular $N$-gon---$N$ identical vortices equally spaced around a
circle---is a critical point of the energy on this constraint surface.
Paper~I proved it is a \emph{minimum}, not a maximum or saddle. Paper~II
makes this into physics: the polygon is the equilibrium configuration
that a vortex system naturally settles into. It is the bottom of the
bowl.

There is a specific deformation path through configuration space that
Paper~II analyzes in detail: the centered logarithmic spiral. Start
with the regular $N$-gon. Deform it so the vortices spiral inward,
each one shifted slightly toward or away from the center in a pattern
that respects the $\mathbb{Z}_N$ symmetry. This spiral is the unique
$\mathbb{Z}_N$-equivariant perturbation tangent to a M\"obius orbit
---a mouthful, but the idea is straightforward: it is the most
symmetric way to deform the polygon while keeping the overall rotational
pattern intact.

Along this spiral, the energy curvature $H''(0)$ is strictly negative.
The polygon sits at a maximum of the unconstrained energy along the
spiral direction. But the spiral does not stay on the constraint
surface---it violates the angular-impulse conservation at second order.
Once you project back onto the constraint surface (the physically
accessible configurations), the maximum becomes a minimum. The
Lagrange multiplier from the angular-impulse constraint flips the sign.
This is the core of the constrained-minimum result: the polygon is
simultaneously an unconstrained saddle and a constrained minimum. Both
statements are true; they describe different slices through the energy
landscape.

The Onsager statistical-mechanics picture operates on the unconstrained
landscape, where negative temperatures drive vortices to cluster into
the highest-energy configurations accessible at given conserved
quantities. The constrained-minimum picture operates on the physical
surface. The two viewpoints are complementary: Onsager explains
\emph{why} vortices end up at the polygon (it is the energetically
favored state at negative temperature); the constrained Hessian
explains \emph{why} the polygon is stable once they get there.

\textbf{Status: (Proved)}---the constrained-minimum result is
Theorem~1 of Paper~II, using the Havelock eigenvalue identity from
Paper~I.


%% ====================================================================
\section{Stabilizing the unstable: central vortices and the
\texorpdfstring{$N=7$}{N=7} bifurcation}
\noindent\textit{[Technical reference: Paper~II, \S4--5]}

\medskip

\noindent\textit{Bottom line: For eight or more vortices, a strong
central vortex can rescue an otherwise unstable polygon.}

\medskip

At $N = 8$, the ring polygon on a flat surface is unstable. The most
dangerous wobble mode ($m = 4$, the one that splits the octagon into
two interleaved squares) has eigenvalue $\lambda_4 = 7 - 8 = -1$.
Negative. The polygon flies apart.

But suppose there is a central vortex---a strong spinning core sitting
at the exact center of the ring. Every planet with a polar polygon has
one: Jupiter has a massive central cyclone at each pole, surrounded by
the ring of smaller cyclones. The central vortex adds a positive shift
to every eigenvalue. If it is strong enough, it pushes every eigenvalue
back above zero.

Two effects contribute to this shift. First, the Lagrange multiplier
changes: the central vortex alters the constraint surface, and the
angular-impulse condition shifts in a way that adds $+\kappa_0$ to each
eigenvalue, where $\kappa_0$ is the circulation of the central vortex.
Second, the central vortex itself exerts a radial force on each ring
vortex; the Hessian of this interaction projected onto the critical
eigenvector adds another $+\kappa_0$. Total shift: $+2\kappa_0$.

Setting the shifted eigenvalue to zero and solving gives the critical
central-vortex strength. The formulas are clean:
\begin{itemize}
\item For even $N$: $\kappa_{\mathrm{crit}} = (N^2 - 8N + 8)/16$.
\item For odd $N$: $\kappa_{\mathrm{crit}} = (N-1)(N-7)/16$.
\end{itemize}
The first few values: for $N \le 7$, the critical strength is zero---no
central vortex needed. For $N = 8$: $\kappa_{\mathrm{crit}} = 1/2$.
For $N = 9$: $\kappa_{\mathrm{crit}} = 1$. For $N = 10$: $7/4$.
Each step demands a stronger central vortex.

Paper~II also proves that multi-ring configurations---two or more
concentric polygons---cannot achieve what a single central vortex can.
If the outer ring is stable ($N \le 7$), adding an inner ring of
vortices \emph{destabilizes} it. If the outer ring is already unstable
($N \ge 8$), an inner ring reduces but never eliminates the
instability. Only a single vortex at the $\mathbb{Z}_N$ fixed point
(the dead center) provides the uniform eigenvalue shift needed for full
stabilization. This is the center-ring dichotomy, and it matches
observation: Jupiter's south pole has eight cyclones in a ring with one
central cyclone, not two nested rings.

\subsection*{The BEC prediction}

In a Bose--Einstein condensate---an ultracold quantum gas where atoms
occupy the same quantum state---vortices carry quantized circulation.
The circulation is an integer multiple of $h/m_{\mathrm{atom}}$
(Planck's constant divided by the atomic mass). You cannot have half a
quantum of circulation.

This quantization turns the continuous threshold
$\kappa_{\mathrm{crit}} = 1/2$ into a sharp, integer-valued prediction.
A ring of eight unit-winding vortices in a BEC has
$\kappa_{\mathrm{crit}} = 1/2$. The smallest possible central vortex
has winding number $q_0 = 1$, which exceeds $1/2$. So: a ring of eight
vortices should be unstable alone and stable upon adding a single central
vortex. No free parameters. No fitting. The healing length of the
condensate (the ``blob width'' of the vortex core) is so small compared
to the ring radius that the correction from finite core size is less
than one part in ten thousand.

For $N = 9$: $\kappa_{\mathrm{crit}} = 1$, so a single central vortex
with $q_0 = 1$ is marginally stable; solid stability requires $q_0 \ge 2$.
For $N = 10$: $\kappa_{\mathrm{crit}} = 7/4$, so $q_0 = 1$ fails and
$q_0 \ge 2$ is needed. Each step up in $N$ demands a stronger
central vortex, and the quantization of circulation makes the
predictions binary: stable or not, with no wiggle room.

\subsection*{The $N = 7$ quartic resolution}

The case $N = 7$ sits at the boundary. The minimum eigenvalue is
$\lambda_3 = 0$: the linear restoring force has vanished. To determine
stability, Paper~II computes the next term in the Taylor expansion---the
quartic (fourth-order) coefficient. If this coefficient is positive,
the marble sits in a flat-bottomed bowl that curves back up; if
negative, the flat bottom tilts away and the marble slides off.

The computation is exact. No numerical approximation, no floating-point
rounding---exact rational arithmetic throughout. The positions of the
seven vortices are seventh roots of unity (complex numbers on the unit
circle), the interaction is the logarithm, and every intermediate
quantity is an algebraic number. The Galois group $\mathbb{Z}_7$ acts on
these algebraic numbers and forces the final answer to be rational (a
$\mathbb{Z}_7$-invariant sum of elements in $\mathbb{Q}(\zeta_7)$
must lie in the fixed field $\mathbb{Q}$).

The unconstrained quartic coefficient is $153/7$. The constraint-surface
correction comes from a log-scaling identity: projecting back onto the
angular-impulse surface produces an additive correction of $-18/7$. The
constrained quartic is therefore $153/7 - 18/7 = 135/7$, and the
normal-form coefficient is $\alpha_0 = (135/7)/6 = 45/14 \approx 3.21$.

Positive. The flat-bottomed bowl curves back up. The heptagon is stable.

The physical consequence: the $N = 7$ polygon has a quartic restoring
force ($\propto x^3$) instead of the standard linear restoring force
($\propto x$). Perturb the hexagon, and it snaps back quickly---linear
spring. Perturb the heptagon, and it drifts back slowly---the
amplitude-dependent oscillation frequency vanishes as the displacement
goes to zero. In a BEC experiment, this would show up as an oscillation
period that grows without bound as the perturbation amplitude shrinks, a
signature distinct from the constant-period oscillation of the hexagon.
The predicted frequency for $^{87}$Rb in a $50\;\mu\text{m}$ trap:
$\omega \approx 24\,\varepsilon^2$ per second, where $\varepsilon$ is
the fractional radial displacement. At $5\%$ displacement, the period
is about $100$ seconds---measurable with current BEC imaging.

\textbf{Status: (Proved)}---the central-vortex threshold is
Proposition~4 of Paper~II. The quartic coefficient
$\alpha_0 = 45/14 > 0$ is proved in exact rational arithmetic.
The BEC predictions are \textbf{(Derived)}---they follow from the
quantization of circulation in a condensate.


%% ====================================================================
\section{From perfect to realistic}
\noindent\textit{[Technical reference: Paper~II, \S6--9]}

\medskip

\noindent\textit{Bottom line: The idealized results survive every
realistic correction---finite cores, planetary rotation, and random
circulation variations.}

\medskip

Everything so far treats vortices as mathematical points: zero size,
infinite vorticity concentration, a singularity at the center. Real
vortices---storms on Jupiter, quantum vortices in a condensate---have
finite cores. Saturn's hexagonal jet is not a ring of point vortices;
it is a continuous undulation of a zonal wind. The circulations of
Jupiter's eight polar cyclones are not identical; Juno imagery shows
peak wind speeds ranging from $60$ to $100\;\text{m/s}$ and diameters
from $4{,}000$ to $7{,}000\;\text{km}$.

Paper~II addresses each of these objections with a separate bridge
between the idealized theory and the physical system.

\subsection*{The blob bridge: finite vortex cores}

Replace each point vortex with a smooth ``blob''---a Gaussian bump, a
Cauchy profile, any smooth distribution of vorticity with a finite core
radius $\varepsilon$. The interaction between two blobs at distance $d$
is not exactly $-\ln d$; it picks up a correction of order
$\varepsilon^2/d^2$. The question is whether this correction changes
the stability.

Paper~II proves that it does not, at leading order. The constrained
eigenvalues shift by $O(\varepsilon^2)$, and the shift has two pieces:
a universal Lagrange-multiplier shift $-(N^2 - 1)/12$ (the same for
every Fourier mode, coming from the fact that the equilibrium itself
shifts when the blobs have finite size) and a mode-dependent Hessian
projection $P_m$ (from the modified pairwise interaction). Both pieces
are exact rational numbers, computed in closed form.

The Cauchy blob is the slowest-converging profile---it has algebraic
tails ($\sim r^{-2}$) and infinite second moment, so its Fourier
transform decays at rate $\varepsilon$ rather than $\varepsilon^2$.
Any profile with a finite second moment (Gaussian, compact support,
anything smoother than Cauchy) converges \emph{faster}. So the Cauchy
blob is the worst case, and the stability conclusions proved for
Cauchy carry over \emph{a fortiori} to all smoother profiles. This
profile independence is not a lucky accident; it follows from the
structure of the Fourier transform at low wavenumber.

Three specific consequences. For $N \le 5$: the eigenvalue correction
is $c_m = -1$ (the Lagrange shift dominates), so the eigenvalues
decrease by $\varepsilon^2$, but since the point-vortex eigenvalue is
at least $1$, stability holds for any $\varepsilon < 1$. For $N = 6$:
$c_m > 0$, so the blob correction actually \emph{enhances} stability---
the hexagon is more stable with finite cores than with point vortices.
For $N = 7$: $c_m = +4$, so the marginal eigenvalue
$\lambda_3 = 0 + 4\varepsilon^2 = 4\varepsilon^2 > 0$ for any nonzero
core size. The marginal stability of the point-vortex heptagon is an
artifact of the zero-size limit; any finite core stabilizes it.

The full infinite-dimensional analysis (Appendix~A of Paper~II) goes
further: it shows that the blob \emph{shape} deformations---internal
wobbles of the vortex core---are unconditionally stable. The self-energy
of a mean-zero shape perturbation scales as $1/\varepsilon^2$ (by a
Poincar\'e inequality), so shape modes are stiff; the coupling between
shape and center-of-mass degrees of freedom is quadrupole-order
($O(\varepsilon^2/d^3)$) and vanishes in the point-vortex limit. No
eigenvalue is lost, no sign is changed, no new instability arises from
the infinite-dimensional shape freedom. The point-vortex stability
eigenvalues control the full problem.

\subsection*{The Rossby bridge: Saturn's hexagon}

Saturn's hexagon is not a ring of discrete cyclones. It is a standing
wave on a zonal jet---a continuous ribbon of wind that undulates into
six lobes. The mechanism connecting this wave to the point-vortex
picture is the Rossby bridge.

On a rotating planet, the vorticity gradient $\beta$ (from the
variation of the Coriolis force with latitude) supports Rossby waves
---large-scale meanders of the jet stream. A stationary Rossby wave
has azimuthal wavenumber
$n^* = R\cos\varphi\,\sqrt{\beta_{\mathrm{eff}}/U_{\max}}$,
where $R\cos\varphi$ is the ring radius at latitude $\varphi$,
$U_{\max}$ is the peak jet speed, and $\beta_{\mathrm{eff}}$ includes
both the planetary vorticity gradient and the jet curvature. For Saturn
at $76^\circ$N, the jet curvature dominates $\beta$ by a factor of
about thirteen. Plugging in Cassini-measured values:
$n^* \approx 5.4$ from the leading-order dispersion relation. The
nearest integer is $6$---the wave must fit an integer number of
wavelengths around the planet. (The discrepancy between $5.4$ and $6$
is within the measurement uncertainty of the jet width; a $15\%$
narrower jet gives $n^* = 6.0$ exactly.)

The connection to the point-vortex energy landscape proceeds through
the critical layer of the saturated wave. At finite amplitude, the
stationary Rossby wave develops a cat's-eye structure: vorticity is
trapped into $N$ lobes, each a spinning patch of fluid near a vertex
of the polygon. As the jet sharpens, these lobes concentrate into $N$
isolated patches. In the Prandtl--Batchelor limit (the physically
relevant model for Saturn), the vorticity is constant inside each lobe
and constant outside; the separatrix between lobes carries zero
circulation. The lobes \emph{are} the vortices, and the blob bridge
proves that their finite size does not change the stability
eigenvalues.

Both planets arrive at the same energy landscape through different
limiting procedures. Jupiter concentrates $N$ discrete cyclones into
point vortices (blob radius $\varepsilon \to 0$). Saturn concentrates a
continuous jet meander into $N$ patches (jet width $\sigma \to 0$).
Both limits converge to the Havelock spectrum.

The dimensionless number $\beta R^3/\kappa$ separates the two regimes.
When $\beta R^3/\kappa \ll 1$, vortex interactions dominate Rossby-wave
propagation: this is Jupiter ($\beta R^3/\kappa \approx 0.6$). When
$\beta R^3/\kappa \gg 1$, the wave mechanism dominates: this is Saturn
($\beta R^3/\kappa \approx 7.6$).

\subsection*{The deformation radius: stratification effects}

On a rotating planet with density stratification, the vortex
interaction is modified by the Rossby deformation radius $L_R$. The
Green's function transitions from $-\ln r$ (at separations much less
than $L_R$) to exponential decay $K_0(r/L_R)$ (at separations much
greater than $L_R$). The exponential tail destroys long-range
correlations between non-adjacent vortices, which are what hold the
polygon together.

The polygon-suppression transition: for $N \le 5$, the polygon is
stable at any ratio $R/L_R$. For $N = 6$, the polygon is stable only
if $R/L_R < 1.29$---the ring radius must be less than about $1.3$
deformation radii. For $N = 7$, any finite $R/L_R$ destabilizes the
marginally stable heptagon. For $N \ge 8$, the polygon is already
unstable in the barotropic limit.

Saturn's hexagon persists because Saturn's barotropic column runs deep.
Cassini Grand Finale gravity measurements constrain the differential
rotation to a depth of $8{,}000$--$9{,}000\;\text{km}$, implying a
coherent column of order $10^4\;\text{km}$---far exceeding the
hexagon's vertical scale. The effective deformation radius is large
enough that $R/L_R$ stays below the critical ratio.

Jupiter's situation is less clean. Juno gravity measurements constrain
the differential rotation to about $3{,}000\;\text{km}$ depth ($4\%$ of
Jupiter's radius), far shallower than Saturn's. Whether Jupiter's polar
cyclones sit on deep barotropic Taylor columns or in a shallow weather
layer is not yet settled. The $N = 8$ prediction requires the deep
limit ($R/L_R \lesssim 0.8$, i.e.\ $L_R > 9{,}700\;\text{km}$). A
direct measurement of the depth of the polar cyclone roots would
resolve this; the computation sets a quantitative target.

\subsection*{Circulation disorder: imperfect vortices}

Jupiter's eight polar cyclones are not identical. Their peak wind speeds
vary by a factor of nearly two; their diameters range by $75\%$. The
coefficient of variation in circulation is roughly $15$--$25\%$.
Saturn's hexagonal jet is more uniform (about $5\%$ variation).

Paper~II models this as random perturbations:
$\kappa_k = \kappa(1 + \eta_k)$, where $\eta_k$ is drawn from a
Gaussian with standard deviation $\sigma_\eta$. Monte Carlo
simulations over $1{,}000$ realizations compute the probability that
the minimum eigenvalue drops below zero.

The result: the stability domain is a domain, not a knife-edge.
For $N = 6$ at physically relevant disorder ($\sigma_\eta \le 0.2$),
the probability of instability stays below $10\%$. For $N = 8$ on the
hyperbolic plane (curvature-stabilized), the probability stays below
$15\%$ at Jupiter's estimated disorder level. The $N = 7$ marginal ring
is more sensitive---the marginal mode is the first to be destabilized---
but even there, at Jupiter's estimated $\sigma_\eta \approx 0.2$, a
hypothetical $N = 7$ ring would have essentially zero probability of
instability. The sensitivity appears only at unphysically large
disorder ($\sigma_\eta \ge 0.5$).

Configurations comfortably inside the stable region remain stable under
the circulation fluctuations observed at Jupiter and Saturn. The
$N_{\mathrm{crit}}$ boundary holds up.

\textbf{Status: (Proved)}---the blob bridge and profile independence are
mathematical theorems (Proposition~5 of Paper~II). The Rossby bridge
Saturn prediction is \textbf{(Derived)}---it depends on Saturn's
measured jet parameters. The circulation disorder results are
\textbf{(Derived)}---Monte Carlo with controlled statistical errors.


%% ====================================================================
\section{Vortices meet gravity}
\noindent\textit{[Technical reference: Paper~II, \S10--12]}

\medskip

\noindent\textit{Bottom line: In $2{+}1$ dimensional gravity, a point
mass and a point vortex are the same mathematical object.}

\medskip

This section is the bridge to everything that follows in Papers~III
through VI. The claim is precise, so I want to state it carefully.

In $2+1$ dimensional general relativity---gravity in a universe with
two spatial dimensions plus time---a point mass does something
peculiar. It does not create a gravitational well in the Newtonian
sense; there is no $1/r$ force, no gravitational waves, no propagating
degrees of freedom. What it creates is a \emph{conical deficit angle}.
Take a flat sheet of paper. Cut out a wedge. Tape the edges together.
The result is a cone: flat everywhere except at the tip, where the
angular range is less than $360^\circ$. The missing angle is the
deficit. For a point mass $m$ in $2+1$D gravity, the deficit angle is
$\varepsilon = 8\pi G m$, proportional to the mass.

A point vortex on a two-dimensional surface creates the same kind of
singularity in the stream function. The circulation $\kappa$ plays
the same role as the deficit angle. Both objects satisfy the same
equation: the Poisson equation for the two-dimensional Laplacian,
$\nabla^2\Phi = (\text{source strength})\cdot\delta(z - z_0)$.
Both have the logarithm as their Green's function.

The identification is: deficit angle $= 2\pi\kappa$, or equivalently,
$\kappa = 8\pi Gm/(2\pi) = 4Gm$. Once you make this substitution, the
vortex Hamiltonian $H = -\sum_{j<k}\ln|z_j - z_k|$ and the
gravitational interaction energy
$E_{\mathrm{grav}} = -(8\pi G)^2\sum_{j<k}m^2 G_S(z_j, z_k)$ are
the same function up to a positive multiplicative constant. The
equations of motion are the same differential equations. The Havelock
eigenvalues are the same numbers. The stability thresholds---$N = 7$
on a flat surface, the palindromic hierarchy on curved surfaces---are
the same thresholds.

I want to be precise about what this identification is and is not. It
is not an analogy. It is not a metaphor. It is not a qualitative
resemblance. The two systems share the same Green's function, the same
Poisson equation, and therefore the same energy landscape up to overall
scale. The Hessian matrices are proportional; the eigenvalues are
proportional; the stability boundaries are identical. Anything proved
about the vortex system transfers verbatim to the gravitational system,
and vice versa.

There is a difference in dynamics. Vortex equations of motion are
first-order: the Hamiltonian directly prescribes velocity. Gravitational
equations of motion are second-order: there is kinetic energy, there is
potential energy, and the Hamiltonian generates acceleration. But the
stability criterion---whether the polygon is an energy extremum---depends
only on the \emph{potential energy landscape}, not on the kinetic term.
A positive eigenvalue means stable oscillation in the gravitational case
and stable circulation in the vortex case. A negative eigenvalue means
exponential growth in both. The criterion $N \le N_{\mathrm{crit}}$ is
identical.

Paper~II also addresses the backreaction question. Each point mass
creates a conical deficit; does the accumulation of deficit angles
distort the geometry enough to invalidate the flat-plane calculation?
The answer involves the \emph{developing map}---a conformal isometry
from the cone surface to the Euclidean plane, constructed using a
hypergeometric function. The key result: the developing map preserves
$\mathbb{Z}_N$ symmetry (the image of the $N$-gon is still a regular
$N$-gon), and the two-dimensional Green's function is conformally
invariant ($G(z,w) = -(2\pi)^{-1}\log|z - w|$ in conformal coordinates,
regardless of the cone angles). Consequently, the Havelock eigenvalues
are $(N-1) - m(N-m)/2$ for all deficit angles in the allowed range
$Gm \in [0, 1/(2N))$. The stability threshold $N = 7$ does not shift.
The deficit angles do not backreact on the stability.

At the endpoint $Gm = 1/(2N)$, the total deficit exhausts the curvature
budget and the spatial slice compactifies to a sphere. On the sphere,
$C_1 = (N-1)\cos\varphi < N - 1$, and $N_{\mathrm{crit}}$ drops to $6$
or below. The transition from $N_{\mathrm{crit}} = 7$ (flat) to
$N_{\mathrm{crit}} \le 6$ (sphere) is discontinuous---it happens at the
topology change, not through a gradual weakening. The cosmological
constant $\Lambda$ (positive for the sphere, negative for the hyperbolic
plane) controls the stability threshold through $C_1$; the deficit
angles do not.

The convergence proof in Appendix~A closes the last gap: it shows that
the infinite-dimensional fluid system (continuous vorticity fields)
converges to the finite-dimensional point-vortex system in the
appropriate limit. The Hessian of the full Euler energy, including
blob shape deformations and center-shape coupling, has the same
inertia (same count of positive, zero, and negative eigenvalues) as
the point-vortex Havelock spectrum. No information is lost in the
idealization. The point-vortex model is not a cartoon; it is the
correct leading-order description.

\textbf{Status: (Proved)}---the vortex-gravity identification
(Proposition~8 of Paper~II), conformal invariance of flat-plane
stability (Proposition~9), and the convergence theorem
(Proposition~10, Appendix~A) are all mathematical results. The
planetary predictions---Saturn's hexagon from the Rossby bridge,
Jupiter's octagon from the central-vortex threshold---are
\textbf{(Derived)}, depending on measured planetary parameters.


\part{How Gravity Emerges}


\section{The graviton is a wobble of seven}
\noindent\textit{[Technical reference: Paper~III, \S1--2]}

\medskip

\noindent\textit{Bottom line: The marginally stable wobble mode at
$N = 7$ carries spin~$2$---the same spin as the graviton.}

\medskip

At $N = 7$ on a flat surface, the most dangerous wobble mode has
$m = 3$ and eigenvalue $\lambda_3 = 0$. This mode does not grow and
does not decay; it sits on the threshold. The question is: what kind
of object does this marginal wobble represent in the language of
particle physics?

Every fundamental particle has a spin---an intrinsic angular momentum
quantum number $j$. Electrons have $j = 1/2$. Photons have $j = 1$.
The graviton, the hypothetical particle that mediates gravity, has
$j = 2$. This is not a choice; it is forced by the structure of
Einstein's field equations, which describe a symmetric rank-2 tensor.

The Havelock Casimir for the $m$-th wobble mode is $f(m, N) = m(N-m)/2$.
In the Chern--Simons formulation of $2{+}1$ dimensional gravity, this
same quantity appears as the quadratic Casimir of the gauge algebra
$\mathfrak{sl}(2, \mathbb{R})$, which determines the spin through the
relation $j(j+1) = f(m, N)$. At $N = 7$, $m = 3$: $f(3, 7) = 6$. And
$j(j+1) = 6$ has the solution $j = 2$.

The marginal mode of the heptagon \emph{is} the graviton.

The identification chain has three links. First: the vortex system on
the hyperbolic plane shares its dynamical symmetry algebra with the
Calogero--Moser--Sutherland model (a well-studied integrable system in
mathematical physics). Second: the Casimir of this algebra, restricted to
the $m$-th Fourier mode of the $\mathbb{Z}_N$-symmetric polygon, equals
$m(N-m)/2$. This is a mathematical fact---proved via the Helgason
symmetric-space theorem, verified numerically to fifteen decimal places.
Third: the Chern--Simons formulation of $2{+}1$D gravity uses the same
algebra $\mathfrak{sl}(2, \mathbb{R})$, so the Casimir of the Wilson
line (the gauge-theoretic description of a polygon in the gravitational
language) is the same $m(N-m)/2$.

Three different mathematical frameworks---vortex dynamics,
Calogero--Moser integrability, Chern--Simons gauge theory---produce the
same Casimir eigenvalue on the same space. This is not a coincidence;
all three act as isometries of the hyperbolic plane. The coincidence
would be if they \emph{disagreed}.

Is $N = 7$ the only polygon that gives spin~$2$? Yes. The condition for
integer spin at the critical mode $m = \lfloor N/2 \rfloor$ reduces to a
Pell equation---a type of Diophantine equation that has been studied
since antiquity. The specific equation is $N^2 - 2y^2 = -1$, where
$y = 2j + 1$. The solutions form a sequence generated by the algebraic
unit $1 + \sqrt{2}$:
\begin{center}
\small
\begin{tabular}{ccl}
\toprule
$N$ & $j$ & Notes \\
\midrule
$1$ & $0$ & Trivial \\
$7$ & $2$ & Graviton \\
$41$ & $14$ & Far beyond stability range \\
$239$ & $84$ & $\ldots$ \\
\bottomrule
\end{tabular}
\end{center}
The first nontrivial solution is $N = 7$, $j = 2$. The next is
$N = 41$, which gives $j = 14$---a spin that does not correspond to any
known particle. And $N = 41$ lies far outside the stability range
$N \le 7$. The graviton appears exactly once, at $N = 7$.

Even-$N$ polygons tell a separate story. At $N = 4$, the Casimir gives
$j = 1$: a spin-1 particle, a vector boson. The photon, the $W$ boson,
the $Z$ boson, and the gluon all have spin~$1$. Paper~IV develops this
connection. At $N = 24$, $j = 8$. No other even $N$ gives $j = 2$.

The Chern--Simons level $k = c/6 = 2\,b(N)$ is the coupling constant of
the gravitational gauge theory. It is not an integer for most $N$, but
this is consistent: the gauge group $\mathrm{SL}(2, \mathbb{R})$ is
non-compact, and non-compact gauge groups do not require integer-quantized
levels. (For the compact gauge sectors that appear later---$\mathrm{SU}(2)$
and $\mathrm{SU}(3)$---the levels \emph{are} integer. Only the
gravitational sector carries the irrational level.)

\textbf{Status: (Proved)}---the CMS--CS Casimir identification is
Proposition~1 of Paper~III. The Pell equation uniqueness is
Proposition~2. The graviton identification follows as a mathematical
consequence.


%% ====================================================================
\section{Why the universe has constant curvature}
\noindent\textit{[Technical reference: Paper~III, \S3--4]}

\medskip

\noindent\textit{Bottom line: Three mathematical conditions force the
scalar curvature to be the same everywhere---which IS Einstein's
equation in $2{+}1$ dimensions.}

\medskip

This is the central result of the series. I want to lay it out with
care.

Einstein's field equations say, roughly: the curvature of spacetime is
determined by the distribution of matter and energy. In a vacuum---no
matter, no energy---the equations simplify. In $2{+}1$ dimensions (two
spatial dimensions plus time), the vacuum Einstein equations reduce to a
single statement: the scalar curvature $R$ is constant everywhere. The
surface curves the same amount at every point. The curvature can be
positive (like a sphere), negative (like a saddle), or zero (flat), but
it must be uniform.

The claim: this uniformity is not an assumption. It is a consequence of
three conditions, each of which has an independent mathematical proof or
physical justification.

\subsection*{The three conditions}

\textbf{(H1) The Havelock structure.} The stability eigenvalues split as
$\lambda_m = C_1 - m(N-m)/2$, where $C_1$ depends on the surface and
$m(N-m)/2$ is universal. This is proved in Paper~I from the $\csc^2$
uniqueness theorem: the logarithmic Green's function on any Riemannian
surface has the angular kernel $\csc^2(\pi p/N)$, whose Fourier
transform is $m(N-m)/2$. No physical assumption required; it is a
property of the Laplacian.

\textbf{(H2) Entropy maximization.} The polygon system has an entropy
functional $S[g]$ that depends on the geometry of the surface. This
entropy is \emph{strictly concave} in the scalar curvature $R$. The
strict concavity is a calculable fact:
$d^2S/dC_1^2 = -(1/2)\sum 1/\lambda_m^2 < 0$
(always negative when all modes are stable), and $C_1$ is affine in $R$
(from the Hadamard expansion of the Green's function), so $S$ is
concave in $R$.

The Onsager selection principle then says: at sufficiently high energy
(negative temperature), the system's macroscopic state is dominated by
the entropy-maximizing configuration. Onsager published this principle
in 1949 to explain why two-dimensional vortex systems spontaneously
form large-scale structures. It is the physical content of condition
(H2)---the one assumption in the argument that is not a mathematical
theorem.

\textbf{(H3) The Hadamard parametrix.} The Green's function on
\emph{any} Riemannian surface has the universal short-distance form
$G(x, y) = -(1/2\pi)\log d(x, y) + \gamma_S(x) + O(d^2)$, where
$d(x, y)$ is the geodesic distance. This is a theorem in differential
geometry---the Hadamard construction---and it guarantees that condition
(H1) holds at every point of the surface, not just at special points.

\subsection*{The proof}

Given strict concavity of $S$ in $R$, Jensen's inequality does the
work. Jensen's inequality is a standard result in analysis, and it says:
for any function that curves downward (concave), the average of the
function is less than or equal to the function of the average. Equality
holds \emph{only} when the argument is constant.

Apply this to the entropy. Integrate $S(R(x))$ over the surface. Because
$S$ is strictly concave in $R$:
\[
  \int_{\text{surface}} S(R(x))\, dA
  \;\le\;
  A \cdot S(\bar{R}),
\]
where $\bar{R}$ is the average curvature and $A$ is the total area.
Equality---maximum entropy---holds if and only if $R(x) = \bar{R}$
everywhere. Constant curvature.

The Onsager principle selects this maximum: among all metrics with the
same total energy, the one with maximum entropy dominates the
statistical ensemble. That metric has $R = \text{constant}$.

In $2{+}1$ dimensions, $R = \text{constant}$ implies
$R_{\mu\nu} = \Lambda g_{\mu\nu}$---Einstein's vacuum equation with
cosmological constant $\Lambda$. This implication is Schur's lemma, a
theorem in Riemannian geometry. The polygon system does not need Schur's
lemma as an input; it produces the \emph{hypothesis} of Schur's lemma
(constant curvature) as an output.

\subsection*{The Onsager contraction}

I can be more specific about how strongly the system is driven toward
uniform curvature. Paper~III defines an iteration: start with a metric
(possibly non-uniform), compute the Onsager equilibrium density of
vortices on that metric, map the density back to a new curvature field
via the vortex-gravity identification, and repeat.

At $N = 7$, the variance of the curvature contracts by a factor of
$0.041$ per step. Start with $\text{Var}(R) = 2$ (a fairly bumpy
surface); after one iteration, $\text{Var}(R) \le 0.082$; after two,
$\text{Var}(R) \le 0.003$. The contraction factor $(1/6)^2 \times 1.48
= 0.041$ comes from two ingredients: $1/6$ is the inverse of the
critical Casimir $f(3, 7) = 6$ (controlling how sensitively the density
responds to curvature variations), and $1.48$ is the nonlinear
correction (bounded by the Sobolev constant $C_S = 0.45$ on the Bolza
surface, a specific genus-2 surface with the largest automorphism group
of any genus-2 surface).

The contraction improves with $N$: $1/8$ at $N = 8$, $1/15$ at
$N = 11$. The uniform metric $R = \text{constant}$ is an attracting
fixed point with an explicitly bounded basin of attraction.

\subsection*{What this means}

Three ingredients---each independently established---force the geometry
to be uniform. The Havelock structure (H1) is a theorem. The Hadamard
parametrix (H3) is a theorem. The Onsager principle (H2) is the physical
hypothesis: the assertion that the vortex-gravity system at high energy
is described by the entropy-maximizing macrostate.

If you accept the identification of vortex circulations with
gravitational deficit angles (established in Paper~II), then the entropy
maximum of the vortex system IS the vacuum Einstein equation. The
polygon does not approximate Einstein's equation; it produces it exactly.

\textbf{Status: (Proved)}---conditional on the Onsager selection
principle (H2b). Conditions (H1) and (H3) are mathematical theorems.
The strict concavity (H2a) and the Jensen inequality argument are
theorems. The Onsager contraction at $N = 7$ (factor $0.041$, basin
$\text{Var}(R) \le 2$) is proved with explicit bounds.


%% ====================================================================
\section{The cosmological constant and the breathing mode}
\noindent\textit{[Technical reference: Paper~III, \S5--7]}

\medskip

\noindent\textit{Bottom line: The cosmological constant---the energy
density of empty space---is the ground-state energy of the polygon's
breathing mode.}

\medskip

The previous section established that the curvature must be constant.
It did not determine the \emph{value} of that constant. The value is the
cosmological constant $\Lambda$, and it comes from a separate equation:
the Wheeler--DeWitt equation on the breathing mode.

\subsection*{The breathing mode}

A regular polygon can breathe. Keep the $\mathbb{Z}_N$ symmetry intact
and expand or contract the ring radius $\rho$. The $N$ vortices move in
and out together, like a diaphragm. This collective expansion is the
breathing mode.

The angular wobble modes ($m = 1, 2, \ldots, N-1$) have the Havelock
eigenvalues $\lambda_m(\rho) = C_1(\rho) - m(N-m)/2$. Each eigenvalue
depends on $\rho$ through the coefficient $C_1$. As the ring expands
($\rho$ increases), $C_1$ grows and the polygon becomes more stable. As
it contracts, $C_1$ drops and modes go unstable. The angular modes act
as a bath: they respond to the breathing, and their collective response
produces an effective potential $V(\rho)$ felt by the breathing mode.

This effective potential is computable. The angular modes' contribution to
the effective action is
$S_{\text{eff}}(\rho) = -(1/2)\sum_m \log|\lambda_m(\rho)|$---the
logarithm of the product of the eigenvalues, which is the standard
one-loop effective potential in statistical mechanics. The
Calogero--Moser--Sutherland integrability of the vortex system makes this
one-loop answer exact: no higher-loop corrections appear, because the
angular modes decouple exactly (their eigenvectors are $\rho$-independent
Fourier modes).

\subsection*{The Wheeler--DeWitt equation}

The effective potential $V(\rho)$ governs the breathing mode through a
second-order equation:
\[
  \left[-\frac{1}{2c}\,\frac{d^2}{d\rho^2}
  + V(\rho)\right]\Psi = 0.
\]
This is the Wheeler--DeWitt (WDW) equation---the fundamental equation
of quantum cosmology, first written down by Bryce DeWitt in 1967. In
standard quantum cosmology, it governs the wave function of the universe
as a function of the scale factor. Here, the scale factor is the polygon
radius $\rho$, and the wave function $\Psi(\rho)$ describes the quantum
state of the breathing mode.

The kinetic coefficient $c = 12\,b(N)$ is not a free parameter. Two
independent arguments fix it.

First: the definition. $b(N)$ is the unique mode-independent offset in
the Havelock eigenvalue decomposition, computed analytically in Paper~I
from the Gauss product formula
$\prod_{p=1}^{N-1}\sin(\pi p/N) = N/2^{N-1}$. Setting $c = 12\,b(N)$
extracts the central charge from the eigenvalue data. At $N = 7$:
$c \approx 51.6$. At $N = 11$: $c \approx 130$.

Second: the Chern--Simons gauge theory calculation. The Brown--Henneaux
central charge of $2{+}1$D gravity is $c = 3\ell/(2G)$. The CMS--CS
Casimir identification (from Section~11) matches the Chern--Simons level
to $k = 2\,b(N)$, giving $c = 6k = 12\,b(N)$. The same value by a
completely different route.

The kinetic term in the WDW equation is not the usual ``mass times
velocity squared'' of a particle. The vortex equations are first-order;
there is no momentum. The $d^2/d\rho^2$ term arises from the
path-integral \emph{measure}---specifically, from the Fisher--Rao
information metric, which quantifies how much information the angular
modes carry about the breathing. Any other value of $c$ would break the
quantum Born--Oppenheimer factorization: the wave function would not
separate into a breathing part times an angular part.

\subsection*{The cosmological constant from $N$}

In the classical limit ($c \to \infty$, the kinetic term vanishes), the
WDW equation reduces to $V(\rho^*) = 0$: the turning point of the
potential. Solving for $\rho^*$ gives the cosmological constant:
\[
  \Lambda = -\frac{4[f(m^*, N) - (N-1)]}{(N-1)\varepsilon^2}.
\]
The sign of $\Lambda$ depends on $N$ through the numerator
$f(m^*, N) - (N-1)$:

\begin{itemize}
\item For $N \le 6$: $f(m^*, N) < N - 1$, so $\Lambda > 0$.
  Positive cosmological constant: de~Sitter space. An expanding
  universe.
\item For $N = 7$: $f(3, 7) = 6 = N - 1$, so $\Lambda = 0$. Flat.
\item For $N \ge 8$: $f(m^*, N) > N - 1$, so $\Lambda < 0$. Negative
  cosmological constant: anti-de~Sitter space. The setting for
  holography and the AdS/CFT correspondence.
\end{itemize}

The flat-plane stability threshold $N = 7$---Havelock's 1931 result---sits
exactly at $\Lambda = 0$. Below the threshold, the universe has positive
curvature; above it, negative. The same number that governs the stability
of vortex polygons governs the sign of the cosmological constant.

\textbf{Status: (Derived)}---the WDW equation follows from the
Born--Oppenheimer separation (exact by CMS integrability) and the
Feynman--Kac correspondence. The kinetic coefficient $c = 12\,b(N)$ is
fixed by two independent routes. The sign of $\Lambda$ as a function
of $N$ is a direct algebraic consequence.


%% ====================================================================
\section{Black holes, holograms, and the full Riemann tensor}
\noindent\textit{[Technical reference: Paper~III, \S8--11]}

\medskip

\noindent\textit{Bottom line: The polygon system contains black hole
thermodynamics, holographic duality, and---with one extra
dimension---the complete geometry of four-dimensional spacetime.}

\medskip

\subsection*{The black hole phase transition}

Heat any system enough and it changes phase. Ice melts. Water boils.
The polygon dissolves into a black hole.

In $2{+}1$ dimensional gravity with negative cosmological constant
(anti-de~Sitter space, $\Lambda < 0$), there exists a black hole
solution: the BTZ black hole, named after Ba\~nados, Teitelboim, and
Zanelli. It is simpler than the Schwarzschild or Kerr black holes of
four dimensions, but it has an event horizon, it has a temperature, and
it has entropy proportional to the horizon area---the Bekenstein--Hawking
entropy formula.

The polygon competes with the BTZ black hole as a possible configuration
for $N$ gravitational sources. At low temperature, the polygon wins: it
has lower Euclidean action (the gravitational analogue of free energy)
than the black hole. At high temperature, the black hole wins. The
crossover temperature is
$T_c = (1/2\pi\ell)\sqrt{1 - 8G\,E_{\text{polygon}}}$,
where $E_{\text{polygon}}$ includes the binding energy of the polygon.
This binding energy---computed from the Gauss product
$\prod\sin(\pi p/N) = N/2^{N-1}$---lowers $T_c$ below the standard
Hawking--Page temperature.

The transition has a second physical mechanism beyond thermodynamics.
For $N > N_{\text{crit}}$, the polygon has a negative Havelock
eigenvalue: a wobble mode that grows rather than decaying. This unstable
mode alternately pushes adjacent masses inward and outward, driving
pairwise coalescence. In the nonlinear regime, the coalescing masses
exceed the BTZ threshold and collapse into a black hole. The polygon
does not just become thermodynamically disfavored; it physically
disintegrates.

The Havelock eigenvalue is doing double duty: it is simultaneously the
stability eigenvalue of the vortex polygon and the Lichnerowicz eigenvalue
of the gravitational perturbation. A negative Havelock eigenvalue IS a
negative mode of the gravitational action. The palindromic threshold
$\xi^*(N)$---the curvature at which the $N$-gon first becomes stable---is
the boundary between the polygon phase and the black hole phase.

\subsection*{Holography}

In anti-de~Sitter space, the bulk (the three-dimensional interior) is
encoded on the boundary (a two-dimensional surface). This is the
holographic principle, made precise by the AdS/CFT correspondence.

The polygon system fits into this framework. $N$ equally-spaced heavy
operator insertions on the boundary circle correspond to $N$ point masses
in the bulk, arranged in a regular polygon. The Havelock decomposition
into Fourier modes $m = 1, \ldots, N-1$ corresponds to the
$\mathbb{Z}_N$ conformal block decomposition of the boundary correlator.
The Casimir $m(N-m)/2$ is the angular stiffness---the repulsive force
between operators in the $m$-th channel. The coefficient $C_1(\xi)$ is
the radial confinement---the restoring force from the bulk geometry.
Stability ($\lambda_m > 0$) means the confining potential exceeds the
OPE repulsion in every channel.

The Clausius relation connects the polygon's entropy to the boundary
CFT. The Cardy formula gives the boundary entropy
$S_{\text{Cardy}} = (\pi c/3)\,T\,L$,
where $c = 12\,b(N)$ is the central charge, $T$ is the temperature, and
$L$ is the boundary circumference. The Ryu--Takayanagi formula then
determines Newton's constant from the central charge:
$G = 3\ell/(2c)$---the Brown--Henneaux relation.

\subsection*{The full four-dimensional geometry}

In $2{+}1$ dimensions, the Weyl tensor vanishes identically. There are
no propagating gravitational degrees of freedom---no gravitational waves,
no tidal forces. All gravitational information is in the Ricci tensor,
which the polygon-Einstein theorem determines.

Four-dimensional spacetime has ten independent Weyl components---the
pieces of the Riemann curvature tensor that survive in vacuum, that
carry gravitational waves, that cause tidal stretching and squeezing.
To get from $2{+}1$ to $3{+}1$ dimensions, Paper~III adds one compact
dimension: the circle fiber of a Seifert manifold. The resulting
four-dimensional Riemann tensor is determined in three layers.

\textbf{Layer 1: Background.} The three-dimensional metric satisfies
$R_{\mu\nu}^{(3)} = \Lambda_3\, g_{\mu\nu}$ (from the polygon-Einstein
theorem). The O'Neill formula for the Riemannian submersion decomposes
the three-dimensional Riemann tensor into base curvature plus terms
quadratic in the fiber connection. The fiber radius is stabilized and
the flux is quantized---both determined by $N$.

\textbf{Layer 2: Linearized perturbations.} The Lichnerowicz operator
(the gravitational wave equation linearized around the background)
decomposes by $\mathbb{Z}_N$ symmetry into Fourier modes. Each mode's
eigenvalue is the Havelock eigenvalue $\lambda_m = C_1 - m(N-m)/2$,
determined by $N$. The graviphoton (from the Kaluza--Klein decomposition)
has a spectral gap $\ge 1$. The radion (the breathing mode of the
compact circle) is massive with $m^2 = N^2/4$. All perturbation sectors
are gapped and determined by $N$.

\textbf{Layer 3: Nonlinear reconstruction.} The Fefferman--Graham
expansion reconstructs the bulk metric from the boundary stress tensor.
For a two-dimensional boundary, this expansion terminates at finite
order---it is exact, not approximate. The boundary stress tensor is
determined by the Virasoro algebra at central charge $c = 12\,b(N)$,
which is determined by $N$. Therefore the full bulk metric, and hence the
full four-dimensional Riemann tensor, is determined by $N$.

Paper~III states this as a theorem: on the Seifert manifold
$\mathbf{H}^2 \times_N S^1$ with stabilized fiber and quantized flux,
the full $4$D Riemann tensor---all ten Weyl components---is uniquely
determined by the single integer $N$.

\textbf{Status: (Derived)}---the BTZ phase transition, the holographic
dictionary, and the three-layer Riemann theorem are proved within the
framework. The framework itself rests on the vortex-gravity
identification (Paper~II) and the Onsager principle (H2b).


%% ====================================================================
\section{From statistics to quantum mechanics}
\noindent\textit{[Technical reference: Paper~III, \S12--13]}

\medskip

\noindent\textit{Bottom line: The classical statistical mechanics of
the polygon IS quantum mechanics---not by analogy, but by mathematical
identity.}

\medskip

The Feynman--Kac theorem is a mathematical result from the 1940s. It says
that a certain thermal average (a path integral with Boltzmann weights,
the kind used in statistical mechanics) equals a certain quantum
propagator (the matrix element of $e^{-Ht}$, the kind used in quantum
mechanics). The correspondence maps temperature to imaginary time,
partition function to propagator, thermal fluctuation to quantum
tunneling.

This is not a metaphor or a loose analogy. It is a theorem with
hypotheses and a proof, and the hypotheses are satisfied by the polygon
system.

The polygon's breathing mode $\rho$ fluctuates in the bath of angular
modes. The bath is Gaussian (the Havelock diagonalization is exact), the
system-bath coupling is uniform ($\partial\lambda_m/\partial\rho =
\Phi'(\rho)$ is the same for every mode $m$), and there is a clean
separation of timescales (the tunneling time is exponentially longer than
the bath correlation time). These are the conditions for the
Caldeira--Leggett model---the standard framework for studying how a
quantum system interacts with its environment.

Under the Feynman--Kac correspondence, the Onsager--Machlup path
integral (thermal fluctuations of $\rho$ in the angular bath) maps
directly to the Wheeler--DeWitt propagator (quantum evolution of the
breathing mode). The transfer matrix of the classical statistical system
is the Hamiltonian of the quantum system. The Boltzmann weight
$e^{-\beta V}/Z$ is the Born rule $|\Psi_0|^2$. These are not
analogies between similar-looking expressions; they are the same
mathematical object described in two languages.

\subsection*{Self-decoherence}

In quantum mechanics, decoherence is the process by which a quantum
system loses its ability to exhibit interference. A particle that can
go through two slits simultaneously stops doing so when it interacts with
an environment that records which slit it took. The environment does not
need to be an observer or a measurement device; it can be any collection
of degrees of freedom coupled to the system.

The polygon provides its own environment. The $N - 2$ angular Havelock
modes (all modes except the marginal one and the breathing mode) form a
Caldeira--Leggett bath. They couple to the breathing mode through the
$\rho$-dependence of the eigenvalues. The decoherence rate $\gamma$ is
computed from the system-bath coupling:
\[
  \gamma = \frac{[\Phi'(\rho)]^2}{4}
  \sum_{m \ne m^*} \frac{1}{\lambda_m^2}.
\]
At the physical operating point ($N = 7$ or $N = 11$ at the stability
threshold), $\gamma \gg 1$: the breathing mode is strongly decohered.
The off-diagonal elements of the reduced density matrix are exponentially
suppressed. The system selects its own pointer basis---the $\rho$-basis,
the radius of the polygon ring.

Near the stability threshold, the bath mode closest to marginal
($\lambda_{\min}^{(\text{bath})} = 1/2$ for even $N$, $= 1$ for odd
$N$) drives $1/\lambda^2$ large. The decoherence is structural: it
gets stronger precisely where it needs to be strongest, at the
physically relevant threshold.

The bath correlation time $\tau_{\text{bath}} \le \sqrt{2}$ (computed
from the eigenvalue gap). The tunneling time
$\tau_{\text{tunnel}} \sim e^{S_{\text{BO}}} \gg 1$. The Markov
approximation---the assumption that the bath forgets faster than the
system evolves---is not a rough estimate; it is exponentially sharp.
Non-Markovian corrections are $O(e^{-S_{\text{BO}}})$: negligible by a
factor that is itself exponentially small.

\subsection*{What emerges}

Paper~III catalogs the quantum structures that arise from the
classical polygon dynamics. None of these are postulated. Each is derived
from the Feynman--Kac correspondence applied to the Havelock spectrum:

\begin{itemize}
\item \textbf{(Q1) The WDW equation.} The transfer matrix of the
  Onsager--Machlup path integral IS the WDW Hamiltonian
  $H = -(1/2c)\,d^2/d\rho^2 + V(\rho)$.
\item \textbf{(Q2) The Born rule.} The Boltzmann weight of the
  classical system IS the quantum probability
  $|\Psi_0(\rho)|^2 = e^{-\beta V}/Z$.
\item \textbf{(Q3) Unitary evolution.} The Feynman--Kac propagator is
  the matrix element of a self-adjoint Hamiltonian, which generates
  unitary time evolution upon Wick rotation.
\item \textbf{(Q4) Superposition.} The breathing-mode Hilbert space
  $L^2(\mathbb{R}, d\rho)$ admits superposition of $\rho$-states.
  The WDW constraint $H\Psi = 0$ selects a unique physical state.
\item \textbf{(Q5) Pointer basis.} The angular bath decoheres the
  breathing mode with $\gamma \gg 1$. The $\rho$-basis is
  dynamically selected.
\item \textbf{(Q6) Uncertainty principle.} Near the turning point,
  the ground state is a minimum-uncertainty Gaussian:
  $\Delta\rho \cdot \Delta p = 1/2$.
\item \textbf{(Q7) Entanglement.} The entanglement entropy between the
  breathing mode and the angular bath is $S_{\text{ent}} \gg 1$ bit.
\end{itemize}

The polygon system does not \emph{model} quantum mechanics. By the
Feynman--Kac theorem, the classical statistical mechanics of the polygon
IS a quantum system. The wave function, the Hilbert space, the Born
rule, and decoherence all emerge from the same starting point: $N$
identical point vortices on a hyperbolic surface, interacting
logarithmically, analyzed at negative temperature via the Onsager
principle.

No external observer. No measurement postulate. No collapse axiom. The
polygon measures itself.

\textbf{Status: (Derived)}---the Feynman--Kac correspondence is a
mathematical theorem. The Caldeira--Leggett conditions are verified
from the Havelock spectrum. The decoherence rate, the Markov
approximation, and the pointer basis selection are computed with
explicit error bounds.


\part{The Particles Inside the Polygon}


\section{The shape of spacetime is forced}
\noindent\textit{[Technical reference: Paper~IV, \S1--2]}

\medskip

\noindent\textit{Bottom line: The polygon hierarchy forces the spacetime
geometry to be an eleven-dimensional Seifert manifold---the only option
consistent with both gravity and the electroweak force.}

\medskip

Paper~III established that the graviton requires $N = 7$. It is the
marginal mode of the heptagon, carrying spin~$2$, selected by the
Pell equation. That much is settled.

But the graviton is not the only force carrier in nature. The weak
nuclear force---responsible for radioactive decay, for the fusion
reactions that power the Sun---is carried by vector bosons with
spin~$1$. Those bosons need their own polygon.

The condition for the critical mode of an $N$-gon to carry integer
spin $j$ is $j(j+1) = m^*(N - m^*)/2$, where $m^*$ is the most
dangerous wobble mode. At $N = 4$, $m^* = 2$, and
$f(2, 4) = 2 \times 2/2 = 2$. Since $j(j+1) = 2$ gives $j = 1$:
the square carries spin~$1$. The first even polygon with integer
spin at the critical mode.

Two polygons now need to coexist on one spacetime. $N = 7$ for
gravity. $N = 4$ for the electroweak force. Both live on the same
compact fiber---a circle---and their fluxes add. This is Chern class
additivity, a theorem in algebraic topology: the total flux through a
circle fiber is the sum of the fluxes from each sector. So the total
polygon number on the fiber is $N_{\text{total}} = 4 + 7 = 11$.

Eleven is not adjustable. It is the sum of the only two polygons that
produce integer-spin particles at their critical modes (below $N = 41$,
which lies far beyond any stability range). The number $11$ is forced
by the Pell equation acting on two sectors.

Now: what kind of spacetime admits an $11$-gon? On a flat surface, the
largest stable polygon is $N = 7$. The $11$-gon on a flat surface has
critical Casimir $f(5, 11) = 15$, which exceeds $C_1 = N - 1 = 10$.
The eigenvalue $\lambda_5 = 10 - 15 = -5$. Unstable. A sphere makes
it worse---positive curvature pushes $C_1$ down. Only negative
curvature pushes $C_1$ up. The base surface must be the hyperbolic
plane $\mathbf{H}^2$.

The fiber must be a circle $S^1$. The Havelock decomposition is a
Fourier analysis on a ring---one-dimensional by construction. A
higher-dimensional fiber ($S^2$, a torus) would introduce additional
symmetries not present in the polygon hierarchy.

With a hyperbolic base and a circle fiber, Thurston's geometrization
theorem eliminates the competition. William Thurston classified all
possible geometries for three-dimensional manifolds in the 1980s; there
are exactly eight. Of these eight:
\begin{itemize}
\item Three ($\mathbf{E}^3$, Nil, Sol) have non-negatively curved
  bases. Ruled out.
\item Two ($\mathbf{S}^3$, $\mathbf{S}^2 \times \mathbb{R}$) have
  positive curvature. Ruled out.
\item One ($\mathbf{H}^3$) has no fibration---no circle fiber at all.
  Ruled out.
\item One ($\mathbf{H}^2 \times \mathbb{R}$) has the right base and
  fiber, but it is a trivial product: the fiber has zero twist. Its
  Euler class is $e = 0$. A trivial fiber carries no holonomy---no
  rotational phase when you go around it---and without holonomy there
  is no gauge field. No electroweak force. Ruled out.
\item One ($\widetilde{\mathrm{SL}(2,\mathbb{R})}$) has a negatively
  curved base, a circle fiber, and a nonzero Euler class $e = 1$. The
  fiber twists. The twist generates a gauge field. This is the Seifert
  manifold $\mathbf{H}^2 \times_N S^1$.
\end{itemize}

Seven of eight geometries fail. The Seifert manifold is the unique
survivor. Including the time direction: the full spacetime is
$\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$, which has
$1 + 2 + 1 = 4$ macroscopic dimensions.

\subsection*{The four-dimensional graviton}

In $2+1$ dimensions, pure gravity is topological: it has no propagating
degrees of freedom, no gravitational waves. A flat table cannot carry
gravitons across it; the geometry is locally determined by the sources.
The graviton appears at the boundary.

The Chern--Simons gauge theory on $\mathbf{H}^2$ induces a conformal
field theory on its boundary. This boundary theory has a Virasoro
algebra---the symmetry algebra of conformal transformations---at
central charge $c = 12\,b(N)$. The stress tensor $T(z)$ of this
boundary theory has conformal weight $(2, 0)$: it transforms as a
spin-$2$ field. That is the graviton.

The count: a massless spin-$2$ particle in four dimensions has
$d(d-3)/2 = 2$ polarizations. The boundary stress tensor has two
independent components: $T(z)$ and $\bar{T}(\bar{z})$, corresponding
to the $+$ and $\times$ tensor polarizations. Two matches two. The
graviton has the right spin and the right number of physical degrees
of freedom; both are derived from the boundary algebra, not assumed.

At laboratory scales, the Seifert manifold is indistinguishable from
ordinary $3+1$ dimensional Minkowski space. The hyperbolic base has
curvature radius $\ell \sim 10^{26}$ meters---cosmological. At human
scales the curvature correction is $r^2/\ell^2 < 10^{-44}$. The circle
fiber has radius $R \sim 10^{-21}$ meters---far below the reach of any
collider. Below that scale, the extra dimension opens up; above it,
ordinary four-dimensional physics applies.

\textbf{Status: (Derived)}---the Thurston elimination is a mathematical
theorem. The emergent graviton is derived from the Virasoro algebra of
the boundary CFT. The only physical input is the identification of
$N = 4$ and $N = 7$ polygon sectors with the electroweak and
gravitational forces.


%% ====================================================================
\section{One number sets every coupling}
\noindent\textit{[Technical reference: Paper~IV, \S3--5]}

\medskip

\noindent\textit{Bottom line: The central charge $c = 12\,b(N)$
determines every force constant in physics.}

\medskip

In the Standard Model of particle physics, the coupling constants---the
numbers that set the strength of each force---are free parameters.
Nobody knows why the strong force is strong or the electromagnetic
force is weak; the numbers are measured in experiments and inserted
into the theory by hand.

In the polygon framework, there are no free coupling constants. Every
force strength descends from a single number: the central charge
$c = 12\,b(N)$.

\subsection*{Newton's constant}

The Brown--Henneaux formula (1986) relates the central charge of the
boundary theory to Newton's gravitational constant:
$c = 3\ell/(2G)$, where $\ell$ is the curvature radius. Setting
$\ell = 1$ in natural units: $G = 3/(2c)$. The gravitational constant
is the inverse of the central charge, up to a factor of $3/2$.

\subsection*{The strong coupling constant}

The strong force holds quarks together inside protons and neutrons.
Its strength is measured by $\alpha_s$, and the measured value at the
$Z$ boson mass is $0.1180 \pm 0.0009$.

The derivation runs through three numbers. The $\mathrm{SU}(3)$
Chern--Simons theory has level $k = 1$ (forced by the orbifold
construction---I will explain this in the next section). The dual
Coxeter number of $\mathrm{SU}(3)$ is $h^\vee = 3$. The $3$D coupling
is $1/(k + h^\vee) = 1/4$.

To get the $4$D coupling, divide by the effective volume of the
internal space: $V_{\text{eff}} = k \times 4\pi \times Z(S^3)$. The
factor $4\pi$ is the area of the Bolza surface (the genus-$2$
hyperbolic surface with the largest symmetry group). The factor
$Z(S^3) = \sqrt{2}$ is the Chern--Simons partition function on the
three-sphere---a standard result in topological field theory. The
product: $V_{\text{eff}} = 1 \times 4\pi \times \sqrt{2} = 4\pi\sqrt{2}$.

The result:
\[
  \alpha_s(M_{\text{poly}}) = \frac{1}{4\pi\sqrt{2}} \approx 0.056.
\]
This is the coupling at the polygon scale ($\sim 300$ TeV). Quantum
field theory tells us how couplings change with energy---they ``run.''
The strong coupling runs upward as the energy decreases. One-loop
running from $300$ TeV down to the $Z$ boson mass gives
$\alpha_s(M_Z) = 0.115$. Two-loop running gives $0.122$. The measured
value $0.1180$ sits between the one-loop and two-loop predictions.

\subsection*{The Weinberg angle}

The Weinberg angle $\theta_W$ sets the ratio of electromagnetic to
weak force strength. It determines, among other things, the mass ratio
of the $W$ and $Z$ bosons. The measured value is
$\sin^2\theta_W = 0.2312$ at the $Z$ mass.

The polygon prediction at the Chern--Simons threshold is an arithmetic
exercise. I will write it out fully.

Two quantum numbers enter. From the $N = 4$ polygon: the
$\mathrm{SU}(2)$ spin $j = 1$ (giving a Casimir $j(j+1) = 2$) and the
$\mathrm{U}(1)$ charge $Q = m^*/N = 2/4 = 1/2$. Three gauge-theory
numbers enter: the $\mathrm{SU}(2)$ level $k = 1$ and dual Coxeter
number $h^\vee = 2$ (so $k + h^\vee = 3$), and the $\mathrm{U}(1)$
level $K = 1$.

The mixing angle in Chern--Simons theory is:
\[
  \sin^2\theta_W = \frac{Q^2/K}{Q^2/K + j(j+1)/(k + h^\vee)}.
\]
Substitute:
\[
  \sin^2\theta_W = \frac{(1/2)^2 / 1}{(1/2)^2/1 + 1 \cdot 2/3}
  = \frac{1/4}{1/4 + 2/3}
  = \frac{1/4}{11/12}
  = \frac{3}{11}
  = 0.2727.
\]
Every number in that calculation---$Q$, $K$, $j$, $k$, $h^\vee$---is
determined by the polygon. $Q$ and $j$ come from the Havelock Casimir
at $N = 4$. The levels $k = K = 1$ are forced by the orbifold
construction. The dual Coxeter number $h^\vee = 2$ is a property of
$\mathrm{SU}(2)$. Nothing is adjusted.

The prediction $3/11 = 0.2727$ is at the polygon scale, not at the
$Z$ mass. Standard Model renormalization group running brings it from
$0.2727$ down to $0.231$ at $M_Z$. The measured value: $0.2312$. The
discrepancy after running: $0.1\%$.

(For comparison: the $\mathrm{SU}(5)$ grand unification prediction is
$\sin^2\theta_W = 3/8 = 0.375$ at the GUT scale, which is $62\%$ above
the measured value at tree level. The polygon prediction $3/11$ starts
closer and arrives closer.)

\subsection*{The coupling lock}

There is one more relation worth mentioning, because it serves as an
internal consistency check rather than a prediction.

The ratio of the electromagnetic coupling $\alpha$ to Newton's constant
$G$ is:
\[
  \frac{\alpha}{8\pi G} = \frac{1}{2\pi^2}.
\]
Both $\alpha$ and $G$ are proportional to $1/c$. The central charge
cancels in the ratio. The result $1/(2\pi^2)$ is an algebraic
identity---it does not depend on $N$, on the polygon scale, on any
parameter at all. If the framework is internally consistent, this
ratio must hold. It does.

\textbf{Status: (Derived)}---the coupling relations follow from the
Brown--Henneaux formula and the Chern--Simons/Kaluza--Klein reduction.
The Weinberg angle prediction $3/11$ is arithmetic; the running to
$M_Z$ uses standard one-loop $\beta$-functions.


%% ====================================================================
\section{Where the Standard Model gauge group comes from}
\noindent\textit{[Technical reference: Paper~IV, \S6--8]}

\medskip

\noindent\textit{Bottom line: $\mathrm{SU}(3) \times \mathrm{SU}(2)
\times \mathrm{U}(1)$---the symmetry group of all known
particles---is forced by the Klein quartic at $N = 7$.}

\medskip

The Standard Model of particle physics rests on a symmetry group:
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$.
$\mathrm{SU}(3)$ governs the strong force (quarks and gluons).
$\mathrm{SU}(2)$ governs the weak force (radioactive decay).
$\mathrm{U}(1)$ governs electromagnetism (photons, electric charge).
Why this group and not some other? Within the Standard Model, the
group is an empirical input. Nobody derives it.

The polygon framework derives it. Each factor comes from a different
mechanism, but all three trace back to the polygon hierarchy.

\subsection*{$\mathrm{U}(1)$: the fiber rotation}

The simplest factor first. The Seifert manifold has a circle fiber.
A circle has a rotational symmetry: $\mathrm{U}(1)$, the group of
rotations by any angle. The Kaluza--Klein reduction on this circle
produces a gauge field---the KK photon---with coupling
$\alpha = 6/(\pi c)$. This is standard Kaluza--Klein theory, the
oldest unification idea in physics (1921). The $\mathrm{U}(1)$ factor
requires no new mathematics; it is the rotation symmetry of the
compact dimension.

\subsection*{$\mathrm{SU}(2)$: the electroweak polygon}

The $N = 4$ polygon carries the electroweak force. The Chern--Simons
formulation of $2+1$D gravity produces an $\mathrm{SU}(2) \times
\mathrm{SU}(2)$ gauge symmetry---one copy for left-handed particles,
one for right-handed.

The Seifert manifold breaks this symmetry. The twist of the circle
fiber (the Euler class $e = N/2$) distinguishes left from right: it is
a pseudoscalar, changing sign under orientation reversal. This is
the same Euler class that makes the Seifert manifold non-trivial---the
reason the trivial product $\mathbf{H}^2 \times \mathbb{R}$ was
eliminated in Section~16. The parity anomaly (a one-loop quantum
effect) gaps the right-handed $\mathrm{SU}(2)_R$, leaving only the
left-handed $\mathrm{SU}(2)_L$ as the unbroken gauge factor.

This is the origin of parity violation in the weak interaction.
Experiments since 1957 have confirmed that the weak force treats left-
and right-handed particles differently. The polygon framework says
this asymmetry is geometric: the Seifert manifold is not
parity-invariant, so the gauge theory it generates is not either.

\subsection*{$\mathrm{SU}(3)$: the Klein quartic}

The strong force requires the most intricate derivation. It proceeds
through five algebraic steps.

\textbf{Step 1: The Frobenius action.} The multiplicative group of
integers modulo $7$ (the nonzero elements $\{1, 2, 3, 4, 5, 6\}$
under multiplication mod~$7$) has a unique subgroup of order~$3$:
$\{1, 2, 4\}$. The map $m \mapsto 2m \bmod 7$ generates this subgroup,
and it partitions the six Havelock modes into two orbits:
$O_+ = \{1, 2, 4\}$ and $O_- = \{3, 5, 6\}$. The Havelock Casimirs
of each orbit are $\{3, 5, 6\}$ and $\{6, 5, 3\}$---the same set,
just reordered. This is not a coincidence; it is forced by the
Frobenius symmetry.

\textbf{Step 2: The McKay correspondence.} The Frobenius map acts as a
cyclic permutation of order~$3$ on each orbit: $\phi_1 \mapsto \phi_2
\mapsto \phi_4 \mapsto \phi_1$. Diagonalizing this action produces
eigenvalues $1$, $\omega$, $\omega^2$, where
$\omega = e^{2\pi i/3}$. These eigenvalues define an embedding of the
cyclic group $\mathbb{Z}/3\mathbb{Z}$ into $\mathrm{SU}(2)$:
$\sigma = \text{diag}(\omega, \omega^{-1})$.

The McKay correspondence (John McKay, 1980) is a theorem in algebra
that associates a Dynkin diagram to each finite subgroup of
$\mathrm{SU}(2)$. The diagram determines a Lie group. For
$\mathbb{Z}/3\mathbb{Z}$, the diagram is $A_2$. And $A_2$ IS
$\mathrm{SU}(3)$.

I want to be precise about what is happening here. The polygon has
$N = 7$ modes. The Frobenius action on those modes has order~$3$,
producing a $\mathbb{Z}/3\mathbb{Z}$ subgroup of $\mathrm{SU}(2)$.
McKay's theorem converts that subgroup into the Dynkin diagram $A_2$.
The Lie group corresponding to $A_2$ is $\mathrm{SU}(3)$. This is NOT
direct McKay on $\mathbb{Z}/7\mathbb{Z}$ (which would give
$\mathrm{SU}(7)$, a group that does not match experiment). The
$\text{PSL}(2, \mathbb{F}_7)$ Frobenius reduces the full affine $A_6$
diagram to $A_2$. The reduction from $7$ to $3$ is algebraic, not a
choice.

\textbf{Step 3: The Klein quartic.} The surface
$X(7) = \Gamma(7) \backslash \mathbf{H}^2$ is the Klein quartic: a
genus-$3$ surface with $168$ symmetries, the maximum for its genus.
Its automorphism group is $\text{PSL}(2, \mathbb{F}_7)$---the same
group that governs the $N = 7$ polygon. The holomorphic differentials
on this surface carry a three-dimensional representation $\chi_3$ of
this group.

\textbf{Step 4: The Eightfold Way.} The representation
$\chi_3 \otimes \bar{\chi}_3 = \chi_1 \oplus \chi_8$. The
three-dimensional representation tensored with its conjugate
decomposes into a one-dimensional trivial representation and an
eight-dimensional irreducible representation. This is the SU(3)
Eightfold Way: the adjoint representation has dimension~$8$. The
representation theory of $\text{PSL}(2, \mathbb{F}_7)$ reproduces the
representation theory of $\mathrm{SU}(3)$ exactly.

\textbf{Step 5: Three generations.} The six Havelock modes at $N = 7$
fall into three palindromic pairs: $(m, N-m)$ for $m = 1, 2, 3$. Each
pair corresponds to one generation of matter particles.
$(N-1)/2 = 3$. The number of particle generations is not a free
parameter; it is $(N-1)/2$ evaluated at $N = 7$.

\subsection*{Confinement: why quarks are never found alone}

No experiment has ever detected a free quark. Quarks are always bound
into composite particles---protons, neutrons, pions. This phenomenon
is called confinement, and explaining it from first principles is one
of the outstanding problems in theoretical physics (it is one of the
Clay Millennium Prize problems).

In the polygon framework, confinement follows from two lines of
algebra.

First: the Verlinde formula. The $\mathrm{SU}(3)_1$ Chern--Simons
theory has three integrable representations, and the expectation value
of a Wilson loop (a gauge-theory observable that measures whether a
quark can propagate freely) in the fundamental representation is
proportional to $1 + \omega + \omega^2$, where $\omega = e^{2\pi i/3}$.
This sum is exactly zero. Not approximately zero, not small---zero.
The quark cannot propagate. This is topological confinement.

Second: center symmetry. The Polyakov loop (a thermal version of the
Wilson loop) transforms under $\mathbb{Z}_3$, the center of
$\mathrm{SU}(3)$. In the polygon phase, the discrete symmetry is
$\mathbb{Z}_7$. Since $\gcd(7, 3) = 1$---seven and three share no
common factor---the $\mathbb{Z}_3$ center is unbroken. An unbroken
center forces the Polyakov loop expectation value to zero:
$\langle P \rangle = 0$. Confinement.

\subsection*{The QCD string tension}

How tightly are quarks confined? The answer is the string tension
$\sigma$---the energy per unit length of the flux tube connecting a
quark-antiquark pair. Lattice QCD simulations (brute-force numerical
calculations on a spacetime grid) measure
$\sigma/\Lambda^2 = 6.25 \pm 0.5$.

The polygon prediction: $\sigma/\Lambda^2 = 1.326 \times 9/2 = 5.97$.
The factor $1.326$ is the bare Yang--Mills string tension on
$\mathbf{H}^2$, computed from the Havelock eigenvalue and the Polyakov
monopole mechanism (with two monopole species, one per simple root of
$\mathrm{SU}(3)$). The factor $9/2$ is the non-perturbative
enhancement from the Verlinde dimension
($\dim \mathcal{H} = 9$ for $\mathrm{SU}(3)_1$ on a genus-$2$ surface)
divided by the $S^3$ partition function ($Z(S^3)^2 = 2$).

The match: $5.97$ predicted, $6.25 \pm 0.5$ measured. Five percent
discrepancy, zero free parameters.

\textbf{Status:} The $\mathrm{SU}(3)$ representation structure
(Frobenius $\to$ McKay $\to$ Eightfold Way) is \textbf{(Proved)}---each
step is a theorem in finite group theory or representation theory.
The gauge dynamics (confinement, string tension) are
\textbf{(Derived)}---they follow from Chern--Simons theory at $k = 1$
and the Verlinde formula.


%% ====================================================================
\section{The Weinberg angle and the CKM phase}
\noindent\textit{[Technical reference: Paper~IV, \S9--10]}

\medskip

\noindent\textit{Bottom line: The mixing angle between
electromagnetism and the weak force, and the parameter governing
matter--antimatter asymmetry, both come from the polygon with no free
parameters.}

\medskip

\subsection*{The Weinberg angle (reprise)}

I derived $\sin^2\theta_W = 3/11$ in Section~17. Here I want to add
context.

The Standard Model has $19$ free parameters. The Weinberg angle is
one of them. In grand unified theories ($\mathrm{SU}(5)$, for
instance), the Weinberg angle is predicted---but only at the GUT
scale, around $10^{14}$ GeV, and the tree-level prediction $3/8$ is
$62\%$ above experiment. The polygon prediction $3/11$ is $18\%$
above experiment at tree level, and after standard running lands at
$0.231$---matching the measured $0.2312$ to $0.1\%$.

The running determines the polygon scale: $M_{\text{poly}} \approx 300$
TeV. This is not an input; it is a consistency output. If the running
had landed outside the $50$--$500$ TeV window, the framework would
have a problem. It does not.

\subsection*{The CKM phase}

The Cabibbo--Kobayashi--Maskawa matrix encodes how quarks of different
generations mix when they interact via the weak force. It has four
independent parameters: three mixing angles and one phase. The phase
$\delta$ controls CP violation---the slight asymmetry between matter
and antimatter that allows the universe to contain more matter than
antimatter.

The measured value: $\delta = 69^\circ \pm 3^\circ$ (Particle Data
Group, 2024). The polygon prediction: $\delta = 70.2^\circ$.

The derivation proceeds through the Harish-Chandra $c$-function on the
hyperbolic plane. I will walk through it.

At $N = 7$, the third generation of quarks has a mode ($m = 3$) whose
conformal dimension sweeps from $c = 1/2$ (up-type) to $c = 3/2$
(down-type), crossing the Breitenlohner--Freedman bound at $c = 1$.
The BF bound is the stability threshold for fields on the hyperbolic
plane---below it, the field profile oscillates rather than decaying.
This crossing generates a scattering phase.

The scattering matrix of the Dirac operator on $\mathbf{H}^2$ at
spectral parameter $s$ is
$S(s) = \Gamma(1/2 + is)/\Gamma(1/2 - is)$,
where $\Gamma$ is the gamma function. The accumulated phase as $s$
runs from $0$ to $1$ (the range determined by the BF crossing) is:
\[
  \delta = \int_0^1 \operatorname{Im}\psi\!\left(\frac{1}{2} + it\right) dt,
\]
where $\psi$ is the digamma function---the logarithmic derivative of
the gamma function. The imaginary part
$\operatorname{Im}\psi(1/2 + it) = (\pi/2)\tanh(\pi t)$. This is a
standard identity (NIST Digital Library of Mathematical Functions,
\S5.4.20). Integrating:
\[
  \delta = \int_0^1 \frac{\pi}{2}\tanh(\pi t)\,dt
  = \frac{1}{2}\log\cosh(\pi).
\]
Numerically: $\cosh(\pi) = 11.592$. So $\delta = \log(11.592)/2 =
1.225$ radians $= 70.2^\circ$.

Every quantity in this calculation is determined by the polygon:
the spectral parameter range $\Delta s = 1$ comes from the BF
crossing of the third-generation mode at $N = 7$; the scattering
matrix is the standard Harish-Chandra function on $\mathbf{H}^2$.
No parameter is adjusted. No integral is approximated. The result is
a closed-form expression involving $\pi$ and $\cosh(\pi)$.

\subsection*{The quark mixing parameter $|V_{cb}|$}

The CKM matrix element $|V_{cb}|$ measures the probability amplitude
for a charm quark to transform into a bottom quark via the weak force.
The measured value is approximately $0.042$.

The polygon prediction involves two factors. The tree-level Yukawa
diagonalization gives a perturbative value
$|V_{cb}|_{\text{pert}} = 0.092$---too large by a factor of two.
The correction comes from the orbifold structure: on the quotient
surface $\mathbf{H}^2/\mathbb{Z}_7$, the transition from charm to
bottom requires propagation to the nearest $\mathbb{Z}_7$ image point.
The Dirac Green's function at that distance provides a suppression
factor $T_2 = Q_1(\cosh d) = 0.021$, where $Q_1$ is the Legendre
function of the second kind and $d = 2.02$ is the geodesic distance
to the nearest image.

The result: $|V_{cb}| = \sqrt{0.092 \times 0.021} = 0.044$. The
measured value: $0.041 \pm 0.001$. Match: $6\%$.

\textbf{Status: (Derived)}---the CKM phase is computed from the
Harish-Chandra $c$-function via a standard integral. The mixing
parameter $|V_{cb}|$ combines the tree-level Yukawa texture with the
orbifold image suppression. Both calculations use only the polygon
number $N = 7$ and the Born--Oppenheimer potential.


%% ====================================================================
\section{Fermion masses, the Higgs, and consistency}
\noindent\textit{[Technical reference: Paper~IV, \S11--13]}

\medskip

\noindent\textit{Bottom line: The mass hierarchy (top quark
$340{,}000\times$ heavier than the electron), the Higgs boson mass,
and the QCD string tension all come from the same tunneling
calculation.}

\medskip

\subsection*{Why particles have different masses}

The Standard Model has three generations of matter particles. The top
quark is the heaviest: $173$ GeV. The up quark is among the lightest:
$2.2$ MeV. The ratio is about $80{,}000$ to $1$. Including the
electron ($0.511$ MeV), the full range spans a factor of $340{,}000$.
The Standard Model describes this hierarchy by assigning a separate
Yukawa coupling to each particle---$13$ free parameters in all---and
offers no explanation for their values.

The polygon framework generates this hierarchy from the geometry of
$\mathbf{H}^2$.

Each fermion generation corresponds to a palindromic mode pair
$(m, 7 - m)$ at $N = 7$. The mode number $m$ determines a Kaluza--Klein
mass $\mu_m = |m - (N-1)/2|$, which in turn determines a conformal
dimension $c_m = \sqrt{\mu_m^2 + \mu_4^2}$, where $\mu_4$ comes from
the $N = 4$ electroweak sector (different for up-type and down-type
quarks).

The conformal dimension controls how the fermion's wave function is
distributed along the radial direction of $\mathbf{H}^2$. A large
conformal dimension pushes the wave function toward the ultraviolet
boundary (small radius); a small conformal dimension lets it spread
into the infrared (large radius). The Higgs field lives at a specific
radial position determined by the Born--Oppenheimer potential (the
same potential that governs the breathing mode). The Yukawa coupling
---the number that sets the fermion mass---is the overlap integral
between the fermion wave function and the Higgs profile.

Large conformal dimension $\to$ small overlap $\to$ small Yukawa
$\to$ light particle. Small conformal dimension $\to$ large
overlap $\to$ large Yukawa $\to$ heavy particle. The hierarchy is
exponential in the conformal dimension, which is why the mass ratios
span so many orders of magnitude.

\subsection*{The strange-to-bottom mass ratio}

The cleanest quantitative test of this mechanism is the ratio
$m_s/m_b$: the strange quark mass divided by the bottom quark mass.
The measured value is approximately $0.024$.

The prediction involves one additional ingredient beyond the tree-level
Yukawa texture. The bottom quark mass receives a contribution from
the BF-crossing instanton---the same instanton that generates the CKM
phase. This instanton carries two units of $\mathbb{Z}_7$ charge and
generates an effective Yukawa coupling
$Y_{33}^{\text{inst}} = \mathcal{K}^2 f_3^2$, where
$\mathcal{K} = 0.548$ is the instanton fugacity (determined by the
fractional part of the Chern--Simons level) and $f_3$ is the
third-generation radial profile. The strange quark mass comes from the
tree-level Yukawa $Y_{22} = f_2^2$.

The ratio:
$m_s/m_b = f_2^2/(\mathcal{K}^2 f_3^2) = 0.023$.
Measured: $0.024$. Match: $4\%$.

\subsection*{The Higgs mass}

The Higgs boson was discovered at the Large Hadron Collider in 2012,
with a mass of $125.25 \pm 0.17$ GeV. The Standard Model does not
predict this number.

The polygon prediction starts from the Higgs quartic coupling at the
polygon scale. Standard Model renormalization group running from
$M_{\text{poly}}$ to $M_Z$ gives a tree-level coupling
$\lambda_H \approx 0.10$, corresponding to a Higgs mass of about
$110$ GeV---too low.

Two corrections, both determined by the orbifold structure, raise it.
The $\mathbb{Z}_7$ twisted sectors contribute
$\delta\lambda = \lambda/N = 0.014$. The instanton quartic (from pairs
of instanton--anti-instanton tunnelings) contributes
$\delta\lambda = 0.008$. At leading order:
$\lambda_H = 0.10 + 0.014 + 0.008 = 0.122$, giving
$m_H = \sqrt{2\lambda_H} \times 246\text{ GeV} = 122\text{ GeV}$.
Next-to-leading corrections (two-loop running, top-quark threshold
matching) raise the upper end to $128$ GeV.

The observed $125.25$ GeV lies between $122$ and $128$. The theoretical
uncertainty from higher-order corrections is $\pm 3$ GeV.

\subsection*{Proton stability}

In grand unified theories, the proton eventually decays---just very
slowly (predicted lifetime $\sim 10^{34}$ years). Experiments have
searched for proton decay and found nothing, pushing the lower bound
above $1.6 \times 10^{34}$ years.

The polygon framework predicts that the proton is exactly stable.
Not long-lived---absolutely stable. The $\mathrm{SU}(3)$ and
$\mathrm{SU}(2)$ sectors live in different polygon numbers ($N = 7$
and $N = 4$). Cross-sector couplings would require
$8(m_1 + m_2) \equiv 28 \pmod{56}$, and this congruence has no
integer solution because $\gcd(8, 56) = 8$ does not divide $28$.
The baryon-number-violating operators that would cause proton decay
are forbidden by exact $\mathbb{Z}_{56}$ charge conservation---a
topological selection rule, not an approximate symmetry.

This is a distinguishing prediction. If Hyper-Kamiokande or any future
experiment detects proton decay, the polygon framework is falsified.

\subsection*{The strong CP problem}

The QCD vacuum has a parameter $\theta$ that, if nonzero, would
produce a measurable electric dipole moment for the neutron. The
experimental bound is $\theta < 10^{-10}$. Why $\theta$ is so small
is the strong CP problem; the conventional solution posits a new
particle called the axion.

The polygon framework solves it without an axion. The Chern--Simons
level for $\mathrm{SU}(3)$ is $k = 1$---an integer. Level
quantization (from $\pi_3(\mathrm{SU}(3)) = \mathbb{Z}$) forces the
vacuum angle to be $\theta = 2\pi k_{\text{frac}} = 0$. The fermion
shifts preserve integrality: six quark flavors shift the level by
$\Delta k = 3$ (an integer). The effective level is $k_{\text{eff}} =
4$, still an integer, still giving $\theta = 0$.

The gravitational sector has an irrational level, but it does not
contaminate the gauge sector because the gauge and gravitational traces
are orthogonal:
$\text{Tr}(T^{\mathrm{SU}(3)}_a T^{\mathrm{SL}(2,\mathbb{R})}_b) = 0$.

The prediction: no axion. Null results at ADMX and ABRACADABRA
(axion dark matter searches) are consistent; a positive detection
would falsify this aspect of the framework.

\subsection*{Light quark masses: an honest limitation}

The up and down quark masses are the weakest point in the fermion
sector. The predicted $m_u \approx 1.3$ MeV (measured: $2.2$ MeV,
$40\%$ off) and $m_d \approx 7.6$ MeV (measured: $4.7$ MeV,
$60\%$ off). These are not close enough to claim a match.

The reason is identified: the lightest quarks are sensitive to the
tail of the Higgs boson wave function near $\rho = 0$, where the
Born--Oppenheimer potential is least constrained. The Higgs profile
is broad---its width exceeds the equilibrium radius of the polygon---so
the overlap integral samples a region where the Randall--Sundrum
approximation (exponential profile suppression) breaks down. The
qualitative prediction $m_u < m_d \ll m_s$ is correct and
parameter-free. The quantitative discrepancy is a geometric-tier
limitation, flagged honestly.

\textbf{Status:} The mass hierarchy and $m_s/m_b$ are
\textbf{(Derived)}, depending on the Born--Oppenheimer potential and
the instanton fugacity $\mathcal{K}$. The Higgs mass range $122$--$128$
GeV is \textbf{(Derived)}. Proton stability and $\theta = 0$ are
\textbf{(Derived)} from topological selection rules. Light quark
masses are acknowledged limitations: the framework produces the
correct ordering but not the correct magnitudes.


\part{The Numbers of the Universe}


\section{Why $N = 11$, and the cosmological constant}
\noindent\textit{[Technical reference: Paper~V, \S1--4]}

\medskip

\noindent\textit{Bottom line: The total polygon number is forced to be
eleven---and this single integer determines the curvature of the
universe.}

\medskip

Paper~IV established two polygons: $N = 7$ for gravity (the
graviton, spin~$2$, selected by the Pell equation) and $N = 4$ for
the electroweak force (the vector boson, spin~$1$, the unique
even polygon with integer critical spin below $N = 24$). Both
sectors live on the same circle fiber of the Seifert manifold. Their
fluxes add---this is Chern class additivity, a theorem in algebraic
topology, not a modeling choice. The total flux through the fiber is
$4/2 + 7/2 = 11/2$, giving the total polygon number $N = 11$.

This is not one option among several. I want to show the
elimination table, because the constraint is sharp.

\begin{center}
\small
\begin{tabular}{lcll}
\toprule
$N$ & Decomposition & Why it fails & Failure type \\
\midrule
$4$  & $4$ alone      & Cosmological constant $= 0$ (gauge threshold only) & mathematical \\
$7$  & $7$ alone      & Gives $M_P/v$, not the cosmological constant & structural \\
$8$  & $4 + 4$        & No graviton ($j = 2$ requires $N = 7$) & mathematical \\
$9$  & $4 + 5$        & $N = 5$: $j \approx 1.79$ (non-integer spin) & mathematical \\
$10$ & $3 + 7$        & $N = 3$: $j = 0$ (no gauge content) & mathematical \\
$12$ & $5 + 7$        & $N = 5$: non-integer spin & mathematical \\
$14$ & $7 + 7$        & Two graviton sectors $\to$ extra free parameter & structural \\
$28$ & $4 \times 7$   & Doubled gauge sectors $\to$ undetermined scale & structural \\
\bottomrule
\end{tabular}
\end{center}

\noindent
Every candidate either fails the Pell equation (non-integer spin at
the critical mode---a mathematical impossibility) or duplicates a
gauge sector (introducing an undetermined relative scale---a
structural flaw). $N = 11$ is the unique combination that includes
each gauge sector exactly once.

With $N = 11$ fixed, the cosmological constant follows from one
equation:
\begin{equation}\label{eq:lambda-companion}
  \Lambda_3 \;=\; \frac{N^2 - 16}{16}\,.
\end{equation}
In words: take the polygon number, square it, subtract sixteen,
divide by sixteen.

At $N = 4$ (the electroweak threshold): $(16 - 16)/16 = 0$. Zero
cosmological constant. The universe at this threshold is exactly flat.

At $N = 11$ (the full cosmological sector):
$(121 - 16)/16 = 105/16 = 6.5625$. Positive. The universe has
positive curvature energy---it expands.

The formula comes from a competition between two contributions on the
Seifert manifold. The negative curvature of the hyperbolic base
$\mathbf{H}^2$ wants $\Lambda_3 < 0$ (collapsing). The positive flux
energy from the quantized gauge field on the $S^1$ fiber wants
$\Lambda_3 > 0$ (expanding). The flux energy scales as $N^2/16$; the
base curvature contributes $-1$. They cancel at $N = 4$. For
$N = 11$, the flux wins.

This formula gives $\Lambda_3$ in units of $1/\ell^2$, where $\ell$
is the curvature radius of the hyperbolic base. The physical
four-dimensional cosmological constant is
$\Lambda_4 = \Lambda_3/\ell^2$. The value of $\ell$ comes from the
$N = 11$ tunneling calculation in the next section. The ratio
$\Lambda_3$ is fixed by arithmetic; $\ell$ is fixed by a tunneling
integral. Between them, they determine the expansion rate of the
universe.

The Frobenius automorphism of $\mathbb{Z}_{11}$ (the map
$m \mapsto 3m \bmod 11$, which has order~$5$) provides a consistency
check: it generates $\mathrm{SU}(5)
\supset \mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$,
reproducing the Georgi--Glashow grand-unified embedding. The polygon
framework does not use grand unification as an input---it arrives at
the same group-theoretic structure from the bottom up.

Three fermion generations follow immediately:
$n_{\mathrm{gen}} = (N_{\mathrm{grav}} - 1)/2 = (7 - 1)/2 = 3$.
Each palindromic mode pair $(m, 7 - m)$ for $m = 1, 2, 3$ constitutes
one generation, ordered by Kaluza--Klein mass. No permutation freedom
exists---the hierarchy is fixed by the mode number.

\textbf{Status: (Derived)}---the flux additivity is a topological
theorem. The elimination table is exhaustive below $N = 41$. The
$\Lambda_3$ formula follows from the Kaluza--Klein reduction.


%% ====================================================================
\section{The hierarchy problem solved by tunneling}
\noindent\textit{[Technical reference: Paper~V, \S5--6]}

\medskip

\noindent\textit{Bottom line: The $10^{16}$ ratio between the Planck
mass and the electroweak scale---the most extreme hierarchy in
physics---emerges from a single tunneling calculation.}

\medskip

Gravity is weak. Absurdly, inexplicably weak. Hold a refrigerator
magnet to a nail: the electromagnetic force of that magnet overcomes
the gravitational pull of the entire Earth. The ratio between the
Planck mass (where gravity becomes strong) and the electroweak scale
(where the other forces operate) is approximately $10^{16}$---a one
followed by sixteen zeros. Why?

This is the hierarchy problem, and it has been the central puzzle of
particle physics for forty years. The Standard Model has no answer.
Supersymmetry was proposed as a solution but has not been found at
the Large Hadron Collider. Extra dimensions (Randall--Sundrum models)
address it but introduce free parameters. The polygon framework
derives the ratio from three computable pieces.

The polygon's breathing mode---the collective expansion and
contraction of the ring---tunnels through a potential barrier in the
Born--Oppenheimer potential $V_7(\rho)$. The tunneling action
$S_{\mathrm{BO}}(7)$ is a definite integral:
\[
  S_{\mathrm{BO}}(7) = \int_0^{\rho^*}\!\sqrt{2\,c_7\,|V_7(\rho)|}
  \;d\rho = 18.274,
\]
computed numerically to ten decimal places. The potential $V_7$ and
the kinetic coefficient $c_7 = 12\,b(7) = 51.57$ are both determined
by $N = 7$; no parameter is adjusted.

The hierarchy decomposes into three independently computable
contributions:
\begin{equation}\label{eq:hierarchy-companion}
  \ln\!\frac{M_P}{M_{\mathrm{EW}}}
  = \underbrace{2\,S_{\mathrm{BO}}(7)}_{\text{tunneling}}
  + \underbrace{\Delta\varepsilon\,\ln\varepsilon_7}_{\text{mass gap}}
  + \underbrace{\tfrac{1}{2}\ln\!\bigl(c_{11}/(24\pi^2)\bigr)}_{\text{gravity prefactor}}.
\end{equation}
Each piece has a physical meaning. Let me walk through them.

\textbf{Component~1: The instanton.} The breathing mode tunnels
through the barrier and back---a ``bounce.'' The bounce action is
$2 \times 18.274 = 36.548$. This single number accounts for $94.3\%$
of the hierarchy. It sets the exponential suppression
$e^{-36.548} \approx 10^{-16}$ that makes gravity so much weaker than
the other forces.

\textbf{Component~2: The mass gap.} The Wheeler--DeWitt equation on
the breathing mode has a ground state and a first excited state
separated by an energy gap $\Delta\varepsilon = 0.8031$---a universal
constant of the logarithmic Schr\"odinger equation, independent of
all parameters. Multiplied by the logarithm of the Pell unit
$\varepsilon_7 = 8 + 3\sqrt{7}$ (the fundamental unit of the ring of
integers $\mathbb{Z}[\sqrt{7}]$): $0.8031 \times 2.769 = 2.224$.
This contributes $5.7\%$ of the total. It is the quantum zero-point
energy of the breathing mode, a correction that shifts the tunneling
exponent above its semiclassical value.

\textbf{Component~3: The gravity prefactor.} The Planck mass is
determined by the full $N = 11$ spacetime geometry (not just the
$N = 7$ graviton sector). The central charge $c_{11} = 12\,b(11) =
126.56$ enters through the Brown--Henneaux relation:
$(1/2)\ln(c_{11}/(24\pi^2)) = -0.313$. A small negative correction.

The arithmetic: $36.548 + 2.224 + (-0.313) = 38.458$.

The observed value, from the measured Planck mass and electroweak
vacuum expectation value: $\ln(M_P/v) = 38.442$.

The match: $38.458$ versus $38.442$. A discrepancy of $0.016$ in the
logarithm, or $0.04\%$. In the ratio itself: a $1.5\%$ discrepancy
for a number that spans sixteen orders of magnitude.

Now the Pell connection, which I flagged in Part~I and which closes a
loop through the entire series. The ratio $M_P/M_{\mathrm{EW}}$ is,
at leading order, a power of $8 + 3\sqrt{7}$---the fundamental unit
of $\mathbb{Z}[\sqrt{7}]$. The same algebraic number that governs
the $7 \to 8$ stability threshold on the hyperbolic plane (the
palindromic equation $\xi^2 - 16\xi + 1 = 0$, whose smaller root
is $8 - 3\sqrt{7}$). The Pell identity
$8^2 - 7 \times 3^2 = 1$ that generates the stability threshold also
generates the hierarchy exponent. The connection is forced by the
Selberg trace formula on the Klein quartic: the instanton action
$S_{\mathrm{BO}}(7)$ satisfies
$2S/({\ln\varepsilon_7}) = 2N - \sqrt{2/\pi} - 4/c_7^2 + O(1/c_7^3)$,
verified to $0.00005\%$. Each of the seven vortices contributes
one Pell regulator $\ln\varepsilon_7$ to the tunneling exponent.

\subsection*{Prediction of the Higgs vacuum expectation value}

The hierarchy formula runs in both directions. Given the Planck mass
as input: $v = M_P/\exp(38.458) = 245.9$ GeV. The measured value:
$246.2$ GeV. Match: $0.12\%$.

\textbf{Status: (Derived)}---the tunneling action is computed
numerically from the Born--Oppenheimer potential. The mass gap is a
universal constant of the log-Schr\"odinger equation. The gravity
prefactor uses the Brown--Henneaux relation. All three components are
determined by $N = 7$ and $N = 11$.


%% ====================================================================
\section{The cosmic energy budget}
\noindent\textit{[Technical reference: Paper~V, \S7--9]}

\medskip

\noindent\textit{Bottom line: The $69/27/5$ split of dark energy, dark
matter, and ordinary matter comes from three orders of approximation
in the same calculation.}

\medskip

The universe is made of three things. About $69\%$ is dark energy: a
constant energy density that fills empty space and drives the
accelerating expansion. About $27\%$ is dark matter: invisible mass
that holds galaxies together but does not emit or absorb light. About
$5\%$ is ordinary matter: atoms, stars, planets, people.

The Standard Model of cosmology describes this $69/27/5$ split but
does not explain it. The numbers are measured from the cosmic
microwave background (by the Planck satellite) and from galaxy
surveys (by DESI, among others), then inserted into the equations by
hand. Nobody knows why the split takes these values and not others.

In the polygon framework, the free energy of the $N = 11$ breathing
mode has three contributions, arising at three successive orders of
the saddle-point expansion. Each order maps to one component of the
cosmic energy budget.

\subsection*{Tree level: dark energy}

The lowest-order contribution is the classical minimum of the
Born--Oppenheimer potential: $F_{\mathrm{DE}} = b(N) + N/4$. This
term is independent of the breathing coordinate $\rho$---it is a
constant, the vacuum energy of the polygon configuration. A constant
energy density has the equation of state $w = -1$: pressure equals
negative energy density. That is the equation of state of the
cosmological constant.

The polygon does not predict $w \approx -1$. It predicts $w = -1$
exactly. The DESI DR2 measurement $w = -1.0 \pm 0.1$ is consistent;
any confirmed deviation from $w = -1$ would falsify this piece of the
framework.

\subsection*{One-loop: dark matter}

The next-order contribution comes from the quantum fluctuations of the
frozen angular modes---the $N - 2$ Havelock modes with $m \ne m^*$
that sit at positive eigenvalues and do not participate in the
breathing-mode tunneling. Their one-loop determinant is
$F_{\mathrm{DM}} = (1/2)\sum_{m \ne m^*}\ln|\lambda_m|$.

This term depends on the breathing mode $\rho$ (through the
$\rho$-dependence of each $\lambda_m$) but not on the spatial volume
of the $\mathbf{H}^2$ slice. A contribution that depends on a
dynamical variable evolving with cosmic time but not on spatial
volume has zero pressure: $p = -\partial F/\partial V = 0$, which
gives $w = 0$. Cold matter.

After the cosmological phase transition, each frozen mode becomes a
massive Kaluza--Klein particle with mass
$M_k = \sqrt{\lambda_k} \times M_{\mathrm{poly}}$. At $N = 11$,
the eigenvalue gaps are the triangular numbers $\lambda_m = 1, 3, 6,
10$, giving four species with mass ratios
$1 : \sqrt{3} : \sqrt{6} : \sqrt{10}$ and absolute masses between
$50$ and $950$ TeV (depending on the polygon scale).

These particles are absolutely stable---protected by
$\mathbb{Z}_{11}$ charge conservation, the same topological mechanism
that protects the proton from decay in the $\mathbb{Z}_{56}$ sector.
They interact with ordinary matter only through gravity: the
spin-independent cross section on a nucleon is approximately
$10^{-103}$ cm$^2$, which is $55$ orders of magnitude below the
current LZ experimental bound. They will never be detected in any
direct detection experiment. Ever. They cluster gravitationally like
standard cold dark matter, reproducing rotation curves, the bullet
cluster, gravitational lensing, and CMB acoustic peaks.

\subsection*{Non-perturbative: ordinary matter}

The third contribution is the tunneling amplitude itself: the mass gap
$F_{\mathrm{M}} = \Delta E$ of the Wheeler--DeWitt equation. This is
the non-perturbative piece---it cannot be seen at any finite order of
the saddle-point expansion. The tunneling event localizes the
breathing mode, the amplitude thermalizes below the electroweak
temperature, and the result is baryonic matter.

\subsection*{The numbers}

At $N = 11$, with instanton resummation:
\begin{center}
\small
\begin{tabular}{lccc}
\toprule
Component & Prediction & Planck 2018 & Tension \\
\midrule
Dark energy $\Omega_\Lambda$ & $68.9\%$ & $68.5 \pm 0.7\%$ & $0.6\sigma$ \\
Dark matter $\Omega_{\mathrm{DM}}$ & $26.9\%$ & $26.6 \pm 0.7\%$ & $0.4\sigma$ \\
$\Omega_\Lambda / \Omega_{\mathrm{DM}}$ & $2.56$ & $2.57$ & $0.5\%$ \\
Baryonic matter $\Omega_b$ & $5.0\%$ & $4.9 \pm 0.1\%$ & $0.6\sigma$ \\
\bottomrule
\end{tabular}
\end{center}

\noindent
Every entry is within one standard deviation of the measured value.
The ratio $\Omega_\Lambda/\Omega_{\mathrm{DM}} = 2.56$ (measured:
$2.57 \pm 0.05$) is independent of the curvature radius $\ell$---it
is a pure number determined by the eigenvalue spectrum at $N = 11$.

\subsection*{The baryon asymmetry: an honest factor of three}

The baryon-to-photon ratio $\eta_B$ measures how much ordinary matter
the universe contains per photon. The observed value:
$6.1 \times 10^{-10}$.

The polygon prediction: $\eta_B \approx 2 \times 10^{-9}$. Off by a
factor of three.

I want to be transparent about this. The factor of three is the
largest discrepancy in the cosmological sector. Its origin is
identified: the calculation depends on lattice QCD parameters (the
weak sphaleron rate $\kappa$ and the Yukawa equilibration rate
$\Gamma_y$), each of which carries factor-of-two uncertainties that
are shared by every electroweak baryogenesis model, not specific to
this framework. At the favorable end of the lattice ranges
($\kappa = 10$, $\Gamma_y = 0.3$ instead of the central values
$\kappa = 20$, $\Gamma_y = 0.2$), the prediction matches to $10\%$.

The instanton resummation that brings $\Omega_b$ into line uses the
Reidemeister torsion of the Chern--Simons partition function. Adjacent
flat $\mathrm{SU}(3)$ connections on the Seifert manifold have
spectral flow $\pm 1$, flipping the sign of the analytic torsion.
This produces an alternating geometric series
$\sum(-K^2)^n = 1/(1 + K^2)$, where $K = 0.548$ is the instanton
fugacity. The series converges ($K^2 = 0.300 < 1$); truncating at
$n = 2$ gives a remainder of $2.1\%$. No new parameter enters. The
resummation shifts $\Omega_b$ from a $12\sigma$ discrepancy at tree
level to $0.6\sigma$ after all corrections.

The structural predictions---first-order electroweak phase transition
with $v(T_c)/T_c = 0.84$, CP violation four orders of magnitude above
the Standard Model Jarlskog invariant, and $v_w$-independent
$\eta_B$---carry no lattice uncertainty and are the falsifiable
content of this section.

\textbf{Status: (Derived)} for dark energy and baryonic matter.
\textbf{(Conjectured)} for the dark matter fraction---the eigenvalue
spectrum is computed, but the production mechanism (gravitational
particle creation at the $N$-selection phase transition) has not been
proved from first principles.


%% ====================================================================
\section{Neutrinos, inflation, and the beginning}
\noindent\textit{[Technical reference: Paper~V, \S10--12]}

\medskip

\noindent\textit{Bottom line: The neutrino mass hierarchy, the
mechanism that inflated the early universe, and the arrow of time all
follow from the polygon structure.}

\medskip

\subsection*{Neutrino masses}

Neutrinos are the lightest known matter particles. They barely
interact with anything; trillions pass through your body every second
without touching a single atom. They come in three flavors (electron,
muon, tau), and they have mass---established by the discovery of
neutrino oscillations, which won the 2015 Nobel Prize.

Two mass-squared differences are measured: $\Delta m^2_{\mathrm{atm}}
= 2.45 \times 10^{-3}$ eV$^2$ (from atmospheric neutrinos) and
$\Delta m^2_{\mathrm{sol}} = 7.5 \times 10^{-5}$ eV$^2$ (from solar
neutrinos). Their ratio is $32.5$. One calibration input
($\Delta m^2_{\mathrm{atm}}$) pins the overall mass scale. The ratio
is a genuine zero-parameter prediction.

The polygon mechanism is a Dirac seesaw. Each neutrino generation
($m = 1, 2, 3$ at $N = 7$) has a light state and a heavy
Kaluza--Klein partner. The heavy partner's mass is set by the Pell
unit: $M_{\mathrm{KK}} = M_{\mathrm{poly}} \times \varepsilon_7^7
\times 6 \sim 10^{13}$--$10^{14}$ GeV. The light neutrino mass is
$m_\nu = m_D^2/M_{\mathrm{KK}}$, where $m_D$ is the Dirac mass
(determined by the radial profile on $\mathbf{H}^2$ at the
Born--Oppenheimer turning point $\rho^* = 1.734$).

The predicted mass-squared ratio:
$\Delta m^2_{\mathrm{atm}}/\Delta m^2_{\mathrm{sol}} = 31.5$.
Measured: $32.5 \pm 1.0$. Match: $3\%$.

The predicted ordering: normal hierarchy ($m_3 \gg m_2 \gg m_1$).
The JUNO experiment, currently operating in southern China, will
determine the neutrino mass ordering within the next few years. If
JUNO finds an inverted hierarchy ($m_2 > m_1 \gg m_3$), the polygon
prediction is falsified. This is a live, binary experimental test.

The framework also predicts that neutrinos are Dirac particles, not
Majorana. No Majorana mass term exists in the Seifert geometry; lepton
number is exactly conserved. The experimental consequence: no
neutrinoless double beta decay ($0\nu\beta\beta$). The nEXO and
LEGEND experiments are searching for this process. A confirmed
detection would falsify the framework.

The total neutrino mass: $\Sigma m_\nu = 0.059$ eV, comfortably
below the Planck upper bound of $0.12$ eV.

\subsection*{Inflation}

The early universe underwent a brief period of exponential
expansion---inflation---that stretched quantum fluctuations into the
seeds of all cosmic structure. The Standard Model of cosmology
assumes an ``inflaton'' field to drive this expansion, but the
inflaton is ad hoc: its potential is chosen to fit the data, not
derived from anything.

In the polygon framework, the inflaton is the radion $\sigma$---the
size of the compact circle $S^1$. Its potential is determined by the
Seifert geometry:
\[
  V(\sigma) = \frac{N^2}{8\sigma^2} - \frac{c_{11}}{12\sigma}
  + \Lambda\,\sigma.
\]
Three terms: the flux energy (repulsive, $\propto 1/\sigma^2$), the
Casimir energy (attractive, $\propto 1/\sigma$), and the cosmological
constant (linear growth at large $\sigma$). The potential is stabilized
at $\sigma_{\min} \approx 1.35$ and has an inflection point at
$\sigma_{\mathrm{infl}} \approx 4.3$.

The radion starts above the inflection point---this initial condition
is set by the $N$-selection tunneling, not chosen by hand---and rolls
slowly toward the minimum. Near the inflection point, $V'' \approx 0$:
the potential is nearly flat, the second slow-roll parameter $\eta$ is
small, and the universe inflates.

The predicted spectral index: $n_s = 1 - 8/(3\sqrt{3}\,N_e)$, where
$N_e$ is the number of $e$-folds. For $N_e = 50$: $n_s = 0.969$.
The Planck measurement: $n_s = 0.965 \pm 0.004$. Consistent.

The predicted tensor-to-scalar ratio: $r \sim 10^{-6}$. The BICEP/Keck
bound: $r < 0.036$. The LiteBIRD satellite will reach
$\sigma_r \approx 0.001$. Even at that sensitivity, the predicted
signal is a thousand times too small to detect. Inflection-point
inflation predicts undetectable primordial gravitational waves from the
inflationary epoch.

\subsection*{The Penrose past hypothesis: resolved}

Roger Penrose asked one of the sharpest questions in physics: why was
the early universe in such a special, low-entropy state? The second
law of thermodynamics says entropy increases. If entropy is higher now
than it was at the Big Bang, then the initial state had extraordinarily
low entropy. How low? Penrose estimated the probability of the initial
state at $1/10^{10^{123}}$---a number so small that writing it out
would require more digits than there are atoms in the observable
universe.

The standard answer is: we do not know. The low-entropy initial state
is assumed, not explained. Penrose called this the ``past hypothesis''
---a postulate tacked onto the laws of physics.

The polygon answer is different. At the tunneling threshold
$\rho = \rho^*$, the polygon is regular: all perturbation amplitudes
are zero. The energy-momentum tensor is isotropic
($T_{\mu\nu} \propto g_{\mu\nu}$), the Einstein equations give a
maximally symmetric spacetime
($R_{\mu\nu} \propto g_{\mu\nu}$), and the Weyl curvature tensor
$W_{\mu\nu\rho\sigma} = 0$. The Weyl tensor measures
gravitational inhomogeneity---tidal forces, gravitational waves,
clumping of matter. At the tunneling threshold, it vanishes.
Gravitational entropy is at its minimum.

Not because someone chose special initial conditions. Because the
mathematics forces it. The tunneling event that created spacetime
starts from the unique state---the regular polygon---that minimizes
gravitational entropy. The past hypothesis is a consequence of the
tunneling, not a separate postulate.

I can be quantitative. The one-loop entropy at creation is
$S_{\mathrm{1\text{-}loop}} \approx 1.1$ nats ($1.6$ bits at
$N = 7$). The maximum available entropy---the Bekenstein--Hawking
entropy of the final black-hole state, computed from the Cardy
formula at central charge $c = 12\,b(7)$---is
$S_{\mathrm{BH}} = (\pi c/3)\cosh\rho^* \approx 158$ nats. The
ratio:
\[
  \frac{S_{\mathrm{BH}}}{S_{\mathrm{1\text{-}loop}}} \approx 144
  \qquad (N = 7).
\]
This is the Penrose gap: the quantitative measure of how ``special''
the initial state is. It is $144$---computed, not assumed. For larger
$N$ the gap grows: $5.5 \times 10^4$ at $N = 15$.

As $\rho$ increases past $\rho^*$, thermal perturbation amplitudes
grow: $\langle|\varepsilon_m|^2\rangle = 1/(2\lambda_m)$ by
equipartition. These amplitudes break the isotropy, generate nonzero
Weyl curvature, and drive structure formation. Four arrows point in
the same direction---expansion, gravitational entropy, Weyl curvature,
and structure formation---all increasing with $\rho$. The second law of
thermodynamics, applied to the ordered initial state, does the rest.

The arrow of time is not a mystery in this framework. Time runs in
the direction of increasing $\rho$---the direction away from the
tunneling threshold, the direction in which the polygon dissolves from
perfect regularity into thermal disorder.

\textbf{Status: (Derived)}---the neutrino mass ratio is computed from
the Dirac seesaw with Galois-twisted $M_{\mathrm{KK}}$; the mass
ordering is a binary prediction. Inflation is derived from the radion
potential with initial conditions set by tunneling. The Penrose gap
$S_{\mathrm{BH}}/S_{\mathrm{1\text{-}loop}} = 144$ is computed from
the Cardy entropy and the one-loop determinant at $\rho^*$.


\part{The Scorecard}
% [Mirrors Paper VI --- to be written]


\section*{Epilogue: One Formula}
% [To be written]


\end{document}
