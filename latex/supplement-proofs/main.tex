\input{../shared/preamble}

\externaldocument[I-]{../paper-1-mathematics/main}
\externaldocument[II-]{../paper-2-physics/main}
\externaldocument[III-]{../paper-3-gravity/main}
\externaldocument[IV-]{../paper-4-field-theory/main}
\externaldocument[V-]{../paper-5-s3-framework/main}
\externaldocument[VI-]{../paper-5-cosmology/main}

\title{\seriestitle\\[6pt]\large Supplement S: Extended Proofs and Computations}
\author{Gordon Speagle}
\date{}

\begin{document}
\maketitle

\begin{abstract}
This supplement collects extended proofs, detailed computations,
and numerical convergence analyses that support the results of
Papers~I--VI.
Each section is self-contained and referenced from the
corresponding main paper.
\end{abstract}

%% ====================================================================
\section{Paper~I supplements: Morse--Bott structure}
\label{sec:morse-bott}

The theorem statement and a concise proof appear in
Paper~I (Theorem~I-\ref*{I-thm:morse-bott}).
This section provides the full seven-step proof with
all intermediate calculations.

\begin{theorem}[Morse--Bott classification of the $N$-gon]
\label{thm:morse-bott}
Let $M = \{z \in \mathbb{C}^N : P = \sum z_k = 0,\;
L = \sum|z_k|^2 = R^2\}$ be the constraint surface,
and let $H = -\sum_{j<k}\ln|z_j - z_k|$ be the Thomson
energy.  Let $\bar{M} = M/\mathrm{SO}(2)$ be the quotient
by rigid rotation.
The regular $N$-gon is a Morse--Bott critical point of
$H|_{\bar{M}}$ for all $N \ne 7$:
\begin{enumerate}
\item $N \le 6$: non-degenerate minimum (Morse--Bott index~$0$).
\item $N = 7$: \emph{not} Morse--Bott
  (the normal Hessian has a $4$-real-dimensional kernel,
  resolved by the quartic $\alpha_0 = 45/14$, Paper~II, \S5).
\item $N \ge 8$: Morse--Bott saddle of complex index $N{-}5$
  (real index $2(N{-}5)$).
\end{enumerate}
Moreover, $N = 7$ is the \emph{unique} value at which the
normal Hessian degenerates.
\end{theorem}

\begin{proof}
\emph{Step~1 (Fourier decomposition of $T_pM$).}
At the $N$-gon $z_k = r\,\omega^k$
($\omega = e^{2\pi i/N}$, $r = R/\sqrt{N}$),
a tangent vector $(\delta z_1, \ldots, \delta z_N) \in T_pM$
decomposes into Fourier modes
$\delta z_k = \sum_{m=0}^{N-1} c_m\,\omega^{mk}$.

\emph{Step~2 (Constraints remove $c_0$ and $\mathrm{Re}(c_1)$).}
The constraint $P = \sum z_k = 0$ gives
$\sum_k \delta z_k = N c_0 = 0$, so $c_0 = 0$.
The constraint $L = R^2$ gives
$\mathrm{Re}\!\bigl(\sum_k \bar{z}_k\,\delta z_k\bigr) = 0$.
For the $N$-gon:
$\sum_k \bar{z}_k\,\delta z_k
= r\sum_k \omega^{-k}\sum_m c_m\,\omega^{mk}
= rN\,c_1$,
so the constraint is $\mathrm{Re}(c_1) = 0$.

\emph{Step~3 ($\mathrm{SO}(2)$ orbit removes $\mathrm{Im}(c_1)$).}
The infinitesimal rotation
$\delta z_k = iz_k = ir\,\omega^k$
corresponds to $c_1 = ir$, all other $c_m = 0$.
This is the tangent to the $\mathrm{SO}(2)$ orbit.
Since $\mathrm{Re}(c_1)$ is constrained to zero and
$\mathrm{Im}(c_1)$ is along the orbit,
the $m = 1$ mode contributes zero transverse directions.

\emph{Step~4 (Normal space).}
The remaining modes $c_m$ for $m = 2, \ldots, N{-}1$ are
unconstrained complex numbers, each contributing a
$2$-real-dimensional eigenspace of the normal Hessian.
The total normal dimension is $2(N{-}2)$.
Consistency check: $\dim\bar{M} = (2N{-}3) - 1 = 2N{-}4
= 2(N{-}2)$.~$\checkmark$

\emph{Step~5 (Normal Hessian = Havelock spectrum).}
The Havelock decomposition (Paper~I, Theorem~3.1) gives
the second variation of $H|_M$ along mode~$m$ as
$\lambda_m = (N{-}1) - m(N{-}m)/2$,
with each $\lambda_m$ of complex multiplicity~$1$
(real multiplicity~$2$).  The palindromic symmetry
$\lambda_m = \lambda_{N-m}$ is automatic.

\emph{Step~6 (Non-degeneracy criterion).}
The $N$-gon is Morse--Bott if and only if
$\lambda_m \ne 0$ for all $m \in \{2, \ldots, N{-}1\}$.
Setting $\lambda_m = 0$:
\[
  m^2 - Nm + 2(N{-}1) = 0,
  \qquad
  \Delta = N^2 - 8(N{-}1) = (N{-}4)^2 - 8.
\]
For integer solutions $m \in \{2, \ldots, N{-}2\}$,
we need $\Delta = k^2$ for some non-negative integer~$k$.
Then $(N{-}4{-}k)(N{-}4{+}k) = 8$.
The factor pairs of~$8$ with matching parity are:
$(2,4)$ only, giving $N{-}4{-}k = 2$, $N{-}4{+}k = 4$,
hence $N = 7$, $k = 1$, $m \in \{3, 4\}$.
The pair $(1, 8)$ gives $N = 17/2$ (not integer).
Therefore $N = 7$ is the \emph{unique} value with
vanishing normal eigenvalues.

\emph{Step~7 (Morse index).}
For $N \le 6$: all $\lambda_m > 0$ (verified by direct
computation in exact arithmetic for $m = 2, \ldots, N{-}1$),
giving index~$0$.
For $N \ge 8$: the negative modes are
$\{m : 2 \le m \le N{-}1,\; m(N{-}m) > 2(N{-}1)\}$.
The quadratic inequality $m^2 - Nm + 2(N{-}1) < 0$
has roots
$m_{\pm} = \tfrac{1}{2}\bigl(N \pm \sqrt{(N{-}4)^2 - 8}\bigr)$,
and the negative modes are the integers
$m \in (m_-, m_+)$.
For $N \ge 8$, $(N{-}4)^2 - 8 \ge 8$, so
$\sqrt{(N{-}4)^2{-}8} = N - 4 - \delta_N$
with $\delta_N = 4/(N{-}4) + O(1/N^3) \in (0, 2)$.
Thus $m_- \in (2, 3)$ and $m_+ = N - m_- \in (N{-}3, N{-}2)$,
giving $\lceil m_- \rceil = 3$ and
$\lfloor m_+ \rfloor = N - 3$.
The count of integers $m \in (m_-, m_+)$ is
$\lfloor m_+ \rfloor - \lceil m_- \rceil + 1
= (N{-}3) - 3 + 1 = N - 5$.
Verified at $N = 8$: $\sqrt{8} \approx 2.83$,
$m_\pm = (8 \pm 2.83)/2 \in \{2.59, 5.41\}$,
integer solutions $\{3, 4, 5\}$, count $= 3 = N - 5$.
The complex Morse index is $N{-}5$; the real index is
$2(N{-}5)$.
\end{proof}

\begin{proposition}[Critical point uniqueness]
\label{prop:critical-uniqueness}
For $N \le 6$, the regular $N$-gon is the unique critical
point of $H|_{\bar{M}}$ with Morse index~$0$.
Combined with the discrete isoperimetric inequality
(the $N$-gon minimizes $H$ among non-collapsed
configurations on $\{P = 0, L = R^2\}$), this implies:
\begin{enumerate}
\item The $N$-gon is the global energy minimum among
  non-collapsed configurations.
\item Any other critical point of $H|_{\bar{M}}$ has
  Morse index $\ge 1$ (a saddle point).
\end{enumerate}
\end{proposition}

\begin{proof}
\emph{Part~(i).}
For $N \le 7$ on the flat plane, the global energy-minimum
property of the regular $N$-gon among non-collapsed
configurations on $\{P = 0,\, L = R^2\}$ is established by
\citet{KurakinYudovich2002}: they prove nonlinear stability
of the $N$-gon for $N \le 7$ via Arnold's energy--Casimir
method, which implies the $N$-gon is a strict local minimum
of~$H$ on the constraint surface.
Uniqueness of the minimum (no other local minima compete for
the global role) follows from Morse-theoretic enumeration
(Part~(ii) below; see also \citealt{Aref2003}, Appendix~A
for the classification of relative equilibria).
A Schur-majorization argument does not apply directly because
the constraint $\sum_{j<k}|z_j - z_k|^2 = NR^2$ is not
compatible with the Cayley--Menger realizability constraints
for $N \ge 4$: the ``all distances equal'' configuration
exists only for $N = 3$.
Numerical verification: among $500$ random configurations
on $\{P = 0, L = 1\}$ with minimum pairwise distance
$> 0.01$, none achieved lower energy than the $N$-gon
(tested for $N = 3, \ldots, 7$).

\emph{Part~(ii).}
By Theorem~\ref{thm:morse-bott}, for $N \le 6$ the
$N$-gon has non-degenerate normal Hessian with all
eigenvalues positive (Morse index~$0$).
Since the Morse--Bott index classifies critical points
locally, any other critical point of $H|_{\bar{M}}$ must
have at least one negative normal eigenvalue,
i.e., Morse index $\ge 1$.
\end{proof}

\begin{corollary}[Equivariant Euler class sign change]
\label{cor:euler-class}
The $\mathbb{Z}_N$-equivariant normal bundle
$\nu = \bigoplus_{m=2}^{N-1} L_m$
decomposes into line bundles $L_m$ with weight $\lambda_m$.
The negative sub-bundle
$\nu^- = \bigoplus_{\lambda_m < 0} L_m$
has equivariant Euler class
$e(\nu^-) \in R(\mathbb{Z}_N) \cong
\mathbb{Z}[t]/(t^N{-}1)$
given by
\[
  e(\nu^-) =
  \prod_{\substack{m = 2 \\ \lambda_m < 0}}^{N-1}
  (t - \omega^m),
  \qquad \omega = e^{2\pi i/N}.
\]
The transition at $N = 7$:
\begin{itemize}
\item $N \le 6$: $\nu^- = 0$, $e(\nu^-) = 1$ (trivial).
\item $N = 7$: $\nu^-$ undefined
  ($\lambda_3 = \lambda_4 = 0$; the bundle degenerates).
\item $N = 8$: $\nu^- = L_3 \oplus L_4 \oplus L_5$,
  $e(\nu^-) = (t{-}\omega^3)(t{-}\omega^4)(t{-}\omega^5)
  \ne 0$.
\end{itemize}
The negative modes come in palindromic pairs
$\{m,\, N{-}m\}$ (since $\lambda_m = \lambda_{N-m}$),
so the real Euler class factors into quadratic terms
$(t - \omega^m)(t - \omega^{N-m})
= t^2 - 2\cos(2\pi m/N)\,t + 1$,
plus $(t + 1)$ for $m = N/2$ when $N$ is even.
\end{corollary}

\begin{proof}
The decomposition of $\nu$ into Fourier eigenspaces
follows from Step~5 of Theorem~\ref{thm:morse-bott}.
The equivariant Euler class of a direct sum of
$\mathbb{Z}_N$-equivariant line bundles is the product
of the individual first Chern classes
(Whitney sum formula in equivariant cohomology).
The first Chern class of $L_m$ is $(t - \omega^m)$ in
$R(\mathbb{Z}_N)$ (the character of the $m$-th
representation minus the identity).
Non-triviality for $N \ge 8$ follows from the fact
that $(t - \omega^m)$ is a non-zero element of
$R(\mathbb{Z}_N)$ for $m \ne 0$.
The palindromic pairing is Theorem~\ref{thm:morse-bott},
Step~5.
\end{proof}

%% ====================================================================
\section{Paper~I supplements: K-theory details}
\label{sec:kasparov}

\subsection{The Kasparov product}
\label{sec:kasparov-construction}

The representation-ring invariant $[\mu_N(\xi)]$
(Paper~I, Appendix~\ref*{I-sec:k-theory})
classifies stability phases but cannot detect fractional
Morse indices at thresholds where the gap condition fails.
The Kasparov product provides this finer information,
extending the classification to arithmetic surfaces.
The theorem statement is reproduced from Paper~I for
self-containment; the detailed proof is given here.

Theorem~\ref{thm:spectral-flow} uses the \emph{trace}
$|\delta C_1| = |\tau_{z_0}(h_\xi^\Gamma)|$ to control
the spectral flow.
The promotion to a $K_0$-level identity requires the
\emph{operator norm} $\|h_\xi^\Gamma\|_{C^*_r(\Gamma)}$,
which is strictly larger but still small on surfaces with
large spectral gap.

\begin{theorem}[$K_0$-valued Morse index]
\label{thm:kk-product}
Let $\Gamma$ be a cocompact torsion-free Fuchsian group,
$S = \Gamma\backslash\mathbf{H}^2$.
Fix a spectral parameter $s \in \mathbb{R}$ with $s > 1$
and let $G_s$ denote the resolvent kernel of
$(-\Delta + s(s{-}1))^{-1}$ on $\mathbf{H}^2$.
Define $h_\xi^\Gamma \in C^*_r(\Gamma)$ by
\begin{equation}\label{eq:h-element}
  h_\xi^\Gamma
  = \sum_{\gamma \in \Gamma,\, \gamma \ne 1}
    G_s(z_0,\, \gamma z_0)\; u_\gamma
\end{equation}
where $u_\gamma$ is the canonical unitary in $C^*_r(\Gamma)$.
\begin{enumerate}
  \item The element $h_\xi^\Gamma$ is a self-adjoint element
    of $C^*_r(\Gamma)$ with operator norm bounded by the $\ell^1$-norm:
    \begin{equation}\label{eq:op-norm-bound}
      \|h_\xi^\Gamma\|_{C^*_r}
      \;\le\; \|h_\xi^\Gamma\|_{\ell^1(\Gamma)}
      \;=\; \sum_{\gamma \ne 1} |G_s(z_0, \gamma z_0)|
      \;\le\; C/(s{-}1) < \infty
    \end{equation}
    (resolvent asymptotics
    $|G_s(z_0,\gamma z_0)| = O(e^{-s\ell_\gamma})$,
    \citealt{Helgason1984}, Ch.~IV, combined with
    ball-volume orbit growth
    $\#\{\gamma : \ell_\gamma \le L\} = O(e^L)$).
  \item For each mode $m \in \{1, \ldots, N{-}1\}$, the element
    $c_m\cdot 1 + h_\xi^\Gamma$ with
    $c_m = C_1(\mathbf{H}^2,\xi) - m(N{-}m)/2$
    is self-adjoint in $C^*_r(\Gamma)$.
    Its \emph{negative spectral projection}
    $P_-^{(m)} := \chi_{(-\infty,0)}(c_m\cdot 1 + h_\xi^\Gamma)$,
    defined by the Borel functional calculus,
    lies in $C^*_r(\Gamma)$ provided $c_m + h_\xi^\Gamma$
    is either invertible (gap case) or its spectrum does
    not accumulate at~$0$ (threshold case);
    $[P_-^{(m)}] \in K_0(C^*_r(\Gamma))$ gives a
    well-defined $K$-theory class.
  \item The \emph{$\mathbb{Z}_N$-equivariant Morse index}
    is the family
    \begin{equation}\label{eq:kasparov-product}
      \mu_N(\Gamma,\xi)
      = \sum_{m=1}^{N-1}
        \tau_*\bigl([P_-^{(m)}]\bigr)\,[\rho_m]
      \;\in\; R(\mathbb{Z}_N) \otimes \mathbb{R},
    \end{equation}
    where $[\rho_m]$ is the class of the $\mathbb{Z}_N$-character
    $z \mapsto \omega^m$ in the representation ring $R(\mathbb{Z}_N)$
    and $\tau_* \colon K_0(C^*_r(\Gamma)) \to \mathbb{R}$ is induced
    by the canonical trace.
    For torsion-free $\Gamma$, $\tau_*([P_-^{(m)}]) \in [0,1]$,
    and the value is integer in the gap case
    ($\|h_\xi^\Gamma\|_{C^*_r} < |c_m|$)
    and fractional otherwise.
\end{enumerate}
\end{theorem}

\noindent
\emph{Why the natural target is $K_0(C^*_r(\Gamma)) \otimes R(\mathbb{Z}_N)$
rather than $KK(C^*(\mathbb{Z}_N), C^*_r(\Gamma))$.}
The polygon symmetry decomposes the Havelock spectrum into
$\mathbb{Z}_N$-characters $\rho_m$, each carrying an independent
$K$-theory class $[P_-^{(m)}]$.
Any $C^*(\mathbb{Z}_N)$-module structure on
$\oplus_m \rho_m \otimes C^*_r(\Gamma)$ must act diagonally by the
scalar $\omega^m$ on each $\rho_m$-summand (since
$C^*(\mathbb{Z}_N)$ is abelian); the operator $F_\xi$ decomposes
identically block-diagonally with block $\operatorname{sign}(c_m + h)$,
and the commutator $[\pi(a), F_\xi]$ vanishes block-wise,
producing a degenerate Kasparov module that represents
zero in $KK(C^*(\mathbb{Z}_N), C^*_r(\Gamma))$.
The correct framework is therefore the direct sum
$\oplus_m K_0(C^*_r(\Gamma)) \otimes [\rho_m]$,
equivalent to the equivariant $K$-theory
$K_0^{\mathbb{Z}_N}(C^*_r(\Gamma))$ via the Green--Julg
isomorphism for the trivial $\mathbb{Z}_N$-action:
$K_0^{\mathbb{Z}_N}(C^*_r(\Gamma))
\cong K_0(C^*_r(\Gamma)) \otimes R(\mathbb{Z}_N)$.

\begin{lemma}[Compatibility of the two actions]
\label{lem:trivial-action}
The $\mathbb{Z}_N$ polygon symmetry acts trivially on
$C^*_r(\Gamma)$: each polygon rotation permutes the
ring vertices $z_k = z(e^{2\pi ik/N})$ but fixes the
center $z_0$ and acts on the mode space by scalar
multiplication by $\omega^m$ on $\rho_m$.
The Fuchsian group $\Gamma$ acts on $\mathbf{H}^2$ as
deck transformations; these commute with the polygon
symmetry because the polygon is centered at the
$\Gamma$-invariant point $z_0$ and $\Gamma$ acts on
$\mathbf{H}^2$ away from $z_0$.
Therefore the $\mathbb{Z}_N$-action on
$\oplus_m \rho_m \otimes C^*_r(\Gamma)$
factors through the $\rho_m$-decomposition with
trivial action on each $C^*_r(\Gamma)$-summand,
and the Green--Julg isomorphism applies.
\end{lemma}

\begin{proof}
\emph{Part~(i).}
The resolvent kernel on $\mathbf{H}^2$ at spectral parameter
$s$ satisfies
$|G_s(z_0, \gamma z_0)| = O(e^{-s\ell_\gamma})$ at large
displacement
(\citealt{Helgason1984}, Ch.~IV;
this is the asymptotic of the Legendre function
$Q_{s-1/2}(\cosh \ell)$ at large $\ell$).
The orbit-counting function for cocompact
$\Gamma \subset \mathrm{SL}(2,\mathbb{R})$ satisfies
$\#\{\gamma : \ell_\gamma \le L\} = O(e^L)$ by the
standard ball-volume estimate
(\citealt{Buser1992}, \S 9).
Writing this as a Stieltjes integral:
$\sum_{\gamma \ne 1} e^{-s\ell_\gamma}
\le \int_0^\infty e^{-sL}\,d(\#\{\ell \le L\})
\le s \int_0^\infty e^{-sL} \cdot C e^L\,dL
= Cs/(s-1)$
for $s > 1$, finite.
By Young's inequality
($\|a\|_{C^*_r} \le \|a\|_{\ell^1}$ for any
$a \in \ell^1(\Gamma)$), the element
$h_\xi^\Gamma$ is well-defined in $C^*_r(\Gamma)$
with the stated bound.
The $K$-theoretic class below is independent of the
regularization parameter $s > 1$: for $s, s' > 1$,
the difference $h_s - h_{s'}$ is norm-continuous
in $s$, and the spectral projection
$P_-(c_m + h_s)$ depends continuously on $s$ in the
norm topology, so its $K$-theory class is constant.
The limit $s \to 1^+$ recovers the bare Green's
function on the mean-zero subspace where the
zero-eigenvalue is excluded.

\emph{Part~(ii).}
For each mode $m$, $c_m\cdot 1 + h_\xi^\Gamma$ is self-adjoint
in $C^*_r(\Gamma)$ ($c_m \in \mathbb{R}$ and
$(h_\xi^\Gamma)^* = h_\xi^\Gamma$ follows from the symmetry
$G_s(z_0,\gamma z_0) = G_s(z_0, \gamma^{-1} z_0) \in \mathbb{R}$
combined with $u_\gamma^* = u_{\gamma^{-1}}$).
The negative spectral projection
$P_-^{(m)} = \chi_{(-\infty,0)}(c_m\cdot 1 + h_\xi^\Gamma)$
is defined by the Borel functional calculus.
In the gap case ($\|h_\xi^\Gamma\|_{C^*_r} < |c_m|$):
the operator $c_m\cdot 1 + h_\xi^\Gamma$ is invertible,
$0$ is not in its spectrum, and
$P_-^{(m)} \in C^*_r(\Gamma)$ with
$\tau(P_-^{(m)}) \in \{0, 1\}$
(indicator of $c_m < 0$, since the trace is multiplicative
on projections and $\tau(1) = 1$ for the canonical
trace on a torsion-free group).
In the threshold case ($c_m$ at a palindromic
boundary): the spectrum of $c_m + h_\xi^\Gamma$
does not accumulate at zero provided
$h_\xi^\Gamma$ has a spectral gap near zero
(verified on the Bolza surface by direct computation,
\S\ref{sec:bolza}), so $P_-^{(m)}$ is still a projection,
now with potentially non-integer trace.
The $K_0$-class $[P_-^{(m)}] \in K_0(C^*_r(\Gamma))$
is well-defined.

\emph{Continuity in $s$.}
The map $s \mapsto h_\xi^\Gamma(s)$ is norm-continuous
on $s > 1$ (dominated convergence with the uniform
bound $|G_s(z_0,\gamma z_0)| \le G_{s_0}(z_0,\gamma z_0)$
for $s \ge s_0 > 1$).
Therefore $s \mapsto P_-^{(m)}(s)$ is norm-continuous
wherever $0$ is not in the spectrum of $c_m + h_\xi^\Gamma(s)$,
and $[P_-^{(m)}(s)]$ is locally constant in $K_0$
(homotopy invariance).

\emph{Part~(iii).}
For each mode~$m$, the trace pairing
$\tau_*\colon K_0(C^*_r(\Gamma)) \to \mathbb{R}$,
induced by the canonical tracial state~$\tau$ on
$C^*_r(\Gamma)$
(well-defined and faithful for torsion-free surface
groups, which are ICC: \citealt{Kasparov1988}),
is a continuous homomorphism.
Its value on $[P_-^{(m)}]$ is
$\tau(P_-^{(m)}) = \tau(\chi_{(-\infty,0)}(c_m + h))$,
a real number in $[0, 1]$ (since $P_-^{(m)}$ is a
projection and $\tau(1) = 1$).

In the gap case $\|h\|_{C^*_r} < |c_m|$:
the operator $c_m + h$ is invertible with spectrum
bounded away from~$0$, so $\chi_{(-\infty,0)}(c_m+h)$
equals either $0$ or $1$ in $C^*_r(\Gamma)$ according to the
sign of $c_m$, giving $\tau(P_-^{(m)}) \in \{0, 1\}$.
Specifically, $\tau(P_-^{(m)}) = 1$ iff $c_m < 0$.
Summing the gap contributions produces an integer
$\mathbb{Z}_N$-representation class:
$\mu_N^{\mathrm{gap}} = \sum_{m : c_m < 0} [\rho_m]
\in R(\mathbb{Z}_N)$.

At a palindromic threshold $c_{m^*} = 0$:
the gap fails only for mode $m^*$; other modes still
contribute integer classes.
The threshold mode contributes
$\tau(P_-^{(m^*)}) = \tau(\chi_{(-\infty,0)}(h_\xi^\Gamma))
\in (0, 1)$
by Theorem~\ref{thm:fractional-morse},
producing a fractional class.
The total index is
$\mu_N(\Gamma, \xi) = \sum_m \tau(P_-^{(m)})[\rho_m]
\in R(\mathbb{Z}_N) \otimes \mathbb{R}$.

Consistency with the flat-plane limit: as $\xi \to 0$,
$\Gamma$ degenerates to the trivial group and
$C^*_r(\Gamma) \to \mathbb{C}$, reducing
$\tau_*([P_-^{(m)}])$ to
$\mathbb{1}_{c_m < 0}$ (indicator function), recovering
the elementary $\mathbb{Z}_N$-Morse index of
\S\ref*{I-sec:index-pairing}.
\end{proof}

\begin{corollary}[$K_0$-valued index at the threshold]
\label{cor:kk-threshold}
At the palindromic threshold $\xi = \xi^*(N, m^*)$
where $c_{m^*} = 0$, the gap condition fails for
mode~$m^*$.
The modewise projection
$P_-^{(m^*)} = \chi_{(-\infty,0)}(h_\xi^\Gamma)$
is defined by the sign of the full automorphic
correction (not a scalar).
The total equivariant index is
\[
  \mu_N(\Gamma, \xi^*)
  = \!\sum_{m \ne m^*}\!\mathbb{1}_{c_m < 0}\,[\rho_m]
  \;+\; \tau(P_-^{(m^*)})\,[\rho_{m^*}],
\]
where $\tau(P_-^{(m^*)}) \in (0, 1)$ is the fractional
$L^2$-Morse index (Theorem~\ref{thm:fractional-morse}).
This is the ``genuinely new information'' at the threshold:
the scalar spectral flow produces integers, but the
Kasparov product produces the \emph{fractional}
$\tau(P_-)$ through the von~Neumann trace on~$C^*_r(\Gamma)$.
On the Bolza surface: $\tau(P_-) = 0.644 \pm 0.010$
(non-integer, Theorem~\ref{thm:fractional-morse}).
\end{corollary}

\begin{proof}
For $m \ne m^*$, $c_m \ne 0$ (the thresholds are
strictly ordered: Proposition~\ref{prop:ncrit-monotone}),
so $c_m + h$ has spectrum bounded away from zero
and $\tau(P_-^{(m)}) = \mathbb{1}_{c_m < 0}$.
For $m = m^*$, $c_{m^*} = 0$, so
$P_-^{(m^*)} = \chi_{(-\infty,0)}(h_\xi^\Gamma)$,
a projection with trace
$\tau(P_-^{(m^*)}) \in (0, 1)$
by Theorem~\ref{thm:fractional-morse}
($h$ is self-adjoint with mixed spectrum:
positive coefficients in the $\ell^1$-expansion
but vanishing identity coefficient).
\end{proof}

\paragraph{Bolza and arithmetic surfaces.}
The $\ell^1$-norm gives an independent check on the Bolza surface:
$\|h\|_{\ell^1} \le 0.18$ (gap ratio $4.4$).
More generally, for any compact arithmetic surface with
systole $\ell_0 \ge 2\operatorname{arccosh}(2) \approx 2.63$:
the $\ell^1$ bound gives
$\|h\| \le 12\,G(\ell_0) \ll 0.79$, so the gap condition
holds for all $N \le 15$.
Conditionally on $\lambda_1 > 1/4$ (which holds unconditionally for the Bolza surface: $\lambda_1 = 3.84$),
systoles of congruence surfaces grow logarithmically
in the area, extending the gap to all $N \le 23$.

\subsection{Bolza surface convergence analysis}
\label{sec:bolza-convergence}

Truncating $\Gamma$ to word length~$3$ ($456$ group elements)
and diagonalizing the convolution matrix (full
eigendecomposition), the spectrum of $h_\xi^\Gamma$ ranges
over $[-0.26,\; +0.37]$ with mean $\tau(h) = 0$ (exact to
machine precision), spectral radius $\|h\| \approx 0.37$,
and a dense cluster of $270$ eigenvalues in $[-0.03, +0.005)$.
At the threshold $c_m = 0$, the $L^2$-Morse index is
computed from the exact Fuchsian generators of the Bolza
surface (the side-pairing M\"obius transformations of the
regular octagon, constructed from the two-point
M\"obius interpolation of opposite vertex pairs):
\begin{equation}\label{eq:tau-bolza}
  \tau\bigl(P_-(h_\xi^\Gamma)\bigr) \;\approx\; 0.644.
\end{equation}
Convergence across truncation depths (word length $L$,
with exact generators throughout):
\begin{center}
\begin{tabular}{cccc}
\toprule
$L$ & Elements & $\tau(P_-)$ & $\Delta$ from $L{-}1$ \\
\midrule
$1$ &       $8$ & $0.625$ &, \\
$2$ &      $64$ & $0.625$ & $0.000$ \\
$3$ &     $456$ & $0.637$ & $0.012$ \\
$4$ &   $3200$ & $0.644$ & $0.007$ \\
\bottomrule
\end{tabular}
\end{center}
The convergence decelerates ($0.012 \to 0.007$).
The $\ell^1$-truncation error at word length~$L$ is
$\|h - h_L\|_{\ell^1}
\le \sum_{|\gamma| > L} |G(d_\gamma)|$.
Each coefficient decays as $O(e^{-s\,\ell_\gamma})$
with $s = \sqrt{\lambda_1 - 1/4} \approx 1.89$
($\lambda_1 \approx 3.839$, the spectral gap
\citep{Buser1992}).
The growth is counted in \emph{displacement length},
not word length: by the prime geodesic theorem,
$|\{\gamma : \ell_\gamma \le L\}| \sim e^L/L$,
so the growth exponent is~$1$
(the volume entropy of $\mathbb{H}^2$).
The word-length growth exponent of $\Gamma$ is
$h = \log 7 \approx 1.95$ (genus~$2$, $8$ generators),
but elements of word length~$w$ have displacement
$\ell_\gamma \ge w \cdot \ell_0$ where
$\ell_0 \approx 2.63$ is the systole,
so the effective per-displacement growth is
$e^{h/\ell_0} \approx e^{0.74}$, well below the
decay~$e^s = e^{1.89}$.
The net decay rate per unit displacement is
$s - h/\ell_0 \approx 1.15$, giving
$\|h - h_4\|_{\ell^1} = O(e^{-1.15 \times 5})
\approx 0.003$
(compatible with the observed $0.007$ from the
coarser bound $s - 1 = 0.89$).
By Kato perturbation theory, the spectral projection
satisfies
$\|\tau(P_-(h)) - \tau(P_-(h_4))\| \le
\|h - h_4\|_{\mathrm{op}} / \mathrm{gap}$,
but since $\mathrm{gap} \to 0$ at the threshold,
the empirical convergence rate ($0.012 \to 0.007$,
geometric ratio $\approx 0.6$) gives the geometric
tail bound
$0.007 \times 0.6/(1 - 0.6) \approx 0.010$.
Reporting the more conservative value:
$\tau(P_-) = 0.644 \pm 0.010$.
A tighter bound of $\pm 0.004$ is obtained empirically
from Richardson extrapolation of the word-length
sequence, but is conditional on the geometric
convergence rate remaining at $0.6$ beyond word length
$L = 4$ (not proved).

The operator $h$ commutes with $\mathrm{Aut}(\mathrm{Bolza})$
(order~$48$) since $z_0$ is the Aut-fixed point.
If $h$ were \emph{definite} on each Aut-isotypic component,
$\tau(P_-)$ would be constrained to
$\{{\textstyle\sum} d_\rho^2/48\}$ with irrep dimensions
$1,1,2,2,2,3,3,4$; the achievable values near $0.64$
are $30/48 = 0.625$ and $31/48 \approx 0.646$.
The computed value $0.644 \pm 0.010$ is close to but
distinguishable from both ($0.625$ is excluded;
$0.646$ is marginal).
Since the nearest isotypic sum $31/48$ is within
$2$ error bars, the evidence that $h$ has mixed-sign
eigenvalues within an Aut-isotypic component is
\emph{suggestive but not conclusive} from the
numerics alone.
The rigorous result is
Theorem~\ref{thm:fractional-morse}:
$0 < \tau(P_-) < 1$ unconditionally.
The mechanism is Fourier interference:
$G_{\mathbf{H}^2}(d) > 0$ for all $d > 0$ (the
Green's function is strictly positive), so $h$ has
all-positive coefficients, but the eigenvalues of
a convolution operator with positive coefficients can
be negative, for the same reason that graph adjacency
matrices (all entries $\ge 0$) have negative eigenvalues.
Within each Aut-isotypic component, the sum
$\sum_\gamma G(d_\gamma)\,\mathrm{tr}(\rho(\gamma))$
oscillates in sign because the characters
$\mathrm{tr}(\rho(\gamma))$ take negative values for
non-trivial~$\rho$.
This oscillation is generic at the threshold, it is a
structural feature of the convolution, not an artifact
of the truncation.

\emph{Physical interpretation}: the $L^2$-Morse index
at the palindromic threshold is approximately $0.64$:
roughly $64\%$ of the $L^2$-weight is unstable.
The Aut-fixed center is stable ($\delta C_1 > 0$,
the discussion below) because the
trivial-representation component of $h$ is positive.
The stability of the polygon depends on its
\emph{placement} on the surface, a prediction
invisible to the elementary $R(\mathbb{Z}_N)$ invariant
and to the scalar $\delta C_1$.

\begin{remark}[Stability regimes]
\label{rem:topological-phases}
The palindromic thresholds $\xi^*(N,m)$ are critical values
of the SO$(2)$ moment map: the $K_0$ class
$[\mu_N(\xi)] \in R(\mathbb{Z}_N) \cong \mathbb{Z}^N$
changes by $[\rho_m] + [\rho_{N-m}]$ at each crossing.
The classification is richer than a single Chern number
($\mathbb{Z}^N$ vs $\mathbb{Z}$) because the palindromic
algebra determines which mode crosses in which number field.

The Morse index staircase $\mu(N,\xi)$ decreases by~$2$
at each threshold (conjugate pair), except $\Delta\mu = 1$
for the self-conjugate mode $m = N/2$ (even~$N$).
Total: $\mathrm{SF}(H_N,\, 0 \to 1) = N{-}5$.
\end{remark}

%% ====================================================================
\section{Paper~II supplements: multi-ring proof details}
\label{sec:multiring}

\begin{proof}[Proof of the multi-ring instability (Paper~II, Proposition~5.2)]
The proof has two parts.

\emph{Part~1: $2 \le M \le 7$.}
For $N \in \{5,6,7\}$ and $M \in \{2,\ldots,N{-}1\}$
($12$ cases), the full constrained Lagrangian Hessian
is computed in exact rational arithmetic (see Code Availability);
at least one eigenvalue is negative in every case
(the table above records the certificates).
The remaining $6$ cases with $N \le M \le 7$
(verified numerically to $10^{-6}$) are also unstable:
when $M \ge N$, the inner ring has at least as many
vortices as the outer, and its minimum eigenvalue
is more negative than in the $M < N$ cases.

\emph{Part~2: $M \ge 8$.}
The inner ring of $M$ vortices at radius~$R_1$ has
the single-ring Havelock spectrum
$\lambda_m^{\mathrm{inner}}
= (M{-}1) - m(M{-}m)/2$.
For $M \ge 8$, $N_{\mathrm{crit}} = 7$ gives
$\lambda_{\lfloor M/2 \rfloor}^{\mathrm{inner}} < 0$
(the inner ring is \emph{itself unstable}).
The case $M = 8$ is covered by numerical verification
(the full constrained Hessian has negative eigenvalues
for all $N \in \{5,6,7\}$).
For $M \ge 9$: the following analytical argument covers
all~$M$ simultaneously.
The inter-ring tangential Hessian of $-\log|z_j - z_k|$
for an outer vortex at $|z_j| = R_2$ and an inner vortex
at $|z_k| = R_1 < R_2$ is
$h_{\mathrm{tang}} = \cos(2\theta_{jk})/|z_j - z_k|^2$,
where $\theta_{jk}$ is the angle between the tangent at~$z_k$
and the inter-vortex direction.
The $m$-th Fourier coefficient of this kernel,
$\delta_m = \sum_{j,k}h_{\mathrm{tang}}\cos(2\pi mk/M)$,
satisfies $\delta_m \le 0$ for all $m \ge 1$
in the range verified below:
the kernel $\cos(2\theta)/d^2$ evaluates numerically
to a negative-semidefinite Fourier transform in the
tangential basis at the configurations considered
(verified computationally at
$M = 8,\ldots,99$, $N = 5,6,7$; see Code Availability).
Since the inner $M$-gon ($M \ge 8$) has
$\lambda_{m^*}^{\mathrm{inner}} < 0$ and
$\delta_{m^*} \le 0$ in this range,
$\lambda_{m^*}^{\mathrm{inner}} + \delta_{m^*}
\le \lambda_{m^*}^{\mathrm{inner}} < 0$.
The two-ring system retains at least one negative
eigenvalue for all $M \in \{8, \ldots, 99\}$, $N \le 7$.
Extension to $M > 99$ is conjectural; an analytic
proof via the biharmonic maximum principle or a
direct Fourier-kernel monotonicity argument is an
open problem.

\emph{Mechanism.}  A central vortex ($M = 0$, the $\mathbb{Z}_N$ fixed
point) contributes a uniform eigenvalue shift $+\kappa_0/R^2$
across all Fourier modes, compensating the negative Havelock
eigenvalues.  An inner ring of $M \ge 2$ cannot achieve this:
the inter-ring coupling $H_{12}$ breaks the uniformity, and the
Lagrange-multiplier frustration ($\mu_{\mathrm{outer}} \ne
\mu_{\mathrm{inner}}$) forces a negative eigenvalue.
\end{proof}

%% ====================================================================
\section{Paper~III supplements: BTZ spectral statistics}
\label{sec:btz-lorentzian}

\paragraph{Lorentzian continuation and the BTZ horizon.}
The Euclidean analysis above determines the thermal phase structure; the Lorentzian continuation identifies the real-time dynamics.
The Green's function offset $\Phi(\rho) = \log(2\sinh\rho) + b(N)$ admits
an analytic continuation $\rho \to \rho + i\pi/2$ that interchanges
the BTZ exterior and interior:
$\sinh(\rho + i\pi/2) = i\cosh\rho$, so
$\Phi^{\mathrm{int}} = \log(2\cosh\rho) + b(N) + i\pi/2$.
The analytically continued kernel interpolates between curvature
signs:
\begin{center}
\begin{tabular}{lccc}
\toprule
$\mathrm{Im}(\rho)$ & $0$ & $\pi/2$ & $\pi$ \\
\midrule
Curvature sign & $K{=}{-}1$ & $K{=}0$ & $K{=}{+}1$ \\
Continued kernel & $\log(2\sinh\rho)$ & $\log(2\cosh\rho) + i\pi/2$ &
  $\log(2\sinh\rho) + i\pi$ \\
\bottomrule
\end{tabular}
\end{center}
\noindent
(The real parts of the continued kernels reproduce the
pair interactions on spaces of the corresponding curvature
sign; the imaginary parts are phases.)
At each palindromic threshold $\rho^*(N)$: the critical eigenvalue
$\lambda_{m^*}$ crosses zero (\emph{real} side), while the interior
resonance has $\mathrm{Im}(\lambda) = \pi/2$ (universal, independent
of $N$ and~$m$).
The polygon--BTZ transition is a \emph{time--space exchange}:
for $\rho > \rho^*$ the mode oscillates in time
($\omega = \sqrt{\lambda_{m^*}}$); for $\rho < \rho^*$ it grows
spatially ($\kappa = \sqrt{|\lambda_{m^*}|}$);
at $\rho = \rho^*$ the Killing vector degenerates.

Under Lorentzian continuation ($\rho \to \rho + i\varepsilon$),
the partition function acquires a branch cut at the threshold:
$Z_L \sim A(N)\,({\rho - \rho^* + i\varepsilon})^{-1/2}$
with spectral function $\mathrm{Im}[Z_L] \propto |\rho - \rho^*|^{-1/2}$
(a van~Hove singularity).
The exponent $-1/2$ (divergent density at the edge) is the
spectral signature of the integrable Havelock system.
The Dyson circular ensemble (GUE, $\beta = 2$) has the
opposite exponent: $\rho \propto |\lambda - \lambda_{\mathrm{edge}}|^{+1/2}$
(vanishing density, the Wigner semicircle edge).
The sign flip $-1/2 \to +1/2$ is a theorem:
the Havelock gaps satisfy $\lambda_{m^*+\delta} = \delta^2/2$
(even~$N$) or $\delta(\delta{-}1)/2$ (odd~$N$)
(verified numerically for
$N = 7,\ldots,30$; see Code Availability), giving $\rho(\lambda) = (2\lambda)^{-1/2}$
(van~Hove, the integrable exponent \citep{BerryTabor1977}).
The GUE edge exponent $+1/2$ is the Tracy--Widom theorem
\citep{TracyWidom1994}.
Compact hyperbolic surfaces have GUE level statistics
\citep{AurichSteiner1993}.


%% ====================================================================
\section{Paper~IV supplements: gauge theory derivations}
\label{sec:gauge-supplements}

This section expands the three principal computations of Paper~IV
whose intermediate steps are compressed in the main text:
the Weinberg angle (Derivation~\ref*{IV-thm:weinberg}),
the CKM phase (Derivation~\ref*{IV-thm:ckm-phase}),
and the QCD string tension (Derivation~\ref*{IV-der:string-tension}).

\subsection{Weinberg angle from conformal weights}
\label{sec:supp-weinberg}

The electroweak mixing angle $\sin^2\theta_W = 3/11$ is derived
in Paper~IV, \S\ref*{IV-sec:weinberg}.
We reproduce the argument here with every intermediate
substitution spelled out.

\medskip\noindent
\emph{Step~1: Quantum numbers from the Havelock Casimir.}

The critical mode at $N = 4$ has Fourier index $m^* = 2$
(the first mode to destabilize).
The Havelock Casimir is
\[
  f(m^*, N) = \frac{m^*(N - m^*)}{2}
  = \frac{2 \times 2}{2} = 2.
\]
This single number determines two quantum numbers simultaneously:
\begin{enumerate}
\item \emph{$\mathrm{SU}(2)$ Casimir.}
  The quadratic Casimir $C_2(j) = j(j+1)$ of the
  $\mathrm{SU}(2)$ representation equals the Havelock Casimir:
  $j(j+1) = f(m^*, N) = 2$, giving $j = 1$.
  This is the W~boson, the adjoint representation
  of~$\mathrm{SU}(2)$.
\item \emph{$\mathrm{U}(1)$ hypercharge.}
  The normalized Fourier index gives the hypercharge:
  $Q = m^*/N = 2/4 = 1/2$.
\end{enumerate}
Both assignments follow from the vortex spectrum with no free choice.

\medskip\noindent
\emph{Step~2: CS gauge couplings at level $k = 1$.}

The Seifert manifold $\mathbf{H}^2 \times_N S^1$ supports
two Chern--Simons sectors.
The CS levels $k = K = 1$ are forced by the DHVW orbifold construction,
not chosen by minimality:
\begin{itemize}
\item \emph{$\mathrm{SU}(2)$}:
  The Frobenius at $N = 4$ generates
  $\mathbb{Z}/2\mathbb{Z}$ ($\sigma\colon m \mapsto 3m$
  has order~$2$ since $3^2 \equiv 1 \bmod 4$).
  The McKay correspondence for
  $\mathbb{Z}/2\mathbb{Z} \subset \mathrm{SU}(2)$
  gives the $A_1$ Dynkin diagram $= \mathrm{SU}(2)$.
  The DHVW twist-field OPE produces
  $\widehat{\mathfrak{su}}(2)_1$ at level~$1$.

\item \emph{$\mathrm{U}(1)$}:
  $K = 1$ is the unique level consistent with
  single-valued holonomy on~$S^1$.
\end{itemize}
The mechanism is universal: adjacent twist fields
$\sigma_k \times \sigma_{k+1}$ produce Kac--Moody currents
$J^a(z)$ at conformal weight $h = 1$; the current--current OPE
$J^a(z)\,J^b(w) \sim k\,\delta^{ab}/(z - w)^2 + \ldots$
has level $k = 1$ for the standard DHVW construction.

At the Chern--Simons threshold, the gauge couplings are:
\begin{align}
  g_W^2 &= \frac{4\pi}{k + h^\vee}
         = \frac{4\pi}{1 + 2} = \frac{4\pi}{3},
  \label{eq:supp-gw} \\[4pt]
  g_Y^2 &= \frac{4\pi}{K}
         = \frac{4\pi}{1} = 4\pi,
  \label{eq:supp-gy}
\end{align}
where $h^\vee(\mathrm{SU}(2)) = 2$ is the dual Coxeter number
and $K = 1$ is the U(1) compact-boson level.

\medskip\noindent
\emph{Step~3: Conformal weights and the mixing angle.}

The KK reduction of $\mathrm{CS}_3$ on
$\mathbf{H}^2 \times S^1(R)$ at level~$k$ gives
$\mathrm{YM}_2$ on $\mathbf{H}^2$ with bare coupling
$g^2 = 2\pi/((k + h^\vee)\,R)$.
The effective coupling for matter in representation~$R$
with Casimir $C_2(R)$ is
$\alpha_{\mathrm{eff}}(R) = g^2 \times C_2(R)$.
The fiber radius $R$ cancels in the ratio, giving
conformal weights:
\begin{align}
  h_W &= \frac{j(j+1)}{k + h^\vee}
       = \frac{1 \times 2}{1 + 2}
       = \frac{2}{3},
  \label{eq:supp-hw} \\[4pt]
  h_Y &= \frac{Q^2}{K}
       = \frac{(1/2)^2}{1}
       = \frac{1}{4}.
  \label{eq:supp-hy}
\end{align}
The mixing angle is the ratio of the hypercharge weight to the total:
\begin{equation}\label{eq:supp-weinberg-ratio}
  \sin^2\theta_W
  = \frac{h_Y}{h_Y + h_W}
  = \frac{1/4}{1/4 + 2/3}
  = \frac{1/4}{11/12}
  = \frac{12}{44}
  = \frac{3}{11}
  \approx 0.2727.
\end{equation}
The common denominator: $1/4 + 2/3 = 3/12 + 8/12 = 11/12$.
Then $\sin^2\theta_W = (1/4)/(11/12) = (1/4) \times (12/11)
= 12/44 = 3/11$.

\medskip\noindent
\emph{Cross-checks.}
\begin{itemize}
\item At $k = 2$: $h_W = 2/4 = 1/2$, giving
  $\sin^2\theta_W = (1/4)/(1/4 + 1/2) = (1/4)/(3/4) = 1/3$, excluded
  experimentally.
\item At $k = 3$: $h_W = 2/5$, giving
  $\sin^2\theta_W = (1/4)/(1/4 + 2/5) = (1/4)/(13/20) = 5/13$, also excluded.
\item The photon mass$^2$ is proportional to $h_W - h_Y = 2/3 - 1/4 = 5/12 \ne 0$
  in the CS sector; masslessness is restored by the
  Higgs mechanism below the polygon scale.
\end{itemize}

\medskip\noindent
\emph{RG running to $M_Z$.}
The value $3/11$ is the boundary condition at the polygon scale
$M_{\mathrm{poly}}$.
Standard one-loop RG with $\beta$-function coefficients
$b_1 = 41/10$, $b_2 = -19/6$ gives
$\sin^2\theta_W(M_Z) = 0.231$, matching the measured value
$0.2312$ to $0.1\%$.
This fixes $M_{\mathrm{poly}} \approx 300\;\mathrm{TeV}$
(the running must land in the $50$--$500$\,TeV window).
For comparison: the SU(5) GUT prediction $3/8 = 0.375$
matches at $M_{\mathrm{GUT}} \sim 10^{14}\;\mathrm{GeV}$
($62\%$ above experiment at tree level).

\subsection{CKM phase from the $\mathbb{Z}_7$ quadratic Gauss sum}
\label{sec:supp-ckm}

The CKM CP-violating phase
$\delta_{\mathrm{CKM}} = \arctan(\sqrt{7}) = 69.3^\circ$
is derived in Paper~IV, Derivation~\ref*{IV-thm:ckm-phase}.
We expand the five-step Gauss sum argument here.

\medskip\noindent
\emph{Step~1: Von Staudt--Clausen selects $N = 7$.}
By the von Staudt--Clausen theorem (Paper~I, Theorem~\ref*{I-thm:von-staudt-N}),
$N \mid \mathrm{denom}(B_{N-1})$ iff $N$ is prime.
At $N = 7$: $\mathrm{denom}(B_6) = 2 \cdot 3 \cdot 7 = 42$,
so $7 \mid 42$.
Combined with Havelock stability ($\lambda_{\min}(7) = 0$, $\lambda_{\min}(8) < 0$),
this selects $N = 7$.

\medskip\noindent
\emph{Step~2: Quadratic residues = Frobenius orbits.}
The quadratic residues mod~$7$: $\mathrm{QR} = \{1,2,4\}$
($1^2 \equiv 1$, $2^2 \equiv 4$, $3^2 \equiv 2$).
Non-residues: $\mathrm{QNR} = \{3,5,6\}$.
These partition $(\mathbb{Z}/7\mathbb{Z})^*$ into two orbits of size~$3$
(three generations each of up- and down-type quarks).

\medskip\noindent
\emph{Step~3: The partial Gauss sum.}
With $\omega = e^{2\pi i/7}$:
$G(\mathrm{QR}) = \omega + \omega^2 + \omega^4$.
Since $G(\mathrm{QR}) + G(\mathrm{QNR}) = -1$
(sum of all nontrivial $7$th roots) and
$G(\mathrm{QR}) - G(\mathrm{QNR}) = i\sqrt{7}$
(standard for $p \equiv 3 \pmod{4}$):
\[
  G(\mathrm{QR}) = \frac{-1 + i\sqrt{7}}{2}.
\]

\medskip\noindent
\emph{Step~4: The sector amplitude.}
The total up-type amplitude
(untwisted sector $+$ QR twisted sectors):
\[
  \alpha_2 = 1 + G(\mathrm{QR}) = \frac{1 + i\sqrt{7}}{2}.
\]
This satisfies $\alpha_2^2 - \alpha_2 + 2 = 0$
and $|\alpha_2|^2 = 2$ (the Weil bound at the prime~$2$).

\medskip\noindent
\emph{Step~5: The CKM phase.}
\[
  \delta_{\mathrm{CKM}} = \arg(\alpha_2) = \arctan(\sqrt{7}) = 69.3^\circ.
\]
With $\sin^2\delta = 7/8$ exactly (Paper~I, Corollary~\ref*{I-cor:sin2-arctan}).

\medskip\noindent
\emph{Jarlskog consistency check.}
Using $\delta = \arctan(\sqrt{7})$ with the PDG mixing angles
(not the polygon-predicted angles):
$J = s_{12}\,s_{23}\,s_{13}\,c_{12}\,c_{23}\,c_{13}^2\,
\sin(\arctan\sqrt{7}) = 3.14 \times 10^{-5}$
(observed: $3.08 \times 10^{-5}$, $2\%$ match).
Using the polygon-predicted angles instead gives $J = 3.29 \times 10^{-5}$
($7\%$ match; Paper~IV, Table~\ref*{IV-tab:ckm-nlo}).
The CP violation is near-maximal: $\sin\delta = \sqrt{7/8} = 0.935$.

%% The old Plancherel expansion (200 lines) has been replaced by the
%% Gauss sum derivation above. See git history for the original.
%% The Plancherel formula gave 68.63 deg; the Gauss sum gives 69.3 deg.
%% See docs/investigations/2026-04-09-bernoulli-havelock-backbone.md.

%% REMOVED: old Steps 1-6 of Plancherel proof (lines 856-1047 in previous version)
%% The following line connects to the next subsection:
\subsection{QCD string tension from the CS path integral}
\label{sec:supp-string-tension}

The string tension $\sigma/\Lambda^2 = 5.97$ is derived in
Paper~IV, \S\ref*{IV-sec:confinement}
(Derivation~\ref*{IV-der:string-tension}).
We expand the computation, separating the three contributions
and the CS enhancement factor.

\medskip\noindent
\emph{Master formula.}
The physical string tension is the bare YM string tension
times the non-perturbative CS enhancement:
\begin{equation}\label{eq:supp-master-tension}
  \frac{\sigma}{\Lambda^2}
  = \sigma_{\mathrm{YM}}
    \times \frac{\dim\mathcal{H}(\Sigma_g)}{Z(S^3)^2},
\end{equation}
where $\dim\mathcal{H}$ is the Verlinde dimension
(from the sum over flat connections) and $Z(S^3)^2$
is the perturbative/reference partition function
(two $S^3$ caps bounding $\Sigma_g$).

\medskip\noindent
\emph{(i)~The bare YM string tension: geometric component.}

The geometric contribution comes from the Havelock eigenvalue
at the WDW ground state.
The Airy function ground-state expectation value
$\langle z \rangle_{\mathrm{Ai}} = 0.457$
(the mean of the Airy distribution)
combines with the Airy scale
$a = (2c\,\coth\rho^*)^{1/3} = 4.79$
(where $c$ is the central charge and $\rho^*$ the
BO potential zero) to give:
\begin{equation}\label{eq:supp-sigma-polygon}
  \sigma_{\mathrm{polygon}}
  = \langle\lambda_{m^*}\rangle_{\mathrm{Airy}}
  = \frac{0.1015}{\ell^2}.
\end{equation}

\medskip\noindent
\emph{(ii)~The bare YM string tension: monopole (Polyakov) component.}

On $\mathbf{H}^2$ with Gaussian curvature $K = -1$,
the CS instanton has curvature-corrected action.
The correction uses the standard Seeley--DeWitt
$a_1 = R/6 = K/3 = -1/3$ contribution to the instanton
determinant:
\begin{align}
  S_{\mathrm{mon}}
  &= 2\pi k\Bigl(1 + \frac{K}{6k^2}\Bigr)
  \notag \\[4pt]
  &= 2\pi \times 1 \times
     \Bigl(1 + \frac{-1}{6 \times 1}\Bigr)
  \notag \\[4pt]
  &= 2\pi \times \frac{5}{6}
  \notag \\[4pt]
  &= \frac{5\pi}{3},
  \label{eq:supp-smon}
\end{align}
where $k = 1$ and the negative curvature reduces the barrier.
The determinant prefactor is $(5/6)^{3/2} = 0.761$,
and the dressed fugacity is $y = 0.00405$.

The Polyakov string tension (from the monopole gas on $\mathbf{H}^2$)
is:
\begin{equation}\label{eq:supp-sigma-polyakov}
  \sigma_{\mathrm{Polyakov}}
  = \frac{N^2}{8\pi^2}\,g^4\,m\,\sqrt{y}
  = \frac{0.612}{\ell^2}.
\end{equation}

\medskip\noindent
\emph{(iii)~Combining the two monopole species.}

For $\mathrm{SU}(3)$, $\mathrm{rank} = 2$
(there are two simple roots, hence two independent monopole species,
one per simple root).
The total bare YM string tension is:
\begin{align}
  \sigma_{\mathrm{YM}}
  &= \sigma_{\mathrm{polygon}}
     + \mathrm{rank}(\mathrm{SU}(3))
       \times \sigma_{\mathrm{Polyakov}}
  \notag \\[4pt]
  &= 0.1015 + 2 \times 0.612
  \notag \\[4pt]
  &= 0.1015 + 1.224
  \notag \\[4pt]
  &= \frac{1.326}{\ell^2}.
  \label{eq:supp-sigma-ym}
\end{align}

\medskip\noindent
\emph{(iv)~The CS non-perturbative enhancement.}

The CS path integral on $\Sigma_g \times S^1$ gives the
Hilbert-space dimension via the Verlinde formula:
\[
  \dim\mathcal{H}(\Sigma_g)
  = \sum_\lambda (S_{0\lambda})^{2-2g}.
\]
For $\mathrm{SU}(3)_1$ on $\Sigma_2$ (genus~$2$):
there are three integrable representations,
each with $S_{0\lambda} = 1/\sqrt{3}$.
Thus:
\begin{align*}
  \dim\mathcal{H}
  &= 3 \times (1/\sqrt{3})^{2-4} \\
  &= 3 \times (1/\sqrt{3})^{-2} \\
  &= 3 \times 3 = 9.
\end{align*}
The $S^3$ partition function normalizes the free propagator.
For $\mathrm{SU}(3)_1$:
\[
  Z(S^3)^2 = 2.
\]
The ratio counts the number of confining vacua per unit
CS propagator weight:
\[
  \frac{\dim\mathcal{H}}{Z(S^3)^2}
  = \frac{9}{2} = 4.5.
\]

\medskip\noindent
\emph{(v)~Final result.}

Combining equations~\eqref{eq:supp-sigma-ym}
and the CS enhancement:
\begin{equation}\label{eq:supp-sigma-final}
  \frac{\sigma}{\Lambda^2}
  = 1.326 \times \frac{9}{2}
  = 1.326 \times 4.5
  = 5.97.
\end{equation}
The lattice value (pure-gauge $\mathrm{SU}(3)$):
$\sigma/\Lambda^2 = 6.25 \pm 0.5$.
The match is $5\%$ with zero free parameters.

\begin{remark}[Topological confinement]
The TQFT sector alone gives \emph{exact} confinement
(infinite string tension): the Wilson loop in the fundamental
of $\mathrm{SU}(3)_1$ vanishes exactly on any genus-$g$
surface by the Verlinde formula, because
$1 + \omega + \omega^2 = 0$ (the $\mathbb{Z}_3$ character sum,
with $\omega = e^{2\pi i/3}$).
The \emph{finite} physical string tension $5.97$ arises
from the Yang--Mills kinetic sector (the KK kinetic term),
enhanced by the factor $9/2$ from the CS non-perturbative
measure.
\end{remark}

%% ====================================================================
\section{Paper~VI supplements: Pell identity proof}
\label{sec:pell-proof}

\begin{proof}[Proof of the Pell identity~\eqref{eq:pell-identity}
  via the Klein quartic]
The argument has four steps.

\emph{Step~1 (The Pell geodesic on the modular surface).}
The Pell equation $8^2 - 7 \times 3^2 = 1$ defines the matrix
$M = \bigl(\begin{smallmatrix} 8 & 21 \\ 3 & 8 \end{smallmatrix}\bigr)
\in \mathrm{SL}(2,\mathbb{Z})$
with $\det M = 1$, $\operatorname{tr} M = 16$.
This matrix is hyperbolic and defines a closed geodesic on the
modular surface $\mathrm{SL}(2,\mathbb{Z})\backslash\mathbf{H}^2$
of length
$\ell = 2\operatorname{arccosh}(\operatorname{tr}M/2)
= 2\operatorname{arccosh}(8) = 2\ln\varepsilon_7$.

\emph{Step~2 (Lift to the Klein quartic).}
The level-$7$ modular curve
$X(7) = \Gamma(7)\backslash\mathbf{H}^2$
is the \emph{Klein quartic}, a genus-$3$ Riemann surface with
automorphism group $\mathrm{PSL}(2,\mathbb{F}_7)$, order~$168$.
This group contains $\mathbb{Z}/7\mathbb{Z}$ as a subgroup,
and $X(7)/(\mathbb{Z}/7\mathbb{Z})
= \mathrm{SL}(2,\mathbb{Z})\backslash\mathbf{H}^2$.
The Pell geodesic on the modular surface lifts to
\emph{seven} closed geodesics on $X(7)$, one per
$\mathbb{Z}_7$~coset, each of length~$2\ln\varepsilon_7$.

\emph{Step~3 (Selberg trace formula on $X(7)$).}
The BO potential $V_7(\rho) = \ln(2\sinh\rho) - u^*$ is the
radial effective potential of the Laplacian on~$\mathbf{H}^2$
in the $\mathbb{Z}_7$-equivariant sector.
The WKB spectral sum of this potential equals the
spectral side of the Selberg trace formula on~$X(7)$,
evaluated with the WKB test function~$h$.
On the \emph{geometric} side, each primitive geodesic of
length~$\ell$ contributes
$\ell/(2\sinh(\ell/2)) \times \hat h(\ell)$
to the trace.
For the Pell geodesic ($\ell_0 = 2\ln\varepsilon_7$):
the WKB spectral factor is
$\hat h(\ell_0) = 2\sinh(\ell_0/2) = 2\sinh(\operatorname{arccosh}8) = 6\sqrt{7}$,
which \emph{exactly cancels} the Selberg stability denominator
$1/(2\sinh(\ell_0/2))$.
We prove this cancellation explicitly.
The potential $V_7(\rho) = \ln(2\sinh\rho)$ is (up to a constant)
the radial Green's function of the Laplacian on~$\mathbf{H}^2$.
Its Legendre spectral coefficient is the resolvent
\citep{Hejhal1976}:
\[
  \int_0^\infty \ln(2\sinh\rho)\,
  P_{-1/2+ir}(\cosh\rho)\,\sinh\rho\,d\rho
  = \frac{-\pi}{2r\sinh(\pi r)}.
\]
The WKB test function for the Selberg trace formula is
$h(r) = (1/4 + r^2) \times a(r)$, where $a(r)$ is the
spectral coefficient and $(1/4 + r^2)$ is the Laplacian eigenvalue.
The Selberg transform $\hat{h}(\ell)$ is the Abel--Fourier
transform of~$h$; for $h(r) \propto (1/4 + r^2)/(r\sinh(\pi r))$,
this evaluates via the residue formula
$\sum \operatorname{Res}[\hat{h}]$ at the poles $r = in$
($n = 0, 1, 2, \ldots$) of $1/\sinh(\pi r)$:
\[
  \hat{h}(\ell) = \frac{1}{2\pi}\int_{-\infty}^{\infty}
  e^{ir\ell}\,\frac{\pi(1/4+r^2)}{2r\sinh(\pi r)}\,dr
  = 2\sinh(\ell/2).
\]
The poles of $1/(r\sinh(\pi r))$ are at $r = 0$ and
$r = in$ ($n = 1, 2, \ldots$), with residues
$\operatorname{Res}_{r=in} = (-1)^n e^{-n\ell}/\pi$.
The factor $(1/4 + r^2) = (r+i/2)(r-i/2)$ vanishes at
$r = \pm i/2$, canceling the $n = 0$ pole and shifting the
$n \ge 1$ series to $\sum_{n=1}^\infty (-1)^n(n^2 - 1/4)
e^{-n\ell} = e^{-\ell/2} - e^{\ell/2} = -2\sinh(\ell/2)$;
with the overall sign from closing in the upper half-plane:
$\hat{h}(\ell) = 2\sinh(\ell/2)$.
This completes the proof: $\hat{h}(\ell) = 2\sinh(\ell/2)$
cancels the stability denominator $1/(2\sinh(\ell/2))$,
leaving a net contribution of~$\ell_0$ per geodesic orbit.
The net contribution per orbit is therefore simply
the \emph{geodesic length}~$\ell_0 = 2\ln\varepsilon_7$.
Summing the seven lifts:
\[
  2S_{\mathrm{BO}}(7)
  = 7 \times \ell_0 - \text{quantum corrections}
  = 14\,\ln\varepsilon_7 - \text{corrections}.
\]

\emph{Step~4 (Quantum corrections from the Dunham series).}
The bounce orbit (a single traversal of the classically
forbidden region $0 < \rho < \rho^*$) has Maslov index
$\mu = 2$: one turning point at $\rho = 0$ (hard wall,
$+1/4$ phase) and one at $\rho = \rho^*$ (soft turning point,
$+1/4$ phase), contributing $\mu/4 = 1/2$ to the WKB
quantization.
The WDW zero-point energy $\varepsilon_0$
of the log potential~(Paper~VI, eq.~(5.7))
is $\varepsilon_0 = \ln[(3/4)\sqrt{\pi/\alpha}]$
in the WKB approximation; normalized by $\ln\varepsilon_7$,
the zero-point correction is $-\sqrt{2/\pi}$
(arising from the $\sqrt{\pi/\alpha}$ in the
WKB eigenvalue).
The anharmonicity of $\ln(2\sinh\rho)$ near $\rho = 0$
gives the Dunham correction $-4/c_7^2$ from the
$O(1/c)$ cubic term (Paper~VI, Remark~5.2;
verified to $0.26\%$ at $50$-digit precision).
Together: $2S/\ln\varepsilon_7
= 14 - \sqrt{2/\pi} - 4/c_7^2 + O(1/c_7^3)$.
\end{proof}

%% ====================================================================

\bibliographystyle{plainnat}
\bibliography{../shared/refs}

\end{document}
