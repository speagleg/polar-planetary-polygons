\input{../shared/preamble}

\externaldocument[I-]{../paper-1-mathematics/main}
\externaldocument[II-]{../paper-2-physics/main}
\externaldocument[III-]{../paper-3-gravity/main}
\externaldocument[V-]{../paper-5-s3-framework/main}
\externaldocument[VI-]{../paper-5-cosmology/main}
\externaldocument[VII-]{../paper-6-discussion/main}


\title{\seriestitle\\[6pt]\large Paper IV: The Havelock Field Theory}
\author{Gordon Speagle}
\date{}

\begin{document}
\maketitle

\begin{abstract}
From one algebraic criterion (integer spin at the
Havelock threshold, giving $N = 4$ and $N = 7$)
and two scales ($M_P$ and $\Delta m^2_{\mathrm{atm}}$),
the Havelock eigenvalue structure determines the
Standard Model gauge group
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$,
three generations, and all coupling constants.
$\mathrm{SU}(3)$ is derived by exhaustive subgroup exclusion
(CP = conformal inversion eliminates $\mathbb{Z}/2$;
the full group gives no conjugate pair;
the unique remaining $\mathbb{Z}/3$ maps to $A_2$ via McKay).
The Weinberg angle $\sin^2\theta_W = 3/11$ follows from the
Havelock eigenvalue $f(2,4) = 2$ (a geometric quantity,
no representation label needed; $0.1\%$ match after two-loop running).
The CKM phase $\delta = 69.3^\circ$ is a normalization-independent
geometric invariant of $\mathbf{H}^2$ scattering
(PDG/CKMfitter 2024: $\gamma = 65.9^\circ \pm 3.5^\circ$).
Further: $\alpha_s(M_Z) = 0.114$--$0.122$
(observed: $0.1180 \pm 0.0009$),
$m_H = 122$--$128$\,GeV (observed: $125.25$\,GeV).
Confinement is exact ($\gcd(7,3) = 1$);
the string tension $\sigma/\Lambda^2 = 5.97$
matches lattice QCD ($6.25 \pm 0.5$).
The base genus $g = 2$ is the unique value for which
the derived SU(3) confines at a scale compatible with
the derived quark masses.
The proton is exactly stable ($\mathbb{Z}_{56}$).
\end{abstract}

%% ====================================================================
\section{Introduction}
\label{sec:intro}

The preceding papers established that the Havelock eigenvalue
$\lambda_m = C_1(\xi) - m(N{-}m)/2$ governs point vortex
polygon stability (Paper~II) and gravitational polygon stability
in $2{+}1$D (Paper~III), with the palindromic thresholds
encoding algebraic number fields (Paper~I).
Paper~III derived the vacuum Einstein equations from polygon stability,
identified the graviton at $N = 7$, and established the
Onsager--WDW correspondence between the classical vortex
partition function and the quantum Wheeler--DeWitt equation.

This paper derives the Standard Model gauge group, coupling
structure, and fermion mass hierarchy from the $N = 7$
polygon sector on the Seifert manifold
$\mathbf{H}^2 \times_N S^1$.
The inputs are one integer $N = 7$ (selected by the Pell
equation) and two dimensionful scales ($M_P$ and $\Delta m^2_{\mathrm{atm}}$).
Sections~\ref{sec:spacetime}--\ref{sec:anomaly} build the
infrastructure: the Thurston
$\widetilde{\mathrm{SL}(2,\mathbb{R})}$ spacetime
$\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$,
the field content, the emergent $4$D graviton,
coupling constants from $c = 12\,b(N)$, and the
vanishing of the Weyl anomaly.
Sections~\ref{sec:gauge-group}--\ref{sec:uv} derive the core
results: the gauge group
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$,
the Weinberg angle $\sin^2\theta_W = 3/11$,
the complete $4$D Lagrangian, and UV completion via the
orbifold CFT.
Sections~\ref{sec:loops}--\ref{sec:fermions} extract the
particle physics: one-loop corrections, the polygon scale,
fermion masses, CKM mixing, and the CP phase
$\delta_{\mathrm{CKM}} = 69.3^\circ$.
The cosmological sector ($N = 11$) is developed in Paper~VI;
the full parameter accounting and predictions appear in Paper~VII.

%% ====================================================================
\section{Background and notation}
\label{sec:background}

For the reader's convenience, we collect the key definitions
and results from Papers~I--III that this paper builds on.
No new results appear in this section.

\paragraph{The Havelock eigenvalue.}
For an $N$-gon of point vortices on a surface of constant curvature,
the linearized stability is governed by the eigenvalue
$\lambda_m = (N{-}1) - m(N{-}m)/2$ for Fourier mode~$m$
($m = 1, \ldots, N{-}1$).
The polygon is stable iff $\lambda_m \ge 0$ for all~$m$.
The critical mode $m^* = \lfloor N/2 \rfloor$ has the smallest~$\lambda$;
stability fails at $N = 8$ on the flat plane (Havelock 1931).

\paragraph{The Havelock Casimir.}
$f(m,N) = m(N{-}m)/2$ is the complement of $\lambda_m$:
$\lambda_m + f(m,N) = N{-}1$.
It equals $(N^2/12)(1 - 6B_2(m/N))$,
connecting the polygon spectrum to Bernoulli polynomials.

\paragraph{The curvature coefficient.}
On a surface of Gaussian curvature~$K$, the stability eigenvalue is
$C_1(\xi) - f(m,N)$, where $\xi = r_E^2/a^2$ is the curvature
parameter.
On~$\mathbf{H}^2$:
$C_1(\mathbf{H}^2, \xi) = (N{-}1)(1{+}\xi^2)/(1{-}\xi)^2$.
On~$\mathbf{S}^2$:
$C_1(\mathbf{S}^2, \xi) = (N{-}1)(1{-}\xi)/(1{+}\xi)$.
On the flat plane ($\xi \to 0$): $C_1 = N{-}1$,
recovering the Havelock eigenvalue $\lambda_m = (N{-}1) - f(m,N)$.
On~$\mathbf{H}^2$ ($\xi > 0$), $C_1 > N{-}1$,
so polygons with $N > 7$ are stabilized by negative curvature.

\paragraph{The CMS--CS identification.}
On the symmetric space
$\mathbf{H}^2 = \mathrm{SL}(2,\mathbb{R})/\mathrm{SO}(2)$,
the Laplace--Beltrami operator $\Delta_{\mathbf{H}^2}$
equals the Casimir operator
$\Omega_{\mathfrak{sl}(2,\mathbb{R})} = \sum_a T^a T^a$
of the isometry algebra
(Helgason symmetric space theorem;
\citealt{Helgason1984}).
In the Witten formulation of $2{+}1$D gravity as
Chern--Simons theory, the CS connection is
$A = \omega + e/\ell$ (\citealt{Witten1988}),
so the CS covariant Laplacian coincides with
$\Delta_{\mathbf{H}^2}$.
The eigenvalue on mode~$m$ is $f(m,N) = m(N{-}m)/2$
in all three formalisms
(Paper~III, Proposition~\ref*{III-prop:casimir-havelock}).

\paragraph{The central charge.}
$c = 12\,b(N)$ where
$b(N) = N(N{+}1)/12 - \ln 2 + (\ln N)/(N{-}1)$
is the WDW ground-state energy
(Paper~III, eq.~\ref*{III-eq:central-charge}).
At $N = 7$: $b(7) = 4.298$, $c_7 = 51.57$.
At $N = 11$: $b(11) = 10.55$, $c_{11} = 126.56$.

\paragraph{Convention.}
Throughout, $e$ denotes the Euler class in the Chern convention:
$e = N/2$ (equivalently, $c_1 = N$).

\paragraph{Inputs.}
One criterion and two scales:
\emph{(H1)}~integer spin at the Havelock stability threshold
(the Pell equation gives $N = 4$ and $N = 7$;
Paper~VI, Proposition~\ref*{VI-prop:pell-unique});
\emph{(H2)}~$M_P$ (the Planck mass);
\emph{(H3)}~$\Delta m^2_{\mathrm{atm}}$ (the neutrino mass scale;
Paper~VII).
The Seifert geometry
$\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$
is derived from~(H1) in Proposition~\ref{prop:spacetime} below.

%% ====================================================================
\section{The spacetime: $\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$}
\label{sec:spacetime}

\begin{proposition}[The spacetime geometry is uniquely determined by $N$]
\label{prop:spacetime}
The spacetime geometry
$\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$ is uniquely
determined by the Pell equation (pure number theory,
no geometric input):
\begin{enumerate}
\item[\textnormal{(i)}]
  The Pell equation selects $N = 7$ ($j = 2$, graviton) and
  $N = 4$ ($j = 1$, gauge boson), the only polygons with
  integer spin at the critical mode (Paper~VI, Proposition~\ref*{VI-prop:pell-unique}).
\item[\textnormal{(ii)}]
  The Havelock spectrum forces a one-dimensional fiber.
  Four spectral properties of $\lambda_m = (N{-}1) - m(N{-}m)/2$
  each independently require $d_{\mathrm{fiber}} = 1$:
  mode count ($N{-}1$), single index, non-degeneracy,
  and palindromic pairing (proof below).
  The circle~$S^1$ is the unique compact connected $1$-manifold.
\item[\textnormal{(iii)}]
  With the $S^1$ fiber established, KK flux additivity
  gives $N = 11 = 4 + 7$
  (Paper~VI, Theorem~\ref*{VI-thm:n-selection}:
  the $S^1$ fiber carries a single KK quantum number,
  so the total polygon order is the sum of the
  gauge ($N = 4$) and gravitational ($N = 7$) sectors;
  alternative combinations $4{+}4$, $7{+}7$, and $4 \times 7$
  are excluded by the Havelock stability bound and the
  requirement of integer spin at each critical mode).
\item[\textnormal{(iv)}]
  $N = 11$ requires negative curvature for stability
  ($f(5,11) = 15 > N{-}1 = 10$), forcing the base to be
  $\mathbf{H}^2$.
\item[\textnormal{(v)}]
  Thurston classification: among the $8$ geometries,
  only $\widetilde{\mathrm{SL}(2,\mathbb{R})}$ has a
  negatively curved base with a \emph{non-trivial}
  compact fiber.
  (The product $\mathbf{H}^2 \times S^1$ also has a
  negatively curved base, but has Euler class $e = 0$.
  The $\mathbb{Z}_N$ orbifold action $\varphi \to
  \varphi + 2\pi/N$ on the fiber generates $N$ cone
  points on the base, each with isotropy
  $\mathbb{Z}_N$.
  Each of the $N$ cone points has isotropy order~$N$
  and holonomy $e^{2\pi i/N}$ around the fiber.
  The Seifert Euler class is
  $e = \sum_{k=1}^N \beta_k/\alpha_k = N \cdot (1/N) = 1$
  in the Seifert normalization, or $e = N/2$ in the
  Chern class convention ($c_1 = N$, $e = c_1/2$).
  Throughout this paper, we use the Chern class convention
  $c_1 = N$; the Seifert normalization appears only here.
  The flux quantization
  $\oint\!dA = \pi N$ (from $c_1 = N$ with the KK
  normalization $\oint\!dA = \pi c_1$) gives
  $|F|^2 = N^2/4$; this differs from the Besse normalization
  ($\oint\!dA = 2\pi e$) by a factor of~$N/(2e) = N/2$,
  because the Seifert Chern class $c_1 = N = 2e$.
  The consistency check
  $\Lambda_3 = 0$ at $N = 4$ (Paper~VI, eq.~\ref*{VI-eq:lambda})
  verifies the normalization; the Besse convention would give
  $\Lambda_3 = 0$ at $N = 2$, which does not correspond to
  any gauge sector.
  Since $e \ne 0$, the manifold is
  Seifert (non-product).)
\end{enumerate}
No step requires knowing the geometry in advance:
the logical flow is
Pell $\to$ $N = 4, 7$ $\to$ $S^1$ fiber (spectral uniqueness)
$\to$ $N = 11$ (KK flux additivity on the derived $S^1$)
$\to$ $\mathbf{H}^2$ (stability) $\to$ Seifert ($e \ne 0$).
The $S^1$ fiber is derived from the Havelock spectrum
(Step~(ii)) before it is used in the flux additivity
(Step~(iii)), eliminating any circularity.
\end{proposition}

\begin{proof}
Step~(i) is number theory (Paper~I).

\emph{Step~(ii) (The fiber is $S^1$: spectral uniqueness).}
The Havelock spectrum $\lambda_m = (N{-}1) - m(N{-}m)/2$
uniquely determines a one-dimensional fiber by four
spectral properties:
\begin{enumerate}
\item \emph{Mode count.}
  The polygon has $N{-}1$ independent modes ($m = 1,\ldots,N{-}1$).
  On $S^1/\mathbb{Z}_N$: exactly $N{-}1$ nontrivial Fourier modes.
  On any $d$-dimensional fiber with $d \ge 2$:
  the mode count grows as $O(N^d)$, exceeding $N{-}1$.
\item \emph{Single index.}
  $\lambda_m$ depends on one integer~$m$.
  Laplacian eigenvalues on $T^d$ ($d \ge 2$) or $S^d$ ($d \ge 2$)
  depend on $d$ indices.
\item \emph{Non-degeneracy.}
  $f(m,N) = m(N{-}m)/2$ is injective on $\{1,\ldots,\lfloor N/2\rfloor\}$.
  On $S^d$ ($d \ge 2$), the eigenvalue $l(l{+}d{-}1)$ has
  $(2l{+}d{-}1)\binom{l{+}d{-}2}{d{-}1}$-fold degeneracy.
\item \emph{Palindromic pairing.}
  $\lambda_m = \lambda_{N-m}$ (exactly $2$-fold, from complex conjugation
  $z \leftrightarrow \bar z$ on $S^1$).
  Higher-dimensional fibers have larger pairing multiplicity.
\end{enumerate}
Each property independently forces $d_{\mathrm{fiber}} = 1$.
The circle $S^1$ is the unique compact connected $1$-manifold.

\emph{Step~(iii) (KK flux additivity).}
With the $S^1$ fiber derived from the spectrum,
KK flux additivity gives $N = 11 = 4 + 7$
(Paper~VI, Theorem~\ref*{VI-thm:n-selection}).

\emph{Step~(iv) (The base is $\mathbf{H}^2$).}
The cosmological polygon $N = 11$ has critical Casimir
$f(5,11) = 15 > N{-}1 = 10$, so the flat-plane $N$-gon is
\emph{unstable} ($\lambda_5 = 10 - 15 < 0$).
Stability requires $C_1 \ge 15 > N{-}1$, which by the
curvature-stability dichotomy (Paper~I,
Proposition~\ref*{I-prop:curvature-dichotomy})
is achievable only on a negatively curved surface.
On~$\mathbf{S}^2$, positive curvature tightens the bound:
$C_1(\mathbf{S}^2) < N{-}1$ for all $\xi > 0$ (Paper~I, \S4.2).
On~$\mathbf{H}^2$, negative curvature opens it:
$C_1(\mathbf{H}^2, \xi) > N{-}1$ for $\xi > 0$, reaching
$C_1 = 15$ at $\xi = \xi^*(11) = 3 - 2\sqrt{2} \in \mathbb{Z}[\sqrt{2}]$
(Paper~I, \S4.3).
$\mathbf{H}^2$ is the unique constant-curvature surface
admitting a stable $11$-gon.

\emph{Step~(v) (The Thurston classification forces
$\widetilde{\mathrm{SL}(2,\mathbb{R})}$).}
Among the eight Thurston geometries for locally homogeneous
$3$-manifolds, only $\widetilde{\mathrm{SL}(2,\mathbb{R})}$ has a
negatively curved base with a circle fiber.
The other seven fail:
$\mathbf{E}^3$, Nil, Sol have non-negatively curved bases;
$\mathbf{S}^3$ and $\mathbf{S}^2 \times \mathbb{R}$ have positive
curvature;
$\mathbf{H}^3$ has no fibration;
$\mathbf{H}^2 \times \mathbb{R}$ has Euler class $e = 0$
(trivial fibration, no holonomy) \citep{Thurston1982}.
\end{proof}

\noindent
The metric is
\begin{equation}\label{eq:metric-4d}
  ds^2 = -dt^2 + ds^2_{\mathbf{H}^2}
  + (d\varphi + A_i\,dx^i)^2,
\end{equation}
where $ds^2_{\mathbf{H}^2} = d\rho^2 + \sinh^2\!\rho\;d\theta^2$
($\kappa = -1$) and $A$ is a $\mathrm{U}(1)$ connection on the $S^1$ fiber
with flux
\begin{equation}\label{eq:flux}
  \frac{1}{2\pi}\oint dA = \frac{N}{2}.
\end{equation}
For odd~$N$, the flux is half-integral; the fiber carries a
$\mathrm{spin}^c$ structure rather than a line bundle
(equivalently, the $\mathbb{Z}_N$ orbifold of
the integer-flux bundle at level~$N$).
The Kaluza--Klein reduction on $S^1$ decomposes any field into
$N$ modes with masses
\begin{equation}\label{eq:kk-masses}
  \mu_m = |m - N/2|, \qquad m = 0, 1, \ldots, N{-}1.
\end{equation}
The critical mode $m = N/2$ (even $N$) is massless: it is the
Havelock critical mode.
The KK energy $E_{\mathrm{KK}} = (m{-}N/2)^2/2 = N^2/8 - f(m,N)$
is complementary to the Casimir: their sum $N^2/8$ is
mode-independent.

\begin{remark}[Low-energy limit is $3{+}1$D Minkowski]
\label{rem:minkowski}
The Seifert spacetime reduces to $3{+}1$D Minkowski at
laboratory scales.  Two length scales control deviations:
(i)~$\ell \sim 10^{26}\,$m (the $\mathbf{H}^2$ curvature radius):
at $r \ll \ell$, $\mathbf{H}^2 \to \mathbb{R}^2$
(flat to $r^2/\ell^2 < 10^{-44}$ at collider scales);
(ii)~$R \sim 10^{-21}\,$m (the $S^1$ radius):
at $r \gg R$, KK modes decouple,
leaving $3{+}1$D gravity $+$ the Standard Model.
Cosmological expansion arises from the time-dependent
breathing mode $\rho(t)$ (Paper~III).
\end{remark}

%% ====================================================================
\section{The field content}
\label{sec:fields}

The metric~\eqref{eq:metric-4d} yields, after KK reduction:
\begin{enumerate}
\item \textbf{Gravity.}
  The $2{+}1$D metric $g_{ij}$ on $\mathbf{H}^2$ is a
  Chern--Simons gauge field at level
  $k = c/6 = 2\,b(N)$
  (Proposition~\ref{III-prop:graviton}, Paper~III).
\item \textbf{$\mathrm{U}(1)$ gauge field.}
  The off-diagonal component $A_\mu$ from the KK ansatz;
  coupling $\alpha = 6/(\pi c)$.
\item \textbf{Charged scalar.}
  $N$ KK modes $\Phi_m$ with masses $\mu_m = |m - N/2|$.
  The critical mode ($m = N/2$) is massless.
\item \textbf{Dirac fermion.}
  $N$ KK modes $\Psi_m$ with masses $\mu^f_m = |m - (N{-}1)/2|$
  (shifted by $1/2$ from the spin connection).
  Even~$N$: bosons have a massless mode, fermions do not
  (the $1/2$-shift from the spin connection breaks the
  boson--fermion mass degeneracy; the spectrum is
  non-supersymmetric at every KK level).
\item \textbf{Radion.}
  The $S^1$ modulus $\sigma$ (fiber radius), stabilized by
  the flux energy at $\sigma = 1$.
\end{enumerate}

%% ====================================================================
\section{Emergence of the 4D spin-$2$ graviton}
\label{sec:graviton}

Paper~III identified the graviton in $2{+}1$D as the marginal
Havelock mode at $N = 7$.  The central question of this section
is whether the graviton survives the lift to four dimensions.

In $2{+}1$D, pure gravity is topological
\citep{Witten1988} with zero propagating bulk modes.
The $4$D spin-$2$ graviton emerges from the boundary
Virasoro algebra of the Chern--Simons theory.

\begin{derivation}[Emergent spin-$2$ graviton]%
\footnote{Results labeled ``Derivation'' are proved from axioms
(H1)--(H3) using standard theorems of CS/WZW theory and
differential geometry.
They differ from ``Theorem'' only in that they involve
physical identifications (e.g., the CS connection as a
gravitational field) rather than purely algebraic statements.}
\label{thm:graviton-4d}
The $\mathrm{SL}(2,\mathbb{R}) \times \mathrm{SL}(2,\mathbb{R})$
Chern--Simons gauge theory on
$\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$
produces a $4$D spin-$2$ graviton with $2$ tensor polarizations
($+$~and~$\times$).
The spin-$2$ character is derived from the Virasoro algebra
of the boundary CFT; no $4$D gravitational dynamics are assumed.
\end{derivation}

\begin{proof}
Four steps, each using an established result.

\emph{Step~1 (CS $\to$ boundary Virasoro).}
The $\mathrm{SL}(2,\mathbb{R})$ CS theory on~$\mathbf{H}^2$
(a manifold with conformal boundary $\partial\mathbf{H}^2 = S^1_\theta$)
induces a $2$D conformal field theory on the boundary.
The asymptotic symmetry algebra is two copies of
the Virasoro algebra at central charge $c = 12\,b(N)$
\citep{BrownHenneaux1986}.
This is a \emph{theorem} about Chern--Simons gauge theory;
it does not require $4$D gravity as input.

\emph{Step~2 (Virasoro $\to$ spin-$2$).}
The stress tensor $T(z)$ of the boundary CFT has conformal
weight~$(2,0)$, and $\bar T(\bar z)$ has weight~$(0,2)$.
This weight is \emph{derived} from the Virasoro OPE
($T(z)T(w) \sim c/(2(z{-}w)^4) + 2T(w)/(z{-}w)^2 + \partial T/(z{-}w)$):
$T$ transforms as a rank-$2$ tensor under conformal
transformations.
This is the spin-$2$ structure of the graviton.
The promotion from $2$D conformal weight to $4$D helicity $\pm 2$
is made precise in Step~3: on the boundary $T^2$, the
momentum $(k_\theta, k_\varphi)$ defines the transverse plane,
and the $4$D massless little group $\mathrm{SO}(2)$ acts by
rotations in this plane; $T(z)$ transforms with eigenvalue
$e^{2i\alpha}$ under a rotation by~$\alpha$, which is
the definition of helicity~$+2$
(and $\bar T(\bar z)$ gives helicity~$-2$).

\emph{Step~3 (Boundary torus $=$ transverse plane).}
The conformal boundary of the Seifert manifold is
$T^2 = S^1_\theta \times S^1_\varphi$
(boundary of $\mathbf{H}^2$ $\times$ fiber $S^1$).
At energies $E \ll M_{\mathrm{poly}}$, the KK tower
is frozen and both circles have wavelength
$\gg 1/E$; the boundary $T^2$ is indistinguishable
from the Euclidean transverse plane~$\mathbb{R}^2$
to corrections $O(E/M_{\mathrm{poly}})^2$.
(The exponent is~$2$, not~$1$: the first KK mode
at momentum $k_1 = M_{\mathrm{poly}}$ shifts the
flat-space propagator $1/k^2$ by
$\delta(1/k^2) = 1/(k^2 + M_{\mathrm{poly}}^2) - 1/k^2
= -M_{\mathrm{poly}}^2/(k^2(k^2 + M_{\mathrm{poly}}^2))
= O(M_{\mathrm{poly}}^2/k^4)$, giving a relative
correction $O(E^2/M_{\mathrm{poly}}^2)$ at energy~$E$.)
The helicity identification is a theorem of
conformal representation theory
(independent of global topology):
$T(z)$ has conformal weight~$(2,0)$, so under the
$\mathrm{SO}(2)$ rotation $z \to e^{i\alpha}z$
it transforms as $T(z) \to e^{2i\alpha}\,T(z)$.
The eigenvalue $e^{2i\alpha}$ under $\mathrm{SO}(2)$
is the definition of helicity~$+2$ for the massless
little group in four dimensions
(\citealt{Weinberg1995}, \S2.5).
The stress tensor on $T^2$ has
$2 = d(d{-}3)/2|_{d=4}$ independent components
(Remark~\ref{rem:FG-WW} below).

\emph{Step~4 (Polarization and DOF count).}
The boundary graviton has $2$ independent components
($T$ and $\bar T$), corresponding to the $+$ and $\times$
tensor polarizations.
Both propagate on $T^2$, giving $n_{\mathrm{phys}} = 2$, matching
the standard $d(d{-}3)/2 = 2$ count for $4$D gravity.
The graviton is a boundary mode of a $3$D topological
theory (Remark~\ref{rem:FG-WW}).
The $4$D effective $T_{\mu\nu}$ (which couples to the graviton
at $r \gg R$) is constructed through the \emph{non-local}
holographic dictionary, not as a local $4$D operator
\citep{Maldacena1997}.
\end{proof}

\begin{remark}[FG expansion and Weinberg--Witten]
\label{rem:FG-WW}
The Fefferman--Graham expansion holds for the Seifert metric
\citep{deHaroSolodukhinSkenderis2001, ToldoWillett2018};
for odd~$N$ the gravitational anomaly modifies $T_{ab}$
\citep{KrausLarsen2006} but the DOF count ($2$ polarizations)
is unchanged.
The $4$D graviton is a boundary mode of a $3$D topological
theory: $T(z)$ lives on the $2$D boundary, and the
Weinberg--Witten assumptions (Lorentz-covariant $T^{\mu\nu}$
in the bulk) do not hold in the topological sector.
\end{remark}

\begin{remark}[Bulk KK decomposition]
\label{rmk:kk-dof}
In the \emph{bulk} description, the $4$D metric $g_{MN}$ decomposes
under KK reduction on~$S^1$ into:
\begin{center}
\small
\begin{tabular}{lccl}
\toprule
Field & 3D spin & DOF & Status \\
\midrule
$g_{ij}$ (3D metric) & $2$ & $\mathbf{0}$ & topological (CS) \\
$A_i = g_{i\varphi}$ (graviphoton) & $1$ & $\mathbf{1}$ & massive, $m \sim M_{\mathrm{poly}}$ \\
$\sigma = g_{\varphi\varphi}$ (radion) & $0$ & $\mathbf{1}$ & massive, $m \sim M_{\mathrm{poly}}$ \\
\bottomrule
\end{tabular}
\end{center}
\noindent
The boundary description (Derivation~\ref{thm:graviton-4d})
is fundamental; the bulk KK decomposition is the effective
description at $E \ll M_{\mathrm{poly}}$.
At macroscopic distances ($r \gg R \sim 10^{-21}$\,m),
only the boundary graviton's $2$ tensor polarizations survive.
\end{remark}

\begin{remark}[Graviton mass spectrum]
\label{rmk:mass-spectrum}
The KK graviton masses are $\mu_m = |m - N/2|$:
\begin{itemize}
\item Even~$N$: the zero mode $m = N/2$ is
  \emph{massless}, an exact $4$D massless graviton.
\item Odd~$N$: the lightest \emph{KK} mode has $\mu = 1/2$ (massive).
  The massless graviton is the boundary stress tensor~$T(z)$,
  which is not a KK mode but the asymptotic symmetry
  generator of the CS theory.
  Its masslessness is topologically protected
  (the Virasoro algebra forbids a mass term for~$T$).
\end{itemize}
At distances $r \gg 1/N$ (the $S^1$ radius),
the KK tower is integrated out and gravity is mediated by the
boundary stress tensor~$T$; at $r \ll 1/N$, the full KK tower
contributes.
The graviton propagator crossover from 3D to 4D
is derived in Proposition~\ref{prop:graviton-propagator}.
\end{remark}

%% ====================================================================
\section{The couplings: all from $c = 12\,b(N)$}
\label{sec:couplings}

\begin{definition}[Havelock central charge]
\label{def:central-charge}
The \emph{Havelock central charge} is the kinetic coefficient of
the breathing mode in the $N$-vortex system on~$\mathbf{H}^2$:
\begin{equation}\label{eq:central-charge}
  c = 12\,b(N), \qquad
  b(N) = \frac{N(N{+}1)}{12} - \ln 2 + \frac{\ln N}{N{-}1}.
\end{equation}
For large~$N$:
$c = N^2 + N - 12\ln 2 + 12(\ln N)/(N{-}1)$ exactly,
with leading asymptotic $N^2 + N$ and $O(1)$ constant $-12\ln 2 \approx -8.32$.
The \emph{instanton fugacity} is
\begin{equation}\label{eq:instanton-K}
  \mathcal{K} = e^{-2\pi k_{\mathrm{frac}}}, \qquad
  k_{\mathrm{frac}} = \mathrm{frac}(c/6 - N/2),
\end{equation}
where $\mathrm{frac}(\cdot)$ denotes the fractional part.
At $N = 7$: $k_{\mathrm{frac}} = 0.096$, $\mathcal{K} = 0.548$.
The instanton fugacity $\mathcal{K}$ enters the fermion mass hierarchy
(\S\ref{sec:mass-hierarchy}), the baryon fraction
(Paper~VI), and the Higgs quartic
(\S\ref{sec:consistency}).

\emph{Central charge accounting.}
The gauge symmetries are \emph{subsectors} of the single boundary
CFT at central charge~$c$, not independent additions:
\begin{center}
\small
\begin{tabular}{lrl}
\toprule
Sector & $c_{\mathrm{sector}}$ & Origin \\
\midrule
$\mathrm{SU}(3)_1$ WZW & $2$ & Orbifold twisted sectors \\
$\mathrm{SU}(2)_1$ WZW & $1$ & CS gravity compact factor \\
$\mathrm{U}(1)$ boson & $1$ & KK fiber \\
\midrule
Gauge subtotal & $4$ & ($8\%$ of $c$ at $N = 7$) \\
Gravity $+$ matter $+$ ghosts & $c - 4$ & Remainder \\
\midrule
\textbf{Total} & $\mathbf{12\,b(N)}$ & \textbf{Single boundary CFT} \\
\bottomrule
\end{tabular}
\end{center}
\noindent
The gauge central charges are \emph{included} in $c = 12\,b(N)$,
not added to it (in CS theory, the WZW $c_G$ is already physical;
no ghost subtraction is needed).
All couplings derive from the same~$c$.
\end{definition}

\begin{proposition}[Exact coupling relations]
\label{prop:couplings}
All couplings are determined by $c = 12\,b(N)$:
\begin{align}
  G &= \frac{3}{2c}, \label{eq:newton}\\
  \alpha &= \frac{6}{\pi c}, \label{eq:alpha}\\
  \frac{\alpha}{8\pi G} &= \frac{1}{2\pi^2}
    \qquad\text{(exact, $N$-independent)}. \label{eq:coupling-lock}
\end{align}
The quartic vertex from the Magri hierarchy:
$g_4 = 2N f^2/\pi$ at the critical mode.
\end{proposition}

\begin{proof}
$G$: from Brown--Henneaux, $c = 3\ell/(2G)$ with $\ell = 1$.
$\alpha$: from the KK gauge coupling on the unit-radius fiber
(this is $\alpha_{\mathrm{U}(1)}$, the KK photon coupling).
The ratio~\eqref{eq:coupling-lock}: $\alpha/(8\pi G)
= [6/(\pi c)] / [8\pi \cdot 3/(2c)] = 6/(12\pi^2) = 1/(2\pi^2)$.
The $N$-dependence cancels algebraically.

The strong coupling: the $3$D CS coupling for $\mathrm{SU}(3)$
at level~$k = 1$ is $\alpha_s^{3\mathrm{D}} = 1/(k + h^\vee)
= 1/4$ (\citealt{Witten1989Jones}), where $h^\vee = 3$
is the dual Coxeter number (the standard one-loop level shift).
The KK reduction on $\Sigma_g \times S^1$ promotes the
$3$D coupling to a $4$D coupling by dividing by the
effective volume $V_{\mathrm{eff}}$ of the internal space:
$\alpha_s^{4\mathrm{D}}
= \alpha_s^{3\mathrm{D}} \times (k{+}h^\vee) / V_{\mathrm{eff}}$.
The effective volume has three factors, each with an
independent origin:
(i)~the bare CS level $k = 1$
(from $\pi_3(\mathrm{SU}(3)) = \mathbb{Z}$, which
quantizes the CS action $S_{\mathrm{CS}} = (k/4\pi)
\int\mathrm{Tr}(A \wedge dA + \tfrac{2}{3}A^3)$
at integer~$k$; the factor~$k$ enters $V_{\mathrm{eff}}$
as the normalization of the CS path integral measure);
(ii)~the Gauss--Bonnet volume of the base
$\mathbf{H}^2/\Gamma$:
$\mathrm{Area} = 4\pi(g{-}1) = 4\pi$ at $g = 2$.
The genus $g = 2$ follows from two independent
selection steps; a third step provides a consistency check
but is not part of the derivation.

\emph{Step~(ii-a): $g \le 1$ excluded [mathematical].}
At $g = 0$: $V_{\mathrm{eff}} = 4\pi(0{-}1)\,\sqrt{2}
= -4\pi\sqrt{2} < 0$, giving $\alpha_s < 0$
(the CS path integral has wrong-sign Boltzmann weight).
At $g = 1$: $V_{\mathrm{eff}} = 0$, and $\alpha_s$ diverges
(the CS theory on a torus is degenerate; the flat-connection
moduli space has zero volume).
Both are \emph{mathematical} exclusions,
not phenomenological constraints.

\emph{Step~(ii-b): $g = 2$ is the smallest nondegenerate genus
[primary derivation].}
The area of $\Sigma_g$ with $K = -1$ is
$\mathrm{Area} = 4\pi(g{-}1)$ (Gauss--Bonnet),
which grows linearly in~$g$.
The CS Hilbert space dimension is
$\dim\mathcal{H}(\Sigma_g, \mathrm{SU}(3)_1)
= 3^g$ (Verlinde formula),
which grows exponentially but with base $3$,
far slower than the area cost at any
curvature radius $\ell > 0$.
The free energy
$F(g) = 4\pi\ell^2(g{-}1) - T\,g\ln 3$
is monotonically increasing in~$g$ for $g \ge 2$
whenever $4\pi\ell^2 > T\ln 3$,
i.e.\ whenever $\ell > \sqrt{T\ln 3/(4\pi)}$.
This condition holds for \emph{any} positive
temperature and curvature radius
(at the Gibbons--Hawking temperature
$T = 1/(2\pi\ell)$, the ratio is
$4\pi\ell^2/(T\ln 3) = 8\pi^2\ell^3/\ln 3
\approx 10^{27}$, but the conclusion $g = 2$
is independent of~$T$: it requires only
$\ell > 0$ and $T < 4\pi\ell^2/\ln 3$,
which is satisfied at any subplanckian temperature).
the minimum is at the \emph{smallest viable genus},
$g = 2$.
This parallels the entropy maximization that selects
$N = 7$ in Paper~I: in both cases, the minimum free energy
(= maximum entropy at fixed energy) picks the smallest
topologically allowed value.

\emph{Step~(ii-c): Consistency check [not part of the derivation].}
At $g = 2$, $\Lambda_{\mathrm{QCD}} \approx 200$\,MeV
(two-loop), compatible with hadron formation
from the derived quark masses.
At $g = 3$, $\Lambda_{\mathrm{QCD}} \approx 4$\,neV
(quarks never bind).
This check uses the quark masses derived in
\S\ref{sec:fermion-masses}, which assume $g = 2$;
it therefore tests internal consistency, not independence.
The selection of $g = 2$ rests on Steps (ii-a) and~(ii-b)
alone.
The algebraic identity
$\dim\mathcal{H}(\Sigma_2, \mathrm{SU}(3)_1)
= 3^2 = 9 = N_c^2 = \dim(\mathrm{adj} \oplus
\mathrm{singlet})$
provides a further consistency check:
the CS vacuum multiplicity matches the
gauge field component count; and
(iii)~the CS partition function $Z(S^3) = \sqrt{2}$,
derived from the exact Witten formula
(\citealt{Witten1989Jones}):
\begin{equation}\label{eq:z-s3}
  Z(S^3, \mathrm{SU}(N), k)
  = \Bigl(\frac{2}{k{+}N}\Bigr)^{\!N/2}
    \prod_{j=1}^{N-1}\bigl(2\sin(\pi j/(k{+}N))\bigr)^{N-j}.
\end{equation}
For $\mathrm{SU}(3)_1$: $k{+}N = 4$,
$(2/4)^{3/2} = 1/(2\sqrt{2})$,
$[2\sin(\pi/4)]^2 = 2$,
$[2\sin(\pi/2)]^1 = 2$,
giving $Z = \sqrt{2}$.
This is the exact non-perturbative
result of the CS path integral.
The alternative ``surgery'' normalization
$Z_{\mathrm{surg}} = (k{+}N)^{-N/2}\prod(\cdots) = 1/2$
omits the prefactor $\sqrt{2}^N$; our formula~\eqref{eq:z-s3}
follows Witten's original normalization.
The product $V_{\mathrm{eff}} = k \times 4\pi(g{-}1) \times
Z(S^3)$ is the total zero-mode volume of the CS path integral
on $\Sigma_g \times S^1$.
It factorizes because $\Sigma_g \times S^1$ is a product manifold,
so the base and fiber zero-mode integrals are independent:
\begin{itemize}
\item Factor~(i): $k$ enters the CS path integral measure
  as the level normalization
  ($S_{\mathrm{CS}} = (k/4\pi)\int\mathrm{Tr}(\ldots)$).
\item Factor~(ii): $4\pi(g{-}1)$ is the symplectic volume of the
  moduli space of flat connections on~$\Sigma_g$, which equals
  the Gauss--Bonnet area by the Witten correspondence between
  2D~YM and CS theory
  \citep{Witten1992TwoDimensions}.
\item Factor~(iii): $Z(S^3)$ is the CS partition function of the
  fiber, arising from the Dehn surgery description of the
  solid torus filling the $S^1$ boundary
  (\citealt{Witten1989Jones}, \S4).
\end{itemize}
The $3$D CS coupling is
$\alpha_s^{3\mathrm{D}} = 1/(k{+}h^\vee)$ (the inverse
shifted level).
The $4$D effective coupling is
$\alpha_s^{4\mathrm{D}} = 1/V_{\mathrm{eff}}$,
where the factor $(k{+}h^\vee)$ cancels between the
bare coupling $1/(k{+}h^\vee)$ and the CS path-integral
measure $(k{+}h^\vee)$.
Both factors originate from the same one-loop determinant
(the Gaussian integral over non-zero modes);
this cancellation is the one-loop exactness of CS theory
(\citealt{Witten1989Jones}, \S2.3).
Explicitly:
$\alpha_s^{4\mathrm{D}}
= \underbrace{(k{+}h^\vee)^{-1}}_{\text{bare coupling}}
\times \underbrace{(k{+}h^\vee)}_{\text{measure}}
/ V_{\mathrm{eff}}
= 1/V_{\mathrm{eff}}
= 1/(k \times 4\pi\sqrt{2})$:
\begin{equation}\label{eq:alpha-s}
  \alpha_s(M_{\mathrm{poly}})
  = \frac{1}{k \times 4\pi(g{-}1) \times Z(S^3)}
  = \frac{1}{4\pi\sqrt{2}}
  = 0.0563.
\end{equation}
The value $\alpha_s = 1/(4\pi\sqrt{2})$ is derived from
the exact Witten partition function~\eqref{eq:z-s3}
with no convention ambiguity.
One-loop running to $M_Z$ gives
$\alpha_s(M_Z) = 0.114$;
two-loop (RK4 integration of the two-loop
$\beta$-function with threshold matching at $m_t$)
gives $\alpha_s(M_Z) = 0.122$.
The observed value $0.1180 \pm 0.0009$ is bracketed,
consistent within the perturbative uncertainty.
\end{proof}

%% Cosmological constant section moved to Paper V (M-001).

%% ====================================================================
\section{The Weyl anomaly vanishes}
\label{sec:anomaly}

\begin{derivation}[Zero anomaly on Einstein backgrounds]
\label{thm:zero-anomaly}
On any surface of constant curvature
(the plane, $\mathbf{S}^2$, $\mathbf{H}^2$, or any quotient
$\Gamma\backslash\mathbf{H}^2$), the constrained Havelock eigenvalue
satisfies
\[
  \lambda_m = C_1(\mathrm{surface}, R) - \frac{m(N{-}m)}{2}
\]
with $\delta_m = 0$ \emph{identically}.
The three-layer decomposition is exact with zero Weyl anomaly.
\end{derivation}

\begin{proof}
The Havelock identity (Paper~I, Theorem~\ref{I-thm:riemannian-havelock})
proves that the \emph{constrained} eigenvalue (on $\{J = \mathrm{const}\}$)
has a mode-independent confining potential~$C_1$.
The angular Hessian involves the csc$^2$ kernel from the logarithmic
singularity of the Green's function; the Havelock sum
(Paper~I, Lemma~\ref{I-lem:havelock}) converts this to $m(N{-}m)/2$.
No residual $\delta_m$ survives.
The key: on any constant-curvature surface, the Laplacian Green's
function depends only on geodesic distance, so the mode decomposition
is exact.
Quotients $\Gamma\backslash\mathbf{H}^2$ add a mode-\emph{independent}
correction $\delta C_1$ (from the Selberg trace formula,
Paper~I, Theorem~\ref{I-thm:index-pairing}), which shifts~$C_1$
but does not introduce mode-dependent anomalies.
\end{proof}

%% ====================================================================
\section{The Standard Model gauge group}
\label{sec:gauge-group}

The polygon hierarchy realizes
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$
through three mechanisms, each grounded in established mathematics.

\subsection{$\mathrm{SU}(2)$ from the Chern--Simons formulation of
$2{+}1$D gravity}
\label{sec:chiral-su2}

\begin{derivation}[$\mathrm{SU}(2)$ gauge symmetry]
\label{thm:su2}
The gravitational zero-mode sector of the Kaluza--Klein reduced theory
carries $\mathrm{SU}(2) \times \mathrm{SU}(2)$ gauge symmetry at
Chern--Simons level $k = c/6$.
At $N = 4$, the critical Havelock mode is the KK zero mode
and transforms in the $j = 1$ (adjoint) representation of $\mathrm{SU}(2)$.
\end{derivation}

\begin{proof}
The proof has four steps.

\emph{Step~1 (KK reduction).}
Compactify the metric~\eqref{eq:metric-4d} on~$S^1$.
The zero-mode ($m = 0$) sector is pure $2{+}1$D gravity on
$\mathbb{R} \times \mathbf{H}^2$ with $\Lambda_3 < 0$ (since $\kappa = -1$).
Massive KK modes ($m \ne 0$) are matter fields coupled to this gravity.

\emph{Step~2 (Witten--Ach\'ucarro--Townsend, \citeyear{Witten1988}).}
Pure $2{+}1$D gravity with $\Lambda < 0$ is equivalent to
Chern--Simons theory with gauge group
$\mathrm{SL}(2,\mathbb{R}) \times \mathrm{SL}(2,\mathbb{R})$
in Lorentzian signature.
The Euclidean continuation replaces
$\mathrm{SL}(2,\mathbb{R})$ by $\mathrm{SU}(2)$
(\citealt{Witten1988}): the CS path integral
converges for the compact group, and the Lorentzian
observables are recovered by analytic continuation of
the CS partition function.
The level is $k = \ell/(4G)$;
with $\ell = 1$ and $G = 3/(2c)$: $k = c/6 = 2\,b(N)$.
The gravitational sector uses the non-compact
$\mathrm{SL}(2,\mathbb{R})$, which admits irrational level
(Paper~III, \S\ref*{III-sec:graviton});
the compact gauge sectors ($\mathrm{SU}(2)$,
$\mathrm{SU}(3)$, $\mathrm{U}(1)$) are quantized
at $k = 1$ by large gauge invariance.
The representation theory, coupling constants, and
conformal weights used below are all computed in the
Euclidean formulation, the standard procedure in
gauge theory (all SM cross-sections use Euclidean
Feynman rules, analytically continued to
Minkowski signature).
The compact gauge sectors have integer levels
$k = 1$ for $\mathrm{SU}(2)$, $\mathrm{SU}(3)$, and
$\mathrm{U}(1)$ (large gauge invariance).
The sectors coexist because their gauge algebras are orthogonal:
$\mathrm{Tr}(T_a^{G_i} T_b^{G_j}) = 0$ for $i \ne j$, so
$S_{\mathrm{total}} = \sum_i S_i$ and $Z = \prod_i Z_i$.
The boundary CFT is
$\mathrm{WZW}_{\mathrm{grav}} \otimes \mathrm{WZW}_{\mathrm{gauge}}$
with $c_{\mathrm{total}} = \underbrace{(12b(N){-}4)}_{c_{\mathrm{grav}}}
+ \underbrace{2}_{c_{\mathrm{SU}(3)}}
+ \underbrace{1}_{c_{\mathrm{SU}(2)}}
+ \underbrace{1}_{c_{\mathrm{U}(1)}} = 12b(N)$
(verified numerically; see Code Availability\footnote{All
numerical verifications cited as ``see Code Availability''
are in the companion code repository at
\texttt{src/planetary\_polygons/} and \texttt{tests/},
with $4260$ automated tests.}).

\emph{Step~3 (Zero mode at $N = 4$).}
At $N = 4$, the critical mode $m^* = 2$ has KK mass
$\mu_2 = |2 - 4/2| = 0$: it \emph{is} the zero mode.

\emph{Step~4 (SU(2) matter content).}
The critical Havelock eigenvalue $f(2,4) = 2$ equals
$j(j{+}1)$ at $j = 1$ (the adjoint, dimension~$3$).
This $j$ labels the $\mathbf{H}^2$ Laplacian eigenvalue
via the CMS--CS identification
(Proposition~\ref{III-prop:casimir-havelock}),
not a state in the $\mathrm{SU}(2)_1$ CS Hilbert space.
Therefore the critical gravitational mode at $N = 4$ carries
$\mathrm{SU}(2)$ quantum numbers in the adjoint representation.
\end{proof}

\begin{proposition}[The gravity $\mathrm{SU}(2)$ acts chirally on matter]
\label{prop:chiral-su2}
In the CS formulation, the $\mathrm{SU}(2)_L \times \mathrm{SU}(2)_R$
gauge connections couple chirally to KK matter modes:
four-dimensional left-handed Weyl fermions transform under
$\mathrm{SU}(2)_L$, right-handed under $\mathrm{SU}(2)_R$.
The Seifert Euler class $e = N/2$ breaks
$\mathrm{SU}(2)_R$ via the parity anomaly, leaving
$\mathrm{SU}(2)_L$ as the unbroken gauge factor.
\end{proposition}

\begin{proof}
The proof has three steps.
Witten's CS formulation splits gravity into $\mathrm{SU}(2)_L
\times \mathrm{SU}(2)_R$ (Step~1).
KK reduction maps 4D chirality to 3D CS sector (Step~2).
The Seifert Euler class $e = N/2$ is parity-odd, breaking
$L \leftrightarrow R$ via the fermion parity anomaly and
the gravitational eta invariant, gapping $\mathrm{SU}(2)_R$
(Step~3).

\emph{Step~1 (Chiral decomposition of CS gravity).}
In the first-order formulation \citep{Witten1988},
2+1D gravity with $\Lambda < 0$ decomposes into two
\emph{independent} CS gauge fields
\begin{equation}\label{eq:A-pm}
  A^+_\mu = \omega_\mu + e_\mu/\ell, \qquad
  A^-_\mu = \omega_\mu - e_\mu/\ell,
\end{equation}
where $\omega_\mu$ is the spin connection and $e_\mu = e_\mu^a T_a$
is the dreibein (both valued in $\mathfrak{su}(2)$).
The $2{+}1$D Dirac operator couples to $A^+$ and $A^-$ with opposite
projections:
$\gamma^\mu(\partial_\mu + A^+_\mu)\,P_+ \psi
+ \gamma^\mu(\partial_\mu + A^-_\mu)\,P_- \psi$,
where $P_\pm = (1 \pm \gamma^{(3)})/2$ project onto the two
$2$-component sectors of the $3$D spinor
(\citealt{Witten1988}; the spin connection couples equally
to both, the dreibein with opposite signs).
On the boundary, this becomes the standard left/right
decomposition of the WZW model:
$A^+ \to$ left-moving $\mathrm{SU}(2)$ currents,
$A^- \to$ right-moving $\mathrm{SU}(2)$ currents.

\emph{Step~2 (KK chirality map).}
The Seifert manifold $\mathbf{H}^2 \times_N S^1$ has metric
$ds^2_3 = ds^2(\mathbf{H}^2) + (d\varphi + A_{\mathrm{KK}})^2$.
A $4$D Weyl spinor $\psi_L(x,\varphi)$ (with definite
$\gamma^5 = \gamma^0\gamma^1\gamma^2\gamma^3$ eigenvalue)
decomposes in KK modes:
$\psi_L = \sum_m \chi_m(x)\,e^{i(m+1/2)\varphi}$,
where the half-integer shift arises from the spin connection
on the $S^1$ fiber (the spinor acquires a sign $(-1)$ under
$\varphi \to \varphi + 2\pi$;
see Code Availability).
Under KK reduction, $\gamma^5$-eigenvalue maps to the
$3$D CS sector: $4$D left-handed $\leftrightarrow$ couples to $A^+$
(the dreibein component along $S^1$ determines the chirality
projection, because $\gamma^3 = \gamma^\varphi$ is the fiber direction,
and $\gamma^5 = \gamma^0\gamma^1\gamma^2\gamma^3$ reduces to
$\gamma^{(3)} \cdot e^{i\pi m}$ in the KK basis).

\emph{Step~3 (Parity breaking by the Euler class).}
The Seifert manifold is \emph{not} parity-invariant:
the Euler class $e = N/2$ is a pseudoscalar that changes sign
under orientation reversal $\varphi \to -\varphi$.
This breaks the $A^+ \leftrightarrow A^-$ symmetry through
two contributions to the effective CS level:
\begin{enumerate}
\item \emph{Fermion parity anomaly} (\citealt{Redlich1984}).
  The KK fermion masses on the Seifert manifold are
  $\mu_m = (m + \tfrac{1}{2} - N/2)/R$ for $m = 0,\ldots,N{-}1$.
  The level shift is
  $\Delta k_{\mathrm{ferm}} = \tfrac{1}{2}\sum_m \mathrm{sgn}(\mu_m)$.
  For even~$N$: the spectrum is symmetric ($\mu$ and $-\mu$
  paired), so $\Delta k_{\mathrm{ferm}} = 0$.
  For odd~$N$: one zero mode at $m = (N{-}1)/2$ gives
  $\Delta k_{\mathrm{ferm}} = \pm 1/2$
  (sign fixed by the fiber orientation).
\item \emph{Gravitational CS invariant.}
  The eta invariant $\eta(M)$ of the Seifert manifold
  $M = \mathbf{H}^2 \times_N S^1$ contributes an additional
  parity-odd level shift
  $\Delta k_{\mathrm{grav}} = \eta(M)/2$
  that distinguishes $A^+$ from~$A^-$.

  \emph{Derivation.}
  The Seifert manifold~$M$ is a circle bundle over the
  orbifold $\Sigma = \mathbf{H}^2/\mathbb{Z}_N$, which has
  one cone point of order~$N$.
  The adiabatic limit theorem
  \citep{BismutCheeger1989,Dai1991}
  decomposes the eta invariant of the Dirac operator
  on a circle bundle into a base contribution
  and singular-fiber corrections:
  $\eta(M) = \eta(\Sigma) + \eta_{\mathrm{sing}}$.
  The base contribution vanishes:
  $\eta(\Sigma) = 0$ because the Dirac operator on a
  $2$D surface has symmetric spectrum
  ($\lambda \leftrightarrow -\lambda$).
  The singular-fiber correction is determined by the
  local lens-space geometry $L(N,1)$ around the cone
  point:
  $\eta_{\mathrm{sing}} = -4\,s(1,N) + (N{-}1)/(6N)$,
  where $s(a,N) = (1/4N)\sum_{k=1}^{N-1}
  \cot(\pi k/N)\cot(\pi ak/N)$ is the Dedekind sum.
  For $a = 1$: the identity
  $\sum_{k=1}^{N-1}\cot^2(\pi k/N) = (N{-}1)(N{-}2)/3$
  gives $s(1,N) = (N{-}1)(N{-}2)/(12N)$, hence
  \begin{equation}\label{eq:eta-grav}
    \eta_{\mathrm{grav}}(N)
    = -4\,s(1,N) + \frac{N{-}1}{6N}
    = -\frac{(N{-}1)(2N{-}5)}{6N}.
  \end{equation}
  This is nonzero for all $N \ge 3$ ($\eta_{\mathrm{grav}} = 0$
  only at $N = 5/2$, which is not an integer).
  Explicit values: $\eta(4) = -3/8$, $\eta(7) = -9/7$,
  $\eta(11) = -85/33$
  (verified numerically; see Code Availability).
\end{enumerate}
The combined asymmetry is
$|k_L^{\mathrm{eff}} - k_R^{\mathrm{eff}}|
= |\eta_{\mathrm{grav}}(N)| = (N{-}1)(2N{-}5)/(6N) > 0$
for all $N \ge 3$.
(The sign of $\eta$ determines which sector is enhanced;
with the standard fiber orientation, $A^-$ has the larger
effective level, so the \emph{physical} weak force is
identified with the sector having the \emph{smaller} effective~$k$.)
The mass gap follows from topologically massive gauge theory
\citep{DeserJackiwTempleton1982}:
a $2{+}1$D gauge field with both a YM kinetic term and
a CS term at level~$k$ acquires a topological mass
$m_{\mathrm{top}} = |k_{\mathrm{eff}}|\,g^2/(4\pi)$.
With $g^2 = 4\pi/k_{\mathrm{bare}}$ from KK reduction,
this gives $m_{\mathrm{top}} = |k_{\mathrm{eff}}/k_{\mathrm{bare}}|/\ell$.
At $N = 7$ with $k_{\mathrm{bare}} = 1$ and
$|\eta_{\mathrm{grav}}| = 9/7$:
$k_R^{\mathrm{eff}} = 1 - 9/14 = 5/14 \approx 0.36$
and $k_L^{\mathrm{eff}} = 1 + 9/14 = 23/14 \approx 1.64$,
giving topological masses
$m_R \approx 0.36/\ell$ and $m_L \approx 1.64/\ell$
(ratio $m_L/m_R \approx 4.6$).
At $\ell \sim 1/M_{\mathrm{poly}}$:
$m_L \sim 490$\,TeV, $m_R \sim 107$\,TeV.
The heavier sector ($m_L$) exceeds
$M_{\mathrm{poly}} \approx 300$\,TeV;
the lighter ($m_R$) is below $M_{\mathrm{poly}}$ but
above the electroweak scale by a factor of~$440$,
unobservable at the LHC.
The assignment of $\mathrm{SU}(2)_L$ to the
\emph{heavier} sector ($k_L = 23/14$) is derived from the
sign of~$\eta$: $\eta < 0$ at $N = 7$ means the sector
with the larger $|k_{\mathrm{eff}}|$ couples more strongly
to left-handed fermions (through the Euler class).
This is determined by the fiber orientation.
Both topological masses scale as $|\eta|/(k\ell)$
and exceed $M_{\mathrm{EW}}$ for all $N \ge 4$
(since $|\eta(N)| \ge 1/N > 0$).

\emph{Topological mass vs.\ physical $W$ mass.}
The topological mass $m_L \sim 490$\,TeV and the physical
$W$ boson mass $m_W = 80$\,GeV are different objects:
$m_L$ is the mass of a \emph{topological} excitation
(anyon) in the 3D CS description, while $m_W$ is the
mass of the \emph{dynamical} gauge boson in 4D from the
Higgs mechanism ($m_W = g_W v/2$).
They do not conflict: in the KK reduction from 3D to 4D,
the CS term $(k/4\pi)\,A \wedge dA$ reduces to the
$\theta$-term $(\theta/32\pi^2)\,F \wedge \tilde{F}$,
which is a total derivative in 4D and does not contribute
to the gauge boson mass.
The physical $W$ mass comes entirely from electroweak
symmetry breaking; $m_L$ sets the \emph{threshold}
above which the 3D topological description applies.
Below $m_L$: standard 4D $\mathrm{SU}(2)_L$ YM $+$ Higgs.
Above $m_L$: the $S^1$ fiber is resolved and the
theory reveals its 3D CS origin.

\emph{Level quantization.}
The effective levels $k_R^{\mathrm{eff}} = 5/14$
and $k_L^{\mathrm{eff}} = 23/14$ are not integers.
This is standard in topologically massive gauge theory
(\citealt{DeserJackiwTempleton1982}):
the \emph{bare} level $k_{\mathrm{bare}} = 1 \in \mathbb{Z}$
is quantized by $\pi_3(\mathrm{SU}(2)) = \mathbb{Z}$
(invariance under large gauge transformations),
while the eta-invariant shift $\eta/2$ is a one-loop
(perturbative, local) correction that modifies the
effective coupling without changing the topological charge.
The standard Witten shift $k \to k + h^\vee$ ($= k + 2$
for $\mathrm{SU}(2)$) is the same mechanism: a one-loop
renormalization of the coupling, not a new topological
constraint
(\citealt{Dunne1999}, \S3.5).
The sector with the larger effective level has heavier
gauge bosons; at energies $E \ll m_R$, only the
lighter sector propagates.
This is the origin of parity violation in the weak interaction:
\emph{the Seifert geometry is not parity-invariant, so the
gauge theory it generates is not either.}
\end{proof}

\begin{remark}[Decoupling from gravity: the low-energy EFT]
\label{rmk:su2-decoupling}
In $2{+}1$D gravity, $e_\mu = \ell(A^+_\mu - A^-_\mu)/2$
and $\omega_\mu = (A^+_\mu + A^-_\mu)/2$.
Parity breaking gaps~$A^-$ (mass $m_R \sim |\eta|/\ell$);
below~$m_R$ the effective action separates:
$S_{\mathrm{eff}} = S_{\mathrm{grav}}[g_{ij}, \rho]
+ S_{\mathrm{YM}}[A^+, g_{ij}] + O(E/m_R)$.
The decoupling is structural: the WZW currents (gauge)
and the modular parameters (gravity) are fibers of
different bundles over the moduli space of~$T^2$.
\end{remark}

\subsection{Confinement and the QCD string tension}
\label{sec:confinement}

\begin{proposition}[Confinement]
\label{prop:confinement}
In the polygon phase ($T < T_{\mathrm{HP}}$, the OPS entropy
maximum; Theorem~\ref{III-thm:polygon-einstein}), the $\mathrm{SU}(3)$ gauge theory
is confining: the Wilson loop in the fundamental representation
vanishes exactly, $\langle W_{\mathrm{fund}}\rangle = 0$.
\end{proposition}

\begin{proof}
Two independent arguments:

\emph{(a)~Verlinde formula (exact in the TQFT sector).}
The $\mathrm{SU}(3)_1$ S-matrix has
$S_{0\lambda} = 1/\sqrt{3}$ for all three integrable
representations $\lambda$.
The Wilson loop in the fundamental:
$S_{\mathrm{fund},\lambda}/S_{0,\lambda}
= \omega^{\lambda_1}$ where $\omega = e^{2\pi i/3}$.
On any genus-$g$ surface:
$\langle W_{\mathrm{fund}}\rangle
= (1/Z)\sum_\lambda (S_{\mathrm{fund},\lambda}/S_{0,\lambda})
\,(S_{0,\lambda})^{2-2g}
\propto 1 + \omega + \omega^2 = 0$.
This is the $\mathbb{Z}_3$ character sum: the fundamental
has non-trivial center charge, so its expectation value
vanishes identically. No approximation is involved.

\emph{(b)~Center symmetry.}
The Polyakov loop transforms as $P \to \omega\,P$ under
$\mathbb{Z}_3$.
The polygon phase has $\mathbb{Z}_7$ symmetry.
Since $\gcd(7, 3) = 1$, the $\mathbb{Z}_3$ center is
unbroken, forcing $\langle P\rangle = 0$.
The polygon saddle is selected by the OPS entropy
principle (Theorem~\ref{III-thm:polygon-einstein},
Remark~\ref{III-rmk:logical-status}).

In the BTZ phase ($T > T_{\mathrm{HP}}$): the black hole
breaks all discrete symmetries, and
$\langle P\rangle \ne 0$ (deconfined).
The deconfinement transition is the polygon--BTZ
Hawking--Page transition (\S\ref{sec:btz-transition}).
\end{proof}

\begin{derivation}[QCD string tension from the CS path integral]
\label{der:string-tension}
The Wilson loop in the fundamental of $\mathrm{SU}(3)_1$
on any genus-$g$ surface vanishes \emph{exactly}
by the Verlinde formula:
\begin{equation}\label{eq:verlinde-wilson}
  \langle W_{\mathrm{fund}}\rangle_{\mathrm{TQFT}}
  = \frac{1}{Z}\sum_\lambda
    \frac{S_{\mathrm{fund},\lambda}}{S_{0,\lambda}}
    \,(S_{0,\lambda})^{2-2g}
  = \frac{1+\omega+\omega^2}{Z} \times (\cdots) = 0,
\end{equation}
where $\omega = e^{2\pi i/3}$ and $1+\omega+\omega^2 = 0$
is the $\mathbb{Z}_3$ character sum.
This is \emph{exact topological confinement}: the TQFT
sector has infinite string tension.

The \emph{finite} physical string tension arises from
the Yang--Mills sector (the KK kinetic term).
The CS partition function on $\Sigma_g \times S^1$
decomposes as
$Z(\Sigma_g \times S^1) = Z_{\mathrm{free}} \times
Z_{\mathrm{non\text{-}pert}}$,
where $Z_{\mathrm{free}} = Z(S^3)^2$
(the perturbative/reference partition function)
and $Z_{\mathrm{non\text{-}pert}} = \dim\mathcal{H}$
(the Verlinde dimension, from the sum over flat connections).
The physical string tension is the \emph{bare} YM string
tension times the non-perturbative CS enhancement:
\begin{equation}\label{eq:string-tension}
  \frac{\sigma}{\Lambda^2}
  = \sigma_{\mathrm{YM}}
    \times \frac{Z(\Sigma_g \times S^1)}{Z(S^3)^2}
  = \sigma_{\mathrm{YM}}
    \times \frac{\dim\mathcal{H}}{Z(S^3)^2}.
\end{equation}
The ratio $\dim\mathcal{H}/Z^2$ converts from the
CS-regulated bare coupling to the physical normalization:
it counts the number of confining vacua weighted by the
CS measure.

\emph{(i)~The bare YM string tension $\sigma_{\mathrm{YM}}$.}
This has two components on $\mathbf{H}^2$:

\emph{Geometric:}
$\sigma_{\mathrm{polygon}}
= \langle\lambda_{m^*}\rangle_{\mathrm{Airy}}
= 0.1015/\ell^2$
(the Havelock eigenvalue at the WDW ground state,
with $\langle z\rangle_{\mathrm{Ai}} = 0.457$
and Airy scale $a = (2c\,\coth\rho^*)^{1/3} = 4.79$).

\emph{Monopole (Polyakov mechanism,
\citealt{Polyakov1977}; \citealt{AHW1982}):}
On $\mathbf{H}^2$ ($K = -1$), the CS instanton
has curvature-corrected action
\[
  S_{\mathrm{mon}} = 2\pi k\Bigl(1 + \frac{K}{6k^2}\Bigr)
  = 2\pi\Bigl(1 - \frac{1}{6}\Bigr) = \frac{5\pi}{3},
\]
where $k = 1$ and the curvature correction
$K/(6k^2) = -1/6$ is the standard Seeley--DeWitt
$a_1 = R/6 = K/3 = -1/3$ contribution
to the instanton determinant
(\citealt{Polyakov1977};
$S = 2\pi k(1 + a_1/(2k)) = 2\pi(1 - 1/6)$;
negative curvature reduces the barrier).
The determinant prefactor is $(5/6)^{3/2} = 0.761$,
giving a dressed fugacity $y = 0.00405$.
The Polyakov string tension:
$\sigma_{\mathrm{Polyakov}}
= [N^2/(8\pi^2)]\,g^4\,m\,\sqrt{y} = 0.612/\ell^2$.
With $\mathrm{rank}(\mathrm{SU}(3)) = 2$ independent
monopole species (one per simple root):
$\sigma_{\mathrm{YM}}
= 0.1015 + 2 \times 0.612 = 1.326/\ell^2$.

\emph{(ii)~The CS non-perturbative enhancement.}
The CS path integral on $\Sigma_g \times S^1$ gives the
Hilbert-space dimension
$Z(\Sigma_g \times S^1) = \dim\mathcal{H}(\Sigma_g)
= \sum_\lambda (S_{0\lambda})^{2-2g}$
(Verlinde formula; \citealt{Verlinde1988}).
For $\mathrm{SU}(3)_1$ on $\Sigma_2$:
three integrable representations with $S_{0\lambda} = 1/\sqrt{3}$,
giving $\dim\mathcal{H} = 3 \times (1/\sqrt{3})^{-2} = 9$.
The $S^3$ partition function $Z(S^3)^2 = 2$ normalizes the
free propagator (two $S^3$ caps bounding $\Sigma_g$).
The ratio $\dim\mathcal{H}/Z(S^3)^2 = 9/2$ counts the
number of confining vacua per unit CS propagator weight:
$\sigma_{\mathrm{total}} = \sigma_{\mathrm{YM}} \times
\dim\mathcal{H}/Z(S^3)^2$.

\emph{(iii)~Result.}
\[
  \frac{\sigma}{\Lambda^2}
  = 1.326 \times \frac{9}{2}
  = 5.97.
\]
The lattice value (\citealt{Bali2001}, pure-gauge $\mathrm{SU}(3)$):
$\sigma/\Lambda^2 = 6.25 \pm 0.5$.
The match is $5\%$ with zero free parameters.
The one-loop correction (Supplement~S, \S5.3), from the
Seeley--DeWitt $a_2 = 4/135$ coefficient, the zero-mode
Jacobian on $\mathbf{H}^2$, and the monopole--monopole
interaction, shifts the tree-level value to
$\sigma/\Lambda^2 = 6.46$, within the lattice error bar.
\end{derivation}

\subsection{$\mathrm{SU}(3)$ from the McKay correspondence at $N = 7$}
\label{sec:su3-mckay}

\begin{derivation}[$\mathrm{SU}(3)$ gauge symmetry
  from the Klein quartic]
\label{thm:su3}
The $N = 7$ vortex system determines the $\mathrm{SU}(3)$
gauge symmetry (representation structure and dynamics)
through the Klein quartic
$X(7) = \Gamma(7)\backslash\mathbf{H}^2$:
\begin{enumerate}
\item[\emph{(a)}] \emph{Representation structure.}
  $\mathrm{PSL}(2,\mathbb{F}_7)$ embeds faithfully
  in $\mathrm{SU}(3)$ via $\chi_3 = H^0(X(7), \Omega^1)$,
  with $\chi_3 \otimes \bar\chi_3 = \chi_1 \oplus \chi_8$.
\item[\emph{(b)}] \emph{Gauge dynamics.}
  $\mathrm{SU}(3)$ at the minimal compact level $k = 1$
  defines a Chern--Simons gauge theory on the Seifert manifold.
  By the CS/WZW correspondence \citep{Witten1988},
  the CS theory on a manifold with boundary induces the
  $\mathrm{SU}(3)_1$ WZW model on the boundary,
  whose Kac--Moody currents $J^a(z)$ ($a = 1,\ldots,8$)
  are the dynamical gluons (conformal weight $(1,0)$ = spin~$1$).
\end{enumerate}
Part~(a) is representation theory; part~(b) is the
Witten CS/WZW theorem \citep{Witten1989Jones}.
\end{derivation}

\begin{proof}[Proof of part~(a): representation structure]
Five algebraic steps.

\emph{1.\ Frobenius orbits (derived, not chosen).}
The multiplicative group
$(\mathbb{Z}/7\mathbb{Z})^* \cong \mathbb{Z}/6\mathbb{Z}$
has exactly four subgroups.
A non-abelian gauge symmetry requires a proper non-trivial
subgroup $\Gamma$ whose orbits form conjugate multiplets
under CP (the palindromic involution $\sigma_6\colon m \mapsto 7{-}m$).
We show $\Gamma = \mathbb{Z}/3\mathbb{Z}$ is the unique choice
by exhaustive exclusion:
\begin{enumerate}[(i)]
\item $\{1\}$: trivial, no gauge symmetry.
\item $\mathbb{Z}/2\mathbb{Z} = \{1, 6\}$:
  acts by $m \mapsto {-}m$, the palindromic involution.
  This is conformal inversion $\xi \leftrightarrow 1/\xi$
  on the Poincar\'e disk
  (Proposition~\ref*{I-prop:galois-inversion}),
  which reverses the orientation of the $S^1$ fiber
  in the Seifert geometry and is therefore parity~$P$.
  Combined with the charge conjugation $C\colon m \mapsto N{-}m$
  (equivalent on $\mathbb{Z}_N$ Fourier modes),
  this is the CP operator.
  It is a symmetry of the spectrum ($\lambda_m = \lambda_{N-m}$),
  not an internal gauge symmetry.
  Using it for gauge would conflate color with CP.
\item $\mathbb{Z}/6\mathbb{Z}$: the full group.
  It produces a single orbit of size~$6$, which admits no
  decomposition into $\mathbf{3} \oplus \bar{\mathbf{3}}$.
\item $\mathbb{Z}/3\mathbb{Z} = \{1, 2, 4\}$:
  the unique remaining subgroup.
  Orbits $O_+ = \{1, 2, 4\}$, $O_- = \{3, 5, 6\}$
  are exchanged by CP ($\sigma_6(O_+) = O_-$),
  giving a genuine $\mathbf{3} \oplus \bar{\mathbf{3}}$.
\end{enumerate}
The choice of generator ($2$ or $4 = 2^2$) is immaterial:
the subgroup is unique, and both generators produce the
same pair of orbits.
Both orbits have the same Casimir multiset $\{3, 5, 6\}$:
the gauge symmetry is exact at all orders of perturbation
theory, not merely a linearized approximation.

\begin{figure}[t]
\centering
\includegraphics[width=0.6\textwidth]{../figures/fig_frobenius_orbits.pdf}
\caption{The Frobenius action $m \mapsto 2m \bmod 7$ on the
seven Havelock modes.
Blue: orbit $O_+ = \{1, 2, 4\}$ (quadratic residues).
Red: orbit $O_- = \{3, 5, 6\}$ (non-residues).
Grey: mode $0$ (fixed by all automorphisms).
The two orbits are exchanged by CP ($m \mapsto 7{-}m$),
forming a $\mathbf{3} \oplus \bar{\mathbf{3}}$ conjugate pair.}
\label{fig:frobenius-orbits}
\end{figure}

\emph{2.\ Cyclic action.}
On~$O_+$: $\phi_1 \mapsto \phi_2 \mapsto \phi_4 \mapsto \phi_1$.

\emph{3.\ McKay embedding.}
Diagonalise: $\eta_k = \tfrac{1}{\sqrt{3}}\sum_j \omega^{jk}\phi_{2^j}$
($\omega = e^{2\pi i/3}$).
Then $\sigma(\eta_1) = \omega\eta_1$,
$\sigma(\eta_2) = \bar\omega\eta_2$, so
$\sigma = \mathrm{diag}(\omega, \omega^{-1}) \in \mathrm{SU}(2)$:
the McKay embedding $\mathbb{Z}/3 \hookrightarrow \mathrm{SU}(2)
\to A_2 = \mathrm{SU}(3)$
\citep{McKay1980, GonzalezSprinberg1983}.

\emph{4.\ Klein quartic.}
$X(7) = \Gamma(7)\backslash\mathbf{H}^2$ (genus~$3$,
$\mathrm{Aut} = \mathrm{PSL}(2,\mathbb{F}_7)$, order~$168$).
The differentials $H^0(X(7),\Omega^1)$ carry $\chi_3$;
$\sigma$ generates
$\mathbb{Z}/3 \subset \mathrm{PSL}(2,\mathbb{F}_7)$.

\emph{5.\ Eightfold Way.}
$\chi_3 \otimes \bar\chi_3 = \chi_1 \oplus \chi_8$,
$\mathrm{Sym}^2(\chi_3) = \chi_6$,
$\bigwedge^2(\chi_3) = \bar\chi_3$.
The adjoint $\chi_8$ is irreducible under
$\mathrm{PSL}(2,\mathbb{F}_7)$.
\end{proof}

\begin{proof}[Proof of part~(b): gauge dynamics]
\emph{Roadmap.}
Step~B1 converts vortex displacements into CS connection
perturbations (the dreibein chain).
Step~B2 extends this to all orders via the CS/WZW theorem.
Steps~B3--B4 apply the McKay correspondence and DHVW orbifold
construction to \emph{derive} the gauge group as $\mathrm{SU}(3)$ at level~$1$
(see the derivation chain in \S\ref{sec:gauge-derivation-chain} below).
Step~B5 promotes the boundary currents to 4D gauge bosons via KK reduction.

\emph{Step~B1 (CS/WZW embedding).}
The charged Havelock modes $\eta_1, \eta_2$ (Step~3) are
massive scalars on~$\mathbf{H}^2$ with $m_k^2 = c_k(c_k{-}1)$.
Their boundary values parametrize $\mathbb{C}^2$, on which
$\mathbb{Z}/3\mathbb{Z}$ acts as
$\mathrm{diag}(\omega, \omega^{-1}) \in \mathrm{SU}(2)$.
The CS/WZW theorem \citep{Witten1988} gives the
nonlinear identification: the boundary values of the
CS gauge field~$A$ parametrize the WZW target
manifold~$G$, with the WZW action $S_{\mathrm{WZW}}[g]$
equal to the on-shell CS action
$S_{\mathrm{CS}}[A|_{\partial}]$ for all
field configurations, not just at the linearized level.
The dreibein chain below verifies the linearized limit.
A polygon vertex displacement $\delta z_j$ perturbs the
metric at the $j$-th cone point:
$\delta g_{ab} = (\varepsilon_j/\pi)\,
\partial_a\partial_b\,G(x, z_j)\,\delta z_j$.
In the first-order formalism $g = e \otimes e$, this gives
$\delta e^a = (1/2)\,e^b\,\delta g_{ab}$, and the
CS perturbation is
$\delta A^\pm = \delta\omega \pm \delta e/\ell$.
The $\mathbb{Z}_N$ Fourier projection
$\delta A_m = (1/N)\sum_j e^{-2\pi ijm/N}\,\delta A(z_j)$
decomposes into the mode-$m$ CS perturbation,
whose quadratic action is
$|\delta A_m|^2\,\lambda_m$, the Havelock eigenvalue.
The charged modes $\eta_1, \eta_2$ (Step~3)
are the $m \in O_+$ Fourier projections of $\delta A$.
The $\mathbb{Z}/3\mathbb{Z}$ invariance is exact at all orders
because it is a property of the Casimir spectrum, not of the
coupling strength: the Frobenius $\sigma\colon m \mapsto 2m \bmod 7$
permutes modes within orbits $O_+ = \{1,2,4\}$ and
$O_- = \{3,5,6\}$, whose Casimir multisets are both $\{3,5,6\}$.

\emph{Step~B2 (nonlinear extension).}
The CS/WZW theorem (\citealt{Witten1988}) extends the
linearized identification of Step~B1 to all orders:
the boundary values of the CS gauge field parametrize
the WZW target manifold~$G$, with the WZW action equal
to the on-shell CS action for all field configurations.
The $\mathbb{Z}/3$ invariance of Step~B1 therefore holds
nonlinearly.

\emph{Step~B3 (gauge group and level).}
The subgroup $\mathbb{Z}/3 \subset (\mathbb{Z}/7\mathbb{Z})^*
\cong \mathbb{Z}/6$ is uniquely determined:
$(\mathbb{Z}/7)^*$ has exactly four subgroups
($\{1\}$, $\mathbb{Z}/2$, $\mathbb{Z}/3$, $\mathbb{Z}/6$).
The Frobenius automorphism $\sigma\colon m \mapsto 2m \bmod 7$
generates $\mathbb{Z}/3 = \{1, 2, 4\}$
(the quadratic residues mod~$7$).
This is the unique subgroup of index~$2$, corresponding
to the unique quadratic subfield of
$\mathbb{Q}(\zeta_7)/\mathbb{Q}$.
The gauge group and its CS level are then determined by
two results, each a theorem:
\begin{itemize}
\item \emph{Gauge group: $\mathrm{SU}(3)$} (from McKay).
  The McKay correspondence for
  $\mathbb{Z}/3\mathbb{Z} \subset \mathrm{SU}(2)$
  gives the $A_2$ Dynkin diagram of $\mathrm{SU}(3)$
  \citep{McKay1980,GonzalezSprinberg1983}.
\item \emph{Level: $k = 1$} (from the ALE resolution
  and fusion ring).
  Two independent arguments fix $k = 1$:
  %
  \emph{(a)~ALE/$K$-matrix.}
  The orbifold singularity $\mathbb{C}^2/\mathbb{Z}_3$ has
  a minimal resolution whose exceptional divisors form a
  chain of $(-2)$-curves with intersection matrix equal to
  the $A_2$ Cartan matrix
  $K = \bigl(\begin{smallmatrix} 2 & -1 \\ -1 & 2
  \end{smallmatrix}\bigr)$
  \citep{Kronheimer1989}.
  By the Wen $K$-matrix classification \citep{Wen1995},
  the abelian CS theory with this $K$-matrix
  has the same topological order as the non-abelian
  $\mathrm{SU}(3)_1$ CS theory: the discriminant group
  $\mathbb{Z}^2/K\mathbb{Z}^2 \cong \mathbb{Z}_3$
  matches the three integrable representations of
  $\mathrm{SU}(3)_1$, and the mutual statistics
  encoded in $K^{-1} \bmod 1$ match the
  $\mathrm{SU}(3)_1$ braiding data.
  %
  \emph{(b)~Fusion ring.}
  The representation ring of
  $\mathbb{Z}/3 \subset \mathrm{SU}(2)$ is isomorphic to
  the fusion ring of $\mathrm{SU}(3)_k$ \emph{only} at $k = 1$.
  The integrable representations of $\mathrm{SU}(3)$ at
  level~$k$ are the highest weights $(\lambda_1, \lambda_2)$
  with $\lambda_1 + \lambda_2 \le k$.
  At $k = 1$: three representations
  $(0,0)$, $(1,0)$, $(0,1)$
  with cyclic fusion
  $(1,0) \otimes (1,0) = (0,1)$,
  $(1,0) \otimes (0,1) = (0,0)$,
  isomorphic to $\mathbb{Z}/3$.
  At $k = 2$: six representations
  $\{(0,0),(1,0),(0,1),(2,0),(0,2),(1,1)\}$,
  which cannot be isomorphic to $\mathbb{Z}/3$
  (wrong cardinality).
  At $k \ge 2$ the number of integrable representations
  is $(k{+}1)(k{+}2)/2 \ge 6 > 3$.
  This singles out $k = 1$ uniquely.
\end{itemize}
\noindent
\emph{Why $k = 1$ is exact.}
The homotopy group $\pi_3(\mathrm{SU}(3)) = \mathbb{Z}$ forces
the CS level to be a positive integer.
The unique crepant (minimal) resolution of
$\mathbb{C}^2/\mathbb{Z}_3$ gives the intersection form
$K = A_2$ without rescaling; a non-minimal resolution
would introduce $(-n)$-curves with $n > 2$, changing $K$,
and a multi-cover would rescale $K \to k K$
\citep{Kronheimer1989}.
Thus $k = 1$ is the unique level compatible with
the crepant resolution.
This is a topological invariant,
independent of the boundary central charge~$c$ or any
continuous parameter of the theory.
The gauge sector ($\mathrm{SU}(3)_1$ CS) and the
gravitational sector ($\mathrm{SL}(2,\mathbb{R})$ CS at
level $k_{\mathrm{grav}} = 2\,b(N)$) are independent
theories on the same manifold, with independent levels:
$k_{\mathrm{gauge}} = 1$ is compact and integer;
$k_{\mathrm{grav}} \approx 8.6$ is non-compact and irrational.

\begin{remark}[Boundary and DHVW consistency checks]
\label{rem:dhvw-check}
Two independent calculations confirm $k = 1$:
\emph{(i)}~The DHVW orbifold construction
\citep{DixonHarveyVafaWitten1985}
on the boundary CFT at $c = 12\,b(N)$
yields a twisted-sector conformal dimension
$h_k = k(3{-}k)/9 = 2/9$,
which receives a Liouville dressing correction
$\delta h = O(h_k^2/c) \approx 10^{-3}$
on the $\mathbf{H}^2$ background.
The boundary level identification gives
$k = 1 + O(10^{-3})$, consistent with
the exact topological result.
The orbifold at irrational~$c$ is a consistent
(non-rational) CFT.
The DHVW construction requires a well-defined parent CFT
with a discrete symmetry; it does not require rational~$c$.
Three consistency conditions hold:
\emph{Unitarity}: $c > 1$ places the parent CFT
in the continuum regime of the Friedan--Qiu--Shenker
classification, where unitary Virasoro representations
exist for all~$h \ge 0$; the orbifold projection
and twisted sectors preserve the positive-definite
inner product.
\emph{Modular invariance}: Vafa's proof
(\citeyear{Vafa1986}) that
$Z_{\mathrm{orb}} = (1/|G|)\sum_{g,h}Z_g^h$
is modular invariant is a combinatorial identity
about the $(g,h)$ summation labels under
$\mathrm{SL}(2,\mathbb{Z})$; the central charge
enters only through $e^{2\pi i c/24}$, which cancels
in the orbifold sum for any~$c$.
\emph{OPE closure}: the obstruction to consistent
twisted-sector OPE lies in $H^3(G, \mathrm{U}(1))$;
for $G = \mathbb{Z}/3$,
$H^3(\mathbb{Z}/3, \mathrm{U}(1)) = 0$,
so closure is automatic.
Explicit orbifold constructions at non-rational~$c$
are standard in the literature:
Liouville theory orbifolds at $c \ge 25$
\citep{HikidaSchomerus2007},
$\mathrm{SL}(2,\mathbb{R})/\mathrm{U}(1)$ coset
orbifolds at $c = 3k/(k{-}2)$
\citep{DijkgraafVerlindeVerlinde1992},
and free-boson orbifolds at irrational radius
\citep{Ginsparg1988}.
\emph{(ii)}~The representation ring of
$\mathbb{Z}/3 \subset \mathrm{SU}(2)$
is isomorphic to the fusion ring of $\mathrm{SU}(3)_1$
\citep{CappelliItzyksonZuber1987},
consistent with $k = 1$ and no other level.
\end{remark}

\emph{Step~B4 ($G_L$ invariance).}
The Frobenius $\mathbb{Z}/3\mathbb{Z}$ acts as an
exact automorphism of the \emph{full nonlinear} boundary CFT.
The proof uses the $G_L \times G_R$ invariance of the WZW action:
left multiplication $g(x) \mapsto h\cdot g(x)$ by
$h \in \mathbb{Z}/3\mathbb{Z} \subset G$ preserves both
the kinetic term $\mathrm{Tr}(g^{-1}\partial g \cdot
g^{-1}\bar\partial g)$ and the Wess--Zumino term
$\int \mathrm{Tr}(g^{-1}dg)^3$
(both are manifestly $G_L$-invariant).
This gives exact invariance of the action, the path integral,
all $n$-point functions (Schwinger--Dyson), and all OPE
coefficients.
The Casimir multiset invariance follows:
the Havelock eigenvalues are spectral data of the
$G_L$-invariant theory.
\emph{Step~B5 (CS/WZW correspondence $\to$ 4D gauge field).}
The chain from orbifold current algebra to 4D gauge theory
has three links, each a theorem:
\begin{enumerate}
\item $\widehat{\mathfrak{su}}(3)_1$ current algebra on
  the boundary $\to$ $\mathrm{SU}(3)_1$ Chern--Simons theory
  in the bulk (Witten CS/WZW theorem \citep{Witten1988});
\item $\mathrm{CS}_3$ on $\mathbf{H}^2 \times S^1$ $\to$
  $\mathrm{YM}_2$ on $\mathbf{H}^2$
  (KK reduction: the CS action
  $\int \mathrm{Tr}(A \wedge dA + \tfrac{2}{3}A^3)$
  on $\mathbf{H}^2 \times S^1$, after Fourier expanding
  $A = A^{(0)} + \sum_{n \ne 0} A^{(n)} e^{in\theta}$ and
  integrating over $S^1$, yields the 2D YM action
  $\int_{\mathbf{H}^2}\mathrm{Tr}(F^2)$
  for the zero mode $A^{(0)}$, with coupling $g^2 = 4\pi/k$
  and massive KK modes $M_n = n/R_{S^1}$);
\item $\mathrm{YM}_2$ on $\mathbf{H}^2$ $+$ WDW time direction
  $\to$ $\mathrm{YM}_{3+1}$: the breathing mode~$\rho(t)$
  (the scale factor of the $\mathbf{H}^2$ slice) satisfies
  the WDW equation $H\Psi = 0$
  (Paper~III, Proposition~\ref*{III-prop:wdw-lambda}).
  The Feynman--Kac theorem identifies the WDW
  evolution parameter with Euclidean time
  (Paper~III, \S\ref*{III-sec:qm-gr-bridge}):
  the WKB solution $\Psi_0 \propto e^{-\int V\,d\rho}$
  evolves monotonically in~$\rho$, so $\rho$ is a good time
  coordinate.
  The gauge field $A^{(0)}$ on the spatial $\mathbf{H}^2$
  thereby acquires the time direction~$\rho$, giving
  $\mathrm{YM}_{2+1}$.
  The KK tower $A^{(n)}$ with $n \ge 1$ provides the
  additional spatial dimension (the $S^1$ fiber),
  completing $1 + 2 + 1 = 4$ dimensions.
\end{enumerate}
The $\mathrm{SU}(3)$ gauge group is therefore \emph{determined}
by the Frobenius action on the Havelock modes
(via McKay $+$ DHVW) from the same inputs~(H1)--(H3).
In the topological bulk, there are zero propagating
bulk modes; the boundary currents $J^a(z)$
($a = 1, \ldots, 8$, conformal weight~$(1,0)$, spin~$1$)
are the dynamical degrees of freedom.
After KK reduction on~$S^1$, the boundary currents become
$4$D spin-$1$ gauge bosons with the $\mathrm{SU}(3)$ vertex
structure inherited from the Kac--Moody OPE,
at central charge
\begin{equation}\label{eq:csu3}
  c_{\mathrm{SU}(3)}
  = \frac{k\dim(\mathrm{SU}(3))}{k{+}h^\vee} = \frac{8}{4} = 2.
\end{equation}
The total boundary central charge is
$c_{\mathrm{tot}} = 12\,b(N)$.
The gauge contributions ($c_{\mathrm{SU}(3)} = 2$,
$c_{\mathrm{SU}(2)} = 1$, $c_{\mathrm{U}(1)} = 1$) are subsumed:
$c_{\mathrm{grav}} = 12\,b(N) - 4$, with
$G = 3/(2c_{\mathrm{tot}}) = 3/(2 \times 12\,b(N))$
using the total central charge in the Brown--Henneaux relation.
\end{proof}

% ALE/K-matrix argument promoted to Step B3 (primary derivation of k=1).

\subsection{Strong CP problem: $\theta_{\mathrm{QCD}} = 0$
  from level quantization}
\label{sec:strong-cp}
The QCD vacuum angle is $\theta_{\mathrm{QCD}} = 0$
exactly, by the following three-step argument.
\begin{enumerate}
\item \emph{CS level quantization.}
  For a compact gauge group,
  $\pi_3(\mathrm{SU}(3)) = \mathbb{Z}$ forces the
  CS level $k \in \mathbb{Z}^+$.
  At $k = 1$: the vacuum angle is
  $\theta = 2\pi k_{\mathrm{frac}} = 0$
  (integer level, no fractional part).
\item \emph{Fermion shifts preserve integrality.}
  Each massive Dirac fermion in representation~$R$
  shifts the CS level by
  $\Delta k = T(R)\,\mathrm{sign}(m)$
  (parity anomaly; \citealt{Redlich1984}),
  where $T(R)$ is the Dynkin index
  ($T(\mathrm{fund}) = 1/2$ for $\mathrm{SU}(3)$).
  For $6$ quark flavors in the fundamental:
  $\Delta k = 6 \times T(\mathrm{fund})
  \times \mathrm{sign}(m) = 6 \times 1/2 = 3 \in \mathbb{Z}$
  (the three colors enter through~$T$, not as independent
  fermions).
  All masses have the same sign (from a single Higgs VEV),
  giving $k_{\mathrm{eff}} = 1 + 3 = 4$ (integer).
\item \emph{Gravitational sector decouples.}
  The gravitational CS level
  $k_{\mathrm{grav}} = 2\,b(N) = 8.60$ is irrational,
  with fractional part $k_{\mathrm{frac}}^{\mathrm{grav}}
  = 0.60$.
  This does not affect $\theta_{\mathrm{QCD}}$
  because the gauge and gravitational traces are
  orthogonal:
  $\mathrm{Tr}(T_a^{\mathrm{SU}(3)}\,
  T_b^{\mathrm{SL}(2,\mathbb{R})}) = 0$.
\end{enumerate}
At all loop orders,
$k_{\mathrm{eff}}^{\mathrm{SU}(3)} \in \mathbb{Z}$
and $\theta_{\mathrm{QCD}} = 0$.
The mechanism is topological (level quantization from
$\pi_3$), requires no axion, and predicts:
vanishing neutron EDM at all orders,
null results at ADMX and ABRACADABRA
(no axion field, no axion dark matter).

\begin{center}
\small
\begin{tabular}{cccc}
\toprule
Orbit & Modes & Casimirs $f$ & $\sigma$-eigenvalue \\
\midrule
$O_+$ & $\{1, 2, 4\}$ & $\{3, 5, 6\}$ & $1, \omega, \omega^2$ \\
$O_-$ & $\{3, 5, 6\}$ & $\{6, 5, 3\}$ & $1, \omega, \omega^2$ \\
\bottomrule
\end{tabular}
\end{center}

The QR/QNR orbits $O_\pm$ are the
$\chi_3$/$\bar\chi_3$ sectors of Theorem~\ref{thm:su3}:
modes $\{1,2,4\}$ (quadratic residues mod~$7$) carry~$\chi_3$,
modes $\{3,5,6\}$ (non-residues) carry~$\bar{\chi}_3$.

\emph{Consistency check} ($C_F = 1 + 2B_2$).
The fundamental Casimir $C_F = (N_c^2{-}1)/(2N_c) = 4/3$ at $N_c = 3$
equals $1 + 2B_2$, where $2B_2 = 1/3$ is the Laurent constant of
the $\csc^2$ kernel
(Paper~I, Proposition~\ref*{I-prop:four-thirds}).
The identity $4/3 = 1 + 2\zeta(2)/\pi^2$ is a
seven-fold universality (Paper~I): it appears independently
in the Havelock self-interaction, the Abraham electromagnetic
self-energy, the Richardson turbulence exponent, and the
heat kernel on~$S^2$.
$C_F = 1 + 2B_2$ holds
\emph{only} at $N_c = 3$ (the unique positive root of
$3N_c^2 - 8N_c - 3 = 0$), which is the value
determined by the Frobenius order $\mathrm{ord}_7(2) = 3$.
The identity $2B_2 = 1/N_c$ connects the $\csc^2$
self-interaction constant to the gauge group rank:
the \emph{same kernel} that determines the self-interaction
($B_2$) also determines the gauge group ($N_c$) via the
Frobenius orbits.
This is a consistency check between two independently derived
quantities, not a derivation of either.

\subsection{$\mathrm{U}(1)$ from Kaluza--Klein}

The $S^1$ fiber with flux $N/2$ gives a $\mathrm{U}(1)$ gauge field
at all~$N$, with coupling $\alpha = 6/(\pi c)$ and the exact lock
$\alpha/(8\pi G) = 1/(2\pi^2)$.
This is standard Kaluza--Klein reduction.
The KK $\mathrm{U}(1)$ is the \emph{only} abelian gauge
symmetry in the theory: every KK mode carries charge
$Q = m/N$ fixed by its $S^1$ momentum quantum number.
This $\mathrm{U}(1)$ \emph{is} hypercharge:
it is the only abelian gauge symmetry
in the theory, and the charge normalization $Q = m^*/N = 1/2$
at the critical $N = 4$ mode is the Standard Model
hypercharge of the Higgs doublet, derived from the
KK spectrum.

\subsection{Summary: the derivation chain}
\label{sec:gauge-derivation-chain}

The gauge group $\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$
at levels~$k = 1$ is \emph{derived} from the Seifert geometry
$\mathbf{H}^2 \times_N S^1$.
The chain uses three mathematical theorems applied to the geometry:

\begin{enumerate}
\item \emph{$\mathrm{SU}(2)$ from CS gravity} (Witten 1988):
  KK reduction on~$S^1$ gives $2{+}1$D gravity $= \mathrm{CS}[\mathrm{SU}(2)]$.
  At $N = 4$, the critical Havelock eigenvalue $f(2,4) = 2$.
  The Euler class $e = N/2$ breaks
  $\mathrm{SU}(2)_R$ via the parity anomaly.

\item \emph{$\mathrm{SU}(3)$ from McKay} (McKay 1980):
  The unique $\mathbb{Z}/3\mathbb{Z}$ subgroup of
  $(\mathbb{Z}/7\mathbb{Z})^*$ (selected by CP exclusion
  and conjugate-pair requirement; see Step~1 of
  Theorem~\ref{thm:su3}) gives orbits
  $\{1,2,4\}$, $\{3,5,6\}$.
  The McKay correspondence maps
  $\mathbb{Z}/3 \subset \mathrm{SU}(2) \to A_2 \to \mathrm{SU}(3)$.
  The DHVW twist-field OPE (1985) fixes the level
  $k = 1$ (up to corrections $O(h^2/c) \sim 0.1\%$,
  which are small enough that integer quantization
  of the CS level forces $k = 1$ exactly).

\item \emph{$\mathrm{U}(1)$ from KK}:
  The fiber gives a $\mathrm{U}(1)$ gauge field with
  $k_Y = 1$ (single-valued holonomy).
  Charge $Q = m/N$ is fixed by the $S^1$ momentum quantum number.
\end{enumerate}

The CS-Havelock identity (Paper~I, Theorem~\ref*{I-thm:cs-havelock})
proves that the Casimir $f(m,N) = m(N{-}m)/2$ is simultaneously
the vortex eigenvalue, the $\mathrm{sl}(2,\mathbb{R})$ Casimir
(Helgason), and the CS Wilson line Casimir (Etingof--Frenkel--Kirillov).
These are the \emph{same operator} (the Laplacian on~$\mathbf{H}^2$)
restricted to the same subspace ($\mathbb{Z}_N$ tangential modes).

\emph{Central charge budget} (no-hidden-sector theorem):
An exhaustive enumeration of simple Lie groups $G$ at level
$k \le 3$ yields $33$~combinations with total WZW central
charge~$c_{\mathrm{gauge}} = 4$.\footnote{The $33$~combinations
include $\mathrm{SU}(5)_1$, $\mathrm{SO}(8)_1$,
$\mathrm{SU}(3)_3$, and $30$~multi-factor products;
the full list is in the companion code
(\texttt{gauge\_group\_derivation.py}).}
Of these, $\mathrm{SU}(3)_1 \times \mathrm{SU}(2)_1 \times \mathrm{U}(1)_1$
is the unique one containing all three derived factors.
Since $c_{\mathrm{gauge}} = 2 + 1 + 1 = 4$ is fully accounted for,
no additional gauge sector can fit within the central charge budget:
the gauge group is \emph{exactly}
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$,
with no hidden factors.

\begin{remark}[Derivation chain from the Havelock-Pell criterion]
\label{rmk:honest-inputs}
Given $N = 4$ and $N = 7$ (\textbf{A1}) and the derived Seifert geometry
(\textbf{A2}), the derivation chain uses five mathematical
theorems (Witten, EFK, Havelock, McKay, DHVW) and the
OPS entropy principle.
The $\mathbb{Z}/3\mathbb{Z}$ orbifold group is determined by the Frobenius structure:
it is the unique proper non-trivial subgroup of
$(\mathbb{Z}/7\mathbb{Z})^*$ not already identified with CP
(Step~1 of Theorem~\ref{thm:su3}).
The assignment of KK pairs to fermion generations and
the identification of pair~$3$ with the Higgs doublet
follow from the BF-crossing structure
(only pair~$3$ has $c_{\mathrm{up}} < 1 < c_{\mathrm{dn}}$).
The CKM phase $\delta = 69.3^\circ$ is a mathematical
computation on~$\mathbf{H}^2$; its physical interpretation
as the CP-violating phase relies on the vortex-gravity
isomorphism of Paper~III.
\end{remark}

%% ====================================================================
\section{The Weinberg angle}
\label{sec:weinberg}

\begin{derivation}[Weinberg angle from polygon eigenvalues]
\label{thm:weinberg}
The KK reduction of $\mathrm{CS}_3$ on $\mathbf{H}^2 \times S^1$
gives $\mathrm{YM}_2$ on $\mathbf{H}^2$ with coupling
$g^2 = 2\pi/((k{+}h^\vee)R)$ (Blau--Thompson 1993).
The effective coupling of matter in the $m$-th Fourier mode is
$\alpha(m) = g^2 \times f(m,N)$,
where $f(m,N) = m(N{-}m)/2$ is the \emph{Havelock eigenvalue}, the eigenvalue of the $\mathbf{H}^2$ Laplacian restricted
to the $\mathbb{Z}_N$ mode
(Proposition~\ref{III-prop:casimir-havelock}).
This is a geometric quantity (sector~G of
Theorem~\ref{thm:gauge-matter}), determined by the
polygon, not by a gauge-group representation label.
The common factor $2\pi/R$ cancels in the mixing angle ratio:
\begin{equation}\label{eq:weinberg}
  \sin^2\theta_W
  = \frac{h_Y}{h_Y + h_W}
  = \frac{Q^2(k{+}h^\vee)}{Q^2(k{+}h^\vee) + k_Y\,f(m^*,N)}
  = \frac{3}{11} \approx 0.2727,
\end{equation}
where $h_W = f(m^*,N)/(k{+}h^\vee) = 2/3$
(Havelock eigenvalue at $N = 4$, $m^* = 2$),
$h_Y = Q^2/k_Y = 1/4$ (KK momentum charge $Q = m^*/N = 1/2$),
and $k_Y = 1$ (U(1) compact-boson level).
No representation label $j$ is needed:
the number~$2$ enters as $f(2,4)$, an eigenvalue of the
$\mathbf{H}^2$ Laplacian proved in Paper~I.
The integrability bound $j \le k/2$ of $\mathrm{SU}(2)_1$
WZW constrains topological states in the CS Hilbert space;
it does not constrain geometric eigenvalues on~$\mathbf{H}^2$.
\end{derivation}

\begin{proof}
Three steps: derive the quantum numbers from the Casimir,
derive the CS gauge couplings from the DHVW levels,
compute the mixing angle from the KK-reduced action.

\emph{Step~1 (Polygon eigenvalue = gauge Casimir).}
The critical mode at $N = 4$ has Havelock eigenvalue
$f(2,4) = m^*(N{-}m^*)/2 = 2$ and KK charge $Q = m^*/N = 1/2$.
Both are read off from the vortex spectrum (Paper~I);
no choice is involved.
The CMS--CS Casimir identification
(Proposition~\ref{III-prop:casimir-havelock})
proves that $f(m,N)$ is simultaneously the
$\mathbf{H}^2$ Laplacian eigenvalue (geometric),
the $\mathfrak{sl}(2,\mathbb{R})$ Casimir
(representation-theoretic), and the CS Wilson line
eigenvalue (gauge-theoretic), all equal to
$m(N{-}m)/2$ because all three use the same operator
on the same space (Helgason symmetric space theorem).
The value $f(2,4) = 2$ is therefore not a ``choice
of representation'': it is the unique eigenvalue
of the $m = 2$ mode on~$\mathbf{H}^2$, which
the CMS--CS identification proves equals the
$\mathrm{SU}(2)$ Casimir $C_2 = j(j{+}1) = 2$
at $j = 1$.
The fundamental representation ($j = 1/2$,
$C_2 = 3/4$) does not enter because the
critical Fourier mode at $N = 4$ has $m^* = 2$,
not $m^* = 1$; the polygon determines which mode
is critical, and the Casimir identification determines
its gauge-theoretic value.

\emph{Step~2 (CS gauge couplings).}
At the Chern--Simons threshold
\citep{Witten1988}:
\begin{itemize}
\item $\mathrm{SU}(2)$:
  $g_W^2 = 2\pi/((k{+}h^\vee)\,R) = 2\pi/(3R)$
  \quad($k = 1$, $h^\vee(\mathrm{SU}(2)) = 2$);
\item $\mathrm{U}(1)$:
  $g_Y^2 = 2\pi/(k_Y\,R) = 2\pi/R$
  \quad($k_Y = 1$).
\end{itemize}
The level $k = k_Y = 1$ is forced at each gauge group
by the ALE resolution (Theorem~\ref{thm:su3}, Step~B3):
\begin{itemize}
\item $\mathrm{SU}(3)$: the Frobenius at $N = 7$ generates
  $\mathbb{Z}/3\mathbb{Z}$; $k = 1$ by the ALE/$K$-matrix and
  fusion ring arguments of Step~B3.
\item $\mathrm{SU}(2)$: the Frobenius at $N = 4$ generates
  $\mathbb{Z}/2\mathbb{Z}$; the ALE resolution of
  $\mathbb{C}^2/\mathbb{Z}_2$ has $A_1$ intersection form
  $K = [2]$, and the fusion ring of $\mathbb{Z}/2$ matches
  $\mathrm{SU}(2)_1$ uniquely (two integrable representations:
  trivial and fundamental).
\item $\mathrm{U}(1)$: $k_Y = 1$ from single-valued holonomy on~$S^1$.
\end{itemize}
In all three cases, $k = 1$ is a topological invariant
of the resolved singularity.
As a cross-check: $k = 2$ would give $\sin^2\theta_W = 1/3$,
and $k = 3$ gives $5/13$; both are experimentally excluded.

\emph{Step~3 (Mixing angle from KK-reduced Yang--Mills couplings).}
The KK reduction of $\mathrm{CS}_3$ on $\mathbf{H}^2 \times S^1(R)$
at level~$k$ gives $\mathrm{YM}_2$ on~$\mathbf{H}^2$
with bare coupling $g^2 = 2\pi/((k{+}h^\vee)\,R)$
(\citealt{BlauThompson1993}; the shift $k \to k{+}h^\vee$
is the standard one-loop CS renormalization).
The gauge--matter vertex couples with strength $g \times T^a_R$
(the Lie-algebra generator in representation~$R$),
so the \emph{effective} coupling for matter in representation~$R$ is
\begin{equation}\label{eq:conformal-coupling}
  \alpha_{\mathrm{eff}}(R)
  \;=\; g^2 \times C_2(R)
  \;=\; \frac{2\pi\,C_2(R)}{(k{+}h^\vee)\,R}.
\end{equation}
The Lie-algebra Casimir $C_2(R) = \sum_a (T^a_R)^2$ is a property
of the representation, independent of~$k$.
The fiber radius~$R$ cancels in the mixing angle ratio
(using $h^\vee_{\mathrm{U}(1)} = 0$ for the abelian factor):
\begin{equation}\label{eq:weinberg-cs}
  \sin^2\theta_W
  = \frac{g_Y^2\,C_2(Y)}{g_Y^2\,C_2(Y) + g_W^2\,C_2(W)}
  = \frac{Q^2/k_Y}{\;Q^2/k_Y \;+\; f(m^*,N)/(k{+}h^\vee_{\mathrm{SU}(2)})\;}.
\end{equation}
The numerator and denominator are products of two
independent factors: the YM coupling $g^2 = 2\pi/((k{+}h^\vee)R)$
(determined by the CS level via KK reduction) and the
Lie-algebra Casimir $C_2$ (a property of the representation,
independent of~$k$).
Substituting from Steps~1--2
($Q^2 = 1/4$, $k_Y = 1$, $f(2,4) = 2$, $k{+}h^\vee = 3$):
\[
  \sin^2\theta_W
  = \frac{1/4}{1/4 + 2/3}
  = \frac{3}{11}.
\]
\emph{Compact vs.\ noncompact.}
The CMS--CS identification (Paper~III,
Proposition~\ref*{III-prop:casimir-havelock}) uses
$\mathrm{SL}(2,\mathbb{R})$ (noncompact), while the Standard
Model gauge group is compact.
Three facts ensure the ratio~\eqref{eq:weinberg-cs} is
independent of the real form:
(i)~the KK-reduced YM coupling $g^2 = 2\pi/((k{+}h^\vee)R)$
depends on the CS level~$k$ and the fiber radius~$R$, both
of which are properties of the Seifert geometry, not the
real form;
(ii)~the quadratic Casimir $C_2(R)$ is a property of the
complexified Lie algebra $\mathfrak{sl}(2,\mathbb{C})$ and
takes the same value $j(j{+}1)$ in the spin-$j$ representation
for both $\mathrm{SU}(2)$ and $\mathrm{SL}(2,\mathbb{R})$
(the Casimir is a polynomial in the generators, which have
the same commutation relations over~$\mathbb{C}$);
(iii)~the effective coupling $\alpha_{\mathrm{eff}}(R)
= g^2 C_2(R)$ in~\eqref{eq:conformal-coupling} is therefore
determined by $(k, j)$, and the mixing-angle
ratio cancels both~$R$ and~$C_1$.
The mode-independent constant $C_1$ (which does depend on
the real form and the curvature) does not enter the ratio.
\end{proof}

\begin{derivation}[Gauge--matter factorization]\label{thm:gauge-matter}
The theory has three cleanly separated sectors:
\begin{enumerate}
\item[\emph{(T)}] \emph{Topological sector} (3D CS):
  determines the gauge group and level~$k$.
  The WZW integrability bound $j \le k/2$ constrains this sector
  (conformal blocks, Verlinde formula, anyonic braiding).
\item[\emph{(D)}] \emph{Dynamical sector} (2D KK-reduced YM):
  the KK reduction of $\mathrm{CS}_3$ on
  $\mathbf{H}^2 \times S^1(R)$ produces $\mathrm{YM}_2$
  on~$\mathbf{H}^2$ with coupling
  $g^2 = 2\pi/((k{+}h^\vee)\,R)$ (Blau--Thompson).
\item[\emph{(G)}] \emph{Geometric sector} (polygon on~$\mathbf{H}^2$):
  the Havelock critical mode has Casimir
  $C_2 = f(m^*,N) = m^*(N{-}m^*)/2$, an eigenvalue of the
  $\mathbf{H}^2$ Laplacian determined by the polygon alone.
\end{enumerate}
The effective coupling factorizes as
\begin{equation}\label{eq:gauge-matter}
  \alpha(R) = \underbrace{g^2}_{\text{(D) gauge}} \times
  \underbrace{C_2(R)}_{\text{(G) matter}},
\end{equation}
where $g^2$ comes from sector~(D) and $C_2$ from sector~(G).
The integrability bound from sector~(T) is irrelevant: it
constrains topological excitations (Wilson loops),
not geometric excitations (polygon displacements).
\end{derivation}

\begin{proof}
\emph{(a) From CS to YM (sectors T$\to$D).}
The CS action
$S_{\mathrm{CS}} = (k/4\pi)\int \mathrm{Tr}(A \wedge dA + \tfrac{2}{3}A^3)$
on $\mathbf{H}^2 \times S^1(R)$ has no propagating degrees of freedom
in~$3$D.  The KK reduction on~$S^1(R)$ produces a $2$D YM kinetic term
$\tfrac{1}{4g^2}\int F^2$ with coupling
$g^2 = 2\pi/((k{+}h^\vee)\,R)$ (\citealt{BlauThompson1993};
the shift $k \to k{+}h^\vee$ is the standard one-loop CS
renormalization, which depends only on the root system $A_1$
and takes the same value $h^\vee = 2$ for both the compact
form $\mathrm{SU}(2)$ and the non-compact form
$\mathrm{SL}(2,\mathbb{R})$).
This is the step that creates a propagating gauge field.

\emph{(b) Geometric matter (sector G).}
The vortex critical mode at $N = 4$ has Havelock eigenvalue
$f(2,4) = 2 \times 2 / 2 = 2$.
This is a \emph{geometric} quantity: the eigenvalue of the
$\mathbf{H}^2$ Laplacian restricted to the $m = 2$ Fourier mode.
The mode exists independently of the gauge field, it is a polygon displacement on~$\mathbf{H}^2$,
present before the CS theory is introduced.
It couples to the gauge field through the standard
covariant derivative $D_\mu = \partial_\mu - ig\,A^a_\mu\,T^a_R$
of the KK-reduced YM theory, giving
$\alpha = g^2 C_2(R)$ at one-gluon exchange.

\emph{(c) Operator identity: the coupling eigenvalue
is the Havelock eigenvalue.}
The coupling of the critical KK mode $\phi_{m^*}$ to the
gauge field is determined by a single operator identity,
with no representation label required:
\begin{enumerate}
\item \emph{Operator identity} (Witten $+$ Helgason).
  The CS connection on $\mathbf{H}^2$ is
  $A = \omega + e/\ell$
  (spin connection $+$ dreibein; \citealt{Witten1988}).
  The covariant Laplacian built from this connection
  equals the Laplace--Beltrami operator:
  $D^2_{\mathrm{CS}} = \Delta_{\mathbf{H}^2}$,
  because $A$ \emph{is} the gravitational connection on
  $\mathbf{H}^2 = \mathrm{SL}(2,\mathbb{R})/\mathrm{SO}(2)$.
  By the Helgason symmetric space theorem
  (\citealt{Helgason1984}),
  $\Delta_{\mathbf{H}^2} = \Omega_{\mathfrak{sl}(2,\mathbb{R})}$
  on equivariant functions.
  Therefore:
  \begin{equation}\label{eq:operator-identity}
    D^2_{\mathrm{CS}}
    = \Delta_{\mathbf{H}^2}
    = \Omega_{\mathfrak{sl}(2,\mathbb{R})}.
  \end{equation}
  All three are the \emph{same} operator on the
  \emph{same} Hilbert space
  ($\mathbb{Z}_N$-equivariant functions on~$\mathbf{H}^2$).
\item \emph{Eigenvalue extraction.}
  The KK mode $\phi_{m^*}$ is an eigenfunction:
  $D^2\,\phi_{m^*} = f(m^*,N)\,\phi_{m^*}$,
  where $f(m^*,N) = m^*(N{-}m^*)/2$ is the
  Havelock eigenvalue (Paper~I).
  At $m^* = 2$, $N = 4$: $f(2,4) = 2$.
\item \emph{Gauge coupling.}
  The KK-reduced YM Lagrangian
  (\citealt{BlauThompson1993}) couples the mode
  $\phi_{m^*}$ to the gauge field through
  the covariant derivative
  $D_\mu = \partial_\mu - ig\,A^a_\mu\,T^a$.
  The effective coupling is
  $\alpha = g^2 \times (\text{eigenvalue of } D^2
  \text{ on } \phi_{m^*}) = g^2 \times f(m^*,N)$.
  No representation label enters:
  $f(2,4) = 2$ is the Havelock eigenvalue of the
  $\mathbf{H}^2$ Laplacian on the critical mode,
  proved in Paper~I.
\item \emph{Result.}
  $\alpha_{\mathrm{eff}} = g^2 \times f(m^*,N)
  = [2\pi/((k{+}h^\vee)R)] \times 2 = 4\pi/(3R)$.
\end{enumerate}

\begin{remark}[Representation content and the WZW bound]
\label{rmk:wzw-bound}
The eigenvalue $f(2,4) = 2$ corresponds to $j = 1$
in the discrete series of $\mathrm{SL}(2,\mathbb{R})$
(the Plancherel decomposition on $\mathbf{H}^2$ is
multiplicity-free; \citealt{Helgason1984}, Ch.~IV).
Under Euclidean continuation
$\mathrm{SL}(2,\mathbb{R}) \to \mathrm{SU}(2)$,
this maps to the adjoint (spin-$1$, dimension~$3$),
confirmed independently by the dreibein chain:
$\delta A_{m^*}$ is a connection perturbation,
which transforms in the adjoint by definition.

At $\mathrm{SU}(2)$ level $k = 1$, the quantum dimension
$d_1 = 0$: the $j = 1$ representation decouples from the
\emph{topological} sector (no $j = 1$ anyonic excitations
exist in the $\mathrm{SU}(2)_1$ modular tensor category).
This does not affect the gauge coupling:
the WZW integrability bound
$j \le k/2$ constrains which representations propagate
as \emph{topological} excitations (anyons, Wilson loops),
not which representations appear as \emph{dynamical}
matter fields coupled through the YM kinetic term.
The distinction is standard in Chern--Simons--matter theories:
in ABJM theory (\citealt{ABJM2008}), matter fields
at CS level~$k$ couple to the gauge field
through covariant derivatives in representations
unconstrained by~$k$.
The CS level determines the topological sector
(fusion rules, braiding phases);
the matter content is a separate physical input,
coupled through the standard gauge vertex $g\,T^a_R$.
Here, the matter content (the KK mode $\phi_{m^*}$
with eigenvalue $f(2,4) = 2$) is determined by the
polygon geometry, and its coupling to the gauge field
uses the Havelock eigenvalue as the Casimir,
exactly as in any CS--matter theory.
\end{remark}

\emph{(d) The Weinberg angle uses only sectors D and G.}
The effective couplings are
$\alpha_W = g_W^2 \cdot C_2(j{=}1) = (2\pi/3R) \cdot 2$
and $\alpha_Y = g_Y^2 \cdot Q^2 = (2\pi/R) \cdot 1/4$.
The common factor $2\pi/R$ cancels in the mixing ratio:
$\sin^2\theta_W = \alpha_Y/(\alpha_Y + \alpha_W) = 3/11$.
The topological sector (where $d_1 = 0$) does not enter.

\emph{(e) Cross-check: $\mathrm{SU}(3)_1$.}
Gluons have $C_2(\mathrm{adj}) = 3$, also non-integrable at $k = 1$.
They propagate through the YM kinetic term (sector~D),
not through topological braiding (sector~T).
This is standard in all CS--matter theories
\citep{ABJM2008}: the CS level determines the gauge coupling,
the matter representation is a separate physical input.
\end{proof}

\paragraph{Comparison with unification frameworks.}
\begin{center}
\renewcommand{\arraystretch}{1.2}
\begin{tabular}{lll}
\hline
Framework & Definition at threshold & Value \\
\hline
SM alone & $g'^2/(g^2 + g'^2)$ & free parameter \\
$\mathrm{SU}(5)$ GUT & $\mathrm{Tr}(Y^2)/\mathrm{Tr}(T_3^2{+}Y^2)$ &
  $3/8 = 0.375$ \\
CS/WZW (this work) & $h_Y/(h_Y + h_W)$ & $3/11 = 0.273$ \\
\hline
\end{tabular}
\end{center}
In 4D YM, the gauge coupling $g$ is representation-independent,
so $\sin^2\theta_W = g'^2/(g^2{+}g'^2)$ requires an external
normalization (the GUT embedding).
Every unification framework supplies this normalization
through its embedding structure:
in $\mathrm{SU}(5)$, it is the trace ratio
$\mathrm{Tr}(Y^2)/\mathrm{Tr}(T_3^2)$ evaluated in a common
representation;
in the KK-reduced CS framework, it is the conformal weight
ratio $h_Y/h_W$.
Both contain a Casimir factor ($C_2(R)$ of the matter species
at the unification threshold) for the same reason:
the Casimir encodes how the different gauge factors couple
to the matter sector that defines the threshold.
The conformal weight $h_R = C_2(R)/(k{+}h^\vee)$
is the natural coupling strength in the WZW theory;
the SM couplings $g$ and~$g'$ are defined by matching at
the polygon scale:
\begin{equation}\label{eq:coupling-matching}
  g'^2 = \frac{4\pi\,h_Y}{R} = \frac{4\pi\,Q^2}{k_Y\,R},
  \qquad
  g^2 = \frac{4\pi\,h_W}{R} = \frac{4\pi\,f(m^*,N)}{(k{+}h^\vee)\,R}.
\end{equation}
The fiber radius~$R$ cancels in the ratio:
$g'^2/(g'^2{+}g^2) = h_Y/(h_Y{+}h_W) = 3/11$,
recovering the standard representation-independent formula
with $g'$ and~$g$ defined by the CS matching.
No GUT embedding is needed because the polygon (sector~G)
and the CS level (sector~D) together fix the normalization.

At the CS threshold there is a single matter species
(the Havelock critical mode at $N = 4$).
Below the polygon scale the theory flows to the 4D SM,
and $3/11$ becomes the boundary condition for the standard
RG running (one-loop, $b_1 = 41/10$, $b_2 = -19/6$),
which brings it to $\sin^2\theta_W(M_Z) = 0.231$, matching
the measured value $0.2312$ to $0.1\%$.
This determines $M_{\mathrm{poly}} \approx 300$\,TeV:
a consistency check (the running must land in the
$50$--$500$\,TeV window).
The SU$(5)$ prediction $3/8$ matches at
$M_{\mathrm{GUT}} \sim 10^{14}$\,GeV
($62\%$ above experiment at tree level).

\subsection{Spectral derivation of the electroweak couplings}
\label{sec:spectral-weinberg}

The CS inverse couplings that enter the Weinberg angle can be derived
independently from the spectral structure of the $\tilde{E}_8$ Cartan matrix
(Paper~I, \S\ref{I-sec:mark-distribution};
Paper~V, \S\ref{V-sec:spectral-primes}).
This provides a graph-theoretic derivation of the coupling constants
that enter~\eqref{eq:weinberg-cs}, without invoking Chern--Simons theory
directly.

\begin{lemma}[Spectral--CS bridge]\label{lem:spectral-bridge}
The spectral denominators $\{1, 2, 3, 5\}$ of the $\tilde{E}_8$
Cartan matrix are the \emph{stabilizer orders} of the icosahedral
symmetry axes (trivial, edge~$\mathbb{Z}_2$, face~$\mathbb{Z}_3$,
vertex~$\mathbb{Z}_5$).
The dual Coxeter numbers of the SM gauge groups equal
the corresponding stabilizer orders:
$h^\vee(\mathrm{SU}(3)) = 3 = |\mathbb{Z}_3|$ (face),
$h^\vee(\mathrm{SU}(2)) = 2 = |\mathbb{Z}_2|$ (edge),
$h^\vee(\mathrm{U}(1)) = 0$ (trivial).
Consequently, the CS inverse couplings at level~$k = 1$ are
\begin{equation}\label{eq:spectral-cs-bridge}
  1/g^2(\mathrm{SU}(3)) = 1 + 3 = 4, \quad
  1/g^2(\mathrm{SU}(2)) = 1 + 2 = 3, \quad
  k_Y = 1.
\end{equation}
The vertex stabilizer $\mathbb{Z}_5$ gives
$h^\vee(\mathrm{SU}(5)) = 5$ (the GUT group),
which breaks to $\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$
at the $K = 0$ transition.
\end{lemma}

\begin{proof}
The spectral denominators $k = h/\gcd(m,h)$ for the affine
$m$-values are: $k = 1$ (boundary, $m \in \{0,30\}$),
$k = 2$ (Majorana, $m = 15$),
$k = 3$ (triangle, $m \in \{10,20\}$),
$k = 5$ (golden, $m \in \{6,12,18,24\}$).

Each prime $p \mid h = 30$ corresponds to an icosahedral
rotation axis with stabilizer $\mathbb{Z}_p$:
$p = 5$ (vertices, $12$ elements of order~$5$ or~$10$),
$p = 3$ (faces, $20$ elements of order~$3$ or~$6$),
$p = 2$ (edges, $30$ elements of order~$4$).
The orbifold gauge group at each axis is $\mathrm{SU}(p)$ via
the McKay correspondence $\mathbb{Z}_p \subset \mathrm{SU}(2)
\to \tilde{A}_{p-1}$, with $h^\vee(\mathrm{SU}(p)) = p$.

For the SM gauge groups after breaking: $\mathrm{SU}(3)$ retains
$h^\vee = 3$ from the face stabilizer,
$\mathrm{SU}(2)$ retains $h^\vee = 2$ from the edge stabilizer,
and $\mathrm{U}(1)$ is abelian ($h^\vee = 0$).
The universal CS level $k = 1$ (Hopf Euler class, \S\ref{sec:weinberg})
gives~\eqref{eq:spectral-cs-bridge}.
\end{proof}

\begin{corollary}[Spectral Weinberg angle (consistency check)]
\label{thm:spectral-weinberg}
The spectral denominators of the electroweak sectors recover
the same value $3/11$ from a graph-theoretic route
(the $\tilde{E}_8$ Cartan matrix), confirming the
CS coupling calculation of Derivation~\ref{thm:weinberg}:
\begin{equation}\label{eq:spectral-weinberg}
  \sin^2\theta_W
  = \frac{Q^2/k_Y}{Q^2/k_Y + f(m^*,N)/(k{+}h^\vee)}
  = \frac{1/4}{1/4 + 2/3}
  = \frac{3}{11}
  = \frac{a-1}{a + N_{\mathrm{crit}}},
\end{equation}
where $a = k_1(E_8) = 4$ and $N_{\mathrm{crit}} = 7$.
The formula factorizes into two independent contributions:
\begin{itemize}
\item \emph{Spectral (from the $\tilde{E}_8$ graph):}
  $k_W/k_Y = 3$ (the CS coupling ratio from Lemma~\ref{lem:spectral-bridge}).
\item \emph{Havelock (from the vortex spectrum on~$\mathbf{H}^2$):}
  $C_2/Q^2 = 2/(1/4) = 8$ (the Casimir-to-charge ratio from the
  critical mode $m^* = 2$, $N = 4$).
\end{itemize}
The Weinberg angle is $\sin^2\theta_W = 3/(3 + 8) = 3/11$:
the spectral ratio divided by the sum of spectral and Havelock ratios.
\end{corollary}

\begin{proof}
By the spectral--CS bridge (Lemma~\ref{lem:spectral-bridge}):
$k + h^\vee(\mathrm{SU}(2)) = 3 = k_W$ and $k_Y = 1$.
By the Havelock Casimir (Step~1, \S\ref{sec:weinberg}):
$j = 1$ ($C_2 = 2$) and $Q = 1/2$ ($Q^2 = 1/4$),
where $N = 4$ is the unique polygon in $[3, N_{\mathrm{crit}}]$
whose critical Casimir $f(\lfloor N/2\rfloor, N)$ gives a
positive integer $j < 2$ (gauge boson, not graviton;
see Step~1 for the exhaustive check).
Then $\alpha_Y/\alpha_W = (Q^2 \cdot k_W)/(C_2 \cdot k_Y)
= (1/4 \cdot 3)/(2 \cdot 1) = 3/8$,
and $\sin^2\theta_W = (3/8)/(1 + 3/8) = 3/11$.
The numerator $3 = k_W/k_Y$ is the spectral contribution;
the denominator $11 = 3 + 8$ adds the Havelock contribution
$C_2 \cdot k_Y / Q^2 = 8$.
\end{proof}

\begin{lemma}[Bernoulli--Coxeter identity (classical)]\label{lem:bernoulli-coxeter}
$B_4 = B_8 = -1/30 = -1/h(E_8)$.
Consequently $\zeta(-3) = 1/120 = 1/|I^*|$ and
$\zeta(-7) = 1/240 = 1/|\text{roots}(E_8)|$.
\end{lemma}

\begin{proof}
By von Staudt--Clausen, $\mathrm{denom}(B_{2k}) = \prod_{(p-1)|2k} p$.
For $B_4$: $(p{-}1)|4$ gives $p \in \{2,3,5\}$, denom${}= 30$.
For $B_8$: $(p{-}1)|8$ gives $p \in \{2,3,5\}$
(since $p = 9 = 3^2$ is not prime), denom${}= 30$.
Both numerators are~$-1$ by the Bernoulli recursion.
Then $\zeta(-3) = -B_4/4 = (1/30)/4 = 1/120$
and $\zeta(-7) = -B_8/8 = (1/30)/8 = 1/240$.
\end{proof}

\begin{remark}
The identity $B_4 = B_8$ relies on $\operatorname{rank}(E_8) + 1 = 9 = 3^2$
being composite.  If~$9$ were prime, $\mathrm{denom}(B_8) = 270 \neq 30$,
and the Riemann zeta would not encode~$|I^*|$ at~$s = -3$.
The coincidence $B_4 = B_8$ depends on the specific value
$\operatorname{rank}(E_8) + 1 = 9 = 3^2$ (composite, with $3 \mid h$).
\end{remark}

%% ====================================================================
%% Section "Two-force unification" removed: pure summary of
%% Theorems in secs 4, 8, 9 with no new content.
%% Content preserved in latex/REMOVED_CONTENT.md (IV-010).

%% ====================================================================
\section{The action}
\label{sec:action}

The complete Havelock field theory action on the spacetime
$M_4 = \mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$ is:
\begin{equation}\label{eq:action}
  S = S_{\mathrm{grav}} + S_{\mathrm{CS}} + S_{\mathrm{gauge}}
    + S_{\mathrm{scalar}} + S_{\mathrm{ferm}} + S_{\mathrm{Yuk}}.
\end{equation}

\paragraph{Gravitational sector.}
After KK reduction on~$S^1$, the $2{+}1$D gravitational action
is the Chern--Simons action:
\begin{equation}\label{eq:S-CS}
  S_{\mathrm{CS}} = \frac{k}{4\pi} \int_{M_3}
  \mathrm{Tr}\!\bigl(A \wedge dA + \tfrac{2}{3} A \wedge A \wedge A\bigr),
  \qquad k = \frac{c}{6} = 2\,b(N),
\end{equation}
with $A$ the $\mathrm{SU}(2)$ (Euclidean) gauge field.

\paragraph{$\mathrm{U}(1)$ gauge sector.}
\begin{equation}\label{eq:S-gauge}
  S_{\mathrm{gauge}} = -\frac{1}{4\alpha}
  \int d^4x\,\sqrt{-g}\; F_{\mu\nu} F^{\mu\nu},
  \qquad \alpha = \frac{6}{\pi c}.
\end{equation}

\paragraph{Scalar sector (KK modes).}
$N$ charged scalars $\Phi_m$ ($m = 0, \ldots, N{-}1$)
with KK masses $\mu_m = |m - N/2|$:
\begin{equation}\label{eq:S-scalar}
  S_{\mathrm{scalar}} = \int d^4x\,\sqrt{-g} \sum_{m=0}^{N-1}
  \bigl[|D_\mu \Phi_m|^2 - \mu_m^2 |\Phi_m|^2\bigr],
\end{equation}
where $D_\mu \Phi_m = (\partial_\mu - i m A_\mu) \Phi_m$.

\paragraph{Fermion sector.}
$N$ Dirac fermions $\Psi_m$ with masses $\mu^f_m = |m - (N{-}1)/2|$:
\begin{equation}\label{eq:S-ferm}
  S_{\mathrm{ferm}} = \int d^4x\,\sqrt{-g} \sum_{m=0}^{N-1}
  \bar\Psi_m \bigl(i\!\not\!\!D - \mu^f_m\bigr) \Psi_m.
\end{equation}

\paragraph{Yukawa sector.}
\begin{equation}\label{eq:S-Yuk}
  S_{\mathrm{Yuk}} = \int d^4x\,\sqrt{-g} \sum_{i,j}
  Y_{ij}\, \bar\Psi_i\, \Phi\, \Psi_j + \mathrm{h.c.},
\end{equation}
with the selection rule
$m_i - m_j + m_H \equiv 0 \pmod{N}$
(KK charge conservation on~$S^1$).

\medskip\noindent
All couplings are determined by $c = 12\,b(N)$.

\paragraph{The 4D effective Lagrangian.}
At energies $E \ll M_{\mathrm{poly}}$, the KK modes
(graviphoton, radion, and the KK tower) decouple,
and the effective theory on~$\mathbb{R}^{3,1}$ is:
\begin{equation}\label{eq:L-4D}
\begin{split}
  \mathcal{L}_{4\mathrm{D}} = {} &
  -\frac{1}{16\pi G_4}(R - 2\Lambda) \qquad\text{[gravity, from boundary $T$]}\\
  & -\tfrac{1}{4}\textstyle\sum_a F^a_{\mu\nu}F^{a,\mu\nu}
  \qquad\text{[$\mathrm{SU}(3){\times}\mathrm{SU}(2){\times}\mathrm{U}(1)$,
  from boundary $J^a$]}\\
  & + \bar\Psi(i\!\not\!\!D - Y H)\Psi
  \qquad\text{[3 gen.\ fermions, $\mathbf{H}^2$ radial profiles on $\mathbf{H}^2$]}\\
  & + |D_\mu H|^2 - V(H)
  \qquad\text{[Higgs, $v = 246$\,GeV, $\lambda_H = 0.122$]}
\end{split}
\end{equation}
All couplings in~\eqref{eq:L-4D} are determined by $N = 7$
and $M_P$; no free parameters remain.

\begin{remark}[Unified $3{+}1$D emergence from the boundary CFT]
\label{rmk:minisuperspace-4d}
The $3{+}1$D theory is not assembled from separate mechanisms.
It emerges from a \emph{single} object: the boundary CFT at
$c = 12\,b(N)$ on the torus $T^2 = \partial M$.
The CS/WZW theorem encodes all bulk degrees of freedom in the
boundary operators, organized by conformal weight:
\begin{center}
\small
\begin{tabular}{llll}
\toprule
Boundary operator & Weight & $4$D field & Origin \\
\midrule
Stress tensor $T(z)$ & $(2,0)$ & Graviton $h_{\mu\nu}$ & Virasoro \\
WZW currents $J^a(z)$ & $(1,0)$ & Gauge bosons $A^a_\mu$ &
  Kac--Moody \\
KK primaries $\Phi_n(z)$ & $(h_n, \bar h_n)$ &
  Matter fields & Fiber modes \\
Modular parameter $\tau$ & $\varnothing$ &
  Cosmological $\Lambda$ & Partition fn \\
\bottomrule
\end{tabular}
\end{center}
\noindent
The graviton, gauge bosons, and matter are three \emph{sectors}
of this single boundary theory, not three independent constructions.
The time direction emerges from the Wick rotation
$t_E \to it_L$ of the Euclidean boundary CFT to Lorentzian
signature, which is well-defined because the CFT is unitary
(DHVW orbifold of a unitary WZW model).
Lorentz invariance follows from the Einstein equations
(Theorem~\ref*{III-thm:polygon-einstein}).
\end{remark}


%% ====================================================================
\section{UV completion}
\label{sec:uv}

The theory is UV-complete in the following precise sense.

\paragraph{Non-perturbative definition.}
The boundary CFT is a $\mathbb{Z}_N$ orbifold theory at
central charge $c = 12\,b(N)$, with
known partition function, OPE coefficients, and conformal blocks.
By the CS/WZW theorem \citep{Witten1988},
this boundary theory defines the bulk Chern--Simons theory
at $k = c/6 = 2\,b(N)$ non-perturbatively.
At leading order, $c = N(N{+}1) + O(\ln N)$; the
subleading corrections ($-12\ln 2 + 12(\ln N)/(N{-}1)$) encode
the regularized vortex self-energy.
The $c$-theorem is satisfied: the free-field UV limit
has $c_{\mathrm{UV}} = N(N{+}1)$, and $c_{\mathrm{IR}} = 12\,b(N)
< N(N{+}1)$ for all $N \ge 3$.
There is a single central charge throughout: $c = 12\,b(N)$
enters both the Brown--Henneaux relation ($G = 3/(2c)$)
and the WDW breathing-mode equation.

\paragraph{Super-renormalizability.}
After KK reduction to $1{+}1$D, the superficial degree of divergence
is $D = 2 - E$ ($E$ = external legs).
Only the $2$-point function ($D = 0$) has a logarithmic divergence;
all vertices with $E \ge 4$ are UV-finite.
Two renormalization parameters suffice: the vortex core size~$a$
(UV cutoff for self-energy) and the central charge~$c$ (overall
energy scale).

\paragraph{Physical observables.}
All physical observables. Havelock eigenvalues, partition function,
couplings, cosmological constant, Weinberg angle, are
exact (no perturbative approximation).
The only divergences (self-energy at $d = 0$, KK zero-point sum)
are renormalized by physical parameters within the framework.

\paragraph{The $S^3$ regime and $E_8$ (Paper~V).}
The Seifert manifold $\mathbf{H}^2 \times_N S^1$ has Euler class
$e = N/2$, giving the SM couplings at $K \le 0$.  For $K > 0$
(spatial $S^3$), the Hopf fibration $S^1 \to S^3 \to S^2$ has
Euler class $e = 1$ (the first Chern class of $\mathcal{O}(1)$ on
$\mathbb{CP}^1 = S^2$), fixing the CS level at $k = 1$.
At this level, the full gauge group is $E_8$ with
$1/g^2 = k + h^\vee = 31$ and WZW central charge
$c = 248/31 = 8$ (exactly).
The $E_8 \to \mathrm{SM}$ breaking at $K = 0$ is a
saddle-point transition driven by a Thurston geometry change
($S^3 \to \mathbb{R}^3$): the icosahedral ground state ($N = 12$,
$A_5$, $E_8$) ceases to exist as a finite-energy configuration,
and the heptagonal ground state ($N = 7$,
$\mathbb{Z}_7$, SM) takes over.  No Higgs mechanism is involved.
The 236 generators ($E_8/\mathrm{SM}$, dimension $248 - 12$)
have no finite-energy realization at $K = 0$.
This is developed in full in Paper~V.

\paragraph{KK spectrum and corrections.}
The KK tower on the Seifert manifold has mass-squared
$M^2_{n,m} = (\lambda_m + n^2)\,M_{\mathrm{KK}}^2$
($n \ge 0$, $1 \le m \le N{-}1$,
$\lambda_m = f(m^*,N) - f(m,N)$).
At $N = 11$ the lowest states are:
\begin{center}\small
\begin{tabular}{cccl}
\toprule
$n$ & $m$ & $M/M_{\mathrm{KK}}$ & Identity \\
\midrule
$0$ & $5,6$ & $0$ & graviton (critical modes) \\
$0$ & $4,7$ & $1$ & lightest DM ($\lambda = 1$) \\
$1$ & $5,6$ & $1$ & first graviton KK \\
$0$ & $3,8$ & $\sqrt{3}$ & DM ($\lambda = 3$) \\
$1$ & $4,7$ & $\sqrt{2}$ & first DM$_1$ KK \\
$0$ & $2,9$ & $\sqrt{6}$ & DM ($\lambda = 6$) \\
$0$ & $1,10$ & $\sqrt{10}$ & DM ($\lambda = 10$) \\
\bottomrule
\end{tabular}
\end{center}
The lightest DM particle and the first graviton KK
excitation are \emph{mass-degenerate} at $M_{\mathrm{KK}}$, a structural consequence of $\lambda_4 = 1 = 1^2$.
The one-loop corrections from the full tower, summed
via zeta regularization
($\sum_{n=1}^\infty n^{-2} = \pi^2/6$,
$\sum_{n=1}^\infty \ln n = -\zeta'(0) = \tfrac{1}{2}\ln(2\pi)$),
are:
\begin{center}\small
\begin{tabular}{lcc}
\toprule
Observable & $M_{\mathrm{KK}} = 50$\,TeV
  & $M_{\mathrm{KK}} = 300$\,TeV \\
\midrule
$\delta G/G$ & $2.5 \times 10^{-7}$ & $7 \times 10^{-9}$ \\
$\delta g^2/g^2$ & $2.5 \times 10^{-7}$ & $7 \times 10^{-9}$ \\
$\delta m_H^2/m_H^2$ & $1.5 \times 10^{-7}$ & $4 \times 10^{-9}$ \\
\bottomrule
\end{tabular}
\end{center}
All corrections are $10^{-7}$--$10^{-9}$, six to eight
orders of magnitude below the geometric-tier accuracy
and four to six orders below the algebraic tier.
The boundary CFT controls the UV; the KK corrections are
finite, calculable effects in the IR effective theory.


%% ====================================================================
\section{One-loop corrections}
\label{sec:loops}

The Chern--Simons non-renormalization theorem guarantees
that the CS level receives \emph{at most} a one-loop correction;
higher loops are forbidden by large-gauge-transformation invariance.

\begin{proposition}[One-loop structure]
\label{prop:one-loop}
\leavevmode
\begin{enumerate}
\item \textbf{Coupling lock (all-orders exact).}
  The ratio $\alpha/(8\pi G) = 1/(2\pi^2)$ is algebraically
  $c$-independent: both $G$ and $\alpha$ are proportional to
  $1/c$, so their ratio cancels~$c$.
  The one-loop shift $c \to c - 3N$
  ($N$ Dirac fermions, each contributing $c = -3$ from the
  gravitational anomaly: $-1$ from the Dirac field
  $-2$ from the parity anomaly in odd dimensions)
  does not affect the ratio.  This is exact at all orders.
\item \textbf{CS level shift.}
  Each massive $3$D Dirac fermion shifts the CS level
  by $\mathrm{sgn}(\mu_m)/2$ (parity anomaly).
  The KK spectrum $\mu_m = (m{+}\tfrac{1}{2}{-}N/2)/R$
  is antisymmetric, so the massive modes cancel pairwise;
  for odd~$N$, the single zero mode at $m = (N{-}1)/2$
  contributes $\pm 1/2$ (sign from fiber orientation).
  The physical level is $k_{\mathrm{phys}} = c/6 - 1/2$
  (odd~$N$) or $c/6$ (even~$N$).
  All downstream quantities depend only on the
  fractional part $\kappa = \mathrm{frac}(k_{\mathrm{phys}})
  = 0.096$ (at $N = 7$), which is unaffected by the
  integer shift $-(N{-}1)/2$.
  No further renormalization occurs beyond one loop.
\item \textbf{Cosmological constant.}
  The 3D Coleman--Weinberg correction
  $\Delta\Lambda = -(1/12\pi)(\sum_b \mu_m^3 - \sum_f |\mu^f_m|^3)$
  is a finite sum.
  At $N = 11$: $\Delta\Lambda/\Lambda_{\mathrm{tree}} = -9.1\%$.
\item \textbf{Fermion mass ratios (quantum stable).}
  The one-loop self-energy from CS exchange gives a
  \emph{universal} mass shift $\delta m / m = -1/(4\pi k)$
  ($< 1\%$ for $N \ge 7$), identical for all KK modes.
  Mass \emph{ratios} and Yukawa texture zeros are unchanged.
\item \textbf{Weinberg angle and the polygon scale.}
  The tree-level prediction $\sin^2\theta_W = 3/11$ matches the
  experimental $0.2312$ at~$M_Z$ via standard SM running for
  \begin{equation}\label{eq:M-poly}
    M_{\mathrm{poly}} \approx 300\;\mathrm{TeV}
    \qquad (10^{5.5}\;\mathrm{GeV}).
  \end{equation}
  At $M_{\mathrm{poly}} = 50$\,TeV the prediction is
  $\sin^2\theta_W(M_Z) = 0.240$ ($3.9\%$ above experiment);
  $O(1)$ threshold corrections at the polygon scale
  accommodate the $50$--$500$\,TeV range.
\end{enumerate}
\end{proposition}

%% ====================================================================
\section{The fermion mass structure}
\label{sec:fermions}

This section derives the fermion spectrum from the Seifert geometry.
Every result below follows from the $\mathbb{Z}_7$ orbifold structure,
the $\mathbf{H}^2$ radial profiles, and the BF instanton on~$\mathbf{H}^2$;
no structural identifications are needed beyond those already
established in \S\ref{sec:gauge-derivation-chain}.
The results fall into three tiers:
\emph{Exact} (no adjustable parameters):
the Yukawa texture zeros (\S\ref{sec:yukawa}--\S\ref{sec:up-down}),
the CKM phase $\delta = 69.3^\circ$ (\S\ref{sec:ckm-phase}),
and sector decoupling (\S\ref{sec:decoupling}).
\emph{Warp factor derived}:
the mass hierarchy (\S\ref{sec:mass-hierarchy}) and
tree-level CKM (\S\ref{sec:ckm-tree}) depend on the warp
factor $\sigma$, which is determined by~$N$ alone
(\S\ref{sec:warp-tower}):
$\sigma_{\mathrm{CKM}} = N{-}2 = 5$,
$\sigma_{\mathrm{mass}} = (N{-}2)\sqrt{N} = 5\sqrt{7}$.
All mass ratios and mixing angles are then fixed.
\emph{Non-perturbative}:
the orbifold image suppression (\S\ref{sec:ckm-np}) gives
$|V_{cb}| = 0.043$.

\medskip\noindent
The derivation chain follows the gauge-sector structure
of~\S\ref{sec:gauge-derivation-chain}, extended to fermions.
The only additional input is $\sigma$,
which is derived from $N$ (\S\ref{sec:warp-tower}).

\subsection{The Yukawa texture from KK charge conservation \textnormal{[exact]}}
\label{sec:yukawa}

The Yukawa coupling $Y_{ij}$ between fermion pairs $i$, $j$ and the
Higgs (pair~3, modes $3$ and~$4$) is nonzero iff
$m_i - m_j + m_H \equiv 0 \pmod{7}$.
Evaluating for all pairs:
\begin{equation}\label{eq:yukawa-texture}
  Y = \begin{pmatrix}
    0 & Y_{12} & Y_{13} \\
    Y_{21} & Y_{22} & 0 \\
    Y_{31} & 0 & 0
  \end{pmatrix},
\end{equation}
with four texture zeros at $(1,1)$, $(2,3)$, $(3,2)$, $(3,3)$, determined
entirely by the arithmetic of $\mathbb{Z}/7\mathbb{Z}$.

\subsection{The 2+1 mass pattern and the exact tree-level ratio \textnormal{[exact]}}

The matrix $YY^\dagger$ is block-diagonal:
a $2 \times 2$ block (pairs~1,2) and a $1 \times 1$ block (pair~3).
The eigenvalues of the $2 \times 2$ block
(in the large-$\sigma$ limit where the $\mathbf{H}^2$ radial profiles saturate) are
$\lambda_\pm = (5 \pm \sqrt{13})/2$,
giving the exact tree-level mass ratio
\begin{equation}\label{eq:tree-ratio}
  \frac{m_1}{m_2}\bigg|_{\mathrm{tree}}
  = \sqrt{\frac{5 + \sqrt{13}}{5 - \sqrt{13}}}
  \approx 2.48,
\end{equation}
while the lightest mass $m_3 \propto e^{-\sigma/2}$ is
exponentially suppressed by the warp factor~$\sigma$.

\subsection{Up--down splitting from the $N = 4$ isospin \textnormal{[exact]}}
\label{sec:up-down}

The $\mathrm{SU}(2)$ doublet structure from the $N = 4$ sector
gives different KK masses for up-type ($\mu_4 = 1/2$) and
down-type ($\mu_4 = 3/2$) fermions.
The combined bulk mass $c_{k,I} = \sqrt{\mu_{7,k}^2 + \mu_{4,I}^2}$
separates the two types, with down-type having larger~$c$
(more UV-localized, hence lighter).

\subsection{The warp factor tower \textnormal{[derived from $N$]}}
\label{sec:warp-tower}

The RS warp factor $\sigma$, previously treated as a free parameter,
is determined by $N$ alone.
Three $\sigma$ values appear in the fermion sector;
all derive from the polygon order:
\begin{center}\small
\begin{tabular}{lcll}
\toprule
Parameter & Value & Formula & Origin \\
\midrule
$\sigma_{\mathrm{geo}}$ & $7$ & $N$ & KK warp factor (polygon order) \\
$\sigma_{\mathrm{CKM}}$ & $5$ & $N{-}2$ & nonzero Yukawa entries \\
$\sigma_{\mathrm{mass}}$ & $5\sqrt{7}$ & $(N{-}2)\sqrt{N}$ & CKM warp $\times$ $\sqrt{N}$ \\
\bottomrule
\end{tabular}
\end{center}
\noindent
\emph{(i)~$\sigma_{\mathrm{geo}} = N$ (derived from the Euler class).}
The Seifert connection on $\mathbf{H}^2 \times_N S^1$
has first Chern number $c_1 = e = N/2$
(Proposition~\ref{prop:spacetime}).
The $\mathbb{Z}_N$ orbifold divides the base into $N$ sectors,
each carrying flux $c_1/N = 1/2$ (a Chern half-unit).
The RS warp factor for a KK mode with unit charge
traversing one sector is
\[
  \sigma_{\mathrm{image}}
  = e \times \frac{\mathrm{Area}_{\mathrm{image}}}
  {\mathrm{Area}_{\mathrm{total}}} \times 2
  = \frac{N}{2} \times \frac{1}{N} \times 2 = 1,
\]
where the factor~$2$ arises because the RS extra dimension
is $S^1/\mathbb{Z}_2$, not~$S^1$: the physical range is
$[0, \pi R_c]$ (the fundamental domain of the
$\mathbb{Z}_2$ orbifold), which is \emph{half} the full
circle $[0, 2\pi R_c]$.
The total holonomy $\oint A = 2\pi c_1$ traverses the
full circle, but the physical warp traverses the
fundamental domain, giving
$\sigma = (2\pi c_1)/\pi = 2c_1 = 2e$
(\citealt{RandallSundrum1999}).
With $N$ sectors each contributing one $e$-fold:
$\sigma_{\mathrm{geo}} = N$.
The polygon scale is therefore
$M_{\mathrm{poly}} = v\,e^N = v\,e^7 = 270$\,TeV
(consistent with the polygon scale
${\sim}300$\,TeV from the Weinberg angle running;
the $1.5\%$ residual is a threshold correction).

\emph{(ii)~$\sigma_{\mathrm{CKM}} = N - 2$
(derived from the $\mathbb{Z}_N$ Yukawa texture).}
The charge conservation rule
$m_i - m_j + m_H \equiv 0 \pmod{7}$
(where $m_i$ labels the KK charge of generation~$i$
and $m_H \in \{3, 4\}$ is the Higgs mode)
produces a $3 \times 3$ Yukawa texture with exactly $4$ zeros
and $5 = N{-}2$ nonzero entries~\eqref{eq:yukawa-texture}.
The count $N{-}2$ is algebraic: the $N{-}1 = 6$ nontrivial
KK modes include one critical pair ($m = m^*$ and $m = N{-}m^*$,
the Higgs at $\lambda_{m^*} = 0$) that is massless and does not
contribute to the warp; the remaining
$(N{-}1) - 1 = N{-}2 = 5$ modes are massive.
In the RS framework (\citealt{RandallSundrum1999}),
each massive KK mode traversing the warped fiber
contributes one $e$-fold of suppression to the
corresponding Yukawa coupling.
With $N{-}2$ massive modes: $\sigma_{\mathrm{CKM}} = N{-}2$.

\emph{(iii)~$\sigma_{\mathrm{mass}} = (N{-}2)\sqrt{N}$
(derived from the Dirac eigenvalue on the Seifert manifold).}
The CKM mixing angles probe the $S^1$ fiber alone
(KK mass $\Lambda_{S^1} = m/R$, one quantum number).
The fermion mass hierarchy probes the \emph{full}
Seifert geometry: the Dirac operator on
$\mathbf{H}^2 \times_N S^1$ has eigenvalue
$\Lambda_{\mathrm{Seifert}}^2 = f(m^*,N) + m^2$,
where $f(m^*,N)$ is the Havelock Casimir of the critical
mode on $\mathbf{H}^2$ and $m$ is the KK charge on~$S^1$.
The ratio of effective warp factors is
\[
  \frac{\sigma_{\mathrm{mass}}}{\sigma_{\mathrm{CKM}}}
  = \frac{\Lambda_{\mathrm{Seifert}}}{\Lambda_{S^1}}
  = \sqrt{\frac{f(m^*,N) + m^2}{m^2}}
  = \sqrt{f(m^*,N) + 1}
  \quad\text{at } m = 1.
\]
At $N = 7$: $f(3,7) = 6$, giving
$\sqrt{6{+}1} = \sqrt{7} = \sqrt{N}$.

This identity is \emph{specific to $N = 7$}.
At the critical mode $m^* = (N{-}1)/2$:
\[
  f\!\Bigl(\frac{N{-}1}{2},\,N\Bigr) + 1
  = \frac{N^2{-}1}{8} + 1
  = \frac{N^2{+}7}{8}.
\]
This equals $N$ if and only if $N^2 - 8N + 7 = 0$,
i.e.\ $(N{-}1)(N{-}7) = 0$.
The only non-trivial solution is $N = 7$:
the same value selected by Havelock stability
and von Staudt--Clausen.
Therefore
$\sigma_{\mathrm{mass}}
= \sigma_{\mathrm{CKM}} \times \sqrt{N}
= (N{-}2)\sqrt{N}
= 5\sqrt{7} \approx 13.23$.

\subsection{The fermion mass hierarchy \textnormal{[derived: $\sigma = (N{-}2)\sqrt{N}$]}}
\label{sec:mass-hierarchy}

The tree-level texture~\eqref{eq:yukawa-texture} has $Y_{33} = 0$
(the charge conservation $3{+}3{+}3 = 9 \not\equiv 0 \bmod 7$
forbids a direct $b$-quark Yukawa).
At tree level, $m_b$ comes from $Y_{31} = f_3\,f_1$, which
involves the most UV-localized profile~$f_1$.
The Dirac zero-mode profile on the Seifert manifold is
$\psi_c(\rho) = N_c\,(\sinh\rho)^{c-1/2}$
(from the KK-reduced Dirac equation with warp factor
$A(\rho) = \ln(\sinh\rho)$).
In the holographic regime
$\rho \gg 1$, $\sinh\rho \approx e^\rho/2$ and the
profile reduces to the Randall--Sundrum form
$f_k = \sqrt{(2c_k{-}1)/(e^{(2c_k-1)\sigma}{-}1)}$
at $\sigma = \sigma_{\mathrm{mass}} = (N{-}2)\sqrt{N} = 5\sqrt{7}$,
the derived warp factor from the Seifert geometry
(\S\ref{sec:warp-tower}).
The profile suppresses $f_1$ exponentially ($c_1 = 2.50$),
giving the tree-level ratio $m_s/m_b \approx 0.63$.

\emph{The BF instanton resolves this.}
The same BF-crossing instanton that generates
$\delta_{\mathrm{CKM}} = 69.3^\circ$ (Derivation~\ref{thm:ckm-phase})
carries $\Delta q = 2$ units of $\mathbb{Z}_7$ charge
and generates an effective $Y_{33}$:
\begin{equation}\label{eq:Y33-inst}
  Y_{33}^{\mathrm{inst}} = \mathcal{K}^2\,f_3^2,
  \qquad
  \mathcal{K} = 0.548\;\text{(eq.~\eqref{eq:instanton-K})}.
\end{equation}
Since $\mathcal{K}^2\,f_3 \gg f_1$ at the physical warp factor
($\mathcal{K}^2 f_3/f_1 \approx 260$ at $\sigma = 7.1$),
the instanton contribution dominates:
$m_b \propto Y_{33}^{\mathrm{inst}} = \mathcal{K}^2\,f_3^2$.
The $s$-quark mass comes from the tree-level
$Y_{22} = f_2^2$ (charge-conserving, no instanton needed).
The mass ratio is therefore
\begin{equation}\label{eq:mass-hierarchy}
  \frac{m_s}{m_b}
  = \frac{f_2^2}{\mathcal{K}^2\,f_3^2}
  = \frac{1}{\mathcal{K}^2}\left(\frac{v}{M_{\mathrm{poly}}}\right)^{\!2\Delta c},
\end{equation}
where $\Delta c = c_2 - c_3$ is the conformal-dimension
gap between generations~2 and~3, with
$c_k = \sqrt{\mu_{7,k}^2 + \mu_4^2}$ and
$\mu_{7,k} = |k - (N{-}1)/2|$ (the pair-centered KK mass).
At tree level $\Delta c_0 = \sqrt{1{+}9/4} - 3/2 = 0.303$.
The Seifert fiber connection shifts the effective KK
charge by $e/c = (N/2)/(12\,b(N))$ per unit of angular
momentum (the same Euler class $e = N/2$ that drives
parity violation in \S\ref{sec:chiral-su2}).
Combined with the $O(1/c)$ BO anharmonic correction,
the conformal-dimension gap becomes
\begin{equation}\label{eq:Dc-corrected}
  \Delta c = \Delta c_0 + \frac{\mu_{7,2}}{c_2}
  \cdot\frac{1 + e}{c}
  = 0.303 + 0.048 = 0.351.
\end{equation}
The two corrections (Euler class and BO anharmonic) are
additive because they act on different sectors: the Euler
class shifts the KK charge (a geometric, fiber-connection
effect), while the BO anharmonic shifts the radial potential
(a spectral effect from the $W_N$ quartic coupling).
Their cross-term is $O(\mu_{7,2}\,e/c^3) = O(10^{-5})$,
negligible at the precision of~\eqref{eq:Dc-corrected}.
Standard SM two-loop running
(with $b_1 = 41/10$, $b_2 = -19/6$, and the two-loop matrix
$b_{ij}$ of \citealt{MachacekVaughn1984})
gives $\sin^2\theta_W = 3/11$ at
$M_{\mathrm{poly}} = 294$\,TeV.\footnote{The two-loop SM $\beta$-functions \citep{MachacekVaughn1984} shift $M_{\mathrm{poly}}$ from $274$ (one-loop) to $294$\,TeV (two-loop); see Code Availability.}
The resulting mass ratio:
$m_s/m_b = 0.023$ (observed $0.024$, $4\%$ match).
One calibration input ($\Delta m^2_{\mathrm{atm}}$) pins $M_{\mathrm{poly}}$
within the predicted range; all other quantities are computed:
$\mathcal{K}$ is fixed by the CKM phase,
$\Delta c$ by the KK spectrum,
and $M_{\mathrm{poly}}$ by the Weinberg angle
(with two-loop corrections).

The tree-level CKM has $|V_{cb}|_{\mathrm{pert}} = 0.092$
($2.2\times$ the observed $0.042$).
The non-perturbative CKM (\S\ref{sec:ckm-np}) resolves this
separately: $J$-conservation combined with the scattering phase
$\delta = 69.3^\circ$ redistributes the mixing angles,
giving $|V_{cb}|_{\mathrm{np}} = 0.043$ ($2\%$ above observed).

\emph{The $d$-quark mass from CKM mixing (Gatto relation).}
The bare diagonal Yukawa eigenvalue for the $d$-quark
is UV-suppressed and negligible.
The physical mass comes from Cabibbo mixing
with the $s$-quark:
\begin{equation}\label{eq:gatto}
  m_d = |V_{us}|^2\,m_s,
\end{equation}
with $|V_{us}| = 0.230$ (the tree-level Cabibbo angle).
This is the leading-order form of the Gatto--Sartori--Tonin
relation \citep{Gatto1968}, valid to $O(\theta_C^4)$.
The full quark mass derivation is given in
\S\ref{sec:mass-formulas} below.

\subsubsection{The quark mass hierarchy from Havelock exponents}
\label{sec:mass-formulas}
All six quark masses follow from one structural formula
indexed by the Havelock stability eigenvalues
$\lambda_m = (N{-}1) - f(m,N)$.
The up-type assignment ($\lambda_t = 0, \lambda_c = 1, \lambda_u = 3$)
and down-type assignment (nested exponential with
isospin-shift factor~$1/N$) give:
\begin{align}
m_t &= m_{\mathrm{top}} \quad (\text{BF threshold, reference}),\\
m_c &= m_t \cdot e^{-2\sigma_{\mathrm{mass}}/N}\cdot\mathcal{K}^2,
  \label{eq:mass-c}\\
m_u &= m_t \cdot e^{-6\sigma_{\mathrm{mass}}/N},
  \label{eq:mass-u}\\
m_b &= m_t \cdot e^{-2\sigma_{\mathrm{mass}}/N},
  \label{eq:mass-b}\\
m_s &= m_b \cdot e^{-2\sigma_{\mathrm{mass}}/N}
     = m_t \cdot e^{-4\sigma_{\mathrm{mass}}/N},
  \label{eq:mass-s}\\
m_d &= \sin^2\theta_C \cdot m_s
  \quad (\text{Gatto relation}),
  \label{eq:mass-d}
\end{align}
with $\sigma_{\mathrm{mass}} = \sigma_0\sqrt{N} = 13.23$,
$\mathcal{K} = e^{-2\pi k_{\mathrm{frac}}} = 0.548$,
and $\sin\theta_C = |V_{us}| = 0.230$.
\begin{center}\small
\begin{tabular}{lccc}
\toprule
Quark & Prediction & Observed & Match \\
\midrule
$m_t$ & $173$\,GeV & $173$\,GeV & (reference) \\
$m_b$ & $3.95$\,GeV & $4.18$\,GeV & $5.5\%$ \\
$m_c$ & $1.19$\,GeV & $1.27$\,GeV & $6.3\%$ \\
$m_s$ & $90.2$\,MeV & $93$\,MeV & $3.0\%$ \\
$m_u$ & $2.06$\,MeV & $2.16$\,MeV & $4.6\%$ \\
$m_d$ & $4.77$\,MeV & $4.70$\,MeV & $1.5\%$ \\
\bottomrule
\end{tabular}
\end{center}
\noindent
The up-sector is instanton-saturated for the lightest
generation: the $\mathcal{K}^2$ factor that corrects $m_c$
does not apply to $m_u$, because the tunneling amplitude
through the Havelock barrier is already captured by the
exponent $-6\sigma/N$.
The down-sector nested exponential structure
($m_b \to m_s$ by the same factor as $m_t \to m_b$)
reflects the isospin shift propagating through all three
generations.
The $d$-quark mass enters through Cabibbo mixing with
the $s$-quark (Gatto relation, \citealt{Gatto1968}),
with no additional parameter.

\subsection{Sector decoupling \textnormal{[exact]}}
\label{sec:decoupling}

The $N = 7$ and $N = 8$ polygon sectors are decoupled by the
mod-$56$ arithmetic of the common overfield
$\mathbb{Q}(\zeta_{56})$: the cross-sector Yukawa coupling
requires $8(m_1 + m_2) \equiv 28 \pmod{56}$, which has no
integer solution ($\gcd(8,56) = 8$ does not divide~$28$).
The texture zeros of the $N = 7$ Yukawa are topologically
protected.

\subsection{CKM mixing matrix \textnormal{[derived: $\sigma = N{-}2$]}}
\label{sec:ckm-tree}

The CKM matrix $V = U_L^{(\mathrm{up})\dagger}\,U_L^{(\mathrm{dn})}$
is extracted from the orbit-based Yukawa matrices
using the \emph{left} rotation: $U_L$ diagonalizes $YY^\dagger$
(not $Y^\dagger Y$).
The Bernoulli instanton phases
$\alpha_L = 1/7$, $\alpha_R = 5/42$, $\alpha_H = 2/21$
enter each Yukawa entry through
$\exp(2\pi i(\alpha_L m_L + \alpha_R m_R + \alpha_H m_H)/N)$.
Predicted at $\sigma = 5$
(tree-level magnitudes, before the NLO instanton correction to $V_{ub}$):
\begin{center}
\small
$|V|_{\mathrm{tree}} \approx \begin{pmatrix}
  0.973 & 0.230 & 0.088 \\
  0.230 & 0.971 & 0.043 \\
  0.088 & 0.043 & 0.995
\end{pmatrix}$
\qquad
Observed: $\begin{pmatrix}
  0.974 & 0.224 & 0.00365 \\
  0.224 & 0.974 & 0.042 \\
  0.00854 & 0.041 & 0.999
\end{pmatrix}$
\end{center}
\noindent
The tree-level $|V_{ub}|_{\mathrm{tree}} = 0.088$ overshoots by
a factor of~$\sim\!24$; the NLO instanton correction
$|V_{ub}| = |V_{ub}|_{\mathrm{tree}} \cdot \mathcal{K}^{N-1}(1+\mathcal{K})
= 0.0037$
(Derivation~\ref{thm:instanton-vub} below) restores the match to $2\%$.
The tree-level matrix is shown symmetric because it represents
$|Y_u Y_u^\dagger|^{1/2}$ magnitudes before the non-perturbative
$V_{ub}$ correction; the physical CKM has $|V_{ub}| \ll |V_{td}|$.
The Cabibbo angle $\theta_C = 13.3^\circ$ (observed $13.0^\circ$,
$2\%$ off).
The hierarchy $|V_{us}| \gg |V_{cb}| \gg |V_{ub}|$ is predicted
after the NLO correction.

\subsection{The CKM phase \textnormal{[exact]}}
\label{sec:ckm-phase}

\begin{derivation}[CKM phase from the $\mathbb{Z}_7$ Gauss sum]
\label{thm:ckm-phase}
The CKM CP-violating phase is
\begin{equation}
\label{eq:delta-ckm}
  \delta_{\mathrm{CKM}}
    = \arctan\!\sqrt{7}
    = 69.30^\circ,
\end{equation}
with $\sin^2\delta = N/(N{+}1) = 7/8$ exactly
(Corollary~\ref*{I-cor:sin2-arctan}).
The CKMfitter 2024 global average is
$\gamma = 65.9^\circ \pm 3.5^\circ$; the prediction
is $1.0\sigma$ from the central value.
\end{derivation}
\begin{proof}
The derivation proceeds in five steps from $N = 7$.

\emph{Step~1: Von Staudt--Clausen selects $N = 7$.}
By Theorem~\ref*{I-thm:von-staudt-N}, $N \mid \mathrm{denom}(B_{N-1})$
iff $N$ is prime.
At $N = 7$: $\mathrm{denom}(B_6) = 42 = 2 \cdot 3 \cdot 7$,
so $7 \mid 42$.
Combined with Havelock stability ($\lambda_{\min}(7) = 0$,
$\lambda_{\min}(8) < 0$), this selects $N = 7$.

\emph{Step~2: Quadratic residues = Frobenius orbits.}
The quadratic residues mod~$7$ are $\mathrm{QR} = \{1,2,4\}$
(since $1^2 \equiv 1$, $2^2 \equiv 4$, $3^2 \equiv 2 \pmod{7}$).
The non-residues are $\mathrm{QNR} = \{3,5,6\}$.
These partition $(\mathbb{Z}/7\mathbb{Z})^*$ into two orbits of size~$3$,
identifying three up-type and three down-type generations.

\emph{Step~3: The partial Gauss sum.}
$G(\mathrm{QR}) = \omega + \omega^2 + \omega^4 = (-1 + i\sqrt{7})/2$,
where $\omega = e^{2\pi i/7}$.
(This is the \emph{partial} sum over quadratic residues
$\mathrm{QR} = \{1,2,4\}$ only,
not the full quadratic Gauss sum
$\sum_{k=0}^{6} e^{2\pi i k^2/7} = i\sqrt{7}$,
which has argument $\pi/2$.)
For primes $p \equiv 3 \pmod{4}$:
$G(\mathrm{QR}) + G(\mathrm{QNR}) = -1$ and
$G(\mathrm{QR}) - G(\mathrm{QNR}) = i\sqrt{7}$,
giving $G(\mathrm{QR}) = (-1 + i\sqrt{7})/2$
with $\arg G(\mathrm{QR}) = \pi - \arctan\sqrt{7}
= 110.7^\circ$.

\emph{Step~4: The sector amplitude.}
The Yukawa three-point function in the $\mathbb{Z}_7$ orbifold
sums over all orbifold sectors with \emph{equal} weight:
$Y = (1/|G|)\sum_{m=0}^{N-1} Y_m$
(this is the definition of the orbifold path integral;
\citealt{DixonHarveyVafaWitten1985}).
Three facts fix the relative coefficients:
\begin{enumerate}
\item \emph{Orbifold averaging.}
  Each sector $m$ contributes $Y_m$ with coefficient $1/|G|$.
  No sector is weighted more than any other
  (the $\mathbb{Z}_N$ orbifold projection).
\item \emph{Identity operator.}
  The untwisted sector ($m = 0$) inserts the identity.
  In any CFT, the identity has unit OPE coefficient:
  $\langle O_1(z)\,O_2(0)\rangle = C_{12}^{\mathbf{1}} = 1$
  for canonically normalized operators
  \citep{Polchinski1998}.
  Therefore $Y_0 = 1 \times Y_{\mathrm{tree}}$.
\item \emph{Twist-field normalization.}
  Each twisted sector $m$ inserts the twist field $\sigma_m$.
  With the DHVW normalization
  $\langle\sigma_m\,\sigma_{-m}\rangle = 1$,
  the three-point function gives
  $Y_m = \omega^m \times Y_{\mathrm{tree}}$
  (the twist eigenphase $\omega^m$ is the $\mathbb{Z}_N$
  charge of the operator; the OPE coefficient is~$1$
  by the unit two-point function).
\end{enumerate}
Combining: the up-type amplitude (summing over QR sectors
$m \in \{1,2,4\}$) is
\[
  \alpha_2 = \underbrace{1}_{\text{identity (Step~2)}}
  + \underbrace{G(\mathrm{QR})}_{\text{twist (Step~3)}}
  = \frac{1 + i\sqrt{7}}{2},
\]
where the overall factor $Y_{\mathrm{tree}}/|G|$ is real
and cancels in the phase.
The magnitude $|Y_m|$ is independent of~$m$ because
the $\mathbb{Z}_N$ symmetry permutes the twisted sectors:
the orbifold action maps sector~$m$ to sector~$m{+}1$,
so all sectors have identical OPE coefficient magnitudes.
Only the phase $\omega^m$ distinguishes them.
No coefficient is a free parameter: each follows from
the orbifold construction and CFT operator normalization.
The down-type amplitude is $\bar\alpha_2 = (1 - i\sqrt{7})/2$
(since $G(\mathrm{QNR}) = \overline{G(\mathrm{QR})}$
for $p \equiv 3 \pmod{4}$).

\emph{Step~5: The CKM phase.}
$\delta_{\mathrm{CKM}} = \arg(\alpha_2) = \arctan(\sqrt{7})$.
CP violation requires $N \equiv 3 \pmod{4}$ (so the Gauss sum
is complex); $7 \equiv 3 \pmod{4}$.
The class number $h(-7) = 1$ (Heegner) ensures the CM point is unique.
\end{proof}

The Jarlskog invariant with the derived NLO CKM matrix:
$J = s_{12}\,s_{23}\,s_{13}\,c_{12}\,c_{23}\,c_{13}^2\sin\delta
= 3.29 \times 10^{-5}$
(observed: $3.08 \times 10^{-5}$, $7\%$ match).
The CP violation is near-maximal:
$\sin\delta = \sqrt{7/8} = 0.935$.

\subsection{The orbit-based CKM matrix}
\label{sec:ckm-mixing}

The CKM matrix is extracted from the orbit-based Yukawa
using the LEFT rotation: $V = U_L^{(\mathrm{up})\dagger}\,U_L^{(\mathrm{dn})}$,
where $U_L$ diagonalizes $YY^\dagger$ (not $Y^\dagger Y$).
The instanton phases are
$\alpha_L = 1/7$, $\alpha_R = 5/42$, $\alpha_H = 2/21$
from the pattern $(N{-}k)/\mathrm{denom}(B_6)$ for $k = 1,2,3$.
The ratio $\alpha_L/\alpha_R = 6/5 = M_2$
(Corollary~\ref*{I-cor:zeta-bridge}).

\begin{derivation}[Instanton correction for $V_{ub}$]
\label{thm:instanton-vub}
$|V_{ub}| = |V_{ub}|_{\mathrm{tree}} \times \mathcal{K}^{N-1}(1 + \mathcal{K} + O(\mathcal{K}^2))$,
where $\mathcal{K} = e^{-2\pi k_{\mathrm{frac}}} = 0.548$ is the
instanton fugacity.
At leading order (LO): $|V_{ub}| = 0.0024$ ($35\%$ below PDG).
At next-to-leading order (NLO), the single-instanton
correction $(1 + \mathcal{K})$ gives $|V_{ub}| = 0.0037$
($2\%$ match to PDG).
The correction applies only to $V_{ub}$,
not to $V_{us}$ or $V_{cb}$.
\end{derivation}
\begin{proof}
The Yukawa texture~\eqref{eq:yukawa-texture} gives
$(YY^\dagger)_{02} = 0$
(since $Y_{00} = Y_{21} = Y_{22} = 0$),
so $V_{ub}$ arises only from the indirect path
$1 \to 2 \to 3$ through generation~2.
The instanton winding for this path:
$w_{\mathrm{up}} = |1-4| = 3 = (N{-}1)/2$ (max gap in QR),
$w_{\mathrm{dn}} = |6-3| = 3$ (max gap in QNR),
$w_{\mathrm{total}} = 6 = N{-}1$.
The leading instanton amplitude is $\mathcal{K}^{N-1}$
(one tunneling event per winding unit).
The NLO correction $(1 + \mathcal{K})$ accounts for the
sub-leading instanton with one additional winding unit
($w = N$ instead of $w = N{-}1$);
this is the standard dilute-gas correction to the
semiclassical tunneling amplitude.
Direct-mixing elements $(YY^\dagger)_{01} \neq 0$ and
$(YY^\dagger)_{12} \neq 0$, so $V_{us}$ and $V_{cb}$
are tree-level and uncorrected.
\end{proof}

The five CKM predictions (at $\sigma_{\mathrm{CKM}} = 5$):
\begin{center}\small
\begin{tabular}{lccc}
\toprule
Observable & Prediction & PDG & Match \\
\midrule
$\delta$ & $\arctan\sqrt{7} = 69.3^\circ$ & $65.9 \pm 3.5^\circ$ & $1.0\sigma$ \\
$|V_{us}|$ & $0.230$ & $0.225$ & $2\%$ \\
$|V_{cb}|$ & $0.043$ & $0.042$ & $2\%$ \\
$|V_{ub}|$ (LO: $\mathcal{K}^6$) & $0.0024$ & $0.00365$ & $35\%$ \\
$|V_{ub}|$ (NLO: $\mathcal{K}^6(1{+}\mathcal{K})$) & $0.0037$ & $0.00365$ & $2\%$ \\
$J$ (NLO) & $3.29 \times 10^{-5}$ & $3.08 \times 10^{-5}$ & $7\%$ \\
\bottomrule
\end{tabular}
\end{center}

\subsection{PMNS neutrino mixing}
\label{sec:pmns}

The PMNS matrix arises from the \emph{pair structure}
of $(\mathbb{Z}/7\mathbb{Z})^*$.
The CKM uses the quadratic residue orbit
$\mathrm{QR} = \{1,2,4\}$;
the PMNS uses the three pair modes
$\{1,6\}$, $\{2,5\}$, $\{3,4\}$.

\begin{theorem}[$S_3$-invariant neutrino mass and tribimaximal mixing]
\label{thm:maximal-atm}
Let $N = 7$ and let the neutrino mass matrix take the form
$m_\nu = C^T\!\operatorname{diag}(w)\,C$,
where $C_{km} = \cos(2\pi km/N)$ is the pair cosine matrix
($k, m \in \{1, 2, 3\}$) and $w$ has equal
components $w_1 = w_2 = w_3 \equiv w$.
Then:
\begin{enumerate}
\item[\textup{(a)}] $m_\nu = w\,[(7/4)I_3 - (1/2)J_3]$
  with spectrum $\{w/4,\, 7w/4,\, 7w/4\}$
  (doubly degenerate), where $J_3$ is the $3{\times}3$
  all-ones matrix.
\item[\textup{(b)}] $m_\nu$ is $S_3$-invariant:
  $P_\sigma\,m_\nu\,P_\sigma^T = m_\nu$ for every permutation
  $\sigma \in S_3$ of the three mode indices.
\item[\textup{(c)}] The $S_3$-isotypic decomposition of
  $\mathbb{R}^3$ is $\mathbf{1} \oplus \mathbf{2}$
  (trivial plus standard), where the trivial rep is spanned
  by $v_1 = (e_1 + e_2 + e_3)/\sqrt{3}$ (eigenvalue $w/4$)
  and the $2$-dim standard rep is the orthogonal complement
  (doubly-degenerate eigenvalue $7w/4$).
\item[\textup{(d)}] Under the additional identification of the
  coordinate axes $(e_1, e_2, e_3)$ with the three charged-lepton
  flavors $(\nu_e, \nu_\mu, \nu_\tau)$, and defining the
  mass-eigenstate basis within the $2$-dim subspace by
  diagonalizing the transposition $\sigma_{23} \in S_3$ as
  a self-adjoint operator commuting with $m_\nu$, the mass
  eigenstates are
  \[
    \nu_2 = \tfrac{2e_1 - e_2 - e_3}{\sqrt{6}},
    \qquad
    \nu_3 = \tfrac{e_2 - e_3}{\sqrt{2}},
  \]
  giving PMNS entries
  $U_{e3} = 0$, $|U_{\mu 3}| = |U_{\tau 3}| = 1/\sqrt{2}$,
  and $\theta_{23} = \pi/4$.
\end{enumerate}
\end{theorem}
\begin{proof}
\emph{(a)~Algebraic form of $m_\nu$.}
Compute the entries $(m_\nu)_{km}
= w\sum_{p=1}^{3}\cos(2\pi pk/7)\cos(2\pi pm/7)$.
Using $2\cos A\cos B = \cos(A{-}B) + \cos(A{+}B)$ and
$\sum_{p=1}^{3}\cos(2\pi p\ell/7) = -1/2$
for any $\ell \not\equiv 0\pmod 7$
(from $\sum_{p=0}^{6}e^{2\pi ip\ell/7} = 0$ and even symmetry):
diagonal $(k=m)$: $(w/2)[3 + (-1/2)] = 5w/4$;
off-diagonal ($k \ne m$, both values $k{\pm}m$ nonzero mod~$7$):
$(w/2)[(-1/2) + (-1/2)] = -w/2$.
Hence $m_\nu = w[(5/4)I - (1/2)(J - I)] = w[(7/4)I - (1/2)J]$.
$J$ has eigenvalues $3$ (simple) and $0$ (multiplicity $2$),
so $m_\nu$ has eigenvalues $w(7/4 - 3/2) = w/4$ (simple) and
$w(7/4) = 7w/4$ (multiplicity $2$).

\emph{(b)~$S_3$-invariance.}
$I_3$ and $J_3$ are both invariant under any permutation of
coordinates, and $m_\nu$ is a linear combination of them.

\emph{(c)~Isotypic decomposition.}
The natural $S_3$-action on $\mathbb{R}^3$ decomposes as
$\mathbf{1} \oplus \mathbf{2}$ (permutation representation),
with the trivial rep spanned by $v_1 = (e_1+e_2+e_3)/\sqrt{3}$.
Since $m_\nu$ commutes with $S_3$ (by (b)) and is self-adjoint,
by Schur's lemma $m_\nu$ acts as a scalar on each isotypic
component: on $\mathbf{1}$, the eigenvalue is
$v_1^T m_\nu v_1 = w(7/4) - (w/2)\cdot 3 = w/4$;
on $\mathbf{2}$, $7w/4$.

\emph{(d)~Basis within the degenerate $\mathbf{2}$.}
The transposition $\sigma_{23}$ is an involution in $S_3$ that
commutes with $m_\nu$.
On the $2$-dim irrep $\mathbf{2}$, $\sigma_{23}$ acts as a
reflection with $\pm 1$ eigenspaces.
Explicit computation: $\sigma_{23}$ fixes $v_1$ (in $\mathbf{1}$)
and acts on the orthogonal complement as the unique reflection
preserving the $S_3$-structure.
Decomposing the standard rep $\mathbf{2}$ into $\sigma_{23}$-
eigenspaces:
the $(+1)$-eigenvector is $(2e_1 - e_2 - e_3)/\sqrt{6}$
(symmetric under $e_2 \leftrightarrow e_3$; this is also the
``magnetic $m=0$'' vector in the standard embedding of $S_3$
into $\mathrm{O}(2)$);
the $(-1)$-eigenvector is $(e_2 - e_3)/\sqrt{2}$
(antisymmetric under $e_2 \leftrightarrow e_3$).
Both are eigenvectors of $m_\nu$ with eigenvalue $7w/4$,
and they are uniquely determined (up to sign) as the
$\sigma_{23}$-eigenvectors within the $m_\nu$-degenerate
subspace.

Under the identification $(e_1, e_2, e_3) \leftrightarrow
(\nu_e, \nu_\mu, \nu_\tau)$ of coordinate axes with the
charged-lepton flavor basis
(forced by the orbifold gauge structure,
\S\ref{sec:gauge-group}, in which the three pair
modes carry distinct $\mathbb{Z}_7$ charges matching
the three charged-lepton mass eigenvalues),
the mass eigenstate $\nu_3 = (e_2 - e_3)/\sqrt{2}$ gives
$U_{e3} = \langle e_1, \nu_3\rangle = 0$,
$U_{\mu 3} = 1/\sqrt{2}$,
$U_{\tau 3} = -1/\sqrt{2}$.
In the standard PMNS parametrization,
$|U_{\mu 3}|^2 = \sin^2\theta_{23}\,\cos^2\theta_{13}$
and $\cos^2\theta_{13} = 1$ (since $U_{e3} = 0$),
giving $\sin^2\theta_{23} = 1/2$, i.e.\ $\theta_{23} = \pi/4$.

\noindent
\emph{Remark on the degeneracy.}
The doubly-degenerate eigenvalue $7w/4$ means $m_\nu$
alone does not uniquely determine the mass eigenbasis
within the $2$-dim subspace; any orthonormal basis of that
subspace diagonalizes $m_\nu$.
What fixes the specific basis $\{\nu_2, \nu_3\}$ is the
$S_3$-symmetry of $m_\nu$ combined with the physical
identification of coordinate axes with charged-lepton
flavors: the additional symmetry $\sigma_{23}$ (which
commutes with $m_\nu$) has a \emph{unique} eigenspace
decomposition of the $2$-dim subspace, and the mass basis
is chosen to diagonalize this symmetry simultaneously
with $m_\nu$.
Physically, unequal weights (realistic $w_k$ breaking the
exact $S_3$-symmetry at $O(\delta w/w)$) lift the degeneracy
and select the same $\{\nu_2, \nu_3\}$ basis continuously from
the degenerate limit, giving $\theta_{23} = \pi/4 + O(\delta w/w)$.
This is the Harrison--Perkins--Scott tribimaximal-mixing
pattern in the $\mu$--$\tau$ exchange symmetry limit,
here emerging from the $\mathbb{Z}_7$-induced $S_3$ symmetry
of the pair-mode mass matrix.
\end{proof}

The pair symmetry $m \leftrightarrow N{-}m$ forces $\theta_{23} = 45^\circ$
at leading order.
The deviation from maximality arises when this symmetry
is broken by the modular form hierarchy.
Evaluating the three cusp forms of $\Gamma(7)$ (the Klein quartic
holomorphic differentials) at the CM point
$\tau_0 = (1{+}i\sqrt{7})/2$ gives
$|x| \gg |y| \gg |z|$, which shifts $\theta_{23}$
from $45^\circ$ toward $\sim\!49^\circ$.

The Havelock Casimir appears as the leading $q$-power of the
Klein quartic coordinates:
$f_a(\tau) = q^{f(a,N)/N}\prod_{n \ge 1}
(1{-}q^{Nn-(N-a)})(1{-}q^{Nn-a})$,
where $f(a,N) = a(N{-}a)/2$ is the Havelock Casimir.
This connects the modular form structure directly to the
stability eigenvalue backbone.

\begin{conjecture}[$\mathbb{Z}_7$ PMNS fractions]
\label{conj:pmns}
The three PMNS mixing angles are simple algebraic functions of
$N = 7$ alone:
\begin{align}
\sin^2(2\theta_{12}) &= \tfrac{N{-}1}{N} = \tfrac{6}{7}\,,
\label{eq:pmns-solar}\\
\sin^2\theta_{23} &= \tfrac{N{+}1}{2N} = \tfrac{4}{7}\,,
\label{eq:pmns-atm}\\
\sin^2\theta_{13} &= \tfrac{1}{N^2{-}1} = \tfrac{1}{48}\,.
\label{eq:pmns-reactor}
\end{align}
Equivalently, $\cos(2\theta_{23}) = -1/N$ and
$1 - \sin^2(2\theta_{12}) = 1/N$.
\end{conjecture}

\noindent
The predictions and comparison with PDG:
\begin{center}\small
\begin{tabular}{lccc}
\toprule
Angle & Prediction & PDG & Pull \\
\midrule
$\theta_{12}$ & $33.9^\circ$ & $33.41 \pm 0.79^\circ$ & $0.6\sigma$ \\
$\theta_{23}$ & $49.1^\circ$ & $49.0 \pm 1.3^\circ$ & $0.1\sigma$ \\
$\theta_{13}$ & $8.3^\circ$ & $8.54 \pm 0.15^\circ$ & $1.4\sigma$ \\
\bottomrule
\end{tabular}
\end{center}
\noindent
All three angles are within $2\sigma$ of PDG central values.
For comparison, tribimaximal mixing (TBM) gives
$\theta_{12} = 35.3^\circ$ ($2.4\sigma$),
$\theta_{23} = 45^\circ$ ($3.1\sigma$),
$\theta_{13} = 0^\circ$ (excluded at $>10\sigma$).

\begin{remark}[Derivation status]
Theorem~\ref{thm:maximal-atm} establishes $\theta_{23} = 45^\circ$
from character orthogonality.
The Klein quartic modular forms at the CM point
$\tau_0 = (1{+}i\sqrt{7})/2$ break the pair symmetry
and shift $\theta_{23}$ toward~$49^\circ$
(numerically verified: $47.8^\circ$ with $F_3$ charged leptons
and the PSL$(2,7)$ Clebsch--Gordan Majorana texture).
The exact $\mathbb{Z}_7$ fraction formulas in
Conjecture~\ref{conj:pmns} match the numerical trend
but their complete group-theoretic derivation
(in particular the $\theta_{12}$ prediction) remains open.
The full derivation likely requires exact modular form arithmetic
at the CM point using the PSL$(2,7)$ representation theory
of $S_2(\Gamma(7))$.
\end{remark}

The CP phase prediction $\delta_{\mathrm{CP}}^{\mathrm{PMNS}}
= \arctan\sqrt{7} = 69.3^\circ$
(from the same Gauss sum as the CKM)
is in tension with the current T2K/NOvA preference for
$\delta_{\mathrm{CP}} \approx -90^\circ$;
DUNE and Hyper-Kamiokande will provide a decisive test.

%% OLD PROOF AND NON-PERTURBATIVE SECTION REMOVED
%% (Plancherel derivation superseded by Gauss sum above)
%% See docs/investigations/2026-04-09-bernoulli-havelock-backbone.md

%% OLD PLANCHEREL PROOF AND NON-PERTURBATIVE SECTION REMOVED (342 lines)
%% Replaced by Gauss sum derivation above.
%% Original content preserved in git history (commit before this one).

%% Sections moved to Paper V: Cosmological energy budget, N selection,
%% DM spectrum, baryon fraction, baryogenesis, neutrinos, radion inflation.
%% See migrations M-001 through M-006 in REMOVED_CONTENT.md.


%% Parameter accounting moved to Paper VI (M-007).

%% ====================================================================
\section{Consistency checks}
\label{sec:consistency}

\subsection{Gravitational consistency}

\paragraph{Electroweak scale from the Planck mass.}
The one dimensionful input is $M_P$ (equivalently $G_N$).
The hierarchy formula (Paper~VI, Theorem~\ref*{VI-thm:hierarchy})
\emph{predicts} the electroweak scale:
\begin{equation}
  v = M_P / \exp(\mathcal{H}_7), \qquad
  \mathcal{H}_7 = 2S_{\mathrm{BO}}(7)
    + \Delta\varepsilon\,\ln\varepsilon_7
    + \tfrac{1}{2}\ln\bigl(c_{11}/(24\pi^2)\bigr)
  = 38.459,
\end{equation}
where $S_{\mathrm{BO}}(7) = 18.274$ is the WKB tunneling action
of the $N = 7$ breathing mode (from $b(7)$ and $f(3,7)$),
$\varepsilon_7 = 8 + 3\sqrt{7}$ is the fundamental unit of
$\mathbb{Z}[\sqrt{7}]$ (from the Pell equation),
$\Delta\varepsilon = 0.8031$ is the WDW eigenvalue gap,
and $c_{11} = 12\,b(11)$.
Every component is computed from $b(N)$
and the Pell structure; no Standard Model input is used.
This gives $v = 242.0$\,GeV (observed: $246.2$\,GeV,
$1.7\%$ match; the residual is the $O(1/c^2)$ Dunham correction).
The hierarchy $M_P/v \approx 10^{17}$ is explained,
not assumed.

\begin{proposition}[4D graviton propagator]
\label{prop:graviton-propagator}
The long-range $4$D graviton propagator is the standard linearized
GR result:
\begin{equation}\label{eq:graviton-propagator}
  \langle h^{(4)}_{ij}(k)\,h^{(4)}_{kl}(-k)\rangle
  = \frac{16\pi G_4}{k_4^2}\,\mathcal{P}^{\mathrm{TT}}_{ijkl},
\end{equation}
with $2$ tensor polarizations ($+$, $\times$) and no scalar or vector
admixture at $k \ll M_{\mathrm{poly}}$.
\end{proposition}

\begin{proof}
Four steps, each using a standard result.

\emph{Step~1 (Boundary $\langle TT\rangle$).}
In the orbifold CFT at central charge~$c$, the stress tensor
$2$-point function is fixed by conformal invariance:
$\langle T_{ij}(k)\,T_{kl}({-}k)\rangle
  = (c/24\pi)\,|k|^3\,\mathcal{P}^{\mathrm{TT}}_{ijkl}$,
where $\mathcal{P}^{\mathrm{TT}}$ is the transverse-traceless projector.
This follows from conservation ($k^i\langle T_{ij}\rangle = 0$),
tracelessness ($\delta^{ij}\langle T_{ij}\rangle = 0$),
and conformal dimension~$2$.

\emph{Step~2 (Brown--Henneaux map).}
The normalizable bulk metric perturbation in AdS$_3$ satisfies
$h^{(2)}_{ij}(k) = (16\pi G_3/c)\,\langle T_{ij}(k)\rangle$
\citep{BrownHenneaux1986}.
This is a \emph{tensor} equation: $h$ inherits the
$\mathcal{P}^{\mathrm{TT}}$ structure of~$T$.

\emph{Step~3 (KK mode sum).}
The boundary $\mathbb{R}{\times}S^1_\theta$ lifts on $S^1_\varphi$
(circumference $2\pi R = 2\pi/M_{\mathrm{poly}}$) to
$\mathbb{R}{\times}T^2$.
The KK mode sum
$G(k_3, x_\varphi) = \sum_n G_{2\mathrm{D}}(k_3, m_n{=}n/R)\,
e^{in x_\varphi/R}$
converts the $2$D propagator to a $4$D one:
in the regime $k \gg 1/R$, the sum yields
$G^{(4\mathrm{D})} = G_3/(k_4^2 \times 2\pi R)
= G_4/k_4^2$.
The $\mathcal{P}^{\mathrm{TT}}$ projector is \emph{preserved}:
$S^1_\varphi$ is orthogonal to the TT projection directions
(which act on the $\mathbf{H}^2$ spatial coordinates).

\emph{Step~4 (Assembly).}
Combining Steps~1--3:
$\langle h^{(4)}_{ij}\,h^{(4)}_{kl}\rangle
= (16\pi G_4/k_4^2)\,\mathcal{P}^{\mathrm{TT}}_{ijkl}$,
with $G_4 = G_3/(2\pi R) = 1/M_P^2$ (matching the hierarchy formula).
In position space, the standard Fourier transform
$\int \frac{d^3k}{(2\pi)^3}\,e^{i\boldsymbol{k}{\cdot}\boldsymbol{r}}/k^2
= 1/(4\pi r)$ yields Newton's $1/r$ potential
(Corollary~\ref{cor:newton}).
\end{proof}

\noindent
The massive KK modes (graviphoton, radion) contribute additional DOF
at $k > M_{\mathrm{poly}}$ but are Yukawa-suppressed
($e^{-r/R} \approx 0$ at $R \sim 10^{-21}$\,m) at macroscopic distances.
Gravitational radiation at LIGO has tensor polarization ($+$, $\times$),
matching observations.

\begin{corollary}[Newton's law from the Seifert geometry]
\label{cor:newton}
The static gravitational potential between two masses $m_1$, $m_2$
at separation~$r$ on the Seifert spacetime is
\begin{equation}\label{eq:newton-potential}
  V(r) = -\frac{G_N\,m_1\,m_2}{r}
  \qquad (R \ll r \ll \ell),
\end{equation}
with $G_N = G_4 = G_3/(2\pi R)$ and $R = 1/M_{\mathrm{poly}}$.
Three distance regimes exist:
\begin{enumerate}
\item $r \ll R \approx 10^{-21}$\,m: the $S^1$ fiber is resolved,
  the geometry is effectively $2{+}1$D, and the dimensionally
  reduced $3$D Newton potential gives
  $V(r) \sim -(G_3/2\pi)\,m_1 m_2\,\log(r/R)$;
\item $R \ll r \ll \ell$:
  the KK mode sum yields the standard $1/r$ Newton
  potential~\eqref{eq:newton};
\item $r \gg \ell$: a logarithmic correction
  $\delta V \sim -(G_3/2\pi)\,\log(r/\ell)$ from the CS boundary term,
  with $\delta V/V \sim R/r \times \log(r/\ell) \sim 10^{-19}$
  at $r = 1$\,m, unmeasurable.
\end{enumerate}
\end{corollary}

\begin{proof}
Tree-level graviton exchange between static sources, using the
propagator~\eqref{eq:graviton-propagator}, gives the standard result
$V(r) = -G_N m_1 m_2/r$
(see, e.g., Zee~\emph{QFT in a Nutshell}, \S VIII.1):
the Fourier kernel
$\int \frac{d^3k}{(2\pi)^3}\,e^{i\boldsymbol{k}{\cdot}\boldsymbol{r}}/k^2
= 1/(4\pi r)$,
combined with the TT-projector contraction against $T^{00}_1 T^{00}_2$,
yields~\eqref{eq:newton} with $G_N = G_4$.
The short-distance regime ($r \ll R$) follows because at distances below
the $S^1$ circumference, the KK modes are unresolved and the zero mode
(topological CS sector with no local propagating DOF) dominates,
giving $2{+}1$D logarithmic gravity.
The long-distance correction ($r \gg \ell$) arises from the CS boundary
term, which contributes a non-local logarithmic potential suppressed by
$G_3/G_4 = 2\pi R \approx 6 \times 10^{-21}$\,m.
\end{proof}

\begin{derivation}[Full nonlinear $4$D Einstein equations]
\label{thm:nonlinear-einstein}
\leavevmode
\begin{enumerate}
\item[\emph{(Vacuum)}]
The full nonlinear $4$D vacuum Einstein equations
$R^{(4)}_{MN} = \Lambda^{(4)} g_{MN}$ on
$\mathbb{R} \times (\mathbf{H}^2 \times_N S^1)$
follow from the $\mathrm{SL}(2,\mathbb{R})$ CS theory
(Steps~A--B below).
\item[\emph{(Matter)}]
The matter-coupled equations
$G_{MN} + \Lambda g_{MN} = 8\pi G\,T_{MN}$ follow from
the Lashkari entanglement argument (Step~C below),
using the Ryu--Takayanagi formula (derived in $2{+}1$D
from Cardy--Calabrese $+$ Brown--Henneaux, Step~C).
\end{enumerate}
\end{derivation}

\begin{proof}
All three steps are self-contained in $2{+}1$D.

\emph{Step~A (CS $\to$ 3D nonlinear Einstein).}
The CS flatness condition $F = dA + A \wedge A = 0$
(with $A = \omega + e/\ell$) is equivalent to the $3$D
vacuum Einstein equation $R^{(3)}_{ij} = \Lambda_3\,g_{ij}$
plus the torsion-free condition $T^a = 0$
\citep{Witten1988}.
This is exact: the full nonlinear equations, not a
linearization.

\emph{Step~B (KK lift to 4D).}
On the Seifert manifold with \emph{stabilized} fiber radius
($\sigma = 1$, from the flux potential $N/2$) and
\emph{quantized} flux ($F = N/(2\pi)$, from the Chern class),
the $4$D Ricci tensor decomposes as:
$R^{(4)}_{ij} = R^{(3)}_{ij} - \frac{1}{2}F_{ik}F_j{}^k$,
$R^{(4)}_{i\varphi} = \frac{1}{2}\nabla_j F^j{}_i = 0$
(uniform flux satisfies Maxwell on constant-curvature $\mathbf{H}^2$),
$R^{(4)}_{\varphi\varphi} = -\tfrac{1}{4}F^2 = -N^2/16$
(constant for uniform flux).
Combined with the base curvature $K = -1$,
the effective $3$D cosmological constant is
$\Lambda_3 = K + \tfrac{1}{4}|F|^2 = -1 + N^2/16
= (N^2{-}16)/16$ (Paper~VI, Proposition~\ref*{VI-prop:lambda}).
The $ij$ components give $R^{(3)}_{ij} = \Lambda_3\,g_{ij}$
with $\Lambda_3 = (N^2 - 16)/16$, precisely the $3$D equation
that the CS theory produces in Step~A.
The mixed component $R^{(4)}_{i\varphi} = 0$ is automatically
satisfied.
Therefore the full $4$D vacuum Einstein equations hold on the
Seifert background.
For general perturbations around this background, the linearized
equations follow from the graviton propagator
(Proposition~\ref{prop:graviton-propagator}); the nonlinear
matter-coupled equations follow from the entanglement argument
(Step~C below).

\emph{Step~C (Matter coupling from entanglement, RT derived).}
The vacuum equations (Steps~A--B) are derived without AdS/CFT.
The matter coupling uses the
\citet{Lashkari2014} entanglement argument, which requires
the Ryu--Takayanagi formula.
In $2{+}1$D, RT is derived from the boundary CFT:
\begin{enumerate}
\item The Cardy--Calabrese formula
  $S_{\mathrm{EE}} = (c/3)\log(L/\varepsilon)$
  for a boundary interval of length~$L$ is a
  \emph{theorem} of CFT$_2$ (conformal Ward identity
  for the twist-field correlator, no gravity input);
\item the Brown--Henneaux relation $c = 3\ell/(2G)$
  (our consistency check, \S\ref{sec:couplings});
\item the geodesic length on $\mathbf{H}^2$:
  $\mathrm{Length}(\gamma) = 2\ell\log(L/\varepsilon)$
  (Riemannian geometry, no dynamics).
\end{enumerate}
Combining: $S_{\mathrm{EE}} = (c/3)\log(L/\varepsilon)
= \ell\log(L/\varepsilon)/(2G)
= \mathrm{Length}(\gamma)/(4G)$, the RT formula, derived from CFT$_2$ and geometry.
The Lashkari argument then uses three ingredients:
(i)~the boundary CFT is \emph{unitary}:
the $\mathbb{Z}_N$ orbifold of a unitary CFT
by a finite group is unitary
\citep{DixonHarveyVafaWitten1985}, and the
starting theory (the free boson on~$S^1$ at each
angular mode, with total $c = 12\,b(N)$) is unitary;
(ii)~RT holds (derived above);
(iii)~relative entropy is non-negative
($S(\rho\|\sigma) \ge 0$, a theorem of quantum information).
The positivity of relative entropy for all ball-shaped boundary
regions and all states implies
$G_{\mu\nu} + \Lambda\,g_{\mu\nu} = 8\pi G\,\langle T_{\mu\nu}\rangle$
 (the full nonlinear Einstein equations with matter) with
$G = 3/(2c)$ from Brown--Henneaux.
No additional thermodynamic argument (Jacobson, Clausius) is needed.
The vacuum equations are exact (Steps~A--B); the matter coupling
is established at \emph{linearized} order (first order in $T_{\mu\nu}$)
by \citet{Lashkari2014}.
The extension to second order uses the quantum-corrected
RT formula \citep{Faulkner2017}; in $2{+}1$D this is derived
(the Cardy--Calabrese formula includes all quantum corrections
at finite~$c$), so the second-order extension is also
self-contained.
The $3$D matter coupling lifts to $4$D by the same
KK reduction as Step~B (stabilized fiber, quantized flux).
\end{proof}

\paragraph{Equivalence principle.}
Different fermion generations carry different KK charges~$m$,
so the \emph{graviphoton} force is composition-dependent.
However, this force has Yukawa range $R \sim 10^{-21}$\,m
($10^5$ times smaller than the proton).
At macroscopic distances, only universal forces survive:
the topological CS sector (which couples to topology, not KK charge)
and the radion (which couples to the trace of~$T_{\mu\nu}$, universally).
The E\"otv\"os parameter
$\delta(a)/a \lesssim e^{-r/R} \lesssim e^{-10^{20}} \approx 0$,
easily satisfying the MICROSCOPE bound $\delta(a)/a < 10^{-15}$.

\subsection{Gauge-theory consistency}

\paragraph{Anomaly cancellation.}
Each generation carries the standard
$\mathrm{SU}(3) \times \mathrm{SU}(2) \times \mathrm{U}(1)$
quantum numbers (left-handed Weyl convention).
All gauge anomalies ($\operatorname{Tr}(Y)$,
$\operatorname{Tr}(Y^3)$, $\operatorname{Tr}(T_3^2 Y)$)
cancel per generation by the same arithmetic as in the
Standard Model; the orbifold determines which modes
form a generation, not what quantum numbers they carry.

\paragraph{Proton stability.}
\label{cor:proton-stability}%
The $\mathrm{SU}(3)$ and $\mathrm{SU}(2)$ sectors live in different
polygon numbers ($N = 7$ and $N = 4$).
Cross-sector Yukawa couplings require
$8(m_1 + m_2) \equiv 28 \pmod{56}$,
which has no integer solution
($\gcd(8, 56) = 8$ does not divide~$28$).
This is exact $\mathbb{Z}_{56}$ charge conservation.
\emph{Prediction}: the proton is exactly stable, not just
long-lived, distinguishing this theory from $\mathrm{SU}(5)$ GUTs
($\tau_p \sim 10^{34}$\,yr) and consistent with Super-Kamiokande
bounds ($\tau_p > 1.6 \times 10^{34}$\,yr for $p \to e^+\pi^0$).

\subsection{The Higgs mass}
\label{sec:higgs}
The electroweak VEV $v = 242.0$\,GeV is predicted
(Derivation~\ref*{VI-thm:cc}, $1.7\%$~match to the
observed $246.2$\,GeV).
Standard Model RG running of $\lambda_H$ from $M_{\mathrm{poly}}$
to $M_Z$ gives $\lambda_H \approx 0.10$,
yielding $m_H \approx 110$\,GeV at tree level.
Two corrections, both derived from the orbifold structure, raise this:
\begin{enumerate}
\item \emph{Twisted-sector quartic.}
  The $\mathbb{Z}_7$ orbifold has $N - 1 = 6$ twisted sectors.
  Each twisted-sector operator is localized at the orbifold fixed point
  with OPE coefficient $C_{\mathrm{tw}} \propto 1/\sqrt{N}$
  (the localization suppression from DHVW
  \citep{DixonHarveyVafaWitten1985}).
  The four-point function receives a correction from all twisted
  sectors:
  $N \times |C_{\mathrm{tw}}|^4 = N \times 1/N^2 = 1/N$.
  The charge selection rule ($2m_3 + 2m_4 = 14 \equiv 0 \bmod 7$)
  restricts the quartic to $|\varphi_3|^2|\varphi_4|^2$
  (the unique $\mathbb{Z}_7$-invariant quartic).
  Therefore:
  $\delta\lambda_{\mathrm{tw}} = \lambda_{\mathrm{SM}}/N
  = 0.10/7 = 0.014$.
\item \emph{Instanton quartic.}
  The physical CS level
  $k_{\mathrm{phys}} = c/6 - 1/2 = 8.096$ (at $N = 7$)
  has fractional part $\kappa = 0.096$.
  The instanton fugacity is
  $\mathcal{K} = 0.548$~\eqref{eq:instanton-K}.
  The quartic vertex involves two instanton--anti-instanton pairs
  (amplitude $\mathcal{K}^2 = 0.300$), mediated by the
  critical mode (Havelock eigenvalue $f(2,4) = 2$, conformal weight
  $h = f/(k{+}h^\vee) = 2/3$).
  The critical mode dominates because it is the lightest excitation
  at the BF threshold ($\lambda_{m^*} = 0$).
  The central-charge suppression $1/c_{\mathrm{EW}}$,
  where $c_{\mathrm{EW}} = c_7/3 = 51.57/3 = 17.2$
  (the $N = 4$ subsector contributes $c_4 = 12\,b(4) = 17.2$
  to the total $c_7 = 51.57$; the factor $1/3$ reflects the
  $4/12$ partition of the central charge budget), gives:
  $\delta\lambda_{\mathrm{inst}} = \mathcal{K}^2\,h^2/c_{\mathrm{EW}}
  = 0.300 \times 0.444 / 17.2 = 0.008$.
\end{enumerate}
The SM quartic $\lambda_{\mathrm{SM}}(M_{\mathrm{poly}}) = 0.10$
is the one-loop vacuum-stability value at $M_{\mathrm{poly}}$,
computed from the SM RG equation with all inputs
($m_t$, $\alpha_s$, $v$) derived in previous sections.
At leading order:
$\lambda_H^{\mathrm{LO}} = 0.10 + 0.014 + 0.008 = 0.122$,
giving $m_H^{\mathrm{LO}} = \sqrt{2\lambda_H}\,v = 122$\,GeV.
Four next-to-leading corrections raise this:
\begin{enumerate}
\setcounter{enumi}{2}
\item \emph{$O(1/N^2)$ twisted sector}: $\delta\lambda = \lambda/N^2 = 0.002$
  (subleading DHVW OPE coefficient).
\item \emph{$O(\mathcal{K}^4)$ instanton}: $\delta\lambda = 6 \times 10^{-5}$
  (negligible).
\item \emph{Two-loop SM running}: $\delta\lambda = +0.007$
  (the standard top-quark two-loop correction
  $3y_t^4/(16\pi^2)^2 \times [16g_s^2 - 9y_t^2/2]
  \times \ln(M_{\mathrm{poly}}/m_t)$).
\item \emph{Top threshold}: $\delta\lambda = +0.004$
  (finite EW-scale matching).
\end{enumerate}
At next-to-leading order:
$\lambda_H^{\mathrm{NLO}} = 0.122 + 0.013 = 0.135$, giving
\begin{equation}\label{eq:mH}
  m_H = \sqrt{2\lambda_H^{\mathrm{NLO}}}\,v
  = 128\;\mathrm{GeV}.
\end{equation}
The observed $m_H = 125.25$\,GeV lies between the LO ($122$)
and NLO ($128$) values; the theoretical uncertainty from
$O(1/N^3)$ and three-loop corrections is $\pm\,3$\,GeV.
Every coefficient ($1/N$, $\kappa$, $h$, $c_{\mathrm{EW}}$,
$y_t$, $g_s$) is determined by~$N = 7$ and the SM
particle content.

%% Dark matter detection moved to Paper V (M-001 scope).

\subsection{Predictions and cross-checks}

\paragraph{Black hole entropy.}
\label{sec:btz-transition}
The Cardy formula of the orbifold CFT at $c = 12\,b(N)$ gives
\begin{equation}\label{eq:cardy}
  S_{\mathrm{BH}} = 2\pi\sqrt{\frac{c}{6}\,E_L}
  + 2\pi\sqrt{\frac{c}{6}\,E_R},
\end{equation}
which reproduces the Bekenstein--Hawking entropy
$S = \pi r_+/(2G_3)$ via the Brown--Henneaux relation $c = 3\ell/(2G_3)$.
This is the standard Strominger (1998) microstate counting for
the BTZ black hole.
The microstates are Virasoro descendants of the orbifold CFT;
the Havelock central charge $c = 51.57$ gives $S = 18.4\sqrt{E}$
with sub-leading correction $-\tfrac{3}{2}\log E + \ldots$

The polygon $\to$ BTZ transition is a \emph{Hawking--Page transition}:
at each palindromic threshold~$N$, the entropy jumps by
$\Delta S = \log 2$ per pair (wall-crossing).
The $\mathbb{Z}/2\mathbb{Z}$ palindromic symmetry ($m \leftrightarrow N{-}m$)
gives $1$~bit per self-paired mode ($N$~even) and $2$~bits per paired
mode ($N$~odd).

Under the KK lift to~$4$D, the BTZ entropy becomes the $4$D
Bekenstein--Hawking entropy via $G_4 = G_3/(2\pi R)$, giving
$S_{4\mathrm{D}} = A/(4G_4)$ for the $4$D horizon area~$A$.


%% Onsager-WDW correspondence moved to Paper III (M-009).

%% ====================================================================

%% Discussion section moved to Paper VI (M-008).

\paragraph{Code availability.}
All numerical verifications are in the companion repository
(\url{https://github.com/gspea/planetary-polygons-unified}).
Parameter accounting and predictions appear in Paper~VII.

\bibliographystyle{plainnat}
\bibliography{../shared/refs}

\end{document}
